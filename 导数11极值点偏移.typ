#import "template.typ": *

#set document(title: [导数极值点偏移])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、什么是"极值点偏移"

  设函数 $f(x)$ 在 $x_0$ 处取得唯一极小（大）值，直线 $y = a$ 与曲线 $y = f(x)$ 交于两点，横坐标 $x_1 < x_0 < x_2$（即 $f(x_1) = f(x_2) = a$）。我们来比较 $x_1 + x_2$ 与 $2x_0$：

  - 若曲线关于直线 $x = x_0$ 对称，则必有 $x_1 + x_2 = 2x_0$；
  - 一般的函数并不关于 $x = x_0$ 对称，$x_1 + x_2$ 会偏向某一侧，这就是*极值点偏移*。

  处理套路是构造*对称函数*

  $ F(x) = f(x) - f(2x_0 - x) $

  用导数判断 $F$ 在 $x_0$ 两侧的符号，再由 $f$ 的单调性把"$F(x_1)$ 的符号"翻译成"$x_2$ 与 $2x_0 - x_1$ 的大小关系"。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 二、通用方法：对称差值函数 $F(x) = f(x) - f(2x_0 - x)$

  前面所有例子其实都是同一套流程。把它写成"通法"，遇到极值点偏移就可以照着走四步。

  *第一步：找极值点，定出两点的位置。* 由 $f'(x) = 0$ 解出极值点 $x_0$，判断 $x_0$ 两侧的单调性；由 $f(x_1) = f(x_2)$ 且 $x_1 != x_2$ 知两点分居 $x_0$ 两侧，不妨设

  $ x_1 < x_0 < x_2 $

  *第二步：构造 $F$ 并判断它的符号。* 令

  $ F(x) = f(x) - f(2x_0 - x) $

  它有一个"自动"的性质：

  $ F(2x_0 - x) = -F(x) quad => quad F(x_0) = 0 $

  所以只要判断 $F$ 在 $x_0$ 一侧的符号即可。求导后用*配方、基本不等式或因式分解*给 $F'$ 定号，再由 $F(x_0) = 0$ 得出（通常取 $x > x_0$ 这一侧）

  - $x > x_0$ 时 $F(x) > 0$（也可能 $F(x) < 0$，后面结论正好相反）。

  *第三步：代入 $x = x_1$（或 $x_2$）。* 注意 $2x_0 - x_1 > x_0$，与 $x_2$ 同在极值点右侧。以 $F(x_1) > 0$ 为例：

  $ f(x_1) > f(2x_0 - x_1) quad => quad f(x_2) > f(2x_0 - x_1) $

  （最后一步用了 $f(x_1) = f(x_2)$。）

  *第四步：用右侧的单调性翻译成和的大小。* 设 $f$ 在 $(x_0, +oo)$ 上单调：

  - 若 $f$ 在右侧*递增*：$f(x_2) > f(2x_0 - x_1) <=> x_2 > 2x_0 - x_1 <=> x_1 + x_2 > 2x_0$；
  - 若 $f$ 在右侧*递减*：$f(x_2) > f(2x_0 - x_1) <=> x_2 < 2x_0 - x_1 <=> x_1 + x_2 < 2x_0$。

  若第二步得到的是 $F < 0$，结论正好相反。这样"和与 $2x_0$ 谁大"就被完全确定。

  *常见变式（本文后面各例就是它们）* ：

  - 结论是 $x_1 x_2$ 型：先*取对数*化回和型（例 9）；
  - 结论是 $x_1 x_2^2$、$x_1 + k x_2$ 这类*加权*型：*比值换元* $t = x_2/x_1$（例 10）；
  - 条件是"两处函数值之*和*为常数"：*固定和、变动差*（例 11）；
  - 需要"双边界"（如 $2 < x_1 + x_2 < e$）：用*两个不同的对称中心*（例 4）；
  - 极值点来自 $f'(x)$ 的因式分解或一个"干净"等式：直接读出（例 6、例 7）；由参数分离得到的：改写成 $g(x) = a$ 来处理（例 8）。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】极值点偏移的判定（选择题）

  *【例 1】* 设 $f(x) = e^x - 3x$，直线 $y = a$ 与曲线 $y = f(x)$ 有两个交点，横坐标分别为 $x_1, x_2$，则（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $x_1 + x_2 < 2 ln 3$], [B. $x_1 + x_2 > 2 ln 3$],
    [C. $x_1 + x_2 < 4 ln 3$], [D. $x_1 + x_2 > 4 ln 3$],
  )

  === 解析

  *① 求极值点，明确 $x_1, x_2$ 的位置。*

  $ f'(x) = e^x - 3 quad => quad f'(x) = 0 <=> x = ln 3 $

  且 $x < ln 3$ 时 $f'(x) < 0$、$x > ln 3$ 时 $f'(x) > 0$，所以 $f$ 在 $(-oo, ln 3)$ 上单调递减、在 $(ln 3, +oo)$ 上单调递增，最小值

  $ f(ln 3) = 3 - 3 ln 3 ≈ -0.2958 $

  因此"直线 $y = a$ 与曲线有两个交点"等价于 $a > 3 - 3 ln 3$，此时两个交点的横坐标满足

  $ x_1 < ln 3 < x_2, quad f(x_1) = f(x_2) = a $

  *② 构造关于 $x = ln 3$ 对称的函数。* 令

  $ F(x) = f(x) - f(2 ln 3 - x) quad (x in RR) $

  把 $f(x) = e^x - 3x$ 代入并化简：

  $ F(x) = (e^x - 3x) - (e^(2 ln 3 - x) - 3 (2 ln 3 - x)) = e^x - 9 e^(-x) - 6x + 6 ln 3 $

  求导并*配方*：

  $ F'(x) = e^x + 9 e^(-x) - 6 = (e^(x/2) - 3 e^(-x/2))^2 >= 0 $

  等号仅在 $e^(x/2) = 3 e^(-x/2)$，即 $e^x = 3$，即 $x = ln 3$ 时成立。所以 $F$ 在 $RR$ 上严格递增，而

  $ F(ln 3) = e^(ln 3) - 9 e^(-ln 3) - 6 ln 3 + 6 ln 3 = 3 - 3 = 0 $

  于是

  - $x < ln 3$ 时 $F(x) < 0$；
  - $x > ln 3$ 时 $F(x) > 0$。

  *③ 把 $F(x_1)$ 的符号翻译成 $x_1 + x_2$ 与 $2 ln 3$ 的大小。* 由 ① 知 $x_1 < ln 3$，故

  $ F(x_1) < 0 quad 即 quad f(x_1) < f(2 ln 3 - x_1) $

  又 $f(x_1) = f(x_2)$，所以

  $ f(x_2) < f(2 ln 3 - x_1) $

  注意 $x_1 < ln 3$ 说明 $2 ln 3 - x_1 > ln 3$，而 $f$ 在 $(ln 3, +oo)$ 上单调*递增*，故

  $ x_2 < 2 ln 3 - x_1 quad 即 quad x_1 + x_2 < 2 ln 3 $

  *④ 判断选项。* 由 ③ 知 A 正确、B 错误；又 $2 ln 3 < 4 ln 3$，所以 C 也成立（但它比 A 弱，本题应选最强的结论），D 错误。故选 *#text(fill: rgb("#c7362e"))[A]*。

  *数值印证：* 取 $a = 0$，方程 $e^x = 3x$ 的两根约为 $x_1 ≈ 0.619$、$x_2 ≈ 1.512$，$x_1 + x_2 ≈ 2.131 < 2 ln 3 ≈ 2.197$（而 $4 ln 3 ≈ 4.394$，可见 C 虽然也对，却远不如 A 精确）。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】再练一次（含对数、注意定义域）

  *【例 2】* 设 $f(x) = x + 3 - ln x$，直线 $y = a$ 与曲线 $y = f(x)$ 有两个交点，横坐标分别为 $x_1, x_2$，则（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $x_1 + x_2 < 2$], [B. $x_1 + x_2 > 2$],
    [C. $x_1 + x_2 < 4$], [D. $x_1 + x_2 > 4$],
  )

  === 解析

  *① 极值点与两交点的位置。* 定义域 $x > 0$，

  $ f'(x) = 1 - 1/x = (x - 1)/x quad => quad f'(x) = 0 <=> x = 1 $

  $0 < x < 1$ 时 $f'(x) < 0$、$x > 1$ 时 $f'(x) > 0$，所以 $f$ 在 $(0, 1)$ 上递减、在 $(1, +oo)$ 上递增，最小值

  $ f(1) = 1 + 3 - 0 = 4 $

  故两个交点存在当且仅当 $a > 4$，且 $x_1 < 1 < x_2$，$f(x_1) = f(x_2) = a$。

  *② 构造对称函数（关于 $x = 1$；注意定义域）。* $f$ 只在 $x > 0$ 上有定义，所以 $f(2 - x)$ 要求 $x < 2$，于是令

  $ F(x) = f(x) - f(2 - x) quad (0 < x < 2) $

  展开：

  $ F(x) = (x + 3 - ln x) - ((2 - x) + 3 - ln(2 - x)) = 2x - 2 + ln((2 - x)/x) $

  求导（用 $x(2 - x) = 1 - (x - 1)^2$）：

  $ F'(x) = 2 - 1/x - 1/(2 - x) = 2 - 2/(x (2 - x)) = (-2 (x - 1)^2)/(x (2 - x)) <= 0 $

  等号仅在 $x = 1$ 处成立，所以 $F$ 在 $(0, 2)$ 上严格递减；又

  $ F(1) = 2 - 2 + ln(1/1) = 0 $

  故 $0 < x < 1$ 时 $F(x) > 0$。

  *③ 翻译成和的大小。* 由 $x_1 < 1$ 得 $F(x_1) > 0$，即

  $ f(x_1) > f(2 - x_1) quad => quad f(x_2) > f(2 - x_1) $

  而 $x_1 < 1$ 说明 $2 - x_1 > 1$，它与 $x_2$ 都落在 $f$ 的递增区间 $(1, +oo)$ 上，所以

  $ x_2 > 2 - x_1 quad 即 quad x_1 + x_2 > 2 $

  故 B 正确、A 错误。

  *④ 为什么 C、D 不能选。* $x_1 + x_2$ 与 4 的大小取决于 $a$：

  - 取 $a = 5$（略大于最小值 $4$）：方程 $x - ln x = 2$ 的两根约为 $x_1 ≈ 0.1586$、$x_2 ≈ 3.1462$，$x_1 + x_2 ≈ 3.305 < 4$，此时 C 成立、D 不成立；
  - 取 $a = 10$：方程 $x - ln x = 7$ 的两根约为 $x_1 ≈ 0.0009$、$x_2 ≈ 9.2215$，$x_1 + x_2 ≈ 9.222 > 4$，此时 D 成立、C 不成立。

  可见 C、D 只是"有时成立"，不能作为结论；只有 B 恒成立。故选 *#text(fill: rgb("#c7362e"))[B]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】证明题：$x e^(-x)$ 型的极值点偏移

  *【例 3】* 已知函数 $f(x) = x e^(-x)$（$x in RR$）。若 $x_1 != x_2$ 且 $f(x_1) = f(x_2)$，求证：$x_1 + x_2 > 2$。

  === 解析

  *① 极值点与两点的位置。*

  $ f'(x) = e^(-x) (1 - x) quad => quad f'(x) = 0 <=> x = 1 $

  $x < 1$ 时 $f'(x) > 0$、$x > 1$ 时 $f'(x) < 0$，所以 $f$ 在 $(-oo, 1)$ 上单调递增、在 $(1, +oo)$ 上单调递减，最大值

  $ f(1) = 1/e $

  既然 $f(x_1) = f(x_2)$ 而 $x_1 != x_2$，由极值点两侧的严格单调性可知两点必分居 $x = 1$ 两侧。不妨设

  $ x_1 < 1 < x_2 $

  *② 构造关于 $x = 1$ 对称的函数。* 令

  $ F(x) = f(x) - f(2 - x) = x e^(-x) - (2 - x) e^(x - 2) quad (x in RR) $

  先注意一个*一般事实*：对任何这样的 $F$ 都有

  $ F(2 - x) = -F(x) $

  所以 $F(1) = 0$ 是自动成立的。再求导：

  $ F'(x) = e^(-x) (1 - x) - e^(x - 2) (1 - x) = e^(-x) (1 - x) (1 - e^(2x - 2)) $

  两个因式 $1 - x$ 与 $1 - e^(2x - 2)$ 的符号*总是相同*（$x < 1$ 时都为正，$x > 1$ 时都为负），故 $F'(x) >= 0$ 恒成立，等号仅在 $x = 1$ 处取得。于是 $F$ 在 $RR$ 上严格递增，结合 $F(1) = 0$ 得：

  - $x < 1$ 时 $F(x) < 0$；
  - $x > 1$ 时 $F(x) > 0$。

  *③ 翻译成和的大小。* 由 $x_1 < 1$ 得 $F(x_1) < 0$，即

  $ f(x_1) < f(2 - x_1) quad => quad f(x_2) < f(2 - x_1) $

  而 $x_1 < 1$ 说明 $2 - x_1 > 1$，它与 $x_2$ 都落在 $f$ 的*递减*区间 $(1, +oo)$ 上，所以由函数值的大小可以反推自变量的大小：

  $ x_2 > 2 - x_1 quad 即 quad x_1 + x_2 > 2 $

  证毕。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】双边界：$2 < 1/a + 1/b < e$

  *【例 4】* 已知函数 $f(x) = x (1 - ln x)$。设 $a, b$ 为两个不相等的正数，且 $b ln a - a ln b = a - b$，证明：$2 < 1/a + 1/b < e$。

  === 解析

  *① 把条件"翻译"成 $f$ 的形式。* 因为 $a b > 0$，把条件两边同除 $a b$：

  $ (ln a)/a - (ln b)/b = (a - b)/(a b) = 1/b - 1/a $

  移项得

  $ (1 + ln a)/a = (1 + ln b)/b $

  记 $g(t) = (1 + ln t)/t$。注意

  $ g(1/x) = (1 + ln (1/x))/(1/x) = x (1 - ln x) = f(x) $

  所以 $g(a) = g(b)$ 就等价于 $f(1/a) = f(1/b)$。记

  $ x_1 = 1/a, quad x_2 = 1/b quad (x_1 != x_2) $

  则 $f(x_1) = f(x_2)$，而待证的结论 $2 < 1/a + 1/b < e$ 就是

  $ 2 < x_1 + x_2 < e $

  *② $f$ 的单调性与两点的位置。*

  $ f'(x) = 1 - ln x - 1 = -ln x quad => quad f'(x) = 0 <=> x = 1 $

  $0 < x < 1$ 时 $f'(x) > 0$、$x > 1$ 时 $f'(x) < 0$，所以 $f$ 在 $(0, 1)$ 上递增、在 $(1, +oo)$ 上递减，最大值 $f(1) = 1$；又 $f(e) = e(1 - 1) = 0$，$x -> 0^+$ 时 $f(x) -> 0^+$。由 $f(x_1) = f(x_2)$ 且 $x_1 != x_2$，不妨设

  $ x_1 < 1 < x_2 $

  且 $f(x_1) in (0, 1)$，故 $x_2 in (1, e)$。

  *③ 下界：以 $x = 1$（极值点）为对称中心。* 令

  $ F(x) = f(x) - f(2 - x) quad (0 < x < 2) $

  求导（用 $f'(x) = -ln x$）：

  $ F'(x) = -ln x - ln(2 - x) = -ln(x (2 - x)) >= 0 $

  因为 $x(2 - x) = 1 - (x - 1)^2 <= 1$，故 $ln(x(2 - x)) <= 0$。等号仅在 $x = 1$ 处成立，所以 $F$ 在 $(0, 2)$ 上严格递增；又 $F(1) = 0$，故 $x < 1$ 时 $F(x) < 0$。取 $x = x_1$：

  $ f(x_1) < f(2 - x_1) quad => quad f(x_2) < f(2 - x_1) $

  注意 $2 - x_1 in (1, 2) ⊂ (1, e)$，它与 $x_2$ 同在 $f$ 的递减区间上，所以

  $ x_2 > 2 - x_1 quad 即 quad x_1 + x_2 > 2 $

  *④ 上界：以 $x = e/2$ 为对称中心。* 令

  $ G(x) = f(x) - f(e - x) quad (0 < x < e) $

  作换元 $y = x/e in (0, 1)$，直接计算可得

  $ G(x) = e dot r(y), quad r(y) = (1 - y) ln(1 - y) - y ln y $

  求二阶导：

  $ r'(y) = -ln(1 - y) - ln y - 2, quad r''(y) = (2y - 1)/(y (1 - y)) $

  在 $(0, 1/2)$ 上 $r''(y) < 0$，即 $r$ 是*凹函数*；又

  $ r(0^+) = 0, quad r(1/2) = 1/2 ln(1/2) - 1/2 ln(1/2) = 0 $

  凹函数的图象在弦的上方，故 $y in (0, 1/2)$ 时 $r(y) > 0$；再由

  $ r(1 - y) = -r(y) $

  得 $y in (1/2, 1)$ 时 $r(y) < 0$。于是

  - $0 < x < e/2$ 时 $G(x) > 0$；
  - $e/2 < x < e$ 时 $G(x) < 0$。

  由 ② 知 $x_1 < 1 < e/2$，取 $x = x_1$ 得 $G(x_1) > 0$，即

  $ f(x_1) > f(e - x_1) quad => quad f(x_2) > f(e - x_1) $

  而 $e - x_1 in (e - 1, e) ⊂ (1, e)$，与 $x_2$ 同在递减区间上，所以

  $ x_2 < e - x_1 quad 即 quad x_1 + x_2 < e $

  *⑤ 结论。* 由 ③ ④ 得 $2 < x_1 + x_2 < e$，即

  $ 2 < 1/a + 1/b < e $

  证毕。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】含参数 $a$ 的极值点偏移（$f(x) = ln x - a x$）

  *【例 5】* 已知函数 $f(x) = ln x - a x$，其中 $a > 0$。若 $f(x_1) = f(x_2)$（$x_1 != x_2$），求证：$x_1 + x_2 > 2/a$。

  === 解析

  *① 极值点。*

  $ f'(x) = 1/x - a = (1 - a x)/x quad (x > 0) quad => quad f'(x) = 0 <=> x = 1/a $

  $0 < x < 1/a$ 时 $f'(x) > 0$、$x > 1/a$ 时 $f'(x) < 0$，所以 $f$ 在 $(0, 1/a)$ 上单调递增、在 $(1/a, +oo)$ 上单调递减，最大值

  $ f(1/a) = ln(1/a) - 1 $

  由 $f(x_1) = f(x_2)$ 且 $x_1 != x_2$，不妨设

  $ x_1 < 1/a < x_2 $

  *② 以 $x = 1/a$ 为对称中心构造对称函数。* 令

  $ F(x) = f(x) - f(2/a - x) quad (0 < x < 2/a) $

  展开整理：

  $ F(x) = [ln x - a x] - [ln(2/a - x) - a (2/a - x)] = ln(x/(2/a - x)) + 2 - 2 a x $

  求导：

  $ F'(x) = 1/x + 1/(2/a - x) - 2a = (2a (x - 1/a)^2)/(x (2/a - x)) >= 0 $

  （推导：$1/x + 1/(2/a - x) = (2/a)/(x (2/a - x))$，再通分，分子恰为 $2a (x - 1/a)^2$。）等号仅在 $x = 1/a$ 处成立，所以 $F$ 在 $(0, 2/a)$ 上严格递增；又

  $ F(1/a) = ln 1 + 2 - 2 = 0 $

  故 $x < 1/a$ 时 $F(x) < 0$。

  *③ 翻译成和的大小。* 取 $x = x_1$（由 ① 知 $x_1 < 1/a$）：

  $ f(x_1) < f(2/a - x_1) quad => quad f(x_2) < f(2/a - x_1) $

  而 $x_1 < 1/a$ 说明 $2/a - x_1 > 1/a$，它与 $x_2$ 都落在 $f$ 的递减区间 $(1/a, +oo)$ 上，所以

  $ x_2 > 2/a - x_1 quad 即 quad x_1 + x_2 > 2/a $

  证毕。

  *法 2（换元化归为 $a = 1$）。* 令 $y = a x$（$x > 0$ 时 $y > 0$），则

  $ f(x) = ln(y/a) - y = (ln y - y) - ln a = h(y) - ln a, quad h(y) = ln y - y $

  所以 $f(x_1) = f(x_2)$ 等价于 $h(y_1) = h(y_2)$（$y_1 = a x_1$、$y_2 = a x_2$）。对 $h(y) = ln y - y$ 用完全相同的对称函数手法（中心 $y = 1$）可得

  $ y_1 + y_2 > 2 quad 即 quad a (x_1 + x_2) > 2 quad => quad x_1 + x_2 > 2/a $

  可见参数 $a$ 只是把结论"按比例缩放"，中心也从 $1$ 变成 $1/a$。

  *法 3（分离参数 + 对数平均不等式）。* 不妨设 $0 < x_1 < x_2$。由 $f(x_1) = f(x_2)$：

  $ ln x_1 - a x_1 = ln x_2 - a x_2 quad => quad a (x_2 - x_1) = ln x_2 - ln x_1 $

  把参数 $a$ *分离*出来：

  $ a = (ln x_2 - ln x_1)/(x_2 - x_1) quad 即 quad 1/a = (x_2 - x_1)/(ln x_2 - ln x_1) $

  右边的式子正是 $x_1, x_2$ 的*对数平均*，记作

  $ L = L(x_1, x_2) = (x_2 - x_1)/(ln x_2 - ln x_1) $

  于是"要证 $x_1 + x_2 > 2/a$"就等价于

  $ 1/a = L < (x_1 + x_2)/2 quad 即 quad x_1 + x_2 > 2 L = 2/a $

  这正是*对数平均不等式*（对数平均小于算术平均）。下面证明它。令 $t = x_2/x_1 > 1$，则 $x_2 = t x_1$，从而

  $ L = (x_1 (t - 1))/(ln t), quad (x_1 + x_2)/2 = (x_1 (1 + t))/2 $

  所以 $L < (x_1 + x_2)/2$ 等价于

  $ 2(t - 1)/(t + 1) < ln t quad (t > 1) $

  设 $phi(t) = ln t - 2(t - 1)/(t + 1)$，求导：

  $ phi'(t) = 1/t - 4/(t + 1)^2 = ((t + 1)^2 - 4t)/(t (t + 1)^2) = (t - 1)^2/(t (t + 1)^2) > 0 quad (t > 1) $

  所以 $phi$ 在 $(1, +oo)$ 上单调递增，$phi(1) = 0$，故 $phi(t) > 0$，即

  $ ln t > 2(t - 1)/(t + 1) quad (t > 1) $

  于是 $L < (x_1 + x_2)/2$，代入 $L = 1/a$ 得

  $ 1/a < (x_1 + x_2)/2 quad => quad x_1 + x_2 > 2/a $

  证毕。

  （完整的对数平均不等式是 $sqrt(x_1 x_2) < L(x_1, x_2) < (x_1 + x_2)/2$，等号仅在 $x_1 = x_2$ 时成立；本题只需要右边那一半。法 3 的好处是先把参数"解出来"，把参数问题变成纯粹的*平均*不等式，思路更直白。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 6】判断 $(x_1 + x_2)/2$ 处的导数符号

  *【例 6】* 设函数 $f(x) = x^2 - (a - 2) x - a ln x$。若方程 $f(x) = c$ 有两个不相等的实数根 $x_1, x_2$，求证：$f'((x_1 + x_2)/2) > 0$。

  === 解析

  *① 先看 $f'$：能因式分解，就不必分离参数。*

  $ f'(x) = 2x - (a - 2) - a/x = (2x^2 - (a - 2)x - a)/x = ((x + 1)(2x - a))/x quad (x > 0) $

  因为 $x + 1 > 0$、$x > 0$，所以 $f'(x)$ 的符号完全由 $2x - a$ 决定。若 $a <= 0$，则 $2x - a > 0$，$f'(x) > 0$ 恒成立，$f$ 在 $(0, +oo)$ 上严格递增，方程 $f(x) = c$ 至多有一个根，与"有两个不相等的实根"矛盾。所以必有

  $ a > 0 $

  此时 $f$ 在 $(0, a/2)$ 上递减、在 $(a/2, +oo)$ 上递增，唯一的极值点是

  $ x_0 = a/2 $

  *② 把结论翻译成极值点偏移。* 记 $m = (x_1 + x_2)/2$。又

  $ f''(x) = 2 + a/x^2 > 0 $

  说明 $f'$ 在 $(0, +oo)$ 上严格递增，而 $f'(x_0) = 0$，所以

  $ f'(m) > 0 <=> m > x_0 <=> x_1 + x_2 > a = 2x_0 $

  于是问题化为标准的极值点偏移：要证"和大于 $2x_0$"。不妨设 $x_1 < x_0 < x_2$。

  *③ 对称函数（中心 $x_0 = a/2$）。* 令

  $ F(x) = f(x) - f(a - x) quad (0 < x < a) $

  展开（先把平方差与一次项合并）：

  $ F(x) = [x^2 - (a - x)^2] - (a - 2)[x - (a - x)] - a ln(x/(a - x)) = 2(2x - a) - a ln(x/(a - x)) $

  求导：

  $ F'(x) = 4 - a (1/x + 1/(a - x)) = 4 - (a^2)/(x (a - x)) <= 0 $

  （最后一步：$x(a - x) <= (a/2)^2 = a^2/4$。）等号仅在 $x = a/2$ 处成立，所以 $F$ 在 $(0, a)$ 上严格*递减*；又 $F(a/2) = 0$，故

  - $0 < x < a/2$ 时 $F(x) > 0$。

  *④ 翻译成和的大小。* 取 $x = x_1$（$x_1 < a/2$）：$F(x_1) > 0$，即

  $ f(x_1) > f(a - x_1) quad => quad f(x_2) > f(a - x_1) $

  而 $a - x_1 > a/2$，与 $x_2$ 都落在 $f$ 的递增区间上，所以

  $ x_2 > a - x_1 quad 即 quad x_1 + x_2 > a = 2x_0 $

  于是 $m = (x_1 + x_2)/2 > x_0$，再由 $f'$ 递增、$f'(x_0) = 0$ 得

  $ f'(m) > 0 $

  证毕。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 7】$e^x$ 与直线的两个交点

  *【例 7】* 已知 $f(x) = e^x$，$g(x) = 2a x + 1$，其中 $a > 0$。若方程 $f(x) = g(x)$ 有两个不同的根 $x_1, x_2$，求证：$(x_1 + x_2)/2 < ln 2a$。

  === 解析

  *① 化为 $h(x) = 0$，找极值点。* 令

  $ h(x) = e^x - 2a x - 1 quad (x in RR) $

  题设即 $h(x) = 0$ 有两个不同实根。求导：

  $ h'(x) = e^x - 2a, quad h''(x) = e^x > 0 $

  所以 $h'$ 严格递增、$h$ 严格下凸，$h'(x) = 0$ 给出唯一极值点

  $ x_0 = ln 2a, quad 即 quad e^(x_0) = 2a $

  于是 $h$ 在 $(-oo, x_0)$ 上递减、在 $(x_0, +oo)$ 上递增；"两个不同实根"等价于 $h(x_0) < 0$。不妨设

  $ x_1 < x_0 < x_2 $

  *② 要证的结论。* 它等价于

  $ (x_1 + x_2)/2 < x_0 quad 即 quad x_1 + x_2 < 2x_0 $

  仍是极值点偏移，只是方向与例 6 相反（和"偏小"）。

  *③ 对称函数（中心 $x_0 = ln 2a$，用 $e^(x_0) = 2a$ 化简）。* 令

  $ F(x) = h(x) - h(2x_0 - x) quad (x in RR) $

  展开：

  $ F(x) = e^x - e^(2x_0 - x) - 2a x + 2a (2x_0 - x) = e^x - e^(2x_0 - x) - 4a x + 4a x_0 $

  求导并配方（用 $e^(x_0) = 2a$，即 $e^(2x_0) = (2a)^2$、$4a = 2 dot 2a$）：

  $ F'(x) = e^x + e^(2x_0 - x) - 4a = e^x + (2a)^2 e^(-x) - 2 dot 2a = (e^(x/2) - 2a e^(-x/2))^2 >= 0 $

  等号仅在 $e^(x/2) = 2a e^(-x/2)$，即 $e^x = 2a$，即 $x = x_0$ 处成立。所以 $F$ 在 $RR$ 上严格递增，又 $F(x_0) = 0$，故

  - $x < x_0$ 时 $F(x) < 0$。

  *④ 翻译成和的大小。* 取 $x = x_1$（$x_1 < x_0$）：$F(x_1) < 0$，即

  $ h(x_1) < h(2x_0 - x_1) quad => quad h(x_2) < h(2x_0 - x_1) $

  而 $2x_0 - x_1 > x_0$，与 $x_2$ 都落在 $h$ 的递增区间上，所以

  $ x_2 < 2x_0 - x_1 quad 即 quad (x_1 + x_2)/2 < x_0 = ln 2a $

  证毕。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 8】两个极值点：把 $f'(x) = 0$ 改写成 $g(x) = a$

  *【例 8】* 已知函数 $f(x) = x^2 - 2x + 1 + a e^x$ 有两个极值点 $x_1, x_2$，求证：$x_1 + x_2 > 4$。

  === 解析

  *① 把"极值点"翻译成"$f'(x) = 0$ 有两个根"。*

  $ f'(x) = 2x - 2 + a e^x $

  $x_1, x_2$ 是 $f$ 的两个极值点，即 $x_1 != x_2$ 且

  $ f'(x_1) = f'(x_2) = 0 $

  *② 分离参数，变成"两处函数值相等"。* 由 $f'(x) = 0$ 解出参数：

  $ 2(x - 1) + a e^x = 0 quad => quad a = -2(x - 1) e^(-x) = 2(1 - x) e^(-x) $

  记

  $ g(x) = 2(1 - x) e^(-x) $

  则 $x_1, x_2$ 就是方程 $g(x) = a$ 的两个不同实根。

  *③ 研究 $g$，定出两点的位置。*

  $ g'(x) = 2 e^(-x) (x - 2) $

  所以 $g$ 在 $(-oo, 2)$ 上单调递减、在 $(2, +oo)$ 上单调递增，最小值

  $ g(2) = 2(1 - 2) e^(-2) = -2 e^(-2) ≈ -0.2707 $

  又 $x -> -oo$ 时 $g(x) -> +oo$，$x -> +oo$ 时 $g(x) -> 0^-$。于是"$g(x) = a$ 有两个不同实根"等价于

  $ a in (-2 e^(-2), 0) $

  且不妨设 $x_1 < 2 < x_2$；进一步，由 $g(1) = 0 > a$ 及 $g$ 在 $(-oo, 2)$ 上递减，还可得 $1 < x_1 < 2$。

  *④ 对称函数（中心 $x = 2$）。* 令

  $ G(x) = g(x) - g(4 - x) = 2(1 - x) e^(-x) - 2(x - 3) e^(x - 4) $

  求导：

  $ G'(x) = 2(x - 2)[e^(-x) - e^(x - 4)] = 2(x - 2) e^(-x) (1 - e^(2x - 4)) < 0 quad (x != 2) $

  因为两个因式 $x - 2$ 与 $1 - e^(2x - 4)$ 总是异号。所以 $G$ 在 $RR$ 上严格递减；又

  $ G(2) = 2(1 - 2) e^(-2) - 2(2 - 3) e^(-2) = 0 $

  故 $x < 2$ 时 $G(x) > 0$。

  *⑤ 翻译成和的大小。* 取 $x = x_1$（$x_1 < 2$）：$G(x_1) > 0$，即

  $ g(x_1) > g(4 - x_1) quad => quad g(x_2) > g(4 - x_1) $

  而 $4 - x_1 > 2$，与 $x_2$ 都落在 $g$ 的递增区间上，所以

  $ x_2 > 4 - x_1 quad 即 quad x_1 + x_2 > 4 $

  证毕。

  （本题是"含参可不分参"的反向用法：先把参数*分离*出来写成 $g(x) = a$，再对 $g$ 使用极值点偏移——$g$ 的极值点是 $x = 2$，于是结论正是 $x_1 + x_2 > 2 dot 2 = 4$。顺带还得到 $a in (-2e^(-2), 0)$，说明 $a$ 必须为负。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 9】零点之积：先取对数化为"和"

  *【例 9】* 已知函数 $f(x) = ln x - a x$（其中 $a > 0$）有两个零点 $x_1, x_2$，求证：$x_1 x_2 > e^2$。

  === 解析

  *① 两个零点给出的信息。*

  $ f'(x) = 1/x - a = (1 - a x)/x quad => quad f'(x) = 0 <=> x = 1/a $

  $f$ 在 $(0, 1/a)$ 上递增、在 $(1/a, +oo)$ 上递减，最大值 $f(1/a) = ln(1/a) - 1$。"有两个零点"等价于

  $ f(1/a) > 0 quad <=> quad ln(1/a) > 1 quad <=> quad a < 1/e $

  即 $a in (0, 1/e)$，且不妨设

  $ x_1 < 1/a < x_2 $

  *② 用"零点方程"把积化成和。* 由 $f(x_1) = f(x_2) = 0$ 得

  $ ln x_1 = a x_1, quad ln x_2 = a x_2 $

  两式相加：

  $ ln(x_1 x_2) = ln x_1 + ln x_2 = a (x_1 + x_2) $

  于是"证明 $x_1 x_2 > e^2$"就等价于"证明 $a (x_1 + x_2) > 2$"，也就是

  $ x_1 + x_2 > 2/a $

  ——这正是例 5 证过的极值点偏移结论。

  *③ 证明 $x_1 + x_2 > 2/a$（对称函数，中心 $1/a$）。* 令

  $ F(x) = f(x) - f(2/a - x) quad (0 < x < 2/a) $

  求导：

  $ F'(x) = 1/x + 1/(2/a - x) - 2a = (2a (x - 1/a)^2)/(x (2/a - x)) >= 0 $

  等号仅在 $x = 1/a$ 处成立，所以 $F$ 在 $(0, 2/a)$ 上严格递增；又 $F(1/a) = 0$，故 $x < 1/a$ 时 $F(x) < 0$。取 $x = x_1$：

  $ f(x_1) < f(2/a - x_1) quad => quad f(x_2) < f(2/a - x_1) $

  而 $2/a - x_1 > 1/a$，与 $x_2$ 都落在 $f$ 的递减区间上，所以 $x_2 > 2/a - x_1$，即

  $ x_1 + x_2 > 2/a $

  *④ 回到积。* 把 ③ 代入 ②：

  $ ln(x_1 x_2) = a (x_1 + x_2) > a dot 2/a = 2 quad => quad x_1 x_2 > e^2 $

  证毕。

  （当 $a -> (1/e)^-$ 时两个零点都趋于 $e$（此时 $x = e$ 是二重零点），$x_1 x_2 -> e^2$，所以下界 $e^2$ 是*最优*的，但取不到。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 10】"加权积"型结论：比值换元 $t = x_2/x_1$

  *【例 10】* 已知函数 $f(x) = ln x - a x - 1$（$a in RR$）。若方程 $f(x) + 2 = 0$ 有两个实数根 $x_1, x_2$，且 $x_2 > 2 x_1$，求证：$x_1 x_2^2 > 32/e^3$。（参考数据：$ln 2 ≈ 0.693$。）

  === 解析

  *① 写出两根满足的方程。* 方程 $f(x) + 2 = 0$ 即

  $ ln x - a x + 1 = 0 quad 即 quad ln x = a x - 1 quad (x > 0) $

  所以

  $ ln x_1 + 1 = a x_1, quad ln x_2 + 1 = a x_2 $

  两式相除即可消去参数 $a$：

  $ (ln x_1 + 1)/x_1 = (ln x_2 + 1)/x_2 $

  *② 比值换元，把两根都用 $t$ 表示。* 由 $x_2 > 2 x_1 > 0$，令

  $ t = x_2/x_1 > 2 $

  把 $x_2 = t x_1$ 代入上式：

  $ (ln x_1 + 1)/x_1 = (ln(t x_1) + 1)/(t x_1) quad => quad t (ln x_1 + 1) = ln t + ln x_1 + 1 $

  整理得

  $ (t - 1) ln x_1 = ln t + 1 - t quad => quad ln x_1 = (ln t + 1 - t)/(t - 1) $

  进而

  $ ln x_2 = ln t + ln x_1 = (t ln t + 1 - t)/(t - 1) $

  *③ 把结论也换成 $t$。* 待证结论两边取对数，等价于

  $ ln(x_1 x_2^2) = ln x_1 + 2 ln x_2 > ln(32/e^3) = 5 ln 2 - 3 $

  把 ② 中两式代入左边：

  $ ln(x_1 x_2^2) = ((1 + 2t) ln t + 3 - 3t)/(t - 1) := Phi(t) quad (t > 2) $

  *④ 证明 $Phi(t)$ 在 $(1, +oo)$ 上递增。* 记分子 $N(t) = (1 + 2t) ln t + 3 - 3t$，则 $N'(t) = 2 ln t + 1/t - 1$，于是

  $ Phi'(t) = (N'(t)(t - 1) - N(t))/(t - 1)^2 = (3(t - 1) - 3 ln t - (t - 1)^2/t)/(t - 1)^2 $

  记 $P(t) = 3(t - 1) - 3 ln t - (t - 1)^2/t$，求导：

  $ P'(t) = 3 - 3/t - (1 - 1/t^2) = (2t^2 - 3t + 1)/t^2 = ((2t - 1)(t - 1))/t^2 > 0 quad (t > 1) $

  又 $P(1) = 0$，所以 $P(t) > 0$，从而 $Phi'(t) > 0$：$Phi$ 在 $(1, +oo)$ 上严格递增。

  *⑤ 收尾。* 因为 $t > 2$，

  $ Phi(t) > Phi(2) = ((1 + 4) ln 2 + 3 - 6)/1 = 5 ln 2 - 3 ≈ 0.465 > 0 $

  于是

  $ ln(x_1 x_2^2) = Phi(t) > 5 ln 2 - 3 = ln(32/e^3) quad => quad x_1 x_2^2 > 32/e^3 $

  证毕。

  （$t = 2$ 时恰好取等：$x_1 x_2^2 = 32/e^3$，所以 $32/e^3$ 是"最优"的下界；题设 $x_2 > 2x_1$ 是严格的，故等号取不到。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 11】条件为"两处函数值之和"：固定和、变动差

  *【例 11】* 已知函数 $f(x) = 2 ln x + x^2 + x$。若正实数 $x_1, x_2$ 满足 $f(x_1) + f(x_2) = 4$，求证：$x_1 + x_2 >= 2$。

  === 解析

  *① 先把条件改写成 $2 f(1)$。* 定义域为 $x > 0$，

  $ f'(x) = 2/x + 2x + 1 > 0 quad (x > 0) $

  所以 $f$ 在 $(0, +oo)$ 上*严格递增*，并且

  $ f(1) = 0 + 1 + 1 = 2 quad => quad f(x_1) + f(x_2) = 4 = 2 f(1) $

  可见 $x_1 = x_2 = 1$ 恰好取等，要证的就是"和的最小值为 $2$"。

  *② 固定和、变动差。* 记

  $ m = (x_1 + x_2)/2, quad t = (x_2 - x_1)/2 >= 0 $

  则 $x_1 = m - t$、$x_2 = m + t$（$0 <= t < m$），条件成为

  $ phi(t) := f(m - t) + f(m + t) = 2 ln(m^2 - t^2) + 2 (m^2 + t^2) + 2m $

  *③ 当 $m <= 1$ 时 $phi(t)$ 随 $t$ 递减。* 求导：

  $ phi'(t) = 4t [1 - 1/(m^2 - t^2)] quad (0 < t < m) $

  若 $m <= 1$，则 $0 < m^2 - t^2 <= m^2 <= 1$，从而 $1/(m^2 - t^2) >= 1$，于是

  $ phi'(t) <= 0 quad (0 < t < m) $

  所以 $phi$ 在 $[0, m)$ 上单调递减，最大值为

  $ phi(0) = 2 f(m) quad (m <= 1) $

  *④ 用 $f$ 的单调性逼出 $m >= 1$。* 当 $m <= 1$ 时，由 ③ 与 ①：

  $ 4 = f(x_1) + f(x_2) = phi(t) <= 2 f(m) <= 2 f(1) = 4 $

  首尾都是 $4$，所以中间的"$<=$"必须取等，即 $2f(m) = 2f(1)$，由 $f$ 严格递增得 $m = 1$。也就是说：$m$ 要么本来就大于 $1$，要么只能等于 $1$，总之

  $ m >= 1 quad => quad x_1 + x_2 = 2m >= 2 $

  证毕（等号当且仅当 $x_1 = x_2 = 1$，此时 $t = 0$）。

  *另解（和型对称函数法，与例 1—例 10 同一套路）。* 条件 $f(x_1) + f(x_2) = 4 = 2 f(1)$ 提示我们考察关于 $x = 1$ 对称的两个点的函数值之*和*：

  $ H(x) = f(x) + f(2 - x) quad (0 < x < 2) $

  求导（用 $f'(x) = 2/x + 2x + 1$）：

  $ H'(x) = f'(x) - f'(2 - x) = 4(1 - x) (1/(x (2 - x)) - 1) = (4 (1 - x)^3)/(x (2 - x)) $

  所以 $H$ 在 $(0, 1)$ 上递增、在 $(1, 2)$ 上递减，最大值为

  $ H(1) = 2 f(1) = 4 $

  即 $0 < x < 2$ 时恒有 $H(x) <= 4$，等号仅在 $x = 1$ 处成立。

  下面用*反证法*：若 $x_1 + x_2 < 2$，不妨设 $x_1 <= x_2$（此时 $x_1 < 1$），则 $x_2 < 2 - x_1$；由 $f$ 在 $(0, +oo)$ 上严格递增得

  $ f(x_2) < f(2 - x_1) $

  于是

  $ 4 = f(x_1) + f(x_2) < f(x_1) + f(2 - x_1) = H(x_1) <= 4 $

  矛盾。所以 $x_1 + x_2 >= 2$，证毕。

  （两种写法其实是一回事：$m = 1$ 时 $H(1 - t) = phi(t)$，"证 $H(x) <= H(1)$"就是"证 $phi(t) <= phi(0)$"。区别只在于本解法把它整理成"以 $1$ 为中心的*和型对称函数*"来讨论，与例 1—例 10 的对称函数法完全一致，更好记。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *条件为"两处函数值之和"时用固定和、变动差（例 11）*：若已知 $f(x_1) + f(x_2) = $ 常数，令 $m = (x_1+x_2)/2$、$t = (x_2-x_1)/2$，把条件写成 $phi(t) = f(m-t) + f(m+t)$；先证明在 $m$ 较小时 $phi$ 关于 $t$ 递减（即 $phi(t) <= phi(0) = 2f(m)$），再借 $f$ 的严格单调性把 $2m$ 与常数端点（例 11 中 $2 = 2 dot 1$）的大小逼出来。
  - *比值换元（例 10）*：当结论是 $x_1 x_2^2$、$x_1 + k x_2$ 这类"加权"形式（不能直接用和、积的对称手法）时，令 $t = x_2/x_1 > 1$，用两根满足的等式（例 10 中 $(ln x_1 + 1)/x_1 = (ln x_2 + 1)/x_2$）把 $ln x_1$、$ln x_2$ 都用 $t$ 表示，结论就化为关于 $t$ 的单变量不等式，求导判断单调性即可；端点值（例 10 中 $Phi(2) = 5 ln 2 - 3$）往往正好给出题目里的那个常数，说明下界是最优的。
  - *零点问题里的"积"化"和"（例 9）*：$f(x) = ln x - a x$ 的两个零点满足 $ln x_i = a x_i$，两式相加得 $ln(x_1 x_2) = a (x_1 + x_2)$，于是"证 $x_1 x_2 > e^2$"就等价于"证 $x_1 + x_2 > 2/a$"，回到了极值点偏移。遇到乘积型（或除法型）结论时，先取对数常常能把它变回熟悉的和型。
  - *极值点偏移的标准三步*：① 求极值点 $x_0$；② 构造对称函数 $F(x) = f(x) - f(2x_0 - x)$，用导数（或配方、基本不等式）判断它在 $x_0$ 两侧的符号；③ 由 $f(x_1) = f(x_2)$ 与 $f$ 在极值点两侧的单调性，把 $F(x_1)$ 的符号翻译成 $x_1 + x_2$ 与 $2x_0$ 的大小。
  - *符号与大方向*：若 $x < x_0$ 时 $F(x) < 0$（如本题），则 $f(x_1) < f(2x_0 - x_1)$，结合 $f(x_1) = f(x_2)$ 得 $x_2 < 2x_0 - x_1$，即 $x_1 + x_2 < 2x_0$（和"偏小"）；若 $x < x_0$ 时 $F(x) > 0$，则得 $x_1 + x_2 > 2x_0$。
  - *常用技巧*：$e^x + 9 e^(-x) - 6 = (e^(x/2) - 3 e^(-x/2))^2$，这类"$t + k/t$"形状常常能配成完全平方（或直接用基本不等式 $t + k/t >= 2 sqrt(k)$），符号一眼可见。
  - *选择题策略*：若出现多个真结论，要选*最强*（最精确）的那个——例 1 中 A（$< 2 ln 3$）强于 C（$< 4 ln 3$）。
  - *注意定义域*：含 $ln x$ 的函数只在 $x > 0$ 上有定义，构造对称函数时要写清它的定义域（例 2 中 $F(x) = f(x) - f(2 - x)$ 只在 $0 < x < 2$ 上有意义）；另外要先确认 $x_1$、$2x_0 - x_1$ 都落在同一个单调区间里，才能由 $f$ 的大小反推自变量的大小。
  - *与 $a$ 有关的选项要排除*（例 2 的 C、D）：可以用两个不同的 $a$ 各算一次（数值或估值即可），说明该选项并非恒成立。
  - *两个一般事实*：① 对称函数总满足 $F(2x_0 - x) = -F(x)$（关于 $x = x_0$ "反对称"），所以 $F(x_0) = 0$ 自动成立，剩下的工作只是判断 $F$ 的单调性（例 3 中 $F'(x) = e^(-x)(1 - x)(1 - e^(2x - 2)) >= 0$，两个因式同号）；② 题目说"$f(x_1) = f(x_2)$"时通常隐含 $x_1 != x_2$（若允许相等，取 $x_1 = x_2 = 0$ 结论就不成立），答题时先写一句"由极值点两侧的单调性得 $x_1 < x_0 < x_2$"即可。
  - *先把条件"翻译"成 $f$ 的形式（例 4）*：条件 $b ln a - a ln b = a - b$ 两边同除 $a b$ 后化为 $(1 + ln a)/a = (1 + ln b)/b$，再用 $g(1/x) = f(x)$（其中 $g(t) = (1 + ln t)/t$）把它变成熟悉的 $f(x_1) = f(x_2)$（$x_1 = 1/a$、$x_2 = 1/b$），结论也随之变成 $2 < x_1 + x_2 < e$。
  - *双边界要用两个不同的对称中心（例 4）*：下界 $x_1 + x_2 > 2$ 以极值点 $x_0 = 1$ 为中心（$F(x) = f(x) - f(2 - x)$）；上界 $x_1 + x_2 < e$ 则以 $e/2$ 为中心（$G(x) = f(x) - f(e - x)$）。只要"待比较常数的一半"落在 $x_1$ 与 $x_2$ 之间（本题 $1$ 与 $e/2$ 都落在 $(x_1, x_2)$ 内），就能照搬同一套流程。判断 $G$ 的符号时可以用换元 $y = x/e$ 把问题化为研究 $r(y) = (1 - y)ln(1 - y) - y ln y$：在 $(0, 1/2)$ 上 $r'' < 0$（凹函数）且 $r(0^+) = r(1/2) = 0$，故 $r > 0$，再由 $r(1 - y) = -r(y)$ 得 $r < 0$（$y > 1/2$）。
  - *含参数 $a$ 的情形（例 5）*：$f(x) = ln x - a x$（$a > 0$）的极值点是 $x_0 = 1/a$，结论是 $x_1 + x_2 > 2/a$；求导时 $1/x + 1/(2/a - x)$ 通分后分子恰是完全平方 $2a (x - 1/a)^2$，符号一眼可见。也可以用 $y = a x$ 把问题化为 $h(y) = ln y - y$（即 $a = 1$）——参数 $a$ 只起"按比例缩放"的作用，中心与结论同步缩放。
  - *分离参数 + 对数平均（例 5 法 3）*：由 $f(x_1) = f(x_2)$ 可以把参数"解出来"：
    $a = (ln x_2 - ln x_1)/(x_2 - x_1)$，于是 $1/a$ 恰是 $x_1, x_2$ 的*对数平均* $L$，待证结论化为经典的*对数平均不等式* $L < (x_1 + x_2)/2$（令 $t = x_2/x_1$ 后等价于 $ln t > 2(t - 1)/(t + 1)$，求导即得）。这条路线把"参数问题"变成"平均不等式"，当结论里出现 $2/a$ 这类与参数有关的常数时特别好用。
  - *含参可以不分离（例 6、例 7）*：若 $f'(x)$ 能因式分解，就直接读出极值点（例 6 中 $f'(x) = ((x + 1)(2x - a))/x$，得 $x_0 = a/2$）；若极值点满足一个"干净"的等式，就用它来化简对称函数（例 7 中 $e^(x_0) = 2a$，于是 $F'(x) = (e^(x/2) - 2a e^(-x/2))^2 >= 0$）。两题都用"有两个不同实根"这一前提反推参数信息（例 6 得 $a > 0$，例 7 得 $h(x_0) < 0$），全程不必把参数分离出来。
  - *把 $f'(x) = 0$ 改写成 $g(x) = a$（例 8）*：若 $f$ 有两个极值点 $x_1, x_2$，它们就是 $f'(x) = 0$ 的两根；把参数*分离*出来写成 $g(x) = a$（例 8 中 $g(x) = 2(1 - x) e^(-x)$），就可以对 $g$ 直接使用极值点偏移的整套手法——$g$ 的极值点是 $x = 2$，结论 $x_1 + x_2 > 4$ 正是 $2 dot 2$。注意先由"$g(x) = a$ 有两根"定出 $a$ 的范围（本题 $a in (-2e^(-2), 0)$）与两点所在区间（$1 < x_1 < 2 < x_2$）。
  - *结论中出现 $(x_1 + x_2)/2$（例 6、例 7）*：只要先把 $m = (x_1 + x_2)/2$ 与极值点 $x_0$ 比大小——由 $f'$ 的单调性把"$f'(m) > 0$"翻译成"$m > x_0$"，问题就回到"$x_1 + x_2$ 与 $2x_0$ 谁大"的极值点偏移。
]
