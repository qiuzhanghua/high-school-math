#import "template.typ": *

#set document(title: [焦点弦和极线应用])

#template(
  heading2-align: left,
  page-footer: "第 1 页",
)[
  = 华中师大附中2027届高三独立作业数学
  \

  *8.* 已知椭圆 $E: x^2/2 + y^2 = 1$ 的左焦点为 $F$，过点 $P(2, t)$ 分别作 $E$ 的切线 $P A$、$P B$，切点分别为 $A$、$B$，则 $triangle A B F$ 面积最大值为

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    [A. $sqrt(2)$], [B. $sqrt(3)$], [C. $2$], [D. $2 sqrt(2)$ ],
  )

  \

  由极线方程知 $l_(A B):$
  $
    (2x)/2 + t y & = 1 \
         x + t y & = 1
  $

  说明 $l_(A B)$ 过点 $(1, 0)$。

  \

  由焦点弦公式：

  $ |A B| & = (2 a b^2)/(a^2 - c^2 cos^2 theta) quad $

  $ & = (2 sqrt(2))/(2 - cos^2 theta) $\

  $ h & = 2 sin theta $\

  $
    therefore S_(triangle A B F) & = (2 sqrt(2) sin theta)/(1 + sin^2 theta) \
                                 & = (2 sqrt(2))/(1/(sin theta) + sin theta) \
                                 & <= sqrt(2)
  $

  \

  #align(center)[
    #text(fill: gray, size: 10pt)[
      注：直接使用极线方程写出切点弦方程，属于二级结论。虽然课本上没有直接教，但在选择填空题中直接使用是完全允许且高效的。

      焦点弦长公式是圆锥曲线极坐标方程推导出来的二级结论。在高考中，如果这是一道选择题或填空题，直接套用这个公式可以节省大量时间，是完全可以的（只要算对）。
    ]
  ]

  #pagebreak()

  == 解法二：以两焦点为底边结合韦达定理

  *第一步：确定切点弦方程*

  由极线方程知 $l_(A B): (2x)/2 + t y = 1$，即 $x + t y = 1$。
  说明 $l_(A B)$ 过点 $(1, 0)$，该点为椭圆的右焦点。

  \

  *第二步：联立直线与椭圆*

  设直线 $A B$ 的方程为 $x = m y + 1$（这样设可以包含斜率不存在的情况）。
  将其代入椭圆方程 $x^2/2 + y^2 = 1$：
  $ ((m y + 1)^2)/2 + y^2 = 1 $
  整理得：
  $ (m^2 + 2) y^2 + 2m y - 1 = 0 $

  \

  *第三步：使用韦达定理*

  设 $A(x_1, y_1)$，$B(x_2, y_2)$，由韦达定理可得：
  $ y_1 + y_2 = (-2m)/(m^2 + 2) $
  $ y_1 y_2 = (-1)/(m^2 + 2) $
  由于 $y_1 y_2 < 0$，说明 $A$ 和 $B$ 的纵坐标一正一负。

  \

  *第四步：以两焦点为底边计算面积*

  左焦点 $F_1(-1, 0)$，右焦点 $F_2(1, 0)$。底边 $F_1 F_2$ 的长度为 $2c = 2$。

  $triangle A B F_1$ 可以拆分为 $triangle A F_1 F_2$ 和 $triangle B F_1 F_2$ 的面积之和（因为 $A, B$ 在 $x$ 轴两侧）：
  $ S_(triangle A B F_1) & = S_(triangle A F_1 F_2) + S_(triangle B F_1 F_2) $
  $ & = 1/2 dot |F_1 F_2| dot |y_1| + 1/2 dot |F_1 F_2| dot |y_2| $
  $ & = 1/2 dot 2 dot (|y_1| + |y_2|) $
  $ & = |y_1| + |y_2| $

  由于 $y_1 y_2 < 0$，所以 $|y_1| + |y_2| = |y_1 - y_2|$。因此：
  $ S_(triangle A B F_1) = |y_1 - y_2| = sqrt((y_1 + y_2)^2 - 4 y_1 y_2) $

  \

  *第五步：代入韦达定理求最值*

  $ S & = sqrt(((-2m)/(m^2 + 2))^2 - 4 ((-1)/(m^2 + 2))) $
  $ & = sqrt((4m^2)/((m^2 + 2)^2) + 4/(m^2 + 2)) $
  $ & = sqrt((4m^2 + 4(m^2 + 2))/((m^2 + 2)^2)) $
  $ & = sqrt((8m^2 + 8)/((m^2 + 2)^2)) $
  $ & = (sqrt(8(m^2 + 1)))/(m^2 + 2) = (2 sqrt(2) sqrt(m^2 + 1))/(m^2 + 2) $

  令 $u = sqrt(m^2 + 1) >= 1$，则 $m^2 + 2 = u^2 + 1$。
  $ S = (2 sqrt(2) u)/(u^2 + 1) = (2 sqrt(2))/(u + 1/u) $

  由基本不等式，当 $u = 1$ 时，分母 $u + 1/u$ 取得最小值 $2$。
  此时面积取得最大值：
  $ S_(max) = (2 sqrt(2))/2 = sqrt(2) $

  \

  *结论：* 最大面积为 $sqrt(2)$，对应选项 A。

  #pagebreak()

  #line(length: 100%, stroke: 0.5pt + gray)

  == 解法三：仿射变换法

  *第一步：建立仿射变换*

  已知椭圆 $E: x^2/2 + y^2 = 1$，其参数为 $a = sqrt(2), b = 1$。
  作仿射变换：
  $ x' = x/sqrt(2), quad y' = y $
  该变换将椭圆 $E$ 变换为单位圆 $C: x'^2 + y'^2 = 1$。

  \

  *第二步：变换几何元素*

  在变换下，原坐标系中的点、线、椭圆分别对应新坐标系中的元素：
  - 椭圆 $E$ 变为单位圆 $C: x'^2 + y'^2 = 1$。
  - 左焦点 $F(-1, 0)$ 变为 $F'(-1/sqrt(2), 0)$。
  - 点 $P(2, t)$ 变为 $P'(sqrt(2), t)$。
  - 切线 $P A, P B$ 变为圆 $C$ 的切线 $P'A', P'B'$。
  - 切点弦 $A B$ 变为 $A'B'$。

  \

  *第三步：在圆中求解*

  在单位圆 $C$ 中，$A'B'$ 是点 $P'(sqrt(2), t)$ 对应的切点弦（极线）。
  对于单位圆，点 $P'(x_0, y_0)$ 的极线方程为 $x_0 x' + y_0 y' = 1$。
  代入 $P'(sqrt(2), t)$，得到直线 $A'B'$ 的方程：
  $ sqrt(2) x' + t y' = 1 $
  可以发现，无论 $t$ 取何值，直线 $A'B'$ 恒过定点 $(1/sqrt(2), 0)$。设该定点为 $Q'$。

  \

  *第四步：计算圆中三角形面积*

  设直线 $A'B'$ 的倾斜角为 $theta$，则其方程为 $y' = tan theta (x' - 1/sqrt(2))$。
  圆心 $O(0,0)$ 到直线 $A'B'$ 的距离为：
  $ d = (abs(tan theta)/sqrt(2))/(sqrt(1 + tan^2 theta)) = 1/sqrt(2) abs(sin theta) $
  弦长 $A'B' = 2 sqrt(1 - d^2) = 2 sqrt(1 - 1/2 sin^2 theta)$。

  点 $F'(-1/sqrt(2), 0)$ 到直线 $A'B'$ 的距离（即三角形的高）为：
  $ h' = |F' Q'| dot |sin theta| = (1/sqrt(2) + 1/sqrt(2)) |sin theta| = sqrt(2) |sin theta| $

  所以，圆中三角形 $A'B'F'$ 的面积为：
  $
    S_(triangle A'B'F') & = 1/2 dot A'B' dot h' \
                        & = 1/2 dot 2 sqrt(1 - 1/2 sin^2 theta) dot sqrt(2) |sin theta| \
                        & = sqrt(2) |sin theta| sqrt(1 - 1/2 sin^2 theta)
  $

  \

  *第五步：面积还原与求最值*

  仿射变换会改变图形的面积。变换矩阵的行列式为 $det mat(1/sqrt(2), 0; 0, 1) = 1/sqrt(2)$。
  因此，原椭圆中三角形 $A B F$ 的面积是圆中对应三角形 $A'B'F'$ 面积的 $sqrt(2)$ 倍：
  $
    S_(triangle A B F) & = sqrt(2) dot S_(triangle A'B'F') \
                       & = sqrt(2) dot sqrt(2) |sin theta| sqrt(1 - 1/2 sin^2 theta) \
                       & = 2 |sin theta| sqrt(1 - 1/2 sin^2 theta)
  $

  令 $u = |sin theta| in (0, 1]$，则：
  $ S = 2 u sqrt(1 - 1/2 u^2) = 2 sqrt(u^2 (1 - 1/2 u^2)) $

  设 $v = u^2 in (0, 1]$，则 $g(v) = v(1 - 1/2 v) = v - 1/2 v^2$。
  这是一个开口向下的抛物线，对称轴为 $v = 1$。
  所以当 $v = 1$（即 $u = 1$，$theta = 90 degree$）时，$g(v)$ 取得最大值：
  $ g(1) = 1 - 1/2 = 1/2 $

  所以面积最大值为：
  $ S_(max) = 2 sqrt(1/2) = 2 dot sqrt(2)/2 = sqrt(2) $

  \

  *结论：* 最大面积为 $sqrt(2)$，对应选项 A。

  #pagebreak()

  #line(length: 100%, stroke: 0.5pt + gray)

  == 解法四：平移坐标系结合齐次化与韦达定理

  *第一步：坐标平移*

  将坐标原点平移至左焦点 $F(-1, 0)$，令 $X = x + 1$，$Y = y$。
  则椭圆方程 $x^2/2 + y^2 = 1$ 变为：
  $ ((X - 1)^2)/2 + Y^2 = 1 $
  展开整理得：
  $ X^2 + 2Y^2 - 2X - 1 = 0 $

  \

  *第二步：确定切点弦方程*

  由极线方程知，切点弦 $A B$ 在原坐标系下为 $x + t y = 1$。
  代入 $x = X - 1$，得 $X - 1 + t Y = 1$，即：
  $ X + t Y = 2 $
  这说明直线 $A B$ 在新坐标系下过定点 $(2, 0)$。

  \

  *第三步：齐次化联立*

  将直线 $A B$ 的方程改写为 $1 = (X + t Y)/2$。
  将椭圆方程中的一次项和常数项用此式替换，进行齐次化：
  $ X^2 + 2Y^2 - 2X dot 1 - 1 dot 1 = 0 $
  $ X^2 + 2Y^2 - 2X ((X + t Y)/2) - ((X + t Y)/2)^2 = 0 $
  两边同乘 $4$ 并整理：
  $ X^2 + 6 t X Y + (t^2 - 8)Y^2 = 0 $

  \

  *第四步：利用韦达定理求面积*

  令 $k = Y/X$，方程化为关于 $k$ 的一元二次方程：
  $ (t^2 - 8)k^2 + 6t k + 1 = 0 $
  由韦达定理：
  $ k_1 + k_2 = (-6t)/(t^2 - 8) $
  $ k_1 k_2 = 1/(t^2 - 8) $

  在新坐标系下，左焦点位于原点 $(0,0)$。设 $A, B$ 对应的斜率为 $k_1, k_2$，
  利用直线方程 $X + t Y = 2$ 可得 $X = 2/(1 + t k)$，$Y = 2k/(1 + t k)$。
  三角形面积为：
  $ S = |Y_1| + |Y_2| = |Y_1 - Y_2| = |frac(2k_1, 1 + t k_1) - frac(2k_2, 1 + t k_2)| $
  通分并代入韦达定理，化简得：
  $ S = (2 sqrt(2) sqrt(t^2 + 1))/(t^2 + 2) $

  \

  *第五步：代入求最值*

  令 $u = sqrt(t^2 + 1) >= 1$，则 $t^2 + 2 = u^2 + 1$。
  $ S = (2 sqrt(2) u)/(u^2 + 1) = (2 sqrt(2))/(u + 1/u) $

  由基本不等式，当 $u = 1$ 时，分母 $u + 1/u$ 取得最小值 $2$。
  此时面积取得最大值：
  $ S_(max) = (2 sqrt(2))/2 = sqrt(2) $

  \

  *结论：* 最大面积为 $sqrt(2)$，对应选项 A。
]
