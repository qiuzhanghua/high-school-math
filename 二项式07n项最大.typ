#import "template.typ": *

#set document(title: [二项式第 n 项与最大项])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、通项与"第 $n$ 项"

  二项展开式的通项是

  $ T_(k+1) = C_n^k a^(n-k) b^k quad (k = 0, 1, dots, n) $

  *"第 $m$ 项"就是 $T_m$，对应 $k = m - 1$*——这一步最容易错位（第 $3$ 项要用 $k = 2$，不是 $k = 3$）。

  同时要分清两个"系数"：

  - *第 $m$ 项的二项式系数*：$C_n^(m-1)$，只与位置有关；
  - *第 $m$ 项的系数*：还要乘上 $a^(n-k) b^k$ 里的数字（例 1 就是这种情形）。

  *二项式系数的最值*：$C_n^0, C_n^1, dots, C_n^n$ 先增后减、关于中间对称，所以

  - $n$ 为*偶数*：中间一项 $C_n^(n/2)$ 最大（唯一）；
  - $n$ 为*奇数*：中间两项 $C_n^((n-1)/2)$ 与 $C_n^((n+1)/2)$ 并列最大。

  若求的是"*含字母的系数最大项*"，则比较相邻两项，由比值 $T_(k+1)/T_k >= 1$ 解出 $k$ 的范围再定项。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】$(x + 1/(2x))^6$ 展开式中的第 $3$ 项（选择题）

  *【例 1】* $(x + 1/(2x))^6$ 的展开式中的第 $3$ 项为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $3 x^4$], [B. $5/2$], [C. $(15)/4 x^2$], [D. $(15)/16 x^2$],
  )

  === 解析

  第 $3$ 项对应 $k = 2$（$T_(k+1) = T_3$），取 $a = x$、$b = 1/(2x)$、$n = 6$：

  $ T_3 = C_6^2 x^(6-2) (1/(2x))^2 = 15 dot x^4 dot 1/(4 x^2) = (15)/4 x^2 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  （注意别错位成 $k = 3$：那样算的是第 $4$ 项 $C_6^3 x^3 (1/(2x))^3 = (5)/2 x^0$，正是干扰项 $B$。另外 $B$ 少了 $x^0$ 这个"常数项"的说明、$D$ 是把 $(1/2)^2$ 算成 $1/16$ 的错误结果。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】第 $5$ 项的二项式系数最大（选择题）

  *【例 2】* $(a + b)^n$ 展开式中第 $5$ 项的二项式系数最大，则 $n$ 的值可以为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $7$], [B. $8$], [C. $9$], [D. $10$],
  )

  === 解析

  第 $5$ 项对应 $k = 4$，它的*二项式系数*是 $C_n^4$。

  二项式系数在中间最大：若 $C_n^4$ 是唯一的（也是最大的）那一项，则 $4$ 就是正中位置，即

  $ n/2 = 4 quad => quad n = 8 $

  此时 $C_8^4 = 70$，确实是全部系数中最大的。故选 *#text(fill: rgb("#c7362e"))[B]*。

  （补充说明：若题目允许"并列最大"，则 $n = 7$ 与 $n = 9$ 也满足——$C_7^3 = C_7^4 = 35$、$C_9^4 = C_9^5 = 126$，第 $5$ 项与相邻项并列第一。本题是单选，按"唯一最大"取 $n = 8$。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 练习

  *【练 1】* 二项式 $(a x + 1/(b x))^n$（$a > 0$，$b > 0$，$n in N^*$）的展开式中*只有第 6 项*的二项式系数最大，且展开式中的第 3 项系数是第 4 项系数的 $3$ 倍，则 $a b$ 的值为 #box(width: 4em, stroke: (bottom: 0.5pt)) 。

  === 解析

  *① 由"只有第 6 项最大"定出 $n$。* 二项式系数要在第 6 项*唯一*最大，说明 $n$ 为偶数、且第 6 项正是中间项。第 6 项对应 $k = 5$，故

  $ k = n/2 = 5 quad => quad n = 10 $

  （"*只有*……最大"这几个字很关键：若允许并列，$n$ 就不是唯一的值了。）

  *② 写出第 3、4 项的系数。* 通项为

  $ T_(k+1) = C_10^k (a x)^(10-k) (1/(b x))^k = (C_10^k a^(10-k))/b^k x^(10 - 2k) $

  - 第 3 项（$k = 2$）的系数：$(C_10^2 a^8)/b^2 = 45 a^8/b^2$；
  - 第 4 项（$k = 3$）的系数：$(C_10^3 a^7)/b^3 = 120 a^7/b^3$。

  *③ 用"3 倍"条件解出 $a b$。* 由题意

  $ 45 a^8/b^2 = 3 times 120 a^7/b^3 $

  两边同除以 $a^7/b^2$：

  $ 45 a = 360/b quad => quad a b = 8 $

  故填 $8$。

  （检验：取 $a = 2$、$b = 4$（$a b = 8$），第 3 项系数 $45 times 256/16 = 720$，第 4 项系数 $120 times 128/64 = 240$，恰好是 $3$ 倍。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *"第 $m$ 项"对应 $k = m - 1$*：这是本类问题唯一的坑，写通项前先把 $k$ 定准（例 1 若取 $k = 3$ 就会错选）。
  - *区分"二项式系数"与"系数"*：第 $m$ 项的二项式系数是 $C_n^(m-1)$；而"系数"还要乘 $a^(n-k) b^k$ 中的数字（例 1 中的 $1/2^2 = 1/4$，例 2 只问二项式系数，不用乘）。
  - *二项式系数最大在中间*：$n$ 偶时唯一（$k = n/2$），$n$ 奇时并列（$k = (n-1)/2$ 与 $(n+1)/2$）——由"第 5 项最大"可反推 $n$（例 2 得 $n = 8$）。
  - *"可以为"要留意并列情形*：$n = 7, 9$ 时第 $5$ 项与相邻项并列最大；单选一般按"唯一最大"作答。
  - *"只有第 $m$ 项的二项式系数最大"是唯一性条件*（练 1）：它说明 $n$ 为偶数、第 $m$ 项就是中间项，即 $k = m - 1 = n/2$；若题目只说"第 $m$ 项最大"而没有"只有"，还要考虑并列的情形。
  - *"第 3 项系数是第 4 项的 3 倍"就是两个通项相比*（练 1）：写出 T₃、T₄ 的系数（注意 $a$、$b$ 的幂次与 $x$ 的次数一起算），约分即得 $a b$。
  - *求"系数最大项"用比值法*：令 $T_(k+1)/T_k >= 1$ 解出 $k$ 的范围，再取整确定是第几项。
]
