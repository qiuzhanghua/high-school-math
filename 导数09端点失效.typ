#import "template.typ": *

#set document(title: [导数端点失效])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 一、什么叫"端点失效"

  端点效应（必要性探路）的前提是：不等式 $f(x) >= a$（或 $f(x) <= a$）在 $[b, +oo)$ 上恒成立，且*在端点处取等*，即 $f(b) = a$。此时由 $f'(b)$ 的符号才能得到参数的必要条件。

  如果端点处*不取等*（$f(b) != a$），那么图象在端点处的"起步方向"与水平线 $y = a$ 之间没有必然联系，端点效应就*失效*了：既得不到必要条件，也得不到充分条件。若强行套用，往往会算出错误的参数范围。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 二、失效之后怎么办

  - *先看端点值*：端点处函数值必须满足不等式，这常常先给出参数的一侧限制（如本题 $f(0) = 1 - a >= 0$ 给出 $a <= 1$）。注意这只是"端点取值"的要求，与端点效应（用导数符号探路）不是一回事。
  - *分离参数*：把参数分离到不等式的一边（如化成 $a >= g(x)$），问题转化为求 $g(x)$ 的最值。要特别小心*参数系数的符号*：系数可能在某点变号，必须分段讨论，除负数时不等号还要反向。
  - *图象与切线*：把不等式看成"曲线在直线上方（或下方）"的位置关系，临界情形通常是*直线与曲线相切*——由"切线过定点"或"切点处斜率相等"解出临界参数。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】端点处不取等的恒成立问题

  *【例 1】* 若不等式 $e^x + 2a x - a >= 0$ 在 $[0, +oo)$ 上恒成立，求实数 $a$ 的取值范围。

  === 解析

  把左边记为

  $ f(x) = e^x + 2a x - a = e^x + a (2x - 1) quad (x >= 0) $

  令 $x = 0$，得端点的直接要求

  $ f(0) = 1 - a >= 0 quad => quad a <= 1 $

  注意 $f(0) = 1 - a$：只有 $a = 1$ 时端点取等；当 $a < 1$ 时 $f(0) > 0$，端点*不取等*，端点效应失效。若强行用 $f'(0) >= 0$，会得到 $2a + 1 >= 0$ 即 $a >= -1/2$，而正确答案可以小到 $-e^(3/2)/2 ≈ -2.24$（见下），可见端点效应在本题完全失效。

  === 法 1（分离参数，注意系数变号）

  原不等式即

  $ a (2x - 1) >= -e^x quad (x >= 0) $

  参数 $a$ 的系数 $2x - 1$ 在 $x = 1/2$ 处变号，所以分三段讨论。

  *① 当 $x = 1/2$ 时：* 左边为 $0$，右边为 $-e^(1/2) < 0$，不等式恒成立，对 $a$ 没有限制。

  *② 当 $x > 1/2$ 时：* $2x - 1 > 0$，两边同除 $2x - 1$：

  $ a >= -e^x/(2x - 1) $

  要让它对一切 $x > 1/2$ 成立，只需 $a$ 不小于右边函数的最大值。令

  $ g(x) = -e^x/(2x - 1) quad (x > 1/2) $

  求导：

  $ g'(x) = -(e^x (2x - 3))/(2x - 1)^2 $

  所以 $g(x)$ 在 $(1/2, 3/2)$ 上单调递增、在 $(3/2, +oo)$ 上单调递减，最大值为

  $ g(3/2) = -e^(3/2)/(2 dot 3/2 - 1) = -e^(3/2)/2 $

  故

  $ a >= -e^(3/2)/2 $

  *③ 当 $0 <= x < 1/2$ 时：* $2x - 1 < 0$，两边同除 $2x - 1$（不等号反向）：

  $ a <= e^x/(1 - 2x) $

  同理只需 $a$ 不超过右边函数的最小值。令

  $ q(x) = e^x/(1 - 2x) quad (0 <= x < 1/2) $

  求导：

  $ q'(x) = (e^x (1 - 2x) + 2 e^x)/(1 - 2x)^2 = (e^x (3 - 2x))/(1 - 2x)^2 > 0 $

  所以 $q(x)$ 单调递增，最小值为

  $ q(0) = 1 $

  故

  $ a <= 1 $

  综合 ①②③ 得

  $ -e^(3/2)/2 <= a <= 1 $

  === 法 2（图象与切线）

  把原不等式写成

  $ e^x >= a (1 - 2x) quad 即 quad e^x >= k (x - 1/2) quad (k = -2a) $

  也就是说：曲线 $y = e^x$ 要恒在*过定点 $P(1/2, 0)$、斜率为 $k$* 的直线之上（$x >= 0$）。由图象，$k$ 被两条临界直线夹住：

  - *上临界（切线）*：设该直线与曲线相切于 $(t, e^t)$，则切线为 $y - e^t = e^t (x - t)$，它过 $P(1/2, 0)$，代入得

  $ -e^t = e^t (1/2 - t) quad => quad t = 3/2 $

  故上临界斜率为 $k = e^(3/2)$（切点为 $(3/2, e^(3/2))$），于是 $k <= e^(3/2)$。
  - *下临界*：直线过 $P$ 且过曲线的左端点 $(0, 1)$，此时斜率 $k = (1 - 0)/(0 - 1/2) = -2$，于是 $k >= -2$。

  所以

  $ -2 <= k <= e^(3/2) quad => quad -2 <= -2a <= e^(3/2) quad => quad -e^(3/2)/2 <= a <= 1 $

  #v(0.2em)

  #align(center)[
    #block(breakable: false, width: 250pt, height: 190pt)[
      #let seg(a, b, stroke: 0.8pt) = place(dx: a.at(0), dy: a.at(1))[
        #line(end: (b.at(0) - a.at(0), b.at(1) - a.at(1)), stroke: stroke)
      ]
      #seg((25pt, 155pt), (245pt, 155pt))
      #seg((245pt, 155pt), (237pt, 151pt))
      #seg((245pt, 155pt), (237pt, 159pt))
      #seg((40pt, 175pt), (40pt, 25pt))
      #seg((40pt, 25pt), (36pt, 33pt))
      #seg((40pt, 25pt), (44pt, 33pt))
      // 曲线 y = e^x
      #let pts = range(0, 18).map(i => {
        let x = i * 0.1
        (40pt + 115pt * x, 155pt - 26pt * calc.exp(x))
      })
      #place(dx: 0pt, dy: 0pt)[
        #curve(curve.move(pts.first()), ..pts.slice(1).map(p => curve.line(p)), stroke: 0.9pt)
      ]
      // 过 P(1/2,0) 且过 (0,1) 的直线（斜率 -2）
      #seg((40pt, 129pt), (172pt, 189pt), stroke: (paint: black, thickness: 0.7pt, dash: "dotted"))
      // 过 P(1/2,0) 的切线（斜率 e^(3/2)，切于 (3/2, e^(3/2))）
      #seg((97.5pt, 155pt), (241pt, 9pt), stroke: (paint: black, thickness: 0.8pt, dash: "dashed"))
      #seg((212.5pt, 155pt), (212.5pt, 38.5pt), stroke: (paint: black, thickness: 0.6pt, dash: "dotted"))
      #place(dx: 96pt, dy: 153.5pt)[#circle(radius: 1.6pt, fill: black)]
      #place(dx: 211pt, dy: 37pt)[#circle(radius: 1.6pt, fill: black)]
      #place(dx: 42pt, dy: 105pt)[$y = e^x$]
      #place(dx: 33pt, dy: 158pt)[$O$]
      #place(dx: 32pt, dy: 126pt)[$1$]
    ]
  ]

  #align(center)[#text(size: 9pt, fill: gray)[曲线 $y = e^x$ 要恒在过定点 $P(1/2, 0)$ 的直线 $y = k (x - 1/2)$ 上方；两条临界直线：过 $(0, 1)$ 的（斜率 $-2$）与在 $(3/2, e^(3/2))$ 处相切的切线（斜率 $e^(3/2)$）]]

  #line(length: 100%, stroke: 0.5pt + gray)

  === 法 3（先同除 $e^x$，化为 $a dot h(x) >= -1$）

  把原不等式移项：

  $ 2a x - a >= -e^x $

  因为 $e^x > 0$ 恒成立，两边同除 $e^x$（不等号方向不变）：

  $ (2a x - a)/e^x >= -1 quad 即 quad a dot (2x - 1) e^(-x) >= -1 quad (x >= 0) $

  记

  $ h(x) = (2x - 1) e^(-x) quad (x >= 0) $

  则条件化为

  $ a dot h(x) >= -1 quad (x >= 0) $

  先研究 $h(x)$。求导：

  $ h'(x) = 2 e^(-x) - (2x - 1) e^(-x) = e^(-x) (3 - 2x) $

  所以 $h(x)$ 在 $(0, 3/2)$ 上单调递增、在 $(3/2, +oo)$ 上单调递减，关键值：

  $ h(0) = -1, quad h(1/2) = 0, quad h(3/2) = 2 e^(-3/2), quad lim_(x -> +oo) h(x) = 0^+ $

  也就是说：$h$ 在 $[0, 1/2)$ 上取*负值*（从 $-1$ 升到 $0^-$），在 $(1/2, +oo)$ 上取*正值*（先升到最大值 $2 e^(-3/2)$，再降到 $0^+$）。于是按 $h$ 的符号分段：

  - *$x = 1/2$：* $h = 0$，不等式成为 $0 >= -1$，恒成立，对 $a$ 无限制；
  - *$x > 1/2$（$h(x) > 0$）：* 两边同除 $h(x)$ 得 $a >= -1/h(x)$，要恒成立只需

  $ a >= -1/(max h) = -1/(2 e^(-3/2)) = -e^(3/2)/2 $

  - *$0 <= x < 1/2$（$h(x) < 0$）：* 两边同除 $h(x)$，*不等号反向*，得 $a <= -1/h(x)$，要恒成立只需

  $ a <= min_(0 <= x < 1/2) (-1/h(x)) = -1/h(0) = 1 $

  （因为 $h$ 在 $[0, 1/2)$ 上从 $-1$ 递增到 $0^-$，而 $t -> -1/t$ 在 $t < 0$ 时递增，故 $-1/h$ 的最小值在 $x = 0$ 处取得。）

  于是同样得到

  $ -e^(3/2)/2 <= a <= 1 $

  （与法 1 相比：这里第一步同除的是恒正的 $e^x$，不需要讨论符号；代价是最后要按 $h$ 的符号分段。两种写法本质相同，法 3 把"讨论参数系数的符号"换成了"讨论 $h(x) = (2x - 1) e^(-x)$ 的符号与最值"。）

  #v(0.2em)

  *验证（充分性）。* 法 1 中两个最值都*能取到*：$x > 1/2$ 时 $-e^x/(2x - 1)$ 的最大值是 $-e^(3/2)/2$（在 $x = 3/2$），$0 <= x < 1/2$ 时 $e^x/(1 - 2x)$ 的最小值是 $1$（在 $x = 0$）。所以 $a in [-e^(3/2)/2, 1]$ 时原不等式恒成立；若 $a$ 超出这个范围，在 $x = 3/2$ 或 $x = 0$ 处就会出现负值。故该范围既必要又充分。

  两个分界情形（可作为验算）：

  - $a = 1$ 时 $f(x) = e^x + 2x - 1$，$f(0) = 0$ 且 $f'(x) = e^x + 2 > 0$，故 $f(x) >= 0$，等号仅在 $x = 0$；
  - $a = -e^(3/2)/2$ 时 $2a = -e^(3/2)$，$f(x) = e^x - e^(3/2) (x - 1/2)$，这正是"曲线与切线"的关系，等号仅在 $x = 3/2$ 处成立。

  综上，$a$ 的取值范围为

  $ [-e^(3/2)/2, 1] $

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】两端同时为零的"退化端点"

  *【例 2】* 已知函数 $f(x) = e^x cos x$，$g(x) = sin x + 1$。当 $x in [-pi/2, 0]$ 时，若 $f(x) >= m g(x)$ 恒成立，求 $m$ 的取值范围。

  === 解析

  *① 先看两端的值。* 在 $[-pi/2, 0]$ 上 $cos x >= 0$，故

  $ f(x) = e^x cos x >= 0, quad g(x) = 1 + sin x in [0, 1] $

  两端分别为

  $ f(-pi/2) = 0, quad g(-pi/2) = 0, quad f(0) = 1, quad g(0) = 1 $

  - *左端点* $x = -pi/2$：不等式成为 $0 >= m dot 0$，对一切 $m$ 都成立，*得不到任何限制*——两端同时为 $0$，这里是一个"退化"的端点，端点效应无从谈起；
  - *右端点* $x = 0$：$1 >= m dot 1$，得到 $m <= 1$（这只是必要条件）。

  *② 分离参数。* 当 $x in (-pi/2, 0]$ 时 $g(x) = 1 + sin x > 0$，两边同除 $g(x)$：

  $ m <= (e^x cos x)/(1 + sin x) quad (x in (-pi/2, 0]) $

  要让它恒成立，只需 $m$ 不超过右边函数的最小值。令

  $ phi(x) = (e^x cos x)/(1 + sin x) quad (x in (-pi/2, 0]) $

  求导（记 $u = (cos x)/(1 + sin x)$，则 $u' = -1/(1 + sin x)$，因为 $(1 + sin x)' = cos x$）：

  $ phi'(x) = e^x [ (cos x)/(1 + sin x) - 1/(1 + sin x) ] = (e^x (cos x - 1))/(1 + sin x) $

  在 $(-pi/2, 0)$ 上 $cos x - 1 < 0$、$1 + sin x > 0$、$e^x > 0$，所以 $phi'(x) < 0$，即 $phi(x)$ 单调递减，最小值在右端点取得：

  $ phi(0) = (e^0 cos 0)/(1 + sin 0) = 1 $

  （左端点处 $phi(x) -> +oo$：$cos x -> 0^+$ 而 $1 + sin x -> 0^+$ 且是二阶小量。）

  于是

  $ m <= 1 quad 即 quad m in (-oo, 1] $

  *充分性*：当 $m <= 1$ 时，对 $x in (-pi/2, 0]$ 有 $phi(x) >= 1 >= m$，即 $e^x cos x >= m (1 + sin x)$；左端点 $x = -pi/2$ 处两边都是 $0$，不等式也成立。故 $m <= 1$ 确实充分。

  *另解（半角公式，不用求导）*：由半角公式

  $ 1 + sin x = (sin(x/2) + cos(x/2))^2, quad cos x = cos^2(x/2) - sin^2(x/2) $

  两式相除得

  $ (cos x)/(1 + sin x) = (cos(x/2) - sin(x/2))/(cos(x/2) + sin(x/2)) = tan(pi/4 - x/2) $

  所以

  $ phi(x) = e^x tan(pi/4 - x/2) quad (x in (-pi/2, 0]) $

  当 $x$ 从 $-pi/2$ 增大到 $0$ 时：$e^x$ 是*正的减函数*；$pi/4 - x/2$ 从 $pi/2$ 减小到 $pi/4$，而 $tan$ 在 $(pi/4, pi/2)$ 上递增，所以 $tan(pi/4 - x/2)$ 也是*正的减函数*。两个正减函数之积仍然递减，故 $phi$ 递减，最小值同样为

  $ phi(0) = e^0 tan(pi/4) = 1 $

  与上面结论一致。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】端点取等、但一次端点效应失效

  *【例 3】* 已知函数 $f(x) = e^x + a x^2 - x$。当 $x >= 0$ 时，若 $f(x) >= 1/2 x^3 + 1$ 恒成立，求 $a$ 的取值范围。

  === 解析

  把不等式整理成"一边为零"的形式，令

  $ F(x) = e^x + a x^2 - x - 1/2 x^3 - 1 quad (x >= 0) $

  题意即 $F(x) >= 0$ 对一切 $x >= 0$ 成立。

  *① 端点处：取等，但一次端点效应失效。*

  $ F(0) = 1 - 1 = 0, quad F'(x) = e^x + 2a x - 1 - 3/2 x^2 quad => quad F'(0) = 1 - 1 = 0 $

  端点取等（$F(0) = 0$），却连一阶导数也为 $0$，所以一次端点效应给不出任何信息——这又是一处"失效"。按前面"注意"里说的，看二阶导数：

  $ F''(x) = e^x + 2a - 3x quad => quad F''(0) = 1 + 2a >= 0 quad => quad a >= -1/2 $

  这只是*必要条件*（二次端点效应的结论），并不充分：取 $a = -1/2$，则

  $ F(1) = e - 1/2 - 1 - 1/2 - 1 = e - 3 < 0 $

  不满足恒成立。可见真正的范围比 $a >= -1/2$ 窄，必须另找办法。

  *② 分离参数，求 $a$ 的下界。* 当 $x > 0$ 时，把不等式写成

  $ a x^2 >= 1/2 x^3 + 1 + x - e^x quad => quad a >= psi(x) = (1/2 x^3 + 1 + x - e^x)/x^2 $

  所以只需 $a >= max psi(x)$。求导：

  $ psi'(x) = ((3/2 x^2 + 1 - e^x) x^2 - (1/2 x^3 + 1 + x - e^x) dot 2x)/x^4 = (1/2 x^3 - x - 2 + e^x (2 - x))/x^3 $

  把分子分解因式：

  $ 1/2 x^3 - x - 2 + e^x (2 - x) = (x - 2)(1/2 x^2 + x + 1 - e^x) $

  （因为 $(x - 2)(1/2 x^2 + x + 1) = 1/2 x^3 - x - 2$。）而由基准不等式

  $ e^x >= 1 + x + 1/2 x^2 quad (x >= 0) $

  （等号仅在 $x = 0$ 时成立）知第二个因式 $<= 0$，于是

  - 当 $0 < x < 2$ 时，$x - 2 < 0$，与另一个因式相乘为正，故 $psi'(x) > 0$；
  - 当 $x > 2$ 时，$x - 2 > 0$，乘积为负，故 $psi'(x) < 0$。

  所以 $psi(x)$ 在 $(0, 2)$ 上单调递增、在 $(2, +oo)$ 上单调递减，最大值在 $x = 2$ 处取得：

  $ psi(2) = (4 + 1 + 2 - e^2)/4 = (7 - e^2)/4 $

  故

  $ a >= (7 - e^2)/4 ≈ -0.0973 $

  *③ 验证。* 取 $a = (7 - e^2)/4$（记 $4a = 7 - e^2$），则

  $ F(2) = e^2 + 4a - 2 - 4 - 1 = e^2 + (7 - e^2) - 7 = 0 $

  $ F'(2) = e^2 + 4a - 1 - 6 = e^2 + (7 - e^2) - 7 = 0, quad F''(2) = e^2 + 2a - 6 ≈ 1.19 > 0 $

  即 $x = 2$ 是 $F$ 的极小值点且 $F(2) = 0$：这一取值恰好让等号在 $x = 2$ 处成立。由 ② 的分析（$psi$ 的最大值就是它）知，$a$ 再小就会有 $F < 0$ 的点，故该下界既必要又充分。

  于是 $a$ 的取值范围为

  $ [(7 - e^2)/4, +oo) $

  （对照：二次端点效应只给出 $a >= -1/2$，而真正的下界是 $(7 - e^2)/4 ≈ -0.097$——端点效应给出的条件可能"远远不够"，必须回到分离参数求最值。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *先算端点值，判断是否"失效"*：本题 $f(0) = 1 - a$，由 $f(0) >= 0$ 得到 $a <= 1$；但 $a < 1$ 时端点不取等，端点效应失效——不能用 $f'(0) >= 0$ 探路（那会得到错误的 $a >= -1/2$，漏掉 $[-e^(3/2)/2, -1/2)$ 这一段）。
  - *分离参数要注意系数的符号*：本题 $a$ 的系数 $2x - 1$ 在 $x = 1/2$ 处变号，必须分成 $x = 1/2$、$x > 1/2$、$x < 1/2$ 三段；除正数不等号不变（求"最大值"），除负数不等号反向（求"最小值"）。
  - *图象与切线*：$e^x >= k (x - x_0)$ 型不等式即"曲线恒在过定点 $(x_0, 0)$ 的直线上方"，上临界由"切线过该定点"解出（本题切点 $t = 3/2$，斜率 $e^(3/2)$），下临界由端点处的高度决定（本题直线过 $(0, 1)$，斜率 $-2$）。
  - *同除恒正因式再分离（法 3）*：遇到含 $e^x$ 的不等式，可先把含 $a$ 的项移到一边、再同除 $e^x$，化成 $a dot h(x) >= -1$ 的结构；先研究 $h$ 的单调性、极值与*符号*，再按 $h$ 的正负分段解出 $a$（除负数要反向，求"最值"也随之变成求 $h$ 的最大值）。本题 $h(x) = (2x - 1) e^(-x)$，最大值 $h(3/2) = 2 e^(-3/2)$、$h(0) = -1$，正好把两侧临界都给出来。
  - *两端同时为零的"退化端点"（例 2）*：若不等式两端在端点处同时为 $0$（例 2 中 $f(-pi/2) = g(-pi/2) = 0$），端点给出的信息只是 $0 >= 0$，完全失效；此时必须*分离参数*，把问题化为求 $f/g$ 的最值（例 2 中 $phi(x) = (e^x cos x)/(1 + sin x)$，最小值在 $x = 0$ 处为 $1$）。
  - *三角函数与指数函数的乘积*：$(cos x)/(1 + sin x)$ 这类式子可用半角公式化为 $tan(pi/4 - x/2)$；再与 $e^x$ 相乘时，"正的减函数 $times$ 正的减函数仍递减"的结论可以完全绕开求导（例 2 的另解）。
  - *端点取等、但一次端点效应失效（例 3）*：若 $F(b) = F'(b) = 0$，一次端点效应给不出信息，就用二次端点效应（$F''(b) >= 0$）；但它同样只是必要条件——例 3 中它给出 $a >= -1/2$，而真正的下界是 $(7 - e^2)/4 ≈ -0.097$。发现条件"不够"时要立刻回到*分离参数*求最值。
  - *端点取等后先用端点效应缩小范围、再用分离参数收尾*：这是最稳妥的顺序——端点效应（必要时二次）给出粗略的必要条件，分离参数求出精确的最值（例 3 的关键是 $psi(2) = (7 - e^2)/4$，恰好是曲线的"相切"点 $x = 2$）。
  - *常用基准结论*（曲线与切线、泰勒型）：$e^x >= x + 1$（等号仅在 $x = 0$）、$e^x >= 1 + x + 1/2 x^2$（等号仅在 $x = 0$）、$ln x <= x - 1$（等号仅在 $x = 1$）、$e^x >= e x$（等号仅在 $x = 1$）等。
]
