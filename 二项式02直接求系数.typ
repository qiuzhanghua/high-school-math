#import "template.typ": *

#set document(title: [二项式求系数])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、直接求系数：用通项公式

  $(a + b)^n$ 的展开式共有 $n + 1$ 项，其通项为

  $ T_(k+1) = C_n^k a^(n-k) b^k quad (k = 0, 1, dots, n) $

  求"展开式中 $x^m$ 的系数"这类问题，步骤是固定的：

  1. 写出通项，把其中含 $x$ 的部分合并成一个幂 $x^(f(k))$（$f(k)$ 是关于 $k$ 的*一次式*）；
  2. 令 $f(k) = m$，解出 $k$；
  3. 若这个 $k$ 是 $0$ 到 $n$ 之间的整数，代回通项算系数；否则说明展开式中*没有* $x^m$ 项，系数为 $0$。

  两个最容易错的地方：

  - *"项的系数"不等于"二项式系数"*：$C_n^k$ 叫*二项式系数*，而*项的系数*还要把 $a^(n-k)$、$b^k$ 中的数字一起乘进去（例 1 中的 $(-sqrt(3))^4 = 9$、例 2 中的 $2^2 = 4$）；
  - *符号要跟着 $b$ 走*：$b$ 带负号时（如 $(x - sqrt(3))^10$），负号要一起算进 $b^k$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】$(x - sqrt(3))^10$ 中 $x^6$ 的系数

  *【例 1】* $(x - sqrt(3))^10$ 的展开式中，$x^6$ 的系数为 #box(width: 4em, stroke: (bottom: 0.5pt)) 。

  === 解析

  取 $a = x$、$b = -sqrt(3)$、$n = 10$，通项为

  $ T_(k+1) = C_10^k x^(10-k) (-sqrt(3))^k $

  要求 $x^6$，令

  $ 10 - k = 6 quad => quad k = 4 $

  $k = 4$ 在 $0$ 到 $10$ 之间，代回通项：

  $ C_10^4 (-sqrt(3))^4 = 210 times 9 = 1890 $

  故填 $1890$。

  （注意 $x^6$ 的*二项式系数*是 $C_10^4 = 210$，但*系数*还要乘 $(-sqrt(3))^4 = 9$，所以是 $1890$。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】$(x^2 + 2/x)^5$ 中 $x^4$ 的系数（选择题）

  *【例 2】* $(x^2 + 2/x)^5$ 的展开式中 $x^4$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $10$], [B. $20$], [C. $40$], [D. $80$],
  )

  === 解析

  取 $a = x^2$、$b = 2/x$、$n = 5$，通项为

  $ T_(k+1) = C_5^k (x^2)^(5-k) (2/x)^k = C_5^k 2^k x^(10-3k) $

  要求 $x^4$，令

  $ 10 - 3k = 4 quad => quad k = 2 $

  $k = 2$ 符合 $0$ 到 $5$ 的要求，代回通项：

  $ C_5^2 dot 2^2 = 10 times 4 = 40 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  （易错选项 $A$ 正是只写了二项式系数 $C_5^2 = 10$，忘了乘 $2^2$。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】$(1/x - 1)^5$ 中 $x^(-2)$ 的系数（选择题）

  *【例 3】* $(1/x - 1)^5$ 的展开式中 $x^(-2)$ 的系数是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $15$], [B. $-15$], [C. $10$], [D. $-10$],
  )

  === 解析

  取 $a = 1/x$、$b = -1$、$n = 5$，通项为

  $ T_(k+1) = C_5^k (1/x)^(5-k) (-1)^k = C_5^k (-1)^k x^(k-5) $

  要求 $x^(-2)$，令

  $ k - 5 = -2 quad => quad k = 3 $

  代回通项：

  $ C_5^3 (-1)^3 = 10 times (-1) = -10 $

  故选 *#text(fill: rgb("#c7362e"))[D]*。

  （$k = 3$ 是奇数，负号保留——这正是"符号跟着 $b$ 走"的典型。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】由 $x^2$ 的系数反求参数，再求 $x^5$ 的系数（选择题）

  *【例 4】* 若 $(a x + 1/(sqrt(x)))^8$ 的展开式中 $x^2$ 的系数为 $35/8$，则 $x^5$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $7/4$], [B. $7/8$], [C. $7/16$], [D. $7/32$],
  )

  === 解析

  *① 写通项，先把指数整理出来。*

  $ T_(k+1) = C_8^k (a x)^(8-k) (1/(sqrt(x)))^k = C_8^k a^(8-k) x^(8 - 3k/2) $

  *② 用 $x^2$ 的系数定出参数。* 令

  $ 8 - 3k/2 = 2 quad => quad k = 4 $

  于是 $x^2$ 的系数为 $C_8^4 a^4 = 70 a^4$，由题意

  $ 70 a^4 = 35/8 quad => quad a^4 = 1/16 quad => quad a^2 = 1/4 $

  （不必真的解出 $a$——注意 $a^6 = (a^2)^3 = (1/4)^3 = 1/64$。）

  *③ 再求 $x^5$ 的系数。* 令

  $ 8 - 3k/2 = 5 quad => quad k = 2 $

  系数为

  $ C_8^2 a^6 = 28 times 1/64 = 7/16 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  （$a$ 的正负不影响结果：$a^4$、$a^6$ 都是偶次方。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】"存在／任意"型判断（多选题）

  *【例 5】*（多选）对于二项式 $(1/x + x^3)^n$（$n in N^*$），以下判断正确的有（#h(0.8em)）

  - A. 存在 $n in N^*$，展开式中有常数项
  - B. 对任意 $n in N^*$，展开式中没有常数项
  - C. 对任意 $n in N^*$，展开式中没有含 $x$ 的项
  - D. 存在 $n in N^*$，展开式中含有 $x$ 的项

  === 解析

  *① 写通项，把 $x$ 的指数整理出来。*

  $ T_(k+1) = C_n^k (1/x)^(n-k) (x^3)^k = C_n^k x^(3k - (n-k)) = C_n^k x^(4k - n) quad (k = 0, 1, dots, n) $

  所以展开式中 $x$ 的指数只可能是 $4k - n$（$k = 0, 1, dots, n$）。

  *② 逐项判断。*

  - *常数项*（指数为 $0$）：需要 $4k - n = 0$，即 $n = 4k$。取 $k = 1$、$n = 4$ 即可——例如

  $ (1/x + x^3)^4 = 1/x^4 + 4 x^2 + 6 + 4 x^6 + x^12 $

  其中第 $3$ 项就是常数项 $6$。故 *A 正确*，而 B 说的"对任意 $n$"被 $n = 4$ 推翻，*B 错误*。
  - *含 $x$ 的项*（指数为 $1$）：需要 $4k - n = 1$，即 $n = 4k - 1$。取 $k = 1$、$n = 3$：

  $ (1/x + x^3)^3 = 1/x^3 + 3 x + 3 x^5 + x^9 $

  第 $2$ 项就是 $3x$。故 *D 正确*，而 C 被 $n = 3$ 推翻，*C 错误*。

  故选 *#text(fill: rgb("#c7362e"))[AD]*。

  （换句话说：有常数项 $<==>$ $n$ 是 $4$ 的倍数；有含 $x$ 的项 $<==>$ $n$ 除以 $4$ 余 $3$。只要举出一个符合条件的 $n$，"存在"型判断就成立；要否定"对任意"，也只需一个反例。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *通项是唯一工具*：$T_(k+1) = C_n^k a^(n-k) b^k$，求哪个幂次就让指数等于它、解出 $k$。
  - *先把指数整理成一个一次式*：把所有含 $x$ 的因式合并成 $x^(f(k))$，再令 $f(k) = m$（例 2 中 $f(k) = 10 - 3k$）。
  - *"系数"不等于"二项式系数"*：例 1 是 $C_10^4 dot 9 = 1890$（不是 $210$），例 2 是 $C_5^2 dot 4 = 40$（不是 $10$）。
  - *符号与常数一起算*：$b = -sqrt(3)$ 时 $b^4 = 9$（负号被偶次方"吃掉"）；若 $k$ 是奇数，结果就带负号。
  - *系数条件可以反求参数*（例 4）：先用通项把"某幂次的系数"写成含参数的式子，与已知值比较解出 $a^n$，再去求另一幂次的系数——多数时候不必真的把 $a$ 解出来。
  - *负号看 $k$ 的奇偶*（例 3）：$b = -1$ 时系数为 $C_n^k (-1)^k$，$k$ 取奇数结果就带负号。
  - *"存在／任意"型判断*（例 5）：把指数写成 $4k - n$ 后，"存在"只需举一个可行的 $n$（如 $n = 4k$、$n = 4k - 1$），"对任意"只需举一个反例——先算通项、再找特例，是最快的路子。
  - *先检验 $k$ 是否有效*：解出的 $k$ 必须是 $0$ 到 $n$ 的整数，否则该幂次在展开式中不出现、系数为 $0$。
]
