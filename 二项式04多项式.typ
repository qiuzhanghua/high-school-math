#import "template.typ": *

#set document(title: [二项式多项式])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 一、三项式（多项式）的展开

  二项式定理可以推广到"多个式子之和"的情形。最常用的三项式公式是

  $ (a + b + c)^n = sum_(k_1 + k_2 + k_3 = n) (n!)/(k_1! k_2! k_3!) a^(k_1) b^(k_2) c^(k_3) $

  求"展开式中某一项（如 $x^5 y^2$、常数项）的系数"，步骤与二项式完全一样：

  1. *写通项*：在 $k_1 + k_2 + k_3 = n$ 时，项为 $n!/(k_1! k_2! k_3!) a^(k_1) b^(k_2) c^(k_3)$；
  2. *列指数方程*：让各个字母的次数都恰好对上目标（几个字母就列几个方程）；
  3. *解出非负整数解*，把每一组解贡献的系数求出、再相加。

  另一条思路常常更快：*先分解因式，把三项式化回二项式*。例如

  $ x + 1/x - 2 = (x^2 - 2x + 1)/x = ((x-1)^2)/x $

  于是原式变成 $(x-1)^10/x^5$ 这样的形式，用二项式定理一步就能读出所需系数。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】$(x^2 + x + y)^5$ 中 $x^5 y^2$ 的系数（选择题）

  *【例 1】* $(x^2 + x + y)^5$ 的展开式中，$x^5 y^2$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $10$], [B. $20$], [C. $30$], [D. $40$],
  )

  === 解析

  把三个加项分别记为 $a = x^2$、$b = x$、$c = y$，通项为

  $ (5!)/(k_1! k_2! k_3!) (x^2)^(k_1) x^(k_2) y^(k_3) quad (k_1 + k_2 + k_3 = 5) $

  要得到 $x^5 y^2$，三个指数条件都要对上：

  $ k_3 = 2, quad 2 k_1 + k_2 = 5, quad k_1 + k_2 + k_3 = 5 $

  由后两式相减得 $k_1 = 2$，再代回得 $k_2 = 1$。于是所求系数为

  $ (5!)/(2! 1! 2!) = 120/4 = 30 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】$(x + 1/x - 2)^5$ 的常数项

  *【例 2】* $(x + 1/x - 2)^5$ 的常数项为 #box(width: 4em, stroke: (bottom: 0.5pt)) 。

  === 解析

  *法 1（按三项式做，用通项）*：取 $a = x$、$b = 1/x$、$c = -2$，通项为

  $ (5!)/(k_1! k_2! k_3!) x^(k_1) x^(-k_2) (-2)^(k_3) quad (k_1 + k_2 + k_3 = 5) $

  常数项要求 $x$ 的指数为 $0$，即 $k_1 = k_2$。记 $k_1 = k_2 = k$，则 $k_3 = 5 - 2k$，$k$ 只能取 $0$、$1$、$2$：

  - $k = 0$（此时 $k_3 = 5$）：$(5!)/(0! 0! 5!) (-2)^5 = -32$；
  - $k = 1$（此时 $k_3 = 3$）：$(5!)/(1! 1! 3!) (-2)^3 = 20 times (-8) = -160$；
  - $k = 2$（此时 $k_3 = 1$）：$(5!)/(2! 2! 1!) (-2)^1 = 30 times (-2) = -60$。

  相加得

  $ -32 - 160 - 60 = -252 $

  *法 2（先分解因式，化回二项式）*：

  $ x + 1/x - 2 = (x^2 - 2x + 1)/x = ((x-1)^2)/x $

  所以

  $ (x + 1/x - 2)^5 = ((x-1)^10)/(x^5) $

  于是原式的*常数项*恰好是 $(x-1)^10$ 中 $x^5$ 的系数：

  $ C_10^5 (-1)^5 = -252 $

  两法一致，故常数项为 $-252$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 练习

  *【练 1】* $(x^2 + 3y - y^2)^7$ 的展开式中 $x^12 y^2$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $7$], [B. $-7$], [C. $42$], [D. $-42$],
  )

  === 解析

  把三个加项记为 $a = x^2$、$b = 3y$、$c = -y^2$，通项为

  $ (7!)/(k_1! k_2! k_3!) (x^2)^(k_1) (3y)^(k_2) (-y^2)^(k_3) quad (k_1 + k_2 + k_3 = 7) $

  要得到 $x^12 y^2$：

  - $x$ 的次数：$2 k_1 = 12$，得 $k_1 = 6$；
  - $y$ 的次数：$k_2 + 2 k_3 = 2$，又 $k_2 + k_3 = 7 - 6 = 1$，两式相减得 $k_3 = 1$、$k_2 = 0$。

  代回通项：

  $ (7!)/(6! 0! 1!) dot 3^0 dot (-1)^1 = 7 times (-1) = -7 $

  故选 *#text(fill: rgb("#c7362e"))[B]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  *【练 2】* $(x - 1 + p x^2)^6$ 的展开式中 $x^5$ 项系数最大时，$p$ 的值为 #box(width: 3em, stroke: (bottom: 0.5pt)) 。

  === 解析

  设展开式中取出 $x^(k_1)$、$(-1)^(k_2)$、$(p x^2)^(k_3)$（$k_1 + k_2 + k_3 = 6$），则 $x$ 的次数为 $k_1 + 2 k_3$，该组贡献的系数是

  $ (6!)/(k_1! k_2! k_3!) (-1)^(k_2) p^(k_3) $

  *① 找出所有能给出 $x^5$ 的组合。* 由 $k_1 + 2 k_3 = 5$ 与 $k_1 + k_2 + k_3 = 6$ 得三组非负整数解：

  - $(k_1, k_2, k_3) = (5, 1, 0)$：系数 $(6!)/(5! 1! 0!) (-1)^1 = -6$；
  - $(3, 2, 1)$：系数 $(6!)/(3! 2! 1!) (+1) p = 60 p$；
  - $(1, 3, 2)$：系数 $(6!)/(1! 3! 2!) (-1)^3 p^2 = -60 p^2$。

  *② 写成 $p$ 的函数再求最值。* 所以 $x^5$ 项的系数是

  $ f(p) = -60 p^2 + 60 p - 6 = -60 (p - 1/2)^2 + 9 $

  它是开口向下的抛物线，在 $p = 1/2$ 时取到最大值 $9$。

  故填 $1/2$。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *三项式的通项*：$(a+b+c)^n$ 的项为 $n!/(k_1! k_2! k_3!) a^(k_1) b^(k_2) c^(k_3)$，其中 $k_1 + k_2 + k_3 = n$——它是二项式通项的推广。
  - *几个字母列几个方程*：字母的次数要逐一对应（例 1 用了 $k_3 = 2$、$2k_1 + k_2 = 5$、$k_1+k_2+k_3 = 5$ 三个条件）。
  - *系数可能来自好几组解*：常数项、对称项往往有多组非负整数解，要把它们*全部*列出再相加（例 2 法 1 的 $k = 0, 1, 2$）。
  - *能分解因式就先分解*：把三项式整理成"某个式子的平方再除以 $x$"的形式（如例 2 中 $(x-1)^2/x$），就能化回二项式，运算量立刻降下来（例 2 法 2 一步得 $-C_10^5 = -252$）。
  - *同一个目标项可能有多组解*（练 1、练 2）：把满足指数条件的非负整数解*全部*列出再分别算系数——练 2 的三组解就分别贡献 $-6$、$60p$、$-60p^2$。
  - *"系数最大"型就是求函数最值*（练 2）：把目标项的系数写成关于参数的式子（这里是 $p$ 的二次函数），用配方或顶点公式求最值即可。
  - *注意"常数项"的等价说法*：$(F(x))^n$ 的常数项就是分子展开后 $x$ 的指数为 $0$ 的项，也等于"分母的幂次"所对应的那一项系数。
]
