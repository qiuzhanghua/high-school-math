#import "template.typ": *

#set document(title: [非对称韦达定理])

#template(heading2-align: left, page-footer: "第 1 页")[

  = 题目

  已知椭圆 $E: x^2/a^2 + y^2/b^2 = 1 space (a > 0, b > 0)$ 的左右顶点分别为 $A, B$，离心率为 $sqrt(3)/2$，过点 $P(1, 0)$ 作直线交椭圆于点 $C, D$（与 $A, B$ 均不重合），当点 $D$ 与椭圆 $E$ 的上顶点重合时，$|A D| = sqrt(5)$。

  1. 求椭圆方程。
  2. 设直线 $A D$、$B C$ 的斜率分别为 $k_1, k_2$，求证：$k_1 / k_2$ 为定值。

  === 第1问：求椭圆方程

  $ e = c / a = sqrt(3) / 2 $
  $ c = sqrt(3) / 2 a $
  $ b^2 = a^2 - c^2 $
  $ b = a / 2 $

  左右顶点为 $A (-a, 0)$，$B (a, 0)$，上顶点为 $D_0 (0, b)$。

  当 $D$ 与上顶点重合时，$D = (0, b)$，此时
  $ |A D| = sqrt((0 + a)^2 + (b - 0)^2) = sqrt(a^2 + b^2) $

  代入 $b = a/2$：
  $ sqrt(a^2 + a^2/4) = sqrt(5 a^2 / 4) = sqrt(5) / 2 a = sqrt(5) $

  解得 $a = 2$，从而 $b = 1$。所以椭圆方程为
  $ x^2 / 4 + y^2 = 1 $

  #line(length: 100%, stroke: 0.5pt + gray)

  === 第2问：证明 $k_1 / k_2$ 为定值

  由第1问，椭圆为
  $ x^2 / 4 + y^2 = 1 $，
  顶点为 $A (-2, 0)$，$B (2, 0)$。

  设过点 $P (1, 0)$ 的直线为
  $x = t y + 1$，
  与椭圆交于 $C (x_1, y_1)$、$D (x_2, y_2)$。

  代入椭圆方程：
  $ (t y + 1)^2 / 4 + y^2 = 1 $

  $ (t y + 1)^2 + 4 y^2 = 4 $

  $ t^2 y^2 + 2 t y + 1 + 4 y^2 - 4 = 0 $

  整理得：
  $ (t^2 + 4) y^2 + 2 t y - 3 = 0 $

  由韦达定理：
  $ y_1 + y_2 = - (2 t) / (t^2 + 4) $
  $ y_1 y_2 = - 3 / (t^2 + 4) $

  于是有比例关系：
  $ t y_1 y_2 = 3 / 2 (y_1 + y_2) $

  直线 $A D$ 的斜率（$A (-2, 0)$，$D (x_2, y_2)$）：
  $ k_1 = y_2 / (x_2 + 2) = y_2 / (t y_2 + 1 + 2) = y_2 / (t y_2 + 3) $

  直线 $B C$ 的斜率（$B (2, 0)$，$C (x_1, y_1)$）：
  $ k_2 = y_1 / (x_1 - 2) = y_1 / (t y_1 + 1 - 2) = y_1 / (t y_1 - 1) $

  因此
  $
    k_1 / k_2 = frac(y_2 / (t y_2 + 3), y_1 / (t y_1 - 1))
    = (y_2 (t y_1 - 1)) / (y_1 (t y_2 + 3))
  $

  展开：
  $ = (t y_1 y_2 - y_2) / (t y_1 y_2 + 3 y_1) $

  利用比例关系 $t y_1 y_2 = 3 / 2 (y_1 + y_2)$ 代入：

  分子：
  $ t y_1 y_2 - y_2 = 3 / 2 (y_1 + y_2) - y_2 = 3 / 2 y_1 + 1 / 2 y_2 $

  分母：
  $ t y_1 y_2 + 3 y_1 = 3 / 2 (y_1 + y_2) + 3 y_1 = 9 / 2 y_1 + 3 / 2 y_2 $

  所以
  $
    k_1 / k_2 = (3 / 2 y_1 + 1 / 2 y_2) / (9 / 2 y_1 + 3 / 2 y_2)
    = (3 y_1 + y_2) / (9 y_1 + 3 y_2)
    = (3 y_1 + y_2) / (3 (3 y_1 + y_2))
    = 1 / 3
  $

  为定值，得证。

]
