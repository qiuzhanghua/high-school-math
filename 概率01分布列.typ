#import "template.typ": *

#set document(title: [离散型随机变量的分布列])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 【例 1】"放回 5 个红球"对应哪个事件（选择题）

  *【例 1】* 袋中装有 $10$ 个红球、$5$ 个黑球。每次随机抽取 $1$ 个球，若取得黑球则另换 $1$ 个红球放回袋中，直到取到红球为止。若抽取的次数为 $xi$，则表示事件"放回 $5$ 个红球"的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $xi = 4$], [B. $xi = 5$], [C. $xi = 6$], [D. $xi <= 5$],
  )

  === 解析

  *① 弄清"另换 1 个红球放回"是什么意思。* 每次抽到黑球，就把这个黑球*换成*红球放回袋中——黑球不再回去，同时多出一个红球。所以

  - 抽到 $1$ 次黑球 $<==>$ 放回 $1$ 个红球（红球多 $1$ 个、黑球少 $1$ 个，而*球的总数始终是 $15$*）；
  - "放回 $5$ 个红球" $<==>$ *抽到过 $5$ 次黑球*。

  *② 把"抽到 5 次黑球"翻译成 $xi$ 的取值。* 过程"直到取到红球为止"，一取到红球就停止，所以：

  - 若共抽到 $5$ 次黑球，则前 $5$ 次全是黑球，此时袋中已全是红球，第 $6$ 次必取到红球，于是

  $ xi = 6; $

  - 反过来，$xi = 6$ 说明前 $5$ 次都是黑球，也恰好放回 $5$ 个红球。

  所以"放回 $5$ 个红球"就是事件 $xi = 6$，选 *#text(fill: rgb("#c7362e"))[C]*。

  *③ 其余选项为什么不对。*

  - A（$xi = 4$）、B（$xi = 5$）分别表示只抽到 $3$ 次、$4$ 次黑球，即放回 $3$ 个、$4$ 个红球；
  - D（$xi <= 5$）表示前 $5$ 次内就出现了红球，最多放回 $4$ 个红球（$xi = 1$ 时一个也没放回）。

  *④ 顺便算出它的概率。* 前 $5$ 次全黑、第 $6$ 次必红；注意球的总数始终是 $15$，所以每一步抽到黑球的概率分母都是 $15$：

  $ P(xi = 6) = 5/15 dot 4/15 dot 3/15 dot 2/15 dot 1/15 = 120/759375 = 8/50625. $

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *先把条件翻译清楚，再定 $xi$ 的取值*："放回 $k$ 个红球" $<==>$ "抽到过 $k$ 次黑球" $<==>$ "前 $k$ 次全黑、第 $k+1$ 次为红"，即 $xi = k + 1$。
  - *"换"是"取走黑的、放入红的"*：球的总数不变（始终 $15$ 个），所以每一步的概率分母都是 $15$。若误以为总数在减少，就会写成 $4/14$、$3/13$ 这样的错误分母。
  - *黑球只有 $5$ 个*：最多放回 $5$ 个红球，此时袋中全是红球，第 $6$ 次必然取到红球——这就是 $xi$ 的最大取值（$xi$ 的取值只有 $1, 2, 3, 4, 5, 6$）。
  - *"$xi <= 5$"这类范围型选项要当心*：它对应"至多放回 $4$ 个红球"，与"恰好 $5$ 个"不是一回事。
  - *求分布列的套路*：先定 $xi$ 的全部取值，再对每个取值写出"前若干次抽黑、最后一次抽红"的概率连乘（每一步的分母都用当时的球数）。
  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】哪张表能作为分布列（选择题）

  *【例 2】* 下列表中能成为随机变量 $X$ 的分布列的是（#h(0.8em)）

  #align(center)[
    #grid(
      columns: (1fr, 1fr),
      gutter: 24pt,
      row-gutter: 14pt,
    [
      A.
      #table(columns: 4, stroke: 0.5pt, inset: 5pt,
        [$X$], [$-1$], [$0$], [$1$], 
        [$P$], [$0.3$], [$0.4$], [$0.4$], 
      )
    ],
    [
      B.
      #table(columns: 4, stroke: 0.5pt, inset: 5pt,
        [$X$], [$1$], [$2$], [$3$], 
        [$P$], [$0.4$], [$0.7$], [$-0.1$], 
      )
    ],
    [
      C.
      #table(columns: 4, stroke: 0.5pt, inset: 5pt,
        [$X$], [$-1$], [$0$], [$1$], 
        [$P$], [$0.3$], [$0.4$], [$0.3$], 
      )
    ],
    [
      D.
      #table(columns: 4, stroke: 0.5pt, inset: 5pt,
        [$X$], [$0.1$], [$0.1$], [$0.7$], 
        [$P$], [$0.3$], [$0.4$], [$0.5$], 
      )
    ],
    )
  ]

  === 解析

  一张表能成为分布列，要满足两条：

  (1) *每个概率都不小于 $0$*（当然也不大于 $1$）：$p_i >= 0$；

  (2) *全部概率之和等于 $1$*：$sum p_i = 1$。

  另外，$X$ 的取值应当*互不相同*——同一个值出现两次时，应把对应的概率合并。

  逐项检查：

  - *A*：概率都不小于 $0$，但 $0.3 + 0.4 + 0.4 = 1.1 != 1$，*不行*；
  - *B*：$0.4 + 0.7 + (-0.1) = 1$，和是 $1$，但出现了*负概率* $-0.1$，*不行*；
  - *C*：$0.3$、$0.4$、$0.3$ 都非负，且 $0.3 + 0.4 + 0.3 = 1$，*可以*；
  - *D*：不仅 $0.3 + 0.4 + 0.5 = 1.2 != 1$，而且 $X$ 的取值 $0.1$ 出现了两次（应合并），*不行*。

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  === 要点

  - *两个基本条件*：$p_i >= 0$ 与 $sum p_i = 1$——判断"能否成为分布列"就核对这两条（B 错在第一条，A、D 错在第二条）。
  - *$X$ 的取值要互不相同*：同一个值出现两次说明表还没整理好，合并概率后再检查上面两条。
  - *别只看求和*：$B$ 的和恰好是 $1$，很有迷惑性，但概率为负就不可能。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】先求参数，再算事件概率

  *【例 3】* 设随机变量 $X$ 的分布列如下，则 $P(|X - 3| = 1)$ 等于 #box(width: 3em, stroke: (bottom: 0.5pt)) 。

  #align(center)[
    #table(
      columns: 5,
      stroke: 0.5pt,
      inset: 6pt,
      [$X$], [$1$], [$2$], [$3$], [$4$],
      [$P$], [$1/3$], [$m$], [$1/4$], [$1/6$],
    )
  ]

  === 解析

  *① 先用"概率之和为 $1$"求出 $m$。* 由分布列的基本条件

  $ 1/3 + m + 1/4 + 1/6 = 1, $

  而

  $ 1/3 + 1/4 + 1/6 = 4/12 + 3/12 + 2/12 = 9/12 = 3/4, $

  所以

  $ m = 1 - 3/4 = 1/4. $

  （$m = 1/4$ 落在 $[0, 1]$ 内，分布列合法。）

  *② 把事件 $|X - 3| = 1$ 翻译成 $X$ 的取值。*

  $ |X - 3| = 1 quad <=> quad X - 3 = 1 "或" X - 3 = -1 quad <=> quad X = 4 "或" X = 2. $

  *③ 用互斥事件相加。* "$X = 2$"与"$X = 4$"不可能同时发生，于是

  $ P(|X - 3| = 1) = P(X = 2) + P(X = 4) = m + 1/6 = 1/4 + 1/6 = 3/12 + 2/12 = 5/12. $

  故填 $5/12$。

  === 要点

  - *分布列里带字母时，先用 $sum p_i = 1$ 定字母*，再算所求概率（本题得 $m = 1/4$）。
  - *把事件化成 $X$ 的取值集合*：$|X - 3| = 1 <=> X in {2, 4}$，再用"互斥事件概率相加"求概率。
  - *形如 $|X - a| = b$、$X^2 = c$、"$X$ 为奇数/偶数"的事件*，都先解出取值、再查表相加。
  - *检查合理性*：结果应落在 $[0, 1]$ 内（本题 $5/12 approx 0.417$）。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】写分布列（计数法）

  *【例 4】* 一袋中装有 $5$ 只球，编号为 $1, 2, 3, 4, 5$，从袋中同时取出 $3$ 只，以 $X$ 表示取出的 $3$ 只球中的最大号码，写出随机变量 $X$ 的分布列。

  === 解析

  *① 确定 $X$ 的所有可能取值。* 取 $3$ 只球，最大号码最小是 $3$、最大是 $5$，所以

  $ X in {3, 4, 5} $

  ——注意 $X$ 不会取到 $1$、$2$。

  *② 求出总的取法数。* 从 $5$ 只球中同时取 $3$ 只，共有

  $ C_5^3 = 10 $

  种等可能的取法。

  *③ 逐个求概率。* 关键是：$X = k$ 表示"$k$ 被取到，另外 $2$ 只从比 $k$ 小的球中取"。

  - $X = 3$：必须取到 $3$，另 $2$ 只从 $1, 2$ 中取，有 $C_2^2 = 1$ 种，

  $ P(X = 3) = 1/10; $

  - $X = 4$：取到 $4$，另 $2$ 只从 $1, 2, 3$ 中取，有 $C_3^2 = 3$ 种，

  $ P(X = 4) = 3/10; $

  - $X = 5$：取到 $5$，另 $2$ 只从 $1, 2, 3, 4$ 中取，有 $C_4^2 = 6$ 种，

  $ P(X = 5) = 6/10 = 3/5. $

  *④ 检验并作答。* $1 + 3 + 6 = 10$，正好是全部取法，概率之和为 $1$，故分布列为

  #align(center)[
    #table(
      columns: 4,
      stroke: 0.5pt,
      inset: 6pt,
      [$X$], [$3$], [$4$], [$5$],
      [$P$], [$1/10$], [$3/10$], [$3/5$],
    )
  ]

  === 要点

  - *先定取值范围*：$X$ 是"取出的 3 只球中的最大号码"，只能取 $3, 4, 5$。
  - *"$X = k$"的计数套路*：先"固定"取到 $k$，再从比 $k$ 小的 $k - 1$ 只球中取剩下的 $2$ 只，共 $C_(k-1)^2$ 种。
  - *古典概型*："同时取球"是等可能的组合，概率等于"有利取法数除以总取法数"（总取法数 $C_5^3 = 10$）。
  - *最后一定要检验*：各概率之和为 $1$（本题 $1 + 3 + 6 = 10$，正好对应全部取法）。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】抽奖问题（中奖概率 + 分布列）

  *【例 5】* 在一次购物抽奖活动中，$10$ 张奖券中有一等奖券 $1$ 张（可获价值 $50$ 元的奖品），二等奖券 $3$ 张（每张可获价值 $10$ 元的奖品），其余 $6$ 张没有奖。某顾客从这 $10$ 张奖券中任抽 $2$ 张，求：(1) 该顾客中奖的概率；(2) 该顾客获得的奖品总价值 $X$（元）的分布列。

  === 解析

  从 $10$ 张中任抽 $2$ 张，共有

  $ C_10^2 = 45 $

  种等可能的取法。

  *(1) 中奖的概率。* 用对立事件（两张都没奖）：

  $ P("中奖") = 1 - P("两张都没奖") = 1 - (C_6^2)/(C_10^2) = 1 - 15/45 = 1 - 1/3 = 2/3. $

  *(2) $X$ 的分布列。* 先列出 $X$ 的所有可能取值：没中奖是 $0$，中 $1$ 张二等奖是 $10$，中 $2$ 张二等奖是 $20$，中一等奖（配没奖的）是 $50$，中一等奖配二等奖是 $60$：

  $ X in {0, 10, 20, 50, 60}. $

  逐个计（总取法都是 $45$）：

  - $X = 0$：$2$ 张都从 $6$ 张无奖券中取，$C_6^2 = 15$ 种，

  $ P(X = 0) = 15/45 = 1/3; $

  - $X = 10$：$1$ 张二等奖（$3$ 种选法）配 $1$ 张无奖券（$6$ 种），$3 times 6 = 18$ 种，

  $ P(X = 10) = 18/45 = 2/5; $

  - $X = 20$：$2$ 张都是二等奖，$C_3^2 = 3$ 种，

  $ P(X = 20) = 3/45 = 1/15; $

  - $X = 50$：一等奖配 $1$ 张无奖券，$1 times 6 = 6$ 种，

  $ P(X = 50) = 6/45 = 2/15; $

  - $X = 60$：一等奖配 $1$ 张二等奖，$1 times 3 = 3$ 种，

  $ P(X = 60) = 3/45 = 1/15. $

  检验：$15 + 18 + 3 + 6 + 3 = 45$，正好是全部取法，故概率之和为 $1$（这与 (1) 的结果也吻合：$1 - 1/3 = 2/3$）。分布列为

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 6pt,
      [$X$], [$0$], [$10$], [$20$], [$50$], [$60$],
      [$P$], [$1/3$], [$2/5$], [$1/15$], [$2/15$], [$1/15$],
    )
  ]

  === 要点

  - *"中奖"用对立事件最快*：$P("中奖") = 1 - P("两张都没奖")$，避免把"中一等奖/二等奖"各种情形相加。
  - *先枚举 $X$ 的全部取值*：$X$ 是*两张券奖品价值之和*，由 $0$、$10$、$50$ 组合得 $0, 10, 20, 50, 60$——别漏了 $60$（一等奖 $+$ 二等奖）。
  - *按类计数*：抽 $2$ 张时，把"选第二个类别"的取法用乘法原理（如 $3 times 6$）或组合数（如 $C_3^2$）算清楚。
  - *两问互相印证*：分布列中除 $X = 0$ 外的概率之和应等于 (1) 的中奖概率 $2/3$（$2/5 + 1/15 + 2/15 + 1/15 = 2/3$ ✓）。
  - *收尾检验*：所有概率之和必须为 $1$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 6】求 $E(Y)$（线性变换）

  *【例 6】* 已知 $X$ 的分布列如下，设 $Y = 2 X + 3$，则 $E(Y)$ 的值为 #box(width: 3em, stroke: (bottom: 0.5pt)) 。

  #align(center)[
    #table(
      columns: 4,
      stroke: 0.5pt,
      inset: 6pt,
      [$X$], [$-1$], [$0$], [$1$],
      [$P$], [$1/2$], [$1/3$], [$1/6$],
    )
  ]

  === 解析

  *法 1（用期望的线性性质，最快）。* 期望有性质

  $ E(a X + b) = a E(X) + b quad (a, b "为常数"), $

  所以先求 $E(X)$：

  $ E(X) = (-1) times 1/2 + 0 times 1/3 + 1 times 1/6 = -1/2 + 1/6 = -3/6 + 1/6 = -1/3. $

  于是

  $ E(Y) = E(2 X + 3) = 2 E(X) + 3 = 2 times (-1/3) + 3 = -2/3 + 3 = 7/3. $

  *法 2（先写出 $Y$ 的分布列，再按定义求）。* 由 $Y = 2X + 3$：

  - $X = -1$ 时 $Y = 1$，$X = 0$ 时 $Y = 3$，$X = 1$ 时 $Y = 5$。

  而 $Y$ 取各值的概率与 $X$ 相同（一一对应，概率不变）：

  $ P(Y = 1) = 1/2, quad P(Y = 3) = 1/3, quad P(Y = 5) = 1/6. $

  所以

  $ E(Y) = 1 times 1/2 + 3 times 1/3 + 5 times 1/6 = 1/2 + 1 + 5/6 = 3/6 + 6/6 + 5/6 = 14/6 = 7/3. $

  两法一致，故 $E(Y) = 7/3$。

  === 要点

  - *先检查分布列*：$1/2 + 1/3 + 1/6 = 1$，合法。
  - *期望的线性性质*：$E(a X + b) = a E(X) + b$——不必先求 $Y$ 的分布列（本题 $2E(X) + 3 = 7/3$）。
  - *按定义求 $E(X)$*：$E(X) = sum x_i p_i$，把每个取值乘它的概率再相加；注意负号（$-1 times 1/2 = -1/2$）。
  - *验证方法*：把 $Y$ 的取值逐个代入（$1, 3, 5$）再按定义算一遍，两法结果应当一致。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 7】期望与方差的计算（多选题）

  *【例 7】*（多选）设离散型随机变量 $X$ 的分布列为

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 6pt,
      [$X$], [$0$], [$1$], [$2$], [$3$], [$4$],
      [$P$], [$q$], [$0.4$], [$0.1$], [$0.2$], [$0.2$],
    )
  ]

  若离散型随机变量 $Y$ 满足 $Y = 2 X + 1$，则下列结果正确的有（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $q = 0.1$], [B. $E(X) = 2$, $D(X) = 1.4$],
    [C. $E(X) = 2$, $D(X) = 1.8$], [D. $E(Y) = 5$, $D(Y) = 7.2$],
  )

  === 解析

  *① 先求 $q$。* 由 $sum p_i = 1$：

  $ q = 1 - (0.4 + 0.1 + 0.2 + 0.2) = 1 - 0.9 = 0.1, $

  *A 正确*。

  *② 求 $E(X)$ 与 $D(X)$。* 期望按定义：

  $ E(X) = 0 times 0.1 + 1 times 0.4 + 2 times 0.1 + 3 times 0.2 + 4 times 0.2 = 2.0. $

  方差用公式 $D(X) = E(X^2) - (E(X))^2$，先算 $E(X^2)$：

  $ E(X^2) = 0 + 1 times 0.4 + 4 times 0.1 + 9 times 0.2 + 16 times 0.2 = 0.4 + 0.4 + 1.8 + 3.2 = 5.8, $

  所以

  $ D(X) = 5.8 - 2^2 = 1.8. $

  *C 正确*，而 B 中的 $D(X) = 1.4$ 错误。

  *③ 求 $E(Y)$ 与 $D(Y)$。* 由 $Y = 2X + 1$ 及期望、方差的性质

  $ E(Y) = 2 E(X) + 1 = 5, quad D(Y) = 2^2 D(X) = 4 times 1.8 = 7.2, $

  *D 正确*。（注意方差是乘以 $a^2 = 4$，不是乘以 $2$。）

  故选 *#text(fill: rgb("#c7362e"))[ACD]*。

  === 要点

  - *分布列里带字母，先由 $sum p_i = 1$ 定字母*（本题 $q = 0.1$）。
  - *期望按定义*：$E(X) = sum x_i p_i$；*方差用公式*：$D(X) = E(X^2) - (E(X))^2$（比 $sum (x_i - E X)^2 p_i$ 少算一步）。
  - *线性变换下的期望与方差*：$E(a X + b) = a E(X) + b$，而 $D(a X + b) = a^2 D(X)$——*方差要乘 $a^2$*，这是最常见的错点（本题 $D(Y) = 4 times 1.8 = 7.2$，而不是 $2 times 1.8$）。
  - *常数不影响方差*：$D(X + b) = D(X)$。
  - *多选题逐个验证*：先算出 $q$、$E(X)$、$D(X)$、$E(Y)$、$D(Y)$ 五个量，再与各选项对照。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 8】滑雪计费问题（分布列与均值）

  *【例 8】* 某滑雪场的收费：滑雪时间不超过 $1$ 小时免费；超过 $1$ 小时的部分每小时 $40$ 元（不足 $1$ 小时按 $1$ 小时计）。甲、乙两人相互独立地来此运动，甲、乙不超过 $1$ 小时离开的概率分别为 $1/4$、$1/6$；$1$ 小时以上且不超过 $2$ 小时离开的概率分别为 $1/2$、$2/3$；两人滑雪时间都不会超过 $3$ 小时。求：

  (1) 甲、乙两人所付滑雪费用相同的概率；

  (2) 设两人所付费用之和为 $X$，求 $X$ 的分布列与均值 $E(X)$。

  === 解析

  *① 先写出两人各自的"费用—概率"表。* 费用只分三档：$<= 1$ 小时为 $0$ 元，$1$ 到 $2$ 小时为 $40$ 元，$2$ 到 $3$ 小时为 $80$ 元。

  - 甲：$P(0) = 1/4$，$P(40) = 1/2$，$P(80) = 1 - 1/4 - 1/2 = 1/4$；
  - 乙：$P(0) = 1/6$，$P(40) = 2/3$，$P(80) = 1 - 1/6 - 2/3 = 1/6$。

  *(1) 费用相同的概率。* 两人独立，故三种"相同"的情形相加：

  $ P = 1/4 dot 1/6 + 1/2 dot 2/3 + 1/4 dot 1/6 = 1/24 + 1/3 + 1/24 = 10/24 = 5/12. $

  *(2) $X$ 的取值与分布列。* $X$ 是两人费用之和，可能为 $0, 40, 80, 120, 160$。用表格把 $3 times 3 = 9$ 种组合一一列出：

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 5pt,
      [$X$], [$0$], [$40$], [$80$], [$120$], [$160$],
      [$P$], [$1/24$], [$1/4$], [$5/12$], [$1/4$], [$1/24$],
    )
  ]

  其中

  - $P(X = 0) = 1/4 dot 1/6 = 1/24$；
  - $P(X = 40) = 1/4 dot 2/3 + 1/2 dot 1/6 = 1/6 + 1/12 = 1/4$；
  - $P(X = 80) = 1/4 dot 1/6 + 1/2 dot 2/3 + 1/4 dot 1/6 = 1/24 + 1/3 + 1/24 = 5/12$；
  - $P(X = 120) = 1/2 dot 1/6 + 1/4 dot 2/3 = 1/12 + 1/6 = 1/4$；
  - $P(X = 160) = 1/4 dot 1/6 = 1/24$。

  检验：$1/24 + 1/4 + 5/12 + 1/4 + 1/24 = 1$，分布列合法。

  *(3) 求 $E(X)$。*

  $ E(X) = 0 dot 1/24 + 40 dot 1/4 + 80 dot 5/12 + 120 dot 1/4 + 160 dot 1/24 = 10 + 100/3 + 30 + 20/3 = 80. $

  （也可用期望的性质分别求两人期望：$E("甲") = 40 dot 1/2 + 80 dot 1/4 = 40$，$E("乙") = 40 dot 2/3 + 80 dot 1/6 = 40$，故 $E(X) = 40 + 40 = 80$。）

  所以费用相同的概率为 $5/12$，$E(X) = 80$。

  === 要点

  - *先把"收费标准"翻译成离散取值*：$<=1$ 小时 $0$ 元、$1$ 到 $2$ 小时 $40$ 元、$2$ 到 $3$ 小时 $80$ 元；第三档的概率用 $1$ 减去前两档得到。
  - *"两人独立"就是概率相乘*，"费用相同/之和为 $X$"则把各种组合相加（互斥）。
  - *求 $X$ 的分布列先列组合*：两人各 $3$ 种情况，共 $9$ 种组合，按和分类合并（本题 $0$ 与 $160$ 各只有 $1$ 种组合，$80$ 有 $3$ 种）。
  - *求 $E(X)$ 的两条路*：按分布列定义算，或用 $E(X + Y) = E(X) + E(Y)$ 分别算两人的期望再相加——后者常更快，也能互相验算。
  - *最后检验概率和为 $1$*。

]
