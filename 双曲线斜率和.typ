#import "template.typ": *
#set document(title: [双曲线割线斜率关系])
#template(page-footer: "第 1 页")[


  == 题目分析

  设曲线 $x^2 - y^2 / 16 = 1$，其中 $x > 0$。
  给定点 $T (1/2, y_0)$，过 $T$ 作两条直线分别交曲线于 $A, B$ 和 $P, Q$，且满足

  $ |T A| dot |T B| = |T P| dot |T Q| $

  求这两条割线斜率之间的关系。

  *猜测，如果$y_0 = 0$，由对称性知两条割线斜率互为相反数。*

  #line(length: 100%, stroke: 0.5pt + gray)

  == 过程解答

  === 1. 斜率范围

  双曲线 $x^2 - y^2 / 16 = 1$ 的渐近线为 $y = plus.minus 4x$。
  由于要求直线与右支（$x>0$）相交，过点 $T(1/2, y_0)$ 的割线斜率 $k$ 必须满足$|k| < 4$，因此 $16 - k^2 > 0$。

  === 2. 曲线与直线参数化

  设过点 $T (1/2, y_0)$ 的直线斜率为 $k$，则直线方程为

  $ y - y_0 = k (x - 1/2) $

  用参数 $t$ 表示从 $T$ 出发沿直线的横坐标变化，令

  $ x = 1/2 + t, quad y = y_0 + k t $

  则直线上两点 $A, B$ 对应参数 $t_1, t_2$，并且

  $ |T A| = |t_1| sqrt(1 + k^2), quad |T B| = |t_2| sqrt(1 + k^2) $

  因此

  $ |T A| dot |T B| = |t_1 t_2| (1 + k^2) $

  === 3. 代入曲线方程

  将参数式代入双曲线方程：

  $ (1/2 + t)^2 - (y_0 + k t)^2 / 16 = 1 $

  展开并乘以 16：

  $ 4 + 16 t + 16 t^2 - (y_0^2 + 2 y_0 k t + k^2 t^2) = 16 $

  整理得关于 $t$ 的一元二次方程：

  $ (16 - k^2) t^2 + (16 - 2 y_0 k) t - (y_0^2 + 12) = 0 $

  由韦达定理，两根之积为

  $ t_1 t_2 = - (y_0^2 + 12) / (16 - k^2) $

  于是，利用 $16 - k^2 > 0$，

  $ |T A| dot |T B| = (y_0^2 + 12) (1 + k^2) / (16 - k^2) $

  === 4. 两条割线乘积相等

  设两条直线斜率分别为 $k_1$ 和 $k_2$，由题意有

  $ (y_0^2 + 12) (1 + k_1^2) / (16 - k_1^2) = (y_0^2 + 12) (1 + k_2^2) / (16 - k_2^2) $

  因 $y_0^2 + 12 > 0$，且 $16 - k_1^2 > 0$、$16 - k_2^2 > 0$，约去公共因子得

  $ (1 + k_1^2) / (16 - k_1^2) = (1 + k_2^2) / (16 - k_2^2) $
  左右同时加一得：
  $ 17 / (16 - k_1^2) = 17 / (16 - k_2^2) $

  即

  $ k_1^2 = k_2^2 $

  所以 $k_1 = k_2$ 或 $k_1 = -k_2$。

  由于两条直线是不同直线，排除 $k_1 = k_2$，因此

  $ k_1 + k_2 = 0 $

]
