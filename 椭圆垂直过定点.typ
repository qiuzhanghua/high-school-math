#import "template.typ": *

#set document(title: [椭圆垂直过定点])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set text(size: 12pt)
  #set enum(numbering: "(1)")

  = 题目1

  已知椭圆 $C: x^2 / a^2 + y^2 / b^2 = 1 (a > b > 0)$ 的离心率为 $sqrt(2)/2$，过点 $A(2,1)$。

  + 求 $C$ 的方程；
  + 点 $M$、$N$ 在 $C$ 上，且 $A M perp A N$，$A D perp M N$ 于 $D$。证明：存在定点 $Q$，使得 $|D Q|$ 为定值。

  #v(0.5em)

  == 解答

  === (1) 求椭圆方程

  由离心率 $e = c/a = sqrt(a^2 - b^2)/a = sqrt(2)/2$，得：

  $ a^2 - b^2 = 1/2 a^2 => b^2 = 1/2 a^2 $

  又椭圆过点 $A(2,1)$，代入得：

  $ 4/a^2 + 1/b^2 = 1 $

  将 $b^2 = 1/2 a^2$ 代入：

  $ 4/a^2 + 2/a^2 = 6/a^2 = 1 => a^2 = 6, b^2 = 3 $

  因此椭圆方程为：

  $ x^2/6 + y^2/3 = 1 $

  #v(0.5em)

  === (2) 证明存在定点 $Q$

  === (2.1) 特殊情况：$M N$ 斜率不存在

  设 $M N: x = t$，$M(t, y_0)$，$N(t, -y_0)$。

  由 $M$ 在椭圆上：

  $ y_0^2 = 3(1 - t^2/6) = 3 - t^2/2 $

  由 $A M perp A N$：

  $ (t-2, y_0-1) dot (t-2, -y_0-1) = 0 $

  $ (t-2)^2 + 1 - y_0^2 = 0 $

  代入 $y_0^2$：

  $ (t-2)^2 + 1 - (3 - t^2/2) = 0 $

  $ 3/2 t^2 - 4t + 2 = 0 => 3t^2 - 8t + 4 = 0 $

  $ t = 2 $（舍去，退化情况）或 $ t = 2/3 $

  因此 $M N: x = 2/3$，过定点 $E(2/3, -1/3)$。

  #v(0.5em)

  === (2.2) 一般情况：$M N$ 斜率存在

  设 $M N: y = k x + m$，与椭圆联立：

  $ x^2/6 + (k x+m)^2/3 = 1 $

  $ (1+2k^2)x^2 + 4k m x + 2m^2 - 6 = 0 $

  设 $M(x_1, y_1)$，$N(x_2, y_2)$，由韦达定理：

  $ x_1 + x_2 = - (4k m)/(1+2k^2), quad x_1 x_2 = (2m^2 - 6)/(1+2k^2) $

  $ y_1 + y_2 = (2m)/(1+2k^2), quad y_1 y_2 = (m^2 - 6k^2)/(1+2k^2) $

  由 $A M perp A N$：

  $ (x_1-2)(x_2-2) + (y_1-1)(y_2-1) = 0 $

  即：

  $ (x_1 x_2 + y_1 y_2) - 2(x_1+x_2) - (y_1+y_2) + 5 = 0 $

  代入韦达定理结果并整理：

  $ 3m^2 + (8k-2)m + 4k^2 - 1 = 0 $

  用十字交叉法分解：

  $ (3m + 2k + 1)(m + 2k - 1) = 0 $

  分析两个因子：

  - $m + 2k - 1 = 0 => m = 1 - 2k$，直线 $y = k x + 1 - 2k$ 过点 $A(2,1)$（退化情况，舍去）；

  - $3m + 2k + 1 = 0 => m = -(2k+1)/3$，直线过定点：

  $ y = k x - (2k+1)/3 => E(2/3, -1/3) $

  因此，所有非退化情况下 $M N$ 均过定点 $E(2/3, -1/3)$。

  #v(0.5em)

  === (2.3) 证明 $|D Q|$ 为定值

  $because A D perp M N$，且 $E in M N$，

  $therefore A D perp E D$，即 $triangle A D E$ 为直角三角形，$angle A D E = 90^circle$。

  取斜边 $A E$ 的中点 $Q$，由直角三角形斜边中线定理：

  $ |D Q| = |Q A| = |Q E| = 1/2 |A E| $

  计算 $A E$ 的长度：

  $ |A E| = sqrt((2 - 2/3)^2 + (1 + 1/3)^2) = sqrt((4/3)^2 + (4/3)^2) = (4sqrt(2))/3 $

  因此：

  $ |D Q| = 1/2 |A E| = (2sqrt(2))/3 $

  定点 $Q$ 的坐标：

  $ Q((2 + 2/3)/2, (1 - 1/3)/2) = Q(4/3, 1/3) $

  #v(0.5em)

  === 结论

  存在定点 $Q(4/3, 1/3)$，使得 $|D Q| = (2sqrt(2))/3$ 为定值。

  $ Q(4/3, 1/3), quad |D Q| = (2sqrt(2))/3 $

  #v(1em)

  #align(center)[*证毕*]

  #pagebreak()

  = 题目2

  已知椭圆 $C: x^2 / a^2 + y^2 / b^2 = 1 (a > b > 0)$ 的离心率为 $1/2$，左焦点到 $P(2,1)$的距离为$sqrt(10)$。

  + 求 $C$ 的方程；
  + 若直线l与椭圆交于A, B 两点(不是左右顶点)，以AB为直径的圆过椭圆的右顶点。证明：l过定点，并求出定点坐标。

  #v(0.5em)

  == 解答

  === (1) 求椭圆方程

  由离心率 $e = c/a = 1/2$，得：

  $ c = a/2 $

  又 $c^2 = a^2 - b^2$，所以：

  $ a^2/4 = a^2 - b^2 => b^2 = 3/4 a^2 $

  左焦点 $F_1(-c, 0) = F_1(-a/2, 0)$。

  由 $F_1$ 到 $P(2,1)$ 的距离为 $sqrt(10)$：

  $ sqrt((2 + a/2)^2 + 1^2) = sqrt(10) $

  $ a = 2 $

  因此：

  $ a^2 = 4, quad b^2 = 3/4 times 4 = 3 $

  所以椭圆方程为：

  $ x^2/4 + y^2/3 = 1 $

  #v(0.5em)

  === (2) 证明直线 $l$ 过定点（全符号运算）

  设椭圆 $C: x^2/a^2 + y^2/b^2 = 1$，右顶点为 $D(a, 0)$。

  设直线 $l: x = t y + m$，与椭圆交于 $A(x_1, y_1)$、$B(x_2, y_2)$。

  联立直线与椭圆：

  $ (t y + m)^2/a^2 + y^2/b^2 = 1 $

  $ b^2(t y + m)^2 + a^2 y^2 = a^2 b^2 $

  $ b^2(t^2 y^2 + 2t m y + m^2) + a^2 y^2 - a^2 b^2 = 0 $

  $ (b^2 t^2 + a^2)y^2 + 2b^2 t m y + b^2(m^2 - a^2) = 0 $

  由韦达定理：

  $ y_1 + y_2 = - (2b^2 t m)/(b^2 t^2 + a^2), quad y_1 y_2 = (b^2(m^2 - a^2))/(b^2 t^2 + a^2) $

  以 $A B$ 为直径的圆过右顶点 $D(a, 0)$，等价于：

  $ (x_1 - a)(x_2 - a) + y_1 y_2 = 0 $

  其中 $x_i = t y_i + m$，代入得：

  $ (t y_1 + m - a)(t y_2 + m - a) + y_1 y_2 = 0 $

  展开：

  $ t^2 y_1 y_2 + t(m-a)(y_1 + y_2) + (m-a)^2 + y_1 y_2 = 0 $

  $ (t^2 + 1)y_1 y_2 + t(m-a)(y_1 + y_2) + (m-a)^2 = 0 $

  代入韦达定理，记分母 $S = b^2 t^2 + a^2$：

  $ (t^2 + 1) b^2(m^2 - a^2)/S + t(m-a)(-2b^2 t m)/S + (m-a)^2 = 0 $

  $ (t^2 + 1)b^2(m^2 - a^2) - 2b^2 t^2 m(m-a) + (m-a)^2(b^2 t^2 + a^2) = 0 $

  展开并整理（$t^2$ 项全部抵消）：

  $ (a^2 + b^2)m^2 - 2a^3 m + a^2(a^2 - b^2) = 0 $

  因式分解：

  $ ((a^2 + b^2)m - a(a^2 - b^2))(m - a) = 0 $

  所以：$m = a$ 或 $m = (a(a^2 - b^2))/(a^2 + b^2)$

  - 若 $m = a$，直线 $x = t y + a$ 过右顶点 $(a, 0)$，与题目"不是左右顶点"矛盾，舍去；

  - 若 $m = (a(a^2 - b^2))/(a^2 + b^2)$，直线恒过定点 $((a(a^2 - b^2))/(a^2 + b^2), 0)$。

  由 (1) 知 $a = 2$，$b^2 = 3$，代入：

  $ (a(a^2 - b^2))/(a^2 + b^2) = (2(4-3))/(4+3) = 2/7 $

  因此直线 $l$ 恒过定点：

  $ (2/7, 0) $

  #align(center)[*证毕*]

  #pagebreak()

  = 题目3

  已知椭圆 $C: x^2/a^2 + y^2/b^2 = 1 (a > b > 0)$ 的左、右焦点分别为 $F_1$、$F_2$，离心率为 $1/2$，过椭圆的左焦点 $F_1$ 作不与 $x$ 轴重合的直线 $M N$ 与椭圆相交于 $M$、$N$ 两点，$triangle M F_2 N$ 的周长为 $8$，过点 $M$ 作直线 $m: x = -4$ 的垂线 $M E$，$E$ 为垂足。

  + 求椭圆 $C$ 的标准方程；
  + 证明：直线 $E N$ 经过定点 $P$，并求定点 $P$ 的坐标。

  #v(0.5em)

  == 解答

  === (1) 求椭圆方程

  由椭圆定义，对椭圆上任意点 $M$、$N$，有：

  $ M F_1 + M F_2 = 2a, quad N F_1 + N F_2 = 2a $

  又 $M$、$N$、$F_1$ 共线，所以 $M N = M F_1 + N F_1$。

  $triangle M F_2 N$ 的周长：

  $ M F_2 + N F_2 + M N = M F_2 + N F_2 + M F_1 + N F_1 = (M F_1 + M F_2) + (N F_1 + N F_2) = 4a $

  由题意 $4a = 8$，得 $a = 2$。

  又离心率 $e = c/a = 1/2$，所以 $c = 1$，从而：

  $ b^2 = a^2 - c^2 = 4 - 1 = 3 $

  因此椭圆方程为：

  $ x^2/4 + y^2/3 = 1 $

  #v(0.5em)

  === (2) 证明直线 $E N$ 过定点（全符号运算）

  由(1)已得 $a = 2$，$c = 1$。但以下推导全程保留 $a$、$b$、$c$ 的一般关系，最后代入具体数值。

  已知 $b^2 = a^2 - c^2$，左焦点 $F_1(-c, 0)$，直线 $m: x = -a^2/c$。

  === (2.1) 设直线 $M N$ 的方程

  直线 $M N$ 过 $F_1(-c, 0)$ 且不与 $x$ 轴重合，设：

  $ M N: x = t y - c quad (t != 0) $

  设 $M(x_1, y_1)$，$N(x_2, y_2)$。

  #v(0.5em)

  === (2.2) 联立椭圆求韦达定理

  椭圆方程：

  $ x^2/a^2 + y^2/b^2 = 1 $

  代入 $x = t y - c$：

  $ (t y - c)^2/a^2 + y^2/b^2 = 1 $

  $ b^2(t y - c)^2 + a^2 y^2 = a^2 b^2 $

  $ (b^2 t^2 + a^2)y^2 - 2b^2 c t y + b^2(c^2 - a^2) = 0 $

  由 $c^2 - a^2 = -b^2$，得：

  $ (b^2 t^2 + a^2)y^2 - 2b^2 c t y - b^4 = 0 $

  由韦达定理：

  $ y_1 + y_2 = (2b^2 c t)/(b^2 t^2 + a^2), quad y_1 y_2 = - (b^4)/(b^2 t^2 + a^2) $

  同时：

  $ x_1 = t y_1 - c, quad x_2 = t y_2 - c $

  #v(0.5em)

  === (2.3) 求点 $E$ 坐标

  直线 $m: x = -a^2/c$，过 $M$ 作其垂线，垂足 $E$ 的横坐标为 $-a^2/c$，纵坐标与 $M$ 相同：

  $ E(-a^2/c, y_1) $

  #v(0.5em)

  === (2.4) 设定点 $P$

  椭圆及题设条件关于 $x$ 轴对称，若直线 $E N$ 恒过定点，则定点必在 $x$ 轴上。设：

  $ P(p, 0) $

  #v(0.5em)

  === (2.5) 由 $P$、$E$、$N$ 三点共线建立方程

  直线 $E N$ 过 $E(-a^2/c, y_1)$、$N(x_2, y_2)$，若 $P(p, 0)$ 在其上，则斜率相等：

  $ (0 - y_1)/(p + a^2/c) = (y_2 - y_1)/(x_2 + a^2/c) $

  交叉相乘：

  $ -y_1(x_2 + a^2/c) = (p + a^2/c)(y_2 - y_1) $

  展开：

  $ -y_1 x_2 - (a^2/c) y_1 = (p + a^2/c)y_2 - (p + a^2/c)y_1 $

  移项：

  $ -y_1 x_2 - (a^2/c) y_1 - (p + a^2/c)y_2 + (p + a^2/c)y_1 = 0 $

  合并 $y_1$ 项（$-(a^2/c) + p + a^2/c = p$）：

  $ -y_1 x_2 + p y_1 - (p + a^2/c)y_2 = 0 $

  即：

  $ p y_1 - y_1 x_2 - (p + a^2/c)y_2 = 0 $

  代入 $x_2 = t y_2 - c$：

  $ p y_1 - y_1(t y_2 - c) - (p + a^2/c)y_2 = 0 $

  $ p y_1 - t y_1 y_2 + c y_1 - (p + a^2/c)y_2 = 0 $

  $ (p + c)y_1 - (p + a^2/c)y_2 - t y_1 y_2 = 0 $

  标记为 (★) 式。

  #v(0.5em)

  === (2.6) 利用韦达定理消去 $y_1$、$y_2$

  由 $y_1 + y_2 = (2b^2 c t)/(b^2 t^2 + a^2)$，得：

  $ y_1 = (2b^2 c t)/(b^2 t^2 + a^2) - y_2 $

  代入 (★) 式：

  $ (p + c)((2b^2 c t)/(b^2 t^2 + a^2) - y_2) - (p + a^2/c)y_2 - t(- (b^4)/(b^2 t^2 + a^2)) = 0 $

  $ (2b^2 c t(p + c))/(b^2 t^2 + a^2) - (p + c)y_2 - (p + a^2/c)y_2 + (b^4 t)/(b^2 t^2 + a^2) = 0 $

  $ (2b^2 c t(p + c) + b^4 t)/(b^2 t^2 + a^2) - (2p + c + a^2/c)y_2 = 0 $

  $ (b^2 t(2c(p + c) + b^2))/(b^2 t^2 + a^2) - (2p + c + a^2/c)y_2 = 0 $

  #v(0.5em)

  === (2.7) 由恒成立条件求 $p$

  上式对任意 $t$ 及对应的 $y_2$ 恒成立，必须同时满足：

  $ 2p + c + a^2/c = 0, quad 2c(p + c) + b^2 = 0 $

  由第一式：

  $ p = - (c + a^2/c)/2 = - (a^2 + c^2)/(2c) $

  由第二式验证：

  $ 2c(p + c) + b^2 = 2c(- (a^2 + c^2)/(2c) + c) + b^2 $

  $ = 2c(- (a^2 + c^2)/(2c) + (2c^2)/(2c)) + b^2 $

  $ = 2c(- (a^2 - c^2)/(2c)) + b^2 $

  $ = -(a^2 - c^2) + b^2 = -b^2 + b^2 = 0 $

  验证通过。因此：

  $ p = - (a^2 + c^2)/(2c) $

  #v(0.5em)

  === (2.8) 代入具体数值

  由(1)知 $a = 2$，$c = 1$，所以：

  $ p = - (4 + 1)/(2 times 1) = - 5/2 $

  因此直线 $E N$ 恒过定点：

  $ P(- 5/2, 0) $

  #v(0.5em)

  === 结论

  (1) 椭圆方程为：

  $ x^2/4 + y^2/3 = 1 $

  (2) 直线 $E N$ 恒过定点：

  $ P(- 5/2, 0) $

  #v(1em)

  #align(center)[*证毕*]

  #pagebreak()

  = 题目4

  已知 $A, B$ 分别为椭圆 $E: x^2/a^2 + y^2 = 1$（$a > 1$）的左、右顶点，$G$ 为 $E$ 的上顶点，$arrow(A G) dot arrow(G B) = 8$。$P$ 为直线 $x = 6$ 上的动点，$P A$ 与 $E$ 的另一交点为 $C$，$P B$ 与 $E$ 的另一交点为 $D$。

  + 求 $E$ 的方程；
  + 证明：直线 $C D$ 过定点。

  #v(0.5em)

  == 解答

  === (1) 求椭圆方程

  由题意：
  $ A(-a, 0), quad B(a, 0), quad G(0, 1) $

  因此：
  $ arrow(A G) = (a, 1), quad arrow(G B) = (a, -1) $

  所以：
  $ arrow(A G) dot arrow(G B) = a^2 - 1 = 8 $

  解得：
  $ a^2 = 9, quad a = 3 quad (a > 1) $

  故椭圆方程为：
  $ x^2/9 + y^2 = 1 $

  #v(0.5em)

  === (2) 证明直线 $C D$ 过定点（全符号运算）

  由(1)已得 $a = 3$，$b = 1$。但以下推导全程保留 $a$、$b$ 的一般关系，最后代入具体数值。

  已知椭圆 $E: x^2/a^2 + y^2/b^2 = 1$（其中 $b = 1$），$A(-a,0)$，$B(a,0)$，直线 $x = 6$。

  === (2.1) 建立斜率关系

  设 $P(6, q)$（$q != 0$），则：
  $ k_(P A) = q/(6 + a), quad k_(P B) = q/(6 - a) $

  因此：
  $ k_(P B) = (6 + a)/(6 - a) k_(P A) $

  又 $P, A, C$ 三点共线，所以 $k_(P A) = k_(A C)$；
  $P, B, D$ 三点共线，所以 $k_(P B) = k_(B D)$。

  $ k_(B D) = (6 + a)/(6 - a) k_(A C) $

  对椭圆 $x^2/a^2 + y^2/b^2 = 1$，设 $M(x_0, y_0)$、$N(-x_0, -y_0)$ 为其上关于原点对称的两点，$Q(x_1, y_1)$ 为椭圆上任意点（且 $x_1 != plus.minus x_0$）。

  由 $M$、$Q$ 在椭圆上：
  $ x_0^2/a^2 + y_0^2/b^2 = 1, quad x_1^2/a^2 + y_1^2/b^2 = 1 $

  两式相减得：
  $ (x_1^2 - x_0^2)/a^2 + (y_1^2 - y_0^2)/b^2 = 0 $

  $ (y_1^2 - y_0^2)/(x_1^2 - x_0^2) = - b^2/a^2 $

  $
    k_(M Q) dot k_(N Q) = (y_1 - y_0)/(x_1 - x_0) dot (y_1 + y_0)/(x_1 + x_0)
    = (y_1^2 - y_0^2)/(x_1^2 - x_0^2) = - b^2/a^2
  $

  因此椭圆上关于原点对称的两点与椭圆上任意点连线斜率之积为 $-b^2/a^2$。

  特别地，$A(-a,0)$ 与 $B(a,0)$ 关于原点对称，且 $C$ 在椭圆上，所以：
  $ k_(A C) dot k_(B C) = - b^2/a^2 $

  由(2)(3)得：
  $ k_(B D) dot k_(B C) = (6 + a)/(6 - a) k_(A C) dot k_(B C) = (6 + a)/(6 - a)(- b^2/a^2) $

  即：
  $ k_(B C) dot k_(B D) = - ((6 + a)b^2)/((6 - a)a^2) $

  #v(0.5em)

  === (2.2) 设直线 $C D$ 的方程

  设直线 $C D$ 的方程为：
  $ x = t y + m $

  其中 $t$ 为参数，$m$ 为待定常数。

  设 $C(x_1, y_1)$，$D(x_2, y_2)$，且 $C$、$D$ 均在椭圆上。

  #v(0.5em)

  === (2.3) 用 $B$ 点坐标表示斜率

  由 $B(a, 0)$：
  $ k_(B C) = (y_1 - 0)/(x_1 - a) = y_1/(x_1 - a) $

  $ k_(B D) = (y_2 - 0)/(x_2 - a) = y_2/(x_2 - a) $

  $ y_1/(x_1 - a) dot y_2/(x_2 - a) = - ((6 + a)b^2)/((6 - a)a^2) $

  即：
  $ y_1 y_2 + ((6 + a)b^2)/((6 - a)a^2) (x_1 - a)(x_2 - a) = 0 $

  #v(0.5em)

  === (2.4) 联立直线与椭圆

  将 $x = t y + m$ 代入椭圆 $x^2/a^2 + y^2/b^2 = 1$：

  $ (t y + m)^2/a^2 + y^2/b^2 = 1 $

  $ b^2(t y + m)^2 + a^2 y^2 = a^2 b^2 $

  $ b^2(t^2 y^2 + 2t m y + m^2) + a^2 y^2 - a^2 b^2 = 0 $

  $ (b^2 t^2 + a^2)y^2 + 2b^2 t m y + b^2(m^2 - a^2) = 0 $

  由韦达定理：
  $ y_1 + y_2 = - (2b^2 t m)/(b^2 t^2 + a^2), quad y_1 y_2 = (b^2(m^2 - a^2))/(b^2 t^2 + a^2) $

  同时：
  $ x_1 = t y_1 + m, quad x_2 = t y_2 + m $

  #v(0.5em)

  === (2.5) 将韦达定理代入

  $ (x_1 - a)(x_2 - a) = (t y_1 + m - a)(t y_2 + m - a) $

  $ = t^2 y_1 y_2 + t(m - a)(y_1 + y_2) + (m - a)^2 $

  $ y_1 y_2 + lambda [t^2 y_1 y_2 + t(m - a)(y_1 + y_2) + (m - a)^2] = 0 $

  其中 $lambda = ((6 + a)b^2)/((6 - a)a^2)$。

  整理：
  $ (1 + lambda t^2)y_1 y_2 + lambda t(m - a)(y_1 + y_2) + lambda (m - a)^2 = 0 $

  代入韦达定理(6)，记分母 $S = b^2 t^2 + a^2$：

  $ (1 + lambda t^2) b^2(m^2 - a^2)/S + lambda t(m - a)(-2b^2 t m)/S + lambda (m - a)^2 = 0 $

  $ (1 + lambda t^2)b^2(m^2 - a^2) - 2lambda b^2 t^2 m(m - a) + lambda (m - a)^2(b^2 t^2 + a^2) = 0 $

  #v(0.5em)

  === (2.6) 化简 并求 $m$

  展开上式的 $t^2$ 项：

  第一项中：$lambda t^2 b^2(m^2 - a^2)$

  第二项：$-2lambda b^2 t^2 m(m - a)$

  第三项中：$lambda (m - a)^2 b^2 t^2$

  三者的 $t^2$ 系数：
  $ lambda b^2[(m^2 - a^2) - 2m(m - a) + (m - a)^2] $

  化简括号内：
  $ m^2 - a^2 - 2m^2 + 2a m + m^2 - 2a m + a^2 = 0 $

  因此 $t^2$ 项全部抵消。

  剩余常数项：
  $ b^2(m^2 - a^2) + lambda a^2(m - a)^2 = 0 $

  代入 $lambda = ((6 + a)b^2)/((6 - a)a^2)$：

  $ b^2(m^2 - a^2) + ((6 + a)b^2)/((6 - a)a^2) a^2(m - a)^2 = 0 $

  约去 $b^2$：
  $ (m^2 - a^2) + (6 + a)/(6 - a)(m - a)^2 = 0 $

  因式分解 $m^2 - a^2 = (m - a)(m + a)$：

  $ (m - a)(m + a) + (6 + a)/(6 - a)(m - a)^2 = 0 $

  $ (m - a)[(m + a) + (6 + a)/(6 - a)(m - a)] = 0 $

  所以 $m = a$ 或 $(m + a) + (6 + a)/(6 - a)(m - a) = 0$。

  解第二个方程：
  $ (m + a)(6 - a) + (6 + a)(m - a) = 0 $

  $ (6 - a)m + a(6 - a) + (6 + a)m - a(6 + a) = 0 $

  $ 12m + a(6 - a - 6 - a) = 0 $

  $ 12m - 2a^2 = 0 $

  $ m = a^2/6 $

  #v(0.5em)

  === (2.7) 舍去增根并代入数值

  若 $m = a$，直线 $x = t y + a$ 过右顶点 $(a, 0)$，与题意"另一交点"矛盾，舍去。

  因此：
  $ m = a^2/6 $

  由(1)知 $a = 3$，所以：
  $ m = 9/6 = 3/2 $

  #v(0.5em)

  === (2.8) 结论

  直线 $C D$ 的方程为：
  $ x = t y + 3/2 $

  即直线 $C D$ 恒过定点：
  $ (3/2, 0) $

  #v(0.5em)

  == 最终答案

  (1) 椭圆方程为：
  $ x^2/9 + y^2 = 1 $

  (2) 直线 $C D$ 恒过定点：
  $ Q(3/2, 0) $

  #v(1em)

  #align(center)[*证毕*]

  #pagebreak()

  = 题目5

  已知抛物线 $C: y^2 = 4x$ 的焦点为 $F$，过 $F$ 的直线 $l$ 交 $C$ 于 $A, B$ 两点，过 $F$ 与 $l$ 垂直的直线交 $C$ 于 $D, E$ 两点，其中 $B, D$ 在 $x$ 轴上方，$M, N$ 分别为 $A B, D E$ 的中点。证明：直线 $M N$ 过定点。

]
