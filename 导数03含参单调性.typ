#import "template.typ": *

#set document(title: [导数含参单调性])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 【1】讨论 $f(x) = ln x - a x + x^2$ 的单调性

  *【1】* 已知函数 $f(x) = ln x - a x + x^2$，讨论 $f(x)$ 的单调性。

  === 解析

  函数的定义域为 $(0, +oo)$。求导：
  $ f'(x) = 1/x - a + 2x = (2x^2 - a x + 1)/x $

  因为 $x > 0$，所以 $f'(x)$ 的符号由分子 $g(x) = 2x^2 - a x + 1$ 决定。$g(x)$ 的判别式为
  $ Delta = a^2 - 8 $

  下面按 $Delta$ 的符号以及 $g(x) = 0$ 的根与定义域的位置关系分类讨论。

  *（1）当 $Delta <= 0$，即 $|a| <= 2 sqrt(2)$ 时：* 因为二次项系数 $2 > 0$，所以 $g(x) >= 0$ 对一切实数成立，从而 $f'(x) >= 0$ 在 $(0, +oo)$ 上恒成立（仅当 $a = 2 sqrt(2)$ 且 $x = sqrt(2)/2$ 时 $f'(x) = 0$）。

  故 $f(x)$ 在 $(0, +oo)$ 上单调递增。

  *（2）当 $Delta > 0$ 且 $a > 2 sqrt(2)$ 时：* 方程 $g(x) = 0$ 有两个正根
  $ x_1 = (a - sqrt(a^2 - 8))/4, quad x_2 = (a + sqrt(a^2 - 8))/4 quad (0 < x_1 < x_2) $

  且 $g(x) = 2(x - x_1)(x - x_2)$，于是

  - 当 $0 < x < x_1$ 或 $x > x_2$ 时，$g(x) > 0$，即 $f'(x) > 0$，$f(x)$ 单调递增；
  - 当 $x_1 < x < x_2$ 时，$g(x) < 0$，即 $f'(x) < 0$，$f(x)$ 单调递减。

  *（3）当 $Delta > 0$ 且 $a < -2 sqrt(2)$ 时：* 两根为 $x = (a plus.minus sqrt(a^2 - 8))/4$。因为
  $ sqrt(a^2 - 8) < -a $
  所以 $a + sqrt(a^2 - 8) < 0$，即两根都是负数，在 $(0, +oo)$ 上恒有 $g(x) > 0$。

  故 $f(x)$ 在 $(0, +oo)$ 上单调递增。

  *综上：*

  - 当 $a <= 2 sqrt(2)$ 时，$f(x)$ 在 $(0, +oo)$ 上单调递增；
  - 当 $a > 2 sqrt(2)$ 时，$f(x)$ 在 $(0, x_1)$ 和 $(x_2, +oo)$ 上单调递增，在 $(x_1, x_2)$ 上单调递减，其中
    $ x_1 = (a - sqrt(a^2 - 8))/4, quad x_2 = (a + sqrt(a^2 - 8))/4 $
    此时 $x_1$ 是极大值点，$x_2$ 是极小值点。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【2】讨论 $f(x) = e^x - m x$ 的单调性

  *【2】* 已知函数 $f(x) = e^x - m x$，$x in (0, +oo)$，讨论 $f(x)$ 的单调性。

  === 解析

  函数的定义域为 $(0, +oo)$。求导：
  $ f'(x) = e^x - m $

  令 $f'(x) = 0$，即 $e^x = m$。

  *（1）当 $m <= 1$ 时：* 因为 $x > 0$，所以 $e^x > 1 >= m$，即 $f'(x) > 0$ 在 $(0, +oo)$ 上恒成立，故 $f(x)$ 在 $(0, +oo)$ 上单调递增。

  *（2）当 $m > 1$ 时：* 方程 $e^x = m$ 有唯一实根
  $ x_0 = ln m > 0 $

  于是

  - 当 $0 < x < ln m$ 时，$e^x < m$，即 $f'(x) < 0$，$f(x)$ 单调递减；
  - 当 $x > ln m$ 时，$e^x > m$，即 $f'(x) > 0$，$f(x)$ 单调递增。

  *综上：*

  - 当 $m <= 1$ 时，$f(x)$ 在 $(0, +oo)$ 上单调递增；
  - 当 $m > 1$ 时，$f(x)$ 在 $(0, ln m)$ 上单调递减，在 $(ln m, +oo)$ 上单调递增，且 $x = ln m$ 是极小值点。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【3】讨论 $f(x) = ln(x + 1) + a x^2$ 的单调性

  *【3】* 已知函数 $f(x) = ln(x + 1) + a x^2$（$a > 0$），讨论函数 $f(x)$ 的单调性。

  === 解析

  由 $x + 1 > 0$ 得定义域为 $(-1, +oo)$。求导：
  $ f'(x) = 1/(x + 1) + 2a x = (2a x^2 + 2a x + 1)/(x + 1) $

  因为 $x + 1 > 0$，所以 $f'(x)$ 的符号由分子 $N(x) = 2a x^2 + 2a x + 1$ 决定，其判别式为
  $ Delta = (2a)^2 - 8a = 4a (a - 2) $

  *（1）当 $0 < a <= 2$ 时：* $Delta <= 0$，又二次项系数 $2a > 0$，所以 $N(x) >= 0$ 对一切实数成立，故 $f(x)$ 在 $(-1, +oo)$ 上单调递增（当 $a = 2$ 时仅在 $x = -1/2$ 处 $f'(x) = 0$）。

  *（2）当 $a > 2$ 时：* $N(x) = 0$ 有两个实根
  $ x_1 = (-a - sqrt(a^2 - 2a))/(2a), quad x_2 = (-a + sqrt(a^2 - 2a))/(2a) $

  因为 $sqrt(a^2 - 2a) < a$，所以 $x_2 < 0$；又 $a > sqrt(a^2 - 2a)$，即
  $ -a - sqrt(a^2 - 2a) > -2a $
  故 $x_1 > -1$。于是
  $ -1 < x_1 < x_2 < 0 $

  且 $N(x) = 2a (x - x_1)(x - x_2)$，所以

  - 当 $-1 < x < x_1$ 或 $x > x_2$ 时，$N(x) > 0$，即 $f'(x) > 0$，$f(x)$ 单调递增；
  - 当 $x_1 < x < x_2$ 时，$N(x) < 0$，即 $f'(x) < 0$，$f(x)$ 单调递减。

  *综上：*

  - 当 $0 < a <= 2$ 时，$f(x)$ 在 $(-1, +oo)$ 上单调递增；
  - 当 $a > 2$ 时，$f(x)$ 在 $(-1, x_1)$ 和 $(x_2, +oo)$ 上单调递增，在 $(x_1, x_2)$ 上单调递减，其中 $x_1 = (-a - sqrt(a^2 - 2a))/(2a)$，$x_2 = (-a + sqrt(a^2 - 2a))/(2a)$；此时 $x_1$ 是极大值点，$x_2$ 是极小值点。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - 讨论含参函数的单调性，一般步骤为：求定义域 → 求导 → 通分 → 判断导数（或其分子）的符号。
  - 若导数的分子是关于 $x$ 的二次函数，则以判别式 $Delta$ 为第一层分类标准，再考察两根是否落在定义域内（如 $ln x$ 要求 $x > 0$，$ln(x + 1)$ 要求 $x > -1$）作为第二层分类标准，做到不重不漏。
  - 分类讨论时注意：$Delta <= 0$ 时二次函数恒非负（二次项系数为正）；$Delta > 0$ 时需比较两根与定义域端点的大小，再按"两根之间、两根之外"写出单调区间。
  - 由 $f'(x) = 0$ 的两根还可直接读出极值点：$f'$ 由正变负处为极大值点，由负变正处为极小值点。
  - 对 $f'(x) = e^x - m$ 型，先解 $e^x = m$ 得 $x = ln m$，再判断该根是否落在定义域内：本题定义域为 $x > 0$，而 $x > 0$ 时 $e^x > 1$，故以 $m = 1$ 为分界讨论。
]
