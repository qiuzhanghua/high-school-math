#import "template.typ": *

#set document(title: [导数极点效应])

#template(heading2-align: left, page-footer: "第 1 页")[

  *例 3.1*（17 新课标卷）已知函数 $f(x) = a x^2 - a x - x ln x$，且 $f(x) >= 0$，求 $a$。

  *答案*：$a = 1$。

  *解答（极点效应：内点取等 $=>$ 导数为零）：*

  *① 先找到一个"取等点"。* 取 $x = 1$：

  $ f(1) = a - a - 1 dot ln 1 = 0, $

  而题设是 $f(x) >= 0$ 对一切 $x > 0$ 成立，所以 $x = 1$ 是 $f$ 的*最小值点*；又 $x = 1$ 在定义域 $(0, +oo)$ 的*内部*（不是端点）。

  *② 内点取最值，必有导数为零。*

  $ f'(x) = 2a x - a - (ln x + 1), $

  由 $f'(1) = 0$ 得

  $ 2a - a - 0 - 1 = a - 1 = 0 quad => quad a = 1. $

  这就把 $a$ 逼成了唯一可能的值。

  *③ 验证充分性。* 当 $a = 1$ 时，

  $ f(x) = x^2 - x - x ln x = x(x - 1 - ln x), $

  由标准不等式 $ln x <= x - 1$（$x > 0$，当且仅当 $x = 1$ 时取等）得 $x - 1 - ln x >= 0$；又 $x > 0$，故 $f(x) >= 0$ 恒成立。

  所以 $a = 1$。

  *另解（同除 $x$ 后直接讨论）。* 由 $x > 0$，把 $f(x) >= 0$ 两边同除 $x$，等价于

  $ h(x) = a(x - 1) - ln x >= 0 quad (x > 0). $

  $h'(x) = a - 1/x$，于是

  - 若 $a > 0$ 且 $a != 1$：$h$ 在 $x = 1/a$ 处取最小值 $h(1/a) = 1 - a + ln a < 0$（因为 $a != 1$ 时 $ln a < a - 1$），不成立；
  - 若 $a <= 0$：$x > 1$ 时 $a(x - 1) <= 0$ 且 $-ln x < 0$，故 $h(x) < 0$，不成立；
  - 若 $a = 1$：$h(x) = (x - 1) - ln x >= 0$ 恒成立。

  故只有 $a = 1$。

  *要点*：若 $f(x) >= 0$（或 $<= 0$）恒成立，而某个*内点* $x_0$ 处恰有 $f(x_0) = 0$，则 $x_0$ 必为极值点，于是 $f'(x_0) = 0$——这就是"*极点效应*"：先由取等点定出参数（本题 $x_0 = 1$ 给 $a = 1$），再验证充分性。注意它是*必要条件*，得到的值必须回代检验。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 3.2* 已知函数 $f(x) = sin x + e^x + a ln(x + 1)$。若 $f(x) >= 1$ 恒成立，求实数 $a$ 的值。

  *答案*：$a = -2$。

  *解答（极点效应）：*

  *① 定义域与取等点。* 定义域为 $x > -1$，且

  $ f(0) = sin 0 + e^0 + a ln 1 = 1. $

  题设 $f(x) >= 1$ 恒成立，即 $f(x) >= f(0)$，所以 $x = 0$ 是 $f$ 的最小值点；又 $x = 0$ 在定义域内部。

  *② 内点取最值 $=>$ 导数为零。*

  $ f'(x) = cos x + e^x + a/(x + 1), quad f'(0) = 1 + 1 + a = 0 quad => quad a = -2. $

  *③ 验证 $a = -2$ 满足要求。* 此时记 $g(x) = f(x) - 1 = sin x + e^x - 2 ln(x + 1) - 1$，则

  $ g'(x) = cos x + e^x - 2/(x + 1), quad g''(x) = -sin x + e^x + 2/(x + 1)^2. $

  对 $x > -1$ 有 $e^x - sin x > 0$（$x >= 0$ 时 $e^x > 1 >= sin x$；$-1 < x < 0$ 时 $sin x < 0 < e^x$），于是

  $ g''(x) > 0 quad (x > -1), $

  即 $g$ 在 $(-1, +oo)$ 上严格下凸；又

  $ g(0) = 0, quad g'(0) = cos 0 + e^0 - 2 = 0, $

  凸函数在导数为零处取到最小值，故 $g(x) >= g(0) = 0$，即 $f(x) >= 1$ 恒成立。

  所以 $a = -2$。

  *要点*：仍是"内点取等 $=>$ 导数为零"——由 $f(0) = 1$ 与 $f(x) >= 1$ 得 $f'(0) = 0$，一步解出 $a = -2$；验证充分性时不必硬做放缩，*看二阶导的符号（凸性）*更快：$g'' > 0$ 且 $g'(0) = 0$，则 $g(0)$ 就是最小值。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 3.3*（24 武汉二调 19）已知函数 $f(x) = (e^x - 1)/x$。若 $f(x) > a^x$ 对定义域内一切 $x$（即 $x != 0$）恒成立，其中 $a > 0$ 且 $a != 1$，求实数 $a$ 的值。

  *答案*：$a = sqrt(e)$。

  *解答：*

  *① 化归成"内点取等"的形式。* 记

  $ F(x) = f(x) - a^x quad (x != 0). $

  当 $x -> 0$ 时 $f(x) = (e^x - 1)/x -> 1$、$a^x -> 1$，故补充定义 $F(0) = 0$ 后 $F$ 在 $x = 0$ 处连续（$f$ 在 $0$ 附近可展开为 $1 + x/2 + x^2/6 + dots$）。题设即

  $ F(x) > 0 quad (x != 0), quad F(0) = 0, $

  所以 $x = 0$ 是 $F$ 的最小值点，且是定义域的内点。

  *② 极点效应：$F'(0) = 0$。* 由展开式 $f(x) = 1 + x/2 + x^2/6 + dots$ 得 $f'(0) = 1/2$；又 $(a^x)' = a^x ln a$，在 $x = 0$ 处为 $ln a$。于是

  $ F'(0) = 1/2 - ln a = 0 quad => quad ln a = 1/2 quad => quad a = sqrt(e). $

  *③ 验证 $a = sqrt(e)$。* 需证 $x != 0$ 时 $(e^x - 1)/x > e^(x/2)$。令 $x = 2u$（$u != 0$），不等式等价于

  $ (e^(2u) - 1)/(2u) > e^u quad <=> quad e^(2u) - 1 > 2u e^u quad <=> quad e^u - e^(-u) > 2u. $

  设 $h(u) = e^u - e^(-u) - 2u$，则 $h(0) = 0$，且

  $ h'(u) = e^u + e^(-u) - 2 >= 0, $

  （由基本不等式 $e^u + e^(-u) >= 2$，等号只在 $u = 0$ 处成立），故 $h$ 严格递增，$u != 0$ 时 $h(u) > h(0) = 0$，原不等式成立。

  所以 $a = sqrt(e)$。

  *要点*：把"$f(x) > g(x)$ 恒成立"移项成 $F(x) = f(x) - g(x) > 0$；若某*内点* $x_0$ 处恰好 $F(x_0) = 0$（本题 $x = 0$ 处两边极限都是 $1$），就又能用极点效应：$F'(x_0) = 0$ 一步定出参数（$ln a = 1/2$）。找取等点常要先看 $f$ 在 $x_0$ 处的展开式（本题 $f(x) = 1 + x/2 + dots$）。

]
