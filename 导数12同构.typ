#import "template.typ": *

#set document(title: [导数同构])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 一、什么是"同构"

  有些式子看起来"东一块西一块"，但只要用 $x = e^(ln x)$（$x > 0$）把字母换成指数，就会露出"同一个外函数作用在两个结构相同的内层式子"的样子。例如

  $ x e^x = e^(ln x) dot e^x = e^(x + ln x) $

  于是

  $ x e^x - x - ln x = e^(x + ln x) - (x + ln x) = g(t) quad (t = x + ln x) $

  其中 $g(t) = e^t - t$——只剩一个"外函数"和一个"内层式子" $t = x + ln x$。

  常用变形（$x > 0$，$a > 0$）：

  - $x = e^(ln x)$；
  - $x e^x = e^(x + ln x)$（乘积化成指数和）；
  - $a e^x = e^(x + ln a)$；
  - $x + ln x = ln(x e^x)$（和化成一个对数）；
  - $x - ln x = ln(e^x / x)$。

  处理套路：

  - *第一步*：把各项都改写成"同一个内层式子 $t$"的形式；
  - *第二步*：设内层式子为 $t$，把原问题化为"外函数 $g(t)$ 的最值"问题；
  - *第三步*：求 $g$ 的最值，最后再说明内层式子 $t$ 确实能取到该值（这一步常要用单调性，往往就是"隐零点"）。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】填空题：$x e^x$ 与 $x + ln x$ 同构

  *【例 1】* 函数 $f(x) = x e^x - x - ln x$ 的最小值为 #underline[#h(2em)]。

  === 解析

  *① 定义域与改写。* 定义域为 $x > 0$。利用 $x = e^(ln x)$：

  $ f(x) = e^(ln x) dot e^x - (x + ln x) = e^(x + ln x) - (x + ln x) $

  *② 换元，露出外函数。* 令

  $ t = x + ln x quad (x > 0) $

  则

  $ f(x) = g(t), quad g(t) = e^t - t quad (t in RR) $

  *③ 求外函数 $g$ 的最小值。*

  $ g'(t) = e^t - 1 quad => quad g'(t) = 0 <=> t = 0 $

  $t < 0$ 时 $g'(t) < 0$、$t > 0$ 时 $g'(t) > 0$，所以 $g$ 在 $(-oo, 0)$ 上递减、在 $(0, +oo)$ 上递增，最小值

  $ g(0) = e^0 - 0 = 1 $

  即对一切 $t in RR$ 都有 $g(t) >= 1$，等号仅当 $t = 0$。

  *④ 内层式子能取到 $t = 0$ 吗？* 记 $h(x) = x + ln x$，则

  $ h'(x) = 1 + 1/x > 0 quad (x > 0) $

  所以 $h$ 在 $(0, +oo)$ 上严格递增；又 $x -> 0^+$ 时 $h(x) -> -oo$，而 $h(1) = 1 > 0$，故存在唯一的 $x_0 in (0, 1)$ 使

  $ h(x_0) = x_0 + ln x_0 = 0 quad (即 x_0 = e^(-x_0), quad x_0 e^(x_0) = 1, quad x_0 ≈ 0.5671) $

  于是 $t$ 可以取到 $0$：此时 $f(x_0) = g(0) = 1$。

  *⑤ 结论。* 由 ③ ④ 得 $f(x) >= 1$ 且 $f(x_0) = 1$，所以 $f(x)$ 的最小值为

  $ 1 $

  故填 $1$。

  *验证（直接求导的"隐零点"做法）*：

  $ f'(x) = e^x + x e^x - 1 - 1/x = ((x + 1)(x e^x - 1))/x $

  所以 $f'$ 的符号由 $x e^x - 1$ 决定：$x e^x < 1$（即 $x < x_0$）时 $f' < 0$，$x e^x > 1$（即 $x > x_0$）时 $f' > 0$，最小值在 $x = x_0$ 处取得，且

  $ f(x_0) = x_0 e^(x_0) - x_0 - ln x_0 = 1 - x_0 + x_0 = 1 $

  （用了 $x_0 e^(x_0) = 1$ 与 $ln x_0 = -x_0$。）这与同构法的结果一致。顺便一提，本题的函数与《导数10 隐零点》例 2 中的 $g(x) = x e^x - x - ln x - 1$ 只相差常数 $1$（那里求得的最小值是 $0$）。

  *法 2（换成"对数型"外函数，式子里不再出现指数）。* 同构不必非往指数上凑，也可以把"乘积"化成一个*对数*：

  $ x + ln x = ln(e^x dot x) = ln(x e^x) $

  所以

  $ f(x) = x e^x - (x + ln x) = x e^x - ln(x e^x) $

  令

  $ u = x e^x quad (x > 0) $

  则

  $ f(x) = g(u), quad g(u) = u - ln u quad (u > 0) $

  这里的外函数 $g$ *只含对数、不含指数*，求导轻便得多：

  $ g'(u) = 1 - 1/u quad => quad g'(u) = 0 <=> u = 1 $

  $0 < u < 1$ 时 $g'(u) < 0$、$u > 1$ 时 $g'(u) > 0$，所以 $g$ 在 $(0, 1)$ 上递减、在 $(1, +oo)$ 上递增，最小值

  $ g(1) = 1 - ln 1 = 1 $

  （这其实就是基本不等式 $ln u <= u - 1$ 的变形：$u - ln u >= 1$，等号仅在 $u = 1$ 处成立。）

  最后验证 $u = 1$ 能取到：由 $u(x) = x e^x$ 在 $(0, +oo)$ 上严格递增（$u'(x) = (x + 1) e^x > 0$），且 $u(0^+) = 0 < 1 < u(1) = e$，知存在唯一的 $x_0 in (0, 1)$ 使 $x_0 e^(x_0) = 1$，此时 $f(x_0) = g(1) = 1$。所以最小值仍为 $1$。

  （两种同构是"同一件事的两面"：$u = x e^x$ 与 $t = x + ln x$ 互为反函数（$u = e^t$、$t = ln u$），于是 $e^t - t$ 与 $u - ln u$ 是同一个外函数。选哪一侧就看谁求导更省事——本题 $u - ln u$ 只含对数，更轻便。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】填空题：$m e^(m x)$ 与 $ln x$ 同构

  *【例 2】* 函数 $f(x) = m e^(m x) - ln x$。当 $x > 0$ 时 $f(x) > 0$ 恒成立，则 $m$ 的取值范围为 #underline[#h(2em)]。

  === 解析

  *① 先定 $m$ 的符号。* 取 $x = 1$：

  $ f(1) = m e^m - ln 1 = m e^m > 0 quad => quad m > 0 $

  （因为 $e^m > 0$ 恒成立，故 $m e^m > 0$ 等价于 $m > 0$。）

  *② 同构：把 $m e^(m x)$ 写成同一个底 $e$ 的幂。* 当 $m > 0$ 时

  $ m e^(m x) = e^(ln m) dot e^(m x) = e^(m x + ln m) $

  于是条件变成

  $ f(x) = e^(m x + ln m) - ln x > 0 quad (x > 0) $

  *③ 用基本不等式 $e^u >= u + 1$（取 $u = m x + ln m$）。* 对一切 $u in RR$ 有 $e^u >= u + 1$，等号仅当 $u = 0$，所以

  $ f(x) >= (m x + ln m + 1) - ln x = (m x - ln x) + (ln m + 1) $

  只要右边恒正，$f(x) > 0$ 就成立。

  *④ 求 $m x - ln x$ 的最小值。* 记 $g(x) = m x - ln x$（$x > 0$）：

  $ g'(x) = m - 1/x quad => quad g'(x) = 0 <=> x = 1/m $

  $g$ 在 $(0, 1/m)$ 上递减、在 $(1/m, +oo)$ 上递增，最小值

  $ g(1/m) = 1 - ln(1/m) = 1 + ln m $

  代回 ③：

  $ f(x) >= (1 + ln m) + (1 + ln m) = 2 (1 + ln m) > 0 quad <=> quad ln m > -1 quad <=> quad m > 1/e $

  即 $m > 1/e$ 时 $f(x) > 0$ 恒成立（充分性）。

  *⑤ 必要性。* 若 $m <= 0$，由 ① 知 $f(1) = m e^m <= 0$，不合题意。若 $0 < m <= 1/e$，取 $x = 1/m$：

  $ f(1/m) = m e^(m dot 1/m) - ln(1/m) = m e + ln m <= 1 - 1 = 0 $

  （因为 $m <= 1/e$ 时 $m e <= 1$，且 $ln m <= -1$。）也不合题意。所以必须有 $m > 1/e$。

  *⑥ 结论。* $m$ 的取值范围为

  $ (1/e, +oo) quad 即 quad m > 1/e $

  故填 $m > 1/e$。

  （等号 $m = 1/e$ 时 $f(e) = 0$：此时 $u = m x + ln m = 0$ 与 $x = 1/m = e$ 同时成立，③ 与 ④ 的不等式同时取等——这正是"临界值"的由来。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】填空题：同乘 $x$ 后同构成 $Phi(t) = t(e^t + 1)$

  *【例 3】* 对任意的 $x > 0$，恒有 $a(e^(a x) + 1) >= 2 (x + 1/x) ln x$，则 $a$ 的最小值为 #underline[#h(2em)]。

  === 解析

  *① 同乘 $x$，把两边整理成同一个函数。* 两边同乘 $x > 0$（等价变形）：

  $ a x e^(a x) + a x >= 2 (x^2 + 1) ln x $

  左边 $= (a x) e^(a x) + (a x)$。右边用 $2 ln x = ln(x^2)$：$2 (x^2 + 1) ln x = (x^2 + 1) ln(x^2)$。若记

  $ Phi(t) = t e^t + t = t (e^t + 1) $

  则左边 $= Phi(a x)$；右边把 $x^2$ 写成 $e^t$（即取 $t = ln(x^2)$）也得到

  $ (x^2 + 1) ln(x^2) = (e^t + 1) t = Phi(t), quad t = ln(x^2) $

  所以原不等式等价于

  $ Phi(a x) >= Phi(ln(x^2)) quad (x > 0) $

  *② 证明 $Phi$ 在 $RR$ 上严格递增。*

  $ Phi'(t) = e^t + t e^t + 1 = e^t (1 + t) + 1, quad Phi''(t) = e^t (2 + t) $

  $Phi'$ 在 $(-oo, -2)$ 上递减、在 $(-2, +oo)$ 上递增，故最小值为

  $ Phi'(-2) = e^(-2) (1 - 2) + 1 = 1 - 1/e^2 > 0 $

  所以 $Phi'(t) > 0$ 恒成立，$Phi$ 在 $RR$ 上严格递增。

  *③ 脱去"外函数"，分离参数。* 由 $Phi$ 严格递增，$Phi(a x) >= Phi(ln(x^2))$ 等价于

  $ a x >= ln(x^2) = 2 ln x quad (x > 0) quad <=> quad a >= (2 ln x)/x quad (x > 0) $

  于是问题化为求 $k(x) = (2 ln x)/x$ 的最大值。

  *④ 求 $k(x) = (2 ln x)/x$ 的最大值。*

  $ k'(x) = 2 dot (1 - ln x)/x^2 quad => quad k'(x) = 0 <=> x = e $

  $0 < x < e$ 时 $k'(x) > 0$、$x > e$ 时 $k'(x) < 0$，所以 $k$ 在 $(0, e)$ 上递增、在 $(e, +oo)$ 上递减，最大值

  $ k(e) = (2 ln e)/e = 2/e $

  *⑤ 结论。* 不等式对一切 $x > 0$ 恒成立当且仅当

  $ a >= 2/e $

  所以 $a$ 的最小值为

  $ 2/e $

  故填 $2/e$。（$a = 2/e$ 且 $x = e$ 时两边相等——这就是临界情形。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】填空题：$ln x - 1/x$ 与 $a x - e^(-a x)$ 同构

  *【例 4】* 若 $ln x - a x - 1/x <= -e^(-a x)$ 对一切 $x > 0$ 恒成立，则 $a$ 的取值范围为 #underline[#h(2em)]。

  === 解析

  *① 移项，把两边写成"同一个函数"的形式。* 原不等式即

  $ ln x - a x - 1/x + e^(-a x) <= 0 quad (x > 0) $

  把 $-a x$ 与 $e^(-a x)$ 移到右边：

  $ ln x - 1/x <= a x - e^(-a x) $

  注意由 $x = e^(ln x)$ 得 $1/x = e^(-ln x)$，所以

  $ ln x - 1/x = ln x - e^(-ln x) $

  若记

  $ Psi(t) = t - e^(-t) quad (t in RR) $

  则左边 $= Psi(ln x)$，右边 $= Psi(a x)$。于是原不等式等价于

  $ Psi(ln x) <= Psi(a x) quad (x > 0) $

  *② 判断 $Psi$ 的单调性。*

  $ Psi'(t) = 1 + e^(-t) > 0 quad (t in RR) $

  所以 $Psi$ 在 $RR$ 上严格递增。

  *③ 脱去"外函数"，分离参数。* 由 $Psi$ 严格递增，$Psi(ln x) <= Psi(a x)$ 等价于

  $ ln x <= a x quad <=> quad a >= (ln x)/x quad (x > 0) $

  于是问题化为求 $k(x) = (ln x)/x$ 的最大值。

  *④ 求 $k(x) = (ln x)/x$ 的最大值。*

  $ k'(x) = (1 - ln x)/x^2 quad => quad k'(x) = 0 <=> x = e $

  $0 < x < e$ 时 $k'(x) > 0$、$x > e$ 时 $k'(x) < 0$，所以 $k$ 在 $(0, e)$ 上递增、在 $(e, +oo)$ 上递减，最大值

  $ k(e) = (ln e)/e = 1/e $

  *⑤ 结论。* 不等式对一切 $x > 0$ 恒成立当且仅当

  $ a >= 1/e $

  故 $a$ 的取值范围为 $[1/e, +oo)$，即填 $a >= 1/e$。

  （$a = 1/e$、$x = e$ 时两边相等：$1 - 1/e = 1 - 1/e$，正是临界情形。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】填空题：两边同加 $x - 1$，凑出 $e^t + t$

  *【例 5】* 已知函数 $f(x) = a e^(x - 1) - ln x + ln a$，若 $f(x) >= 1$ 恒成立，则 $a$ 的取值范围为 #underline[#h(2em)]。

  === 解析

  *① 定义域与整理。* 需 $x > 0$ 且 $a > 0$。把 $f(x) >= 1$ 写成

  $ a e^(x - 1) + ln a >= ln x + 1 quad (x > 0) $

  又 $a e^(x - 1) = e^(ln a) dot e^(x - 1) = e^(x - 1 + ln a)$。记

  $ t = x - 1 + ln a $

  则上式即

  $ e^t + ln a >= ln x + 1 $

  *② 关键一步：两边同时加上 $x - 1$。* 因为 $t - ln a = x - 1$，所以左边加上 $x - 1$ 后恰好凑成 $e^t + t$：

  $ e^t + t >= ln x + x $

  记

  $ Phi(u) = e^u + u $

  注意右边也是同一个函数：$x + ln x = e^(ln x) + ln x = Phi(ln x)$。于是原不等式等价于

  $ Phi(t) >= Phi(ln x), quad t = x - 1 + ln a $

  *③ 判断 $Phi$ 的单调性，脱去"外函数"。*

  $ Phi'(u) = e^u + 1 > 0 quad (u in RR) $

  所以 $Phi$ 在 $RR$ 上严格递增，$Phi(t) >= Phi(ln x)$ 等价于

  $ t >= ln x quad 即 quad x - 1 + ln a >= ln x quad <=> quad ln a >= ln x - x + 1 $

  *④ 分离参数。* 两边取指数：

  $ a >= e^(ln x - x + 1) = x e^(1 - x) quad (x > 0) $

  于是问题化为求 $k(x) = x e^(1 - x)$ 的最大值。

  *⑤ 求 $k(x) = x e^(1 - x)$ 的最大值。*

  $ k'(x) = e^(1 - x) (1 - x) quad => quad k'(x) = 0 <=> x = 1 $

  $0 < x < 1$ 时 $k'(x) > 0$、$x > 1$ 时 $k'(x) < 0$，所以 $k$ 在 $(0, 1)$ 上递增、在 $(1, +oo)$ 上递减，最大值

  $ k(1) = 1 dot e^0 = 1 $

  *⑥ 结论。* 恒成立当且仅当

  $ a >= 1 $

  故 $a$ 的取值范围为 $[1, +oo)$，即填 $a >= 1$。（$a = 1$、$x = 1$ 时 $f(1) = 1 - 0 + 0 = 1$，两边相等。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 6】参数同时在指数与对数里（整体设 $w = a e^(2x)$）

  *【例 6】* 若 $f(x) = x - ln x + a e^(2x) + ln a >= 0$ 在定义域内恒成立，求 $a$ 的取值范围。

  === 解析

  *① 定义域。* $x > 0$ 且 $a > 0$。

  *② 同构：把 $a e^(2x) + ln a$ 也写成"$u + ln u$"的样子。* 记

  $ w = a e^(2x) > 0 $

  则 $ln w = ln a + 2x$，即

  $ ln a = ln w - 2x $

  于是

  $ a e^(2x) + ln a = w + (ln w - 2x) = w + ln w - 2x $

  代回 $f(x)$：

  $ f(x) = x - ln x + w + ln w - 2x = (w + ln w) - (x + ln x) $

  记

  $ Psi(u) = u + ln u quad (u > 0) $

  则

  $ f(x) = Psi(w) - Psi(x) = Psi(a e^(2x)) - Psi(x) $

  *③ 判断 $Psi$ 的单调性。*

  $ Psi'(u) = 1 + 1/u > 0 quad (u > 0) $

  所以 $Psi$ 在 $(0, +oo)$ 上严格递增。

  *④ 脱去"外函数"，分离参数。* 由 $Psi$ 严格递增，

  $ f(x) >= 0 quad <=> quad Psi(a e^(2x)) >= Psi(x) quad <=> quad a e^(2x) >= x quad (x > 0) $

  它对一切 $x > 0$ 成立，等价于

  $ a >= x e^(-2x) quad (x > 0) $

  于是只需求 $k(x) = x e^(-2x)$ 的最大值。

  *⑤ 求 $k(x) = x e^(-2x)$ 的最大值。*

  $ k'(x) = e^(-2x) (1 - 2x) quad => quad k'(x) = 0 <=> x = 1/2 $

  $0 < x < 1/2$ 时 $k'(x) > 0$、$x > 1/2$ 时 $k'(x) < 0$，所以 $k$ 在 $(0, 1/2)$ 上递增、在 $(1/2, +oo)$ 上递减，最大值

  $ k(1/2) = 1/2 dot e^(-1) = 1/(2e) $

  *⑥ 结论。* 恒成立当且仅当

  $ a >= 1/(2e) $

  故 $a$ 的取值范围为 $[1/(2e), +oo)$。（$a = 1/(2e)$、$x = 1/2$ 时 $a e^(2x) = 1/2 = x$，此时 $f(x) = 0$ 取等。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 7】填空题：$e^(-a x) ln x < a$

  *【例 7】* 已知函数 $f(x) = e^(-a x) ln x$（$a > 0$）。若 $f(x) < a$ 恒成立，则 $a$ 的取值范围为 #underline[#h(2em)]。

  === 解析

  *① 只需考察 $x > 1$。* 定义域为 $x > 0$。

  - $0 < x <= 1$ 时 $ln x <= 0$，故 $f(x) <= 0 < a$，条件自动成立；
  - $x > 1$ 时 $ln x > 0$，条件是 $f(x) < a$，两边同乘 $e^(a x) > 0$，等价于

  $ ln x < a e^(a x) quad (x > 1) $

  *② 同构。* 因为 $a > 0$，

  $ a e^(a x) = e^(ln a) dot e^(a x) = e^(a x + ln a) $

  所以条件即

  $ ln x < e^(a x + ln a) quad (x > 1) $

  *③ 用 $e^u >= u + 1$ 放缩。* 取 $u = a x + ln a$，则 $e^(a x + ln a) >= a x + ln a + 1$，所以只要

  $ ln x <= a x + ln a + 1 quad <=> quad ln x - a x <= ln a + 1 quad (x > 1) $

  而 $g(x) = ln x - a x$ 的最大值在 $x = 1/a$ 处取得（$g'(x) = 1/x - a = 0$）：

  $ g(1/a) = ln(1/a) - 1 = -ln a - 1 $

  于是条件化为

  $ -ln a - 1 <= ln a + 1 quad <=> quad ln a >= -1 quad <=> quad a >= 1/e $

  *④ $a = 1/e$ 要舍去。* 当 $a = 1/e$ 时取 $x = e$（此时 $x = 1/a$ 且 $u = a x + ln a = 0$，两处都取等）：

  $ f(e) = e^(-1) ln e = 1/e = a $

  不满足严格的 $f(x) < a$，故 $a != 1/e$。

  *⑤ 必要性。* 对一般的 $a$，把 $x = e$ 代入即要求

  $ f(e) = e^(-a e) < a quad <=> quad -a e < ln a quad <=> quad phi(a) := -a e - ln a < 0 $

  而 $phi'(a) = -e - 1/a < 0$、$phi(1/e) = -1 + 1 = 0$，所以

  $ f(e) < a quad <=> quad a > 1/e $

  与 ③ 的充分性一致。故

  $ a > 1/e quad 即 quad a in (1/e, +oo) $

  填 $a > 1/e$。（"临界值"由 $x = e$ 处的取等给出。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 8】$a e^x ln x < x^2 + x ln a$（$x in (0, 1)$）

  *【例 8】* 已知函数 $f(x) = a e^x ln x$（$a != 0$）。若对一切 $x in (0, 1)$ 都有 $f(x) < x^2 + x ln a$，则 $a$ 的最小值为 #underline[#h(2em)]。

  === 解析

  *① 先说明 $a > 0$。* 式子里出现 $ln a$，故必须 $a > 0$。

  *② 同除 $x > 0$，再同构。* 原不等式即

  $ x^2 + x ln a - a e^x ln x > 0 quad (0 < x < 1) $

  同除 $x$：

  $ x + ln a - a e^x (ln x)/x > 0 $

  记

  $ u = a e^x > 0 $

  则 $ln u = ln a + x$，即 $x + ln a = ln u$，条件变成

  $ ln u - u (ln x)/x > 0 quad <=> quad (ln u)/u > (ln x)/x $

  （两边同除 $u > 0$。）记 $K(t) = (ln t)/t$，则条件就是

  $ K(u) > K(x) quad (0 < x < 1) $

  *③ 分析 $K$ 的单调性。*

  $ K'(t) = (1 - ln t)/t^2 $

  所以 $K$ 在 $(0, e)$ 上递增、在 $(e, +oo)$ 上递减。又 $0 < x < 1$ 时

  $ K(x) = (ln x)/x < 0 $

  于是分两种情况：

  - 若 $u > 1$：$K(u) = (ln u)/u > 0 > K(x)$，条件自动成立，而此时也有 $u > 1 > x$；
  - 若 $0 < u <= 1$：则 $u <= 1 < e$、$x < 1 < e$，两者都在 $K$ 的递增区间上，故 $K(u) > K(x) <=> u > x$。

  两种情况都等价于

  $ u > x quad 即 quad a e^x > x quad (0 < x < 1) $

  *④ 分离参数，求最值。* 上式等价于

  $ a > x e^(-x) quad (0 < x < 1) $

  记 $k(x) = x e^(-x)$，则

  $ k'(x) = e^(-x) (1 - x) > 0 quad (0 < x < 1) $

  所以 $k$ 在 $(0, 1)$ 上递增，上确界为 $k(1) = 1/e$（开区间内取不到）。因此

  $ a >= 1/e $

  *⑤ 结论。* $a$ 的最小值为

  $ 1/e $

  故填 $1/e$。（$a = 1/e$ 时对每个 $x in (0, 1)$ 仍有 $a e^x > x$，条件严格成立，所以 $1/e$ 可以取到；等号只出现在 $x -> 1^-$ 的极限处。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *识别同构*：见到 $x e^x$、$e^x/x$、$x + ln x$、$x ln x$、$a e^(a x)$ 这类"字母与 $ln$ 混在一起"的结构，先试着用 $x = e^(ln x)$ 把它们统一成"同一个内层式子"的函数（本题 $x e^x = e^(x + ln x)$）。
  - *换元设 $t$*：把内层式子设为 $t$（本题 $t = x + ln x$），原问题就变成"外函数 $g(t)$ 的最值"问题；$g$ 往往是很简单的基本函数（本题 $g(t) = e^t - t$，最小值 $g(0) = 1$）。
  - *别忘验证 $t$ 的取值范围*：$t$ 未必能取遍 $RR$，要说明它确实能取到最值点（本题 $h(x) = x + ln x$ 严格递增、$h(0^+) = -oo < 0 < h(1)$，故存在 $x_0$ 使 $t = 0$）。这一步常常就是"隐零点"问题。
  - *两种同构方向（例 1）*：同一个式子既可在*指数侧*同构（$x e^x = e^(x + ln x)$，内层 $t = x + ln x$，外函数 $e^t - t$），也可在*对数侧*同构（$x + ln x = ln(x e^x)$，内层 $u = x e^x$，外函数 $u - ln u$）；两侧由 $u = e^t$ 互换。含指数与对数混在一起时，*对数侧往往更好算*（求导只出现 $1/u$），值得优先试。
  - *同构 + 基本不等式放缩（例 2）*：把 $m e^(m x)$ 写成 $e^(m x + ln m)$ 后，用 $e^u >= u + 1$ 把指数"放缩掉"，条件就化为不含指数的不等式（例 2 中化为 $(m x - ln x) + (ln m + 1) > 0$），再对 $m x - ln x$ 求最小值（在 $x = 1/m$ 处取到 $1 + ln m$）即可。两处放缩的取等条件（$u = 0$ 与 $x = 1/m$）常常同时成立，据此定出临界参数值（例 2 中 $m = 1/e$）。求参数范围时记得*两头都做*：先由必要条件缩小范围（取 $x = 1$、$x = 1/m$ 等特殊值），再证明充分性。
  - *两边同乘一个正因式，凑出同一个函数（例 3）*：像 $a(e^(a x) + 1) >= 2(x + 1/x) ln x$ 这种"参数与 $x$ 纠缠在一起"的不等式，可以同乘 $x$ 化成 $Phi(a x) >= Phi(ln(x^2))$（$Phi(t) = t(e^t + 1)$）；证明 $Phi$ 严格递增后就能"脱去外函数"，得到 $a x >= 2 ln x$，再分离参数求 $(2 ln x)/x$ 的最大值（在 $x = e$ 处取到 $2/e$）。凑同构的常见手法：同乘 $x$（或 $x^2$、$e^x$ 等正因式）、把 $2 ln x$ 写成 $ln(x^2)$、把 $x^2$ 写成 $e^(ln(x^2))$。
  - *"$t - e^(-t)$"型同构（例 4）*：遇到 $ln x - 1/x$ 这样的"对数与倒数"组合，注意 $1/x = e^(-ln x)$，于是 $ln x - 1/x = Psi(ln x)$（$Psi(t) = t - e^(-t)$）；同样 $a x - e^(-a x) = Psi(a x)$。由 $Psi'(t) = 1 + e^(-t) > 0$ 知 $Psi$ 严格递增，脱去外函数即得 $ln x <= a x$，分离参数求 $(ln x)/x$ 的最大值（$1/e$，在 $x = e$ 处）。*关键动作*：把 $x$ 写成 $e^(ln x)$、把 $1/x$ 写成 $e^(-ln x)$，就能让式子里的每一项都归到同一个 $t$（或 $e^(-t)$）上。
  - *两边同加同一个量，凑出 $e^t + t$（例 5）*：$a e^(x - 1) - ln x + ln a >= 1$ 先整理成 $e^t + ln a >= ln x + 1$（$t = x - 1 + ln a$）；再在两边同时加上 $x - 1$（利用 $t - ln a = x - 1$），左边凑成 $e^t + t$、右边凑成 $ln x + x = e^(ln x) + ln x$，两边就都是 $Phi(u) = e^u + u$ 的函数值。$Phi'(u) = e^u + 1 > 0$ 严格递增，脱去外函数得 $t >= ln x$，即 $ln a >= ln x - x + 1$，分离参数求 $x e^(1 - x)$ 的最大值（在 $x = 1$ 处取到 $1$，故 $a >= 1$）。
  - *参数同时出现在指数与对数里（例 6）*：$a e^(2x) + ln a$ 中参数出现了两次，无法直接分离；令 $w = a e^(2x)$，由 $ln w = ln a + 2x$ 得 $ln a = ln w - 2x$，于是 $a e^(2x) + ln a = w + ln w - 2x$，原式立刻变成 $Psi(w) - Psi(x)$（$Psi(u) = u + ln u$）。$Psi' > 0$ 严格递增，脱去外函数得 $a e^(2x) >= x$，再分离参数求 $x e^(-2x)$ 的最大值（在 $x = 1/2$ 处取到 $1/(2e)$，故 $a >= 1/(2e)$）。*要点*：把"参数在指数上、又在对数里"的结构整体设成一个新变量 $w$，参数就只通过 $ln w$ 与 $x$ 关联了。
  - *先"缩小战场"再同构（例 7）*：$e^(-a x) ln x < a$ 在 $x <= 1$ 时左边 $<= 0 < a$，条件自动成立，因此只需讨论 $x > 1$；此时同乘 $e^(a x)$ 得 $ln x < a e^(a x) = e^(a x + ln a)$，用 $e^u >= u + 1$ 放缩成 $ln x - a x <= ln a + 1$，左边最大值 $-ln a - 1$（在 $x = 1/a$）给出 $a >= 1/e$；最后代入 $x = e$ 检查严格性，$a = 1/e$ 时 $f(e) = a$ 取等，故只能 $a > 1/e$。
  - *同除 $x$ 后同构成 $(ln t)/t$（例 8）*：$a e^x ln x < x^2 + x ln a$ 同除 $x$ 得 $x + ln a - a e^x (ln x)/x > 0$；令 $u = a e^x$，则 $x + ln a = ln u$，条件化为 $(ln u)/u > (ln x)/x$，即 $K(u) > K(x)$（$K(t) = (ln t)/t$）。注意 $K$ 只在 $(0, e)$ 上递增、且 $K(x) < 0$（$0 < x < 1$）：当 $u > 1$ 时自动成立，当 $u <= 1$ 时等价于 $u > x$，合起来等价于 $a e^x > x$，即 $a > x e^(-x)$，于是 $a >= 1/e$（上确界在 $x -> 1^-$ 处取到，可取值）。
  - *同构的常见目标形式*：把不等式两边整理成"同一个函数在两个点处的值"（如 $g(t_1) >= g(t_2)$ 或 $g(t_1) = g(t_2)$），再用 $g$ 的单调性把函数值的大小还原成 $t_1$ 与 $t_2$ 的大小。
]
