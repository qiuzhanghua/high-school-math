#import "template.typ": *

#set document(title: [回归分析])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、成对数据的相关关系

  *底层逻辑*：研究两个变量 $x$、$y$ 的成对数据时，常常发现它们*有关联*，但又没精确到"由一个能唯一确定另一个"的程度——这种关系叫做*相关关系*（区别于函数关系）。

  把成对数据 $(x_i, y_i)$ 画成*散点图*，能直观看出关联的形态：

  - 散点大致落在一条*直线*附近 $->$ *线性相关*（斜向上为正相关，斜向下为负相关）；
  - 散点大致落在一条*曲线*附近 $->$ *非线性相关*（曲线相关）；
  - 散点杂乱无章 $->$ 不相关。

  === 1. 相关系数 $r$

  衡量线性相关程度的量是*（样本）相关系数*：

  $ r = (sum_(i=1)^n (x_i - overline(x))(y_i - overline(y)))/(sqrt(sum_(i=1)^n (x_i - overline(x))^2) sqrt(sum_(i=1)^n (y_i - overline(y))^2)), $

  其中 $overline(x)$、$overline(y)$ 分别是两组数据的平均数。它有三个要点：

  - *取值范围*：$r in [-1, 1]$；
  - *符号*：$r > 0$ 表示*正相关*（$y$ 随 $x$ 增大而增大的趋势），$r < 0$ 表示*负相关*；
  - *绝对值*：$|r|$ 越接近 $1$，散点越靠近一条直线（线性相关越强）；$|r|$ 越接近 $0$，线性相关越弱。$|r| = 1$ 时所有点严格在一条直线上。

  === 2. 一元线性回归

  当两个变量线性相关时，可以拟合一条直线来描述它们的关系，这条直线的方程叫做*经验回归方程*：

  $ hat(y) = hat(b) x + hat(a). $

  其中

  $ hat(b) = (sum_(i=1)^n (x_i - overline(x))(y_i - overline(y)))/(sum_(i=1)^n (x_i - overline(x))^2), quad hat(a) = overline(y) - hat(b) overline(x). $

  常用的两条性质：

  - *回归直线必过样本中心点* $(overline(x), overline(y))$；
  - *斜率与相关系数同号*：$hat(b) > 0 <==> r > 0$（正相关），$hat(b) < 0 <==> r < 0$（负相关）；若 $hat(b) = 0$，说明线性相关极弱。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 二、一个具体例子

  *例* 已知成对数据如下（$5$ 组）：

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 5pt,
      [$x$], [$1$], [$2$], [$3$], [$4$], [$5$],
      [$y$], [$2$], [$3$], [$5$], [$4$], [$6$],
    )
  ]

  求相关系数 $r$（精确到 $0.001$）与经验回归方程。

  *解析*：

  *① 求平均数。*

  $ overline(x) = (1 + 2 + 3 + 4 + 5)/5 = 3, quad overline(y) = (2 + 3 + 5 + 4 + 6)/5 = 4. $

  *② 算三个和。*

  $ sum (x_i - overline(x))(y_i - overline(y)) = (-2)(-2) + (-1)(-1) + 0 dot 1 + 1 dot 0 + 2 dot 2 = 9, $

  $ sum (x_i - overline(x))^2 = 4 + 1 + 0 + 1 + 4 = 10, quad sum (y_i - overline(y))^2 = 4 + 1 + 1 + 0 + 4 = 10. $

  *③ 求 $hat(b)$、$hat(a)$。*

  $ hat(b) = 9/10 = 0.9, quad hat(a) = 4 - 0.9 times 3 = 1.3, $

  故经验回归方程为

  $ hat(y) = 0.9 x + 1.3. $

  （它必过样本中心点 $(3, 4)$：$0.9 times 3 + 1.3 = 4$ ✓。）

  *④ 求相关系数。*

  $ r = 9/(sqrt(10) sqrt(10)) = 9/10 = 0.9. $

  因为 $r = 0.9 > 0$ 且接近 $1$，说明 $x$ 与 $y$ *正相关且线性相关程度较强*，与 $hat(b) = 0.9 > 0$ 相互印证。

  *⑤ 预测*：当 $x = 6$ 时，$hat(y) = 0.9 times 6 + 1.3 = 6.7$。

  === 要点

  - *先看散点图再算 $r$*：图形能判断"线性还是曲线"，$r$ 只刻画*线性*相关的强弱——数据明显呈曲线趋势时，$r$ 小并不代表"不相关"。
  - *$r$ 的公式本质是两个"中心化"向量的夹角余弦*：分子是协方差和，分母是两个平方和的平方根，所以 $|r| <= 1$。
  - *回归直线过样本中心点*：$hat(a) = overline(y) - hat(b) overline(x)$ 就是由这一点推出来的，可用它检验计算。
  - *$hat(b)$ 与 $r$ 同号*：$hat(b) = r dot sqrt((sum (y_i - overline(y))^2)/(sum (x_i - overline(x))^2))$，两者符号一致。
  - *"回归"是预测而非确定*：$hat(y)$ 是预测值（带帽子），只有当线性相关较强（$|r|$ 较大）时预测才有意义。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 三、练习

  === 【练 1】点全在一条直线上时的相关系数

  *【练 1】* 在研究线性回归模型时，若样本数据 $(x_i, y_i)$（$i = 1, 2, dots, n$）所对应的点都在直线 $y = -1/3 x + 2$ 上，则两组数据 $x_i$ 和 $y_i$ 的线性相关系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $-1$], [B. $1$], [C. $-1/3$], [D. $2$],
  )

  === 解析

  所有点都落在同一条直线上，说明 $x$ 与 $y$ 之间是*完全线性*关系，此时

  $ |r| = 1. $

  再定符号：直线 $y = -1/3 x + 2$ 的斜率为 $-1/3 < 0$，即 $x$ 增大时 $y$ 减小，属于*负相关*，所以 $r < 0$。故

  $ r = -1. $

  选 *#text(fill: rgb("#c7362e"))[A]*。

  *公式验证*：由 $y_i = -1/3 x_i + 2$ 得 $overline(y) = -1/3 overline(x) + 2$，于是

  $ y_i - overline(y) = -1/3 (x_i - overline(x)), quad sum (y_i - overline(y))^2 = 1/9 sum (x_i - overline(x))^2. $

  代入相关系数公式：

  $ r = (-1/3 sum (x_i - overline(x))^2)/(sqrt(sum (x_i - overline(x))^2) dot sqrt(1/9 sum (x_i - overline(x))^2)) = (-1/3)/(1/3) = -1. $

  === 要点

  - *"点都在同一条直线上"$<==>$ $|r| = 1$*：此时线性相关最强（没有"散开"的成分），相关系数只能取 $+-1$。
  - *符号由斜率决定*：斜率 $< 0$ 时 $r = -1$（负相关）；斜率 $> 0$ 时 $r = +1$（正相关）。
  - *别被干扰项骗了*：$2$ 是直线在 $y$ 轴上的截距（与相关系数无关），$-1/3$ 是斜率（也不是相关系数）——相关系数只反映"线性相关的方向与强弱"。
  - *公式验证最稳妥*：把 $y_i - overline(y)$ 用 $x_i - overline(x)$ 表示后代入 $r$ 的定义式，一眼看出结果是 $-1$。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 2】（多选）概率统计综合判断

  *【练 2】*（2025·湖南）下列关于概率统计的说法，正确的是（#h(0.8em)）

  - A. 数据 $-3, -1, 3, 7, 8, 9, 11, 15$ 的第 $25$ 百分位数是 $1$；
  - B. 若一组样本数据 $(x_i, y_i)$（$i = 1, 2, dots, n$）的对应样本点都在直线 $y = -1/3 x + 1$ 上，则这组样本数据的相关系数为 $-1/3$；
  - C. 已知随机变量 $X tilde B(n, p)$，若 $E(X) = 36$、$D(X) = 9$，则 $n = 48$；
  - D. 某班有 $50$ 名同学，一次考试后的数学成绩服从正态分布 $N(100, 10^2)$，则理论上说在 $90 tilde 100$ 分的人数约为 $17$ 人。（参考数据：$P(mu - sigma <= X <= mu + sigma) approx 0.6827$，$P(mu - 2 sigma <= X <= mu + 2 sigma) approx 0.9545$，$P(mu - 3 sigma <= X <= mu + 3 sigma) approx 0.9973$）

  === 解析

  *A（正确）*：数据已从小到大排好，共 $n = 8$ 个。计算 $n p = 8 times 25% = 2$ 为*整数*，按定义第 $25$ 百分位数取第 $2$ 个与第 $3$ 个数据的平均数：

  $ (-1 + 3)/2 = 1. $

  （若 $n p$ 不是整数，则取"不小于 $n p$ 的最小整数"对应的那个数据。）

  *B（错误）*：样本点全在同一条直线上，说明是完全线性相关，$|r| = 1$；又直线斜率为 $-1/3 < 0$，故 $r = -1$，而不是 $-1/3$（$-1/3$ 是直线的斜率）。

  *C（正确）*：由两点分布的期望、方差公式

  $ E(X) = n p = 36, quad D(X) = n p (1 - p) = 9, $

  两式相除得 $1 - p = 9/36 = 1/4$，故 $p = 3/4$，于是

  $ n = 36/p = 36/(3/4) = 48. $

  *D（正确）*：$N(100, 10^2)$ 中 $mu = 100$、$sigma = 10$，于是

  $ P(90 <= X <= 100) = P(mu - sigma <= X <= mu) = 1/2 P(mu - sigma <= X <= mu + sigma) approx 1/2 times 0.6827 = 0.34135. $

  所以 $90 tilde 100$ 分的人数约为

  $ 50 times 0.34135 approx 17.07 approx 17 ("人"). $

  故选 *#text(fill: rgb("#c7362e"))[ACD]*。

  === 要点

  - *百分位数先算 $n p$*：$n p$ 为整数取相邻两个数的平均，不是整数则向上取整取对应数据（本题 $8 times 25% = 2$ 为整数，故取第 $2$、$3$ 个的平均数 $1$）。
  - *点共线时 $|r| = 1$*：相关系数只可能取 $+-1$，符号由直线斜率的正负决定（本题斜率负，故 $r = -1$）。
  - *二项分布的 $n$、$p$ 由 $E$、$D$ 反求*：$E = n p$、$D = n p (1-p)$，两式相除先得 $1 - p = D/E$，再求 $n = E/p$。
  - *正态分布求人数分两步*：先把区间化为 $k sigma$ 的形式求概率（本题 $90 tilde 100$ 恰好是 $mu - sigma tilde mu$，占总概率的一半），再乘总人数。
  - *多选题逐个验证*：四个选项分别涉及百分位数、相关系数、二项分布、正态分布，是很好的"综合体检"题。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 3】（2025·江西）回归方程的理解

  *【练 3】* 在一定范围内，小麦对氮元素的吸收量与它的根长度具有线性相关关系。某实验中统计了根长度 $x$（单位：cm）与氮元素吸收量 $y$（单位：mg/天）的数据：

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 4pt,
      [$x$], [$9.9$], [$12.1$], [$14.8$], [$18.2$], [$19.9$],
      [$y$], [$0.30$], [$0.34$], [$0.42$], [$0.50$], [$0.55$],
    )
    #v(2pt)
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 4pt,
      [$x$], [$21.8$], [$25.1$], [$27.7$], [$30.4$], [$32.1$],
      [$y$], [$0.60$], [$0.71$], [$0.74$], [$0.78$], [$0.86$],
    )
  ]

  由表中数据可得 $overline(x) = 21.2$、$overline(y) = 0.58$，线性回归方程为 $y = 0.025 x + a$，则（#h(0.8em)）

  - A. $a = -0.05$
  - B. 变量 $y$ 与 $x$ 的相关系数 $r < 0$
  - C. 在一定范围内，小麦的根长度每增加 $1$ cm，它一天的氮元素吸收量平均增加 $0.025$ mg
  - D. 若对小麦的根长度与*钾*元素吸收量的相关数据进行统计，则对应回归方程不变

  === 解析

  *A（错误）*：回归直线必过样本中心点 $(overline(x), overline(y)) = (21.2, 0.58)$，代入 $y = 0.025 x + a$：

  $ 0.58 = 0.025 times 21.2 + a = 0.53 + a quad => quad a = 0.05, $

  不是 $-0.05$。

  *B（错误）*：回归方程的斜率为 $0.025 > 0$，而斜率与相关系数同号（$hat(b) > 0 <==> r > 0$），故 $r > 0$，不是 $r < 0$。

  *C（正确）*：斜率 $hat(b) = 0.025$ 的实际意义就是"$x$ 每增加 $1$ 个单位，$y$ 平均增加 $0.025$ 个单位"，即根长度每增加 $1$ cm，氮元素吸收量平均增加 $0.025$ mg。

  *D（错误）*：换成了"钾元素吸收量"，数据本身变了，回归方程（斜率、截距）通常都会改变，不可能保持不变。

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  === 要点

  - *回归直线过样本中心点* 是求截距最快的办法：$a = overline(y) - hat(b) overline(x) = 0.58 - 0.025 times 21.2 = 0.05$。
  - *斜率与相关系数同号*：$hat(b) > 0$ 必有 $r > 0$（正相关），这是判断"$r$ 的符号"最省事的依据。
  - *斜率的实际含义*：$hat(b)$ 表示"$x$ 每增加 $1$ 个单位时 $y$ 的平均变化量"，回答实际意义类问题就照这句话说。
  - *换变量就要重新拟合*：回归方程依赖于所研究的具体两个变量，换了因变量（氮 $->$ 钾）必须重新统计与计算。
  - *用数据自检*：本题 $overline(x) = 21.2$、$overline(y) = 0.58$ 可直接由表中数据算出，$hat(b) = 0.025$ 也合理，说明题目数据自洽。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 4】（2025·湖南，多选）决定系数与回归综合

  *【练 4】* 小王调查获得如下数据：

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 5pt,
      [$x$], [$2$], [$4$], [$7$], [$17$], [$30$],
      [$y$], [$1$], [$2$], [$3$], [$4$], [$5$],
    )
  ]

  已知 $r = (sum (x_i - overline(x))(y_i - overline(y)))/(sqrt(sum (x_i - overline(x))^2) sqrt(sum (y_i - overline(y))^2))$，$hat(b) = (sum (x_i - overline(x))(y_i - overline(y)))/(sum (x_i - overline(x))^2)$，$hat(a) = overline(y) - hat(b) overline(x)$。下列说法正确的有（#h(0.8em)）

  - A. 该数据组的线性回归方程（系数精确到 $0.01$）为 $hat(y) = 0.13 x + 1.44$；
  - B. 该数据组的相关系数 $r approx 0.94$，$|r|$ 很接近 $1$ 说明该数据组拟合效果很好；
  - C. 所有数据点中残差最小的是 $(2, 1)$；
  - D. 去掉数据点 $(4, 2)$ 后，回归直线会向下移动。

  === 解析

  *先算三个和*：$overline(x) = 12$、$overline(y) = 3$，且

  $ sum (x_i - overline(x))(y_i - overline(y)) = (-10)(-2) + (-8)(-1) + (-5) dot 0 + 5 dot 1 + 18 dot 2 = 69, $

  $ sum (x_i - overline(x))^2 = 100 + 64 + 25 + 25 + 324 = 538, quad sum (y_i - overline(y))^2 = 4 + 1 + 0 + 1 + 4 = 10. $

  *A（正确）*：$hat(b) = 69/538 approx 0.128 approx 0.13$；按题目要求把系数精确到 $0.01$，取 $hat(b) = 0.13$，于是

  $ hat(a) = overline(y) - hat(b) overline(x) = 3 - 0.13 times 12 = 1.44, $

  故 $hat(y) = 0.13 x + 1.44$。

  （若不先取整，$hat(a) = 3 - 0.12825 times 12 approx 1.46$——这正是这类题需要注意"先按精度取 $hat(b)$ 再算 $hat(a)$"的地方。）

  *B（正确）*：由公式

  $ r = 69/(sqrt(538) sqrt(10)) = 69/sqrt(5380) approx 0.94, $

  $|r| approx 0.94$ 很接近 $1$，说明样本点与回归直线贴得较近，拟合效果较好。

  *C（错误）*：把 $hat(y) = 0.13 x + 1.44$ 代入各点算残差

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 4pt,
      [$x$], [$2$], [$4$], [$7$], [$17$], [$30$],
      [残差 $y - hat(y)$], [$-0.70$], [$0.04$], [$0.65$], [$0.35$], [$-0.34$],
    )
  ]

  残差（按绝对值衡量）最小的是数据点 $(4, 2)$，而不是 $(2, 1)$；$(2,1)$ 的残差反而是 $-0.70$（偏得最多）。

  *D（正确）*：点 $(4,2)$ 的残差为 $+0.04$，说明它落在回归直线*上方*。去掉这个"偏高"的点后，回归直线会整体*向下*移动（可验证：新数据算得 $hat(b) approx 0.129$、$hat(a) approx 1.45$，在原 $overline(x) = 12$ 处，新直线给出约 $2.99$，低于原来的 $3.00$）。

  故选 *#text(fill: rgb("#c7362e"))[ABD]*。

  === 要点

  - *"系数精确到 $0.01$"有先后顺序*：先把 $hat(b)$ 取到 $0.01$，再用 $hat(a) = overline(y) - hat(b) overline(x)$ 求截距（本题得 $1.44$，不是 $1.46$）。
  - *$|r|$ 越接近 $1$ 拟合越好*：$r$ 的绝对值刻画线性相关的强弱，$0.94$ 已属"较强"。
  - *"残差最小"要看绝对值*：残差是 $y_i - hat(y_i)$，衡量拟合好坏看它的绝对值；本题最小的在 $x = 4$ 处。
  - *去掉一个点后直线的走向*：若该点在直线上方（残差为正），去掉后直线整体下移；在下方则上移——这是残差与回归直线关系的直观结论。
  - *三个"和"是全部计算的起点*：$sum (x_i - overline(x))(y_i - overline(y))$、$sum (x_i - overline(x))^2$、$sum (y_i - overline(y))^2$ 算好后，$r$、$hat(b)$、$hat(a)$ 都只是代入公式。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 5】（2023·河南）求回归方程与相关系数

  *【练 5】* 下表是某口罩厂今年的月份 $x$ 与订单 $y$（单位：万元）的几组对应数据：

  #align(center)[
    #table(
      columns: 6,
      stroke: 0.5pt,
      inset: 5pt,
      [月份 $x$], [$1$], [$2$], [$3$], [$4$], [$5$],
      [订单 $y$], [$20$], [$24$], [$m$], [$43$], [$52$],
    )
  ]

  (1) 求 $y$ 关于 $x$ 的线性回归方程，并估计 $6$ 月份该厂的订单数；

  (2) 求相关系数 $r$（精确到 $0.01$），说明 $y$ 与 $x$ 之间具有怎样的相关关系。

  *参考数据*：$sum_(i=1)^5 y_i = 175$，$sum x_i y_i = 608$，$sum (y_i - overline(y))^2 = 700$，$sqrt(7) approx 2.646$，$sqrt(10) approx 3.162$。

  *参考公式*：$r = (sum (x_i - overline(x))(y_i - overline(y)))/(sqrt(sum (x_i - overline(x))^2 dot sum (y_i - overline(y))^2))$；回归直线 $y = b x + a$，其中 $hat(b) = (sum x_i y_i - n overline(x) overline(y))/(sum x_i^2 - n overline(x)^2)$，$hat(a) = overline(y) - hat(b) overline(x)$。

  === 解析

  *① 先由 $sum y_i = 175$ 求 $m$。*

  $ 20 + 24 + m + 43 + 52 = 175 quad => quad m = 36. $

  （验算 $sum x_i y_i$：$1 times 20 + 2 times 24 + 3 times 36 + 4 times 43 + 5 times 52 = 20 + 48 + 108 + 172 + 260 = 608$ ✓，与题给一致。）

  *② 求平均数与两个和。*

  $ overline(x) = (1 + 2 + 3 + 4 + 5)/5 = 3, quad overline(y) = 175/5 = 35, $

  $ sum x_i^2 = 1 + 4 + 9 + 16 + 25 = 55, quad n overline(x)^2 = 5 times 9 = 45, quad sum (x_i - overline(x))^2 = 10. $

  *(1) 求回归方程。*

  $ hat(b) = (608 - 5 times 3 times 35)/(55 - 45) = 83/10 = 8.3, quad hat(a) = 35 - 8.3 times 3 = 10.1, $

  所以回归方程为

  $ hat(y) = 8.3 x + 10.1. $

  把 $x = 6$ 代入，估计 $6$ 月份的订单数为

  $ hat(y) = 8.3 times 6 + 10.1 = 49.8 + 10.1 = 59.9 ("万元"). $

  *(2) 求相关系数。* 分子为 $608 - 525 = 83$，分母为 $sqrt(10 times 700) = sqrt(7000)$：

  $ r = 83/sqrt(7000) = 83/(10 sqrt(70)) approx 83/83.67 approx 0.99. $

  （用题给数据：$10 sqrt(70) = 10 sqrt(7) sqrt(10) approx 10 times 2.646 times 3.162 approx 83.67$。）

  因为 $r approx 0.99 > 0$ 且非常接近 $1$，说明 $y$ 与 $x$ *正相关*，而且线性相关程度*很强*——月份增加时订单稳定上升。

  所以 (1) $m = 36$，回归方程 $hat(y) = 8.3 x + 10.1$，$6$ 月份订单约 $59.9$ 万元；(2) $r approx 0.99$，$y$ 与 $x$ 具有很强的正相关关系。

  === 要点

  - *先用 $sum y_i$ 定未知数*：表格里有字母 $m$ 时，一般先由它把 $m$ 解出来（本题 $m = 36$），再算回归方程。
  - *$hat(b)$ 的两个等价公式*：用题给数据直接算 $hat(b) = (sum x_i y_i - n overline(x) overline(y))/(sum x_i^2 - n overline(x)^2)$，或用中心化数据 $hat(b) = (sum (x_i - overline(x))(y_i - overline(y)))/(sum (x_i - overline(x))^2)$，结果相同。
  - *回归直线过样本中心点*：$hat(a) = overline(y) - hat(b) overline(x)$，算完 $hat(b)$ 立刻得 $hat(a)$，也可用它检验。
  - *$r$ 与 $hat(b)$ 的分子相同*：都是 $sum x_i y_i - n overline(x) overline(y) = 83$，先算好这个"公共分子"能省力。
  - *回答"怎样的相关关系"要答两点*：方向（$r > 0$ 是正相关）与强弱（$|r|$ 接近 $1$ 说明相关性强）。
  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 6】（2023·贵州）非线性回归（指数型）

  *【练 6】* 为研究某种细菌随天数 $x$ 变化的繁殖个数 $y$，收集数据如下：

  #align(center)[
    #table(
      columns: 7,
      stroke: 0.5pt,
      inset: 4pt,
      [天数 $x$], [$1$], [$2$], [$3$], [$4$], [$5$], [$6$],
      [繁殖个数 $y$], [$6$], [$12$], [$25$], [$49$], [$95$], [$190$],
    )
  ]

  (1) 作出散点图，并判断 $hat(y) = hat(b) x + hat(a)$ 与 $hat(y) = c_1 e^(c_2 x)$（$c_1 > 0$、$c_2 != 0$ 为常数）哪一个适宜作为 $y$ 关于 $x$ 的回归方程类型（给出判断即可）；

  (2) 对 $hat(y) = c_1 e^(c_2 x)$，令 $z = ln y$，可得 $z$ 关于 $x$ 具有线性关系，求回归方程。

  === 解析

  *(1) 选指数型 $hat(y) = c_1 e^(c_2 x)$。* 观察相邻两天的繁殖个数之比：

  $ 12/6 = 2, quad 25/12 approx 2.08, quad 49/25 approx 1.96, quad 95/49 approx 1.94, quad 190/95 = 2. $

  比值几乎恒为 $2$，说明细菌个数"每天约翻一倍"，增长越来越快（散点图呈向上弯曲的曲线），这是典型的*指数增长*，而线性回归只适合"匀速增长"的数据。故应选

  $ hat(y) = c_1 e^(c_2 x). $

  *(2) 先取对数化线性。* 对 $hat(y) = c_1 e^(c_2 x)$ 两边取自然对数：

  $ z = ln y = ln c_1 + c_2 x, $

  即 $z$ 与 $x$ 成*线性*关系（截距为 $ln c_1$、斜率为 $c_2$）。算出各 $z_i = ln y_i$ 后，用最小二乘法拟合 $z$ 关于 $x$ 的回归直线：

  $ overline(x) = 3.5, quad sum (x_i - overline(x))^2 = 17.5, quad sum (x_i - overline(x))(z_i - overline(z)) approx 12.0781, $

  于是

  $ hat(c)_2 = 12.0781/17.5 approx 0.6902, quad ln hat(c)_1 = overline(z) - hat(c)_2 overline(x) approx 1.1158. $

  所以

  $ hat(z) = 1.1158 + 0.6902 x quad => quad hat(y) = e^(1.1158 + 0.6902 x) approx 3.0519 e^(0.6902 x). $

  取 $x = 7$ 预测约 $382.6$ 个。另外，$r$ 应改用 $z$ 与 $x$ 的相关系数来衡量线性关系的强弱。

  === 要点

  - *先看图定类型*：数据"越涨越快、比值接近常数"$->$ 指数型 $hat(y) = c_1 e^(c_2 x)$；"大致沿直线"$->$ 线性型 $hat(y) = hat(b)x + hat(a)$。
  - *化非线性为线性*：对 $y = c_1 e^(c_2 x)$ 取对数得 $z = ln y = ln c_1 + c_2 x$，于是对 $(x, z)$ 做线性回归，再"还原"：$c_2 = hat(b)_z$、$c_1 = e^(hat(a)_z)$。
  - *换元后要"换回去"*：拟合得到的是 $hat(z)$，最终答案必须是关于 $y$ 的方程，别把 $hat(z) = 1.1158 + 0.6902x$ 当成最终结果。
  - *相关系数也要跟着换*：判断拟合好坏要用 $z$ 与 $x$（而不是 $y$ 与 $x$）的相关系数。
  - *用原始数据检验*：$c_1 approx 3.05$、$c_2 approx 0.69$（即每天约翻倍）与数据的直观走势一致，可用 $x = 1, 2, dots$ 代回核对。
  #line(length: 100%, stroke: 0.5pt + gray)

  === 【练 7】茶水温度模型（向室温衰减）

  *【练 7】* 为获得茶水温度 $y$（单位：℃）关于时间 $x$（单位：min）的回归模型，某小组在 $25$ ℃ 室温下实验，得到 $7$ 组数据，散点图呈"先快后慢地下降"的趋势，并算得

  #align(center)[
    #table(
      columns: 5,
      stroke: 0.5pt,
      inset: 5pt,
      [$overline(y)$], [$overline(w)$], [$sum (x_i - overline(x))(y_i - overline(y))$], [$sum (x_i - overline(x))(w_i - overline(w))$],
      [$73.5$], [$3.85$], [$-95$], [$-2.24$],
    )
  ]

  其中 $w_i = ln (y_i - 25)$，$overline(w) = 1/7 sum_(i=1)^7 w_i$。

  (1) 判断 $y = a + b x$ 与 $y = d c^x + 25$ 哪一个更适宜作为回归方程类型，并用表中数据建立回归方程；

  (2) 茶水温度降至 $60$ ℃ 口感最佳，求大约需要放置多长时间。

  === 解析

  *(1) 选 $y = d c^x + 25$。* 散点图显示温度*下降但越来越慢*（曲线向下凸），最终应趋于室温 $25$ ℃——这正是"向室温指数衰减"的形态。而 $y = a + b x$ 表示匀速下降，与图形不符。

  对 $y = d c^x + 25$ 移项、取对数：

  $ y - 25 = d c^x quad => quad ln (y - 25) = ln d + x ln c. $

  令 $w = ln (y - 25)$，则 $w$ 与 $x$ *线性相关*，可用最小二乘法拟合 $w$ 关于 $x$ 的回归直线。$7$ 组数据的时间为 $x = 1, 2, dots, 7$，故

  $ overline(x) = 4, quad sum (x_i - overline(x))^2 = 9 + 4 + 1 + 0 + 1 + 4 + 9 = 28. $

  于是

  $ ln hat(c) = (sum (x_i - overline(x))(w_i - overline(w)))/(sum (x_i - overline(x))^2) = (-2.24)/28 = -0.08, $

  $ ln hat(d) = overline(w) - (-0.08) overline(x) = 3.85 + 0.08 times 4 = 4.17. $

  所以

  $ hat(w) = 4.17 - 0.08 x quad => quad hat(y) = e^(4.17 - 0.08 x) + 25 approx 64.72 e^(-0.08 x) + 25, $

  即 $hat(c) = e^(-0.08) approx 0.9231$、$hat(d) = e^(4.17) approx 64.72$。

  （检验：$x = 0$ 时 $hat(y) approx 64.72 + 25 = 89.72$ ℃，与实验初始温度约 $90$ ℃ 相符。）

  *(2) 令 $hat(y) = 60$：*

  $ 64.72 e^(-0.08 x) + 25 = 60 quad => quad e^(-0.08 x) = 35/64.72 approx 0.5408. $

  两边取对数：

  $ -0.08 x = ln 0.5408 approx -0.6143 quad => quad x approx 7.68. $

  所以大约需要放置 $7.7$ 分钟（约 $8$ 分钟）才能降到 $60$ ℃、达到最佳口感。

  === 要点

  - *先看图形再定模型*：'先快后慢、趋于常数'$->$ 指数衰减型 $y = d c^x + 25$（那个 $25$ 就是室温，是"水平渐近线"）；"匀速下降"$->$ 线性型。
  - *"减去室温再取对数"是本题的关键一步*：$y - 25 = d c^x$ 才是指数式，$ln(y-25)$ 与 $x$ 成线性；题目给出的 $w = ln(y-25)$ 正是提示。
  - *先算 $sum (x_i - overline(x))^2$*：时间取 $1, 2, dots, 7$，$overline(x) = 4$，和为 $28$（本题没有直接给，需要自己算）。
  - *解方程时两边取对数*：$e^(-0.08x) = 0.5408$ $=>$ $x = (ln 0.5408)/(-0.08) approx 7.68$（注意符号，答案为正值）。
  - *用初始值检验*：把 $x = 0$ 代回，$hat(y)$ 应接近实验初始温度（本题 $89.72$ ℃ $approx 90$ ℃）。
]
