#import "template.typ": *

#set document(title: [导数求参全分])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 【例 1】单调性求参与交点个数求参

  *【例 1】* 已知函数 $f(x) = x^3/3 + a x$，$g(x) = -x^2 - a$（$a in RR$）。

  + 若 $F(x) = f(x) - g(x)$ 在 $[1, +oo)$ 上单调递增，求 $a$ 的最小值；
  + 若函数 $G(x) = f(x) + g(x)$ 的图象与 $y = a x$ 有且只有一个交点，求 $a$ 的取值范围。

  === （1）解析

  先求 $F(x)$：
  $ F(x) = f(x) - g(x) = x^3/3 + a x + x^2 + a = x^3/3 + x^2 + a x + a $

  求导得
  $ F'(x) = x^2 + 2x + a $

  因为 $F(x)$ 在 $[1, +oo)$ 上单调递增，所以 $F'(x) >= 0$ 对一切 $x >= 1$ 恒成立，即
  $ a >= -(x^2 + 2x) $

  对一切 $x >= 1$ 恒成立。函数 $y = -(x^2 + 2x)$ 的对称轴为 $x = -1$，故它在 $[1, +oo)$ 上单调递减，最大值为
  $ -(1^2 + 2 dot 1) = -3 $

  所以 $a >= -3$。当 $a = -3$ 时，
  $ F'(x) = x^2 + 2x - 3 = (x + 3)(x - 1) $
  在 $[1, +oo)$ 上恒有 $F'(x) >= 0$，且在 $(1, +oo)$ 上 $F'(x) > 0$，故 $F(x)$ 在 $[1, +oo)$ 上单调递增，符合题意。

  所以 $a$ 的最小值为 $-3$。

  === （2）解析

  先求 $G(x)$：
  $ G(x) = f(x) + g(x) = x^3/3 - x^2 + a x - a $

  由 $G(x) = a x$ 得
  $ x^3/3 - x^2 + a x - a = a x quad => quad x^3/3 - x^2 = a $

  令
  $ h(x) = x^3/3 - x^2 $

  则问题转化为：直线 $y = a$ 与曲线 $y = h(x)$ 恰有一个公共点。

  求导得
  $ h'(x) = x^2 - 2x = x (x - 2) $

  令 $h'(x) = 0$，得 $x = 0$ 或 $x = 2$。列表如下：

  #table(
    columns: (auto, 1fr, auto, 1fr, auto, 1fr),
    stroke: 0.5pt,
    align: center,
    inset: 5pt,
    [$x$], [$(-oo, 0)$], [$0$], [$(0, 2)$], [$2$], [$(2, +oo)$],
    [$h'(x)$], [$+$], [$0$], [$-$], [$0$], [$+$],
    [$h(x)$], [单调递增], [极大值 $0$], [单调递减], [极小值 $-4/3$], [单调递增],
  )

  所以 $h(x)$ 在 $(-oo, 0)$ 上从 $-oo$ 单调递增到 $0$，在 $(0, 2)$ 上单调递减到 $-4/3$，在 $(2, +oo)$ 上从 $-4/3$ 单调递增到 $+oo$。结合图象讨论：

  - 当 $a > 0$ 时，直线 $y = a$ 与曲线只在 $(2, +oo)$ 上有 1 个公共点；
  - 当 $a = 0$ 时，$h(x) = 0$ 即 $x^2 (x/3 - 1) = 0$，得 $x = 0$、$x = 3$，共 2 个公共点；
  - 当 $-4/3 < a < 0$ 时，在 $(-oo, 0)$ 与 $(2, +oo)$ 上各有 1 个公共点，共 2 个；
  - 当 $a = -4/3$ 时，$h(x) = -4/3$ 即 $x^3 - 3x^2 + 4 = 0$，即 $(x - 2)^2 (x + 1) = 0$，得 $x = -1$、$x = 2$，共 2 个公共点；
  - 当 $a < -4/3$ 时，只在 $(-oo, 0)$ 上有 1 个公共点。

  综上，$a$ 的取值范围为 $(-oo, -4/3) union (0, +oo)$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】单调区间与区间内零点个数求参

  *【例 2】* 已知函数 $f(x) = -x^2 + a x + ln x$（$a in RR$）。

  + 若 $a = 1$，求曲线 $f(x)$ 的单调区间；
  + 设函数 $f(x)$ 在 $[1/e, e]$ 上有两个零点，求实数 $a$ 的取值范围。

  === （1）解析

  当 $a = 1$ 时，$f(x) = -x^2 + x + ln x$，定义域为 $(0, +oo)$。求导：
  $ f'(x) = -2x + 1 + 1/x = (-2x^2 + x + 1)/x = ((2x + 1)(1 - x))/x $

  因为 $x > 0$，所以 $2x + 1 > 0$，$f'(x)$ 的符号由 $1 - x$ 决定：

  - 当 $0 < x < 1$ 时，$f'(x) > 0$，$f(x)$ 单调递增；
  - 当 $x > 1$ 时，$f'(x) < 0$，$f(x)$ 单调递减。

  所以 $f(x)$ 的单调递增区间为 $(0, 1)$，单调递减区间为 $(1, +oo)$。

  === （2）解析

  令 $f(x) = 0$，即 $-x^2 + a x + ln x = 0$。因为 $x > 0$，分离参数得
  $ a = x - ln x/x $

  令
  $ phi(x) = x - ln x/x, quad x in [1/e, e] $

  则问题转化为：直线 $y = a$ 与曲线 $y = phi(x)$ 在 $[1/e, e]$ 上恰有两个公共点。

  求导：
  $ phi'(x) = 1 - (1 - ln x)/x^2 = (x^2 - 1 + ln x)/x^2 $

  记 $g(x) = x^2 - 1 + ln x$，则
  $ g'(x) = 2x + 1/x > 0 $
  故 $g(x)$ 在 $(0, +oo)$ 上单调递增，又 $g(1) = 0$。于是

  - 当 $1/e <= x < 1$ 时，$g(x) < 0$，即 $phi'(x) < 0$，$phi(x)$ 单调递减；
  - 当 $1 < x <= e$ 时，$g(x) > 0$，即 $phi'(x) > 0$，$phi(x)$ 单调递增。

  所以 $phi(x)$ 在 $x = 1$ 处取得最小值
  $ phi(1) = 1 $

  又
  $ phi(1/e) = 1/e + e, quad phi(e) = e - 1/e $
  且 $1 < e - 1/e < e + 1/e$。

  结合图象可知：

  - 当 $a < 1$ 时，无公共点；
  - 当 $a = 1$ 时，只有 $x = 1$ 一个公共点；
  - 当 $1 < a <= e - 1/e$ 时，在 $(1/e, 1)$ 与 $(1, e]$ 上各有 1 个公共点，共 2 个；
  - 当 $e - 1/e < a <= e + 1/e$ 时，只有 $(1/e, 1)$ 上 1 个公共点；
  - 当 $a > e + 1/e$ 时，无公共点。

  综上，$a$ 的取值范围为 $(1, e - 1/e]$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【练 2】讨论单调性与两零点求参

  *【练 2】* 已知函数 $f(x) = ln x/x^2$。

  + 讨论 $f(x)$ 的单调性；
  + 若函数 $g(x) = e^x + x^4 f(x) - x^2 - a x$ 有两个零点，求实数 $a$ 的取值范围。

  === （1）解析

  函数的定义域为 $(0, +oo)$。求导：
  $ f'(x) = (1/x dot x^2 - ln x dot 2x)/x^4 = (x - 2x ln x)/x^4 = (1 - 2 ln x)/x^3 $

  因为 $x^3 > 0$，所以 $f'(x)$ 的符号由 $1 - 2 ln x$ 决定：

  - 当 $1 - 2 ln x > 0$，即 $0 < x < sqrt(e)$ 时，$f'(x) > 0$，$f(x)$ 单调递增；
  - 当 $1 - 2 ln x < 0$，即 $x > sqrt(e)$ 时，$f'(x) < 0$，$f(x)$ 单调递减。

  所以 $f(x)$ 在 $(0, sqrt(e))$ 上单调递增，在 $(sqrt(e), +oo)$ 上单调递减，最大值为
  $ f(sqrt(e)) = 1/(2e) $

  === （2）解析

  先化简 $g(x)$。因为
  $ x^4 f(x) = x^4 dot ln x/x^2 = x^2 ln x $
  所以（定义域为 $x > 0$）
  $ g(x) = e^x + x^2 ln x - x^2 - a x $

  令 $g(x) = 0$，并把等式两边同除以 $x > 0$，得
  $ a = e^x/x + x ln x - x $

  令
  $ phi(x) = e^x/x + x ln x - x quad (x > 0) $
  则问题转化为：直线 $y = a$ 与曲线 $y = phi(x)$ 恰有两个公共点。

  求导：
  $ phi'(x) = (e^x (x - 1))/x^2 + ln x $

  于是

  - 当 $0 < x < 1$ 时，$x - 1 < 0$ 且 $ln x < 0$，故 $phi'(x) < 0$，$phi(x)$ 单调递减；
  - 当 $x > 1$ 时，$x - 1 > 0$ 且 $ln x > 0$，故 $phi'(x) > 0$，$phi(x)$ 单调递增。

  所以 $phi(x)$ 在 $x = 1$ 处取得最小值
  $ phi(1) = e - 1 $

  又当 $x -> 0^+$ 时 $phi(x) -> +oo$（此时 $e^x/x -> +oo$，而 $x ln x - x -> 0$）；当 $x -> +oo$ 时 $phi(x) -> +oo$。

  结合图象可知：

  - 当 $a < e - 1$ 时，无公共点；
  - 当 $a = e - 1$ 时，只有 $x = 1$ 一个公共点；
  - 当 $a > e - 1$ 时，在 $(0, 1)$ 与 $(1, +oo)$ 上各有 1 个公共点，共 2 个。

  综上，$a$ 的取值范围为 $(e - 1, +oo)$。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - "在某区间上单调递增（减）"常转化为导函数在该区间上恒 $>= 0$（$<= 0$）的恒成立问题，再用*分离参数*法求参数的最值：本题即由 $a >= -(x^2 + 2x)$ 对 $x >= 1$ 恒成立，得 $a >= -3$；最后要检验端点取等时是否仍满足严格单调。
  - "两图象恰有 $n$ 个交点"常转化为对应方程恰有 $n$ 个实根；若方程能整理成 $a = h(x)$ 的形式，则进一步转化为*水平直线 $y = a$ 与曲线 $y = h(x)$ 的公共点个数*问题。
  - 判断公共点个数时，只需画出 $h(x)$ 的大致图象：借助 $h'(x)$ 的符号确定单调区间与极值，再让水平线 $y = a$ 上下移动，数清交点个数；特别注意极值点处（如例 1 中 $a = -4/3$、$a = 0$）的临界情形要单独讨论。
  - "在闭区间 $[m, n]$ 上有两个零点"与"直线 $y = a$ 与曲线有两个公共点"本质相同，但要注意区间端点的开闭：端点能取到时对应的 $a$ 值要包含（如例 2 中 $a = e - 1/e$ 时 $x = e$ 是零点，故右端取闭）；同时先比较区间两端点处的函数值与极值的大小，才能确定 $a$ 的确切范围。
  - 由 $g(x) = 0$ 分离参数前先化简（如练 2 中 $x^4 f(x) = x^2 ln x$），再由 $x > 0$ 同除 $x$ 得到 $a = phi(x)$；若 $phi'(x)$ 的每一项在 $0 < x < 1$ 与 $x > 1$ 上分别同号，则可直接分段判定 $phi'$ 的符号。
]
