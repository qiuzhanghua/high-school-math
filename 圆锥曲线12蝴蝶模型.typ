#import "template.typ": *

#set document(title: [圆锥曲线蝴蝶定理与蝴蝶模型])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 一、圆锥曲线蝴蝶定理

  === 命题

  设 $M$ 是圆锥曲线（以椭圆为例）的一条弦 $P Q$ 的中点。过 $M$ 任作两条弦 $A B$、$C D$，直线 $A D$、$B C$ 分别交直线 $P Q$ 于 $E$、$F$，则

  $ M E = M F, $

  即 $E$、$F$ 关于点 $M$ 对称。

  === 证明

  *第一步：把坐标系"摆正"。* 取 $M$ 为原点，直线 $P Q$ 为 $x$ 轴。因为 $M$ 是弦 $P Q$ 的中点，可设

  $ P(-p, 0), quad Q(p, 0) quad (p > 0). $

  把圆锥曲线的方程写成一般式

  $ a x^2 + b x y + c y^2 + d x + e y + f = 0 quad (a != 0). $

  由于 $P$、$Q$ 在曲线上，令 $y = 0$ 得 $a x^2 + d x + f = 0$，而它的两根是 $plus.minus p$，所以

  $ d = 0, quad f = -a p^2. $

  两边除以 $a$，曲线方程化为

  $ x^2 + beta x y + gamma y^2 + delta y = p^2. $ <eq:圆锥曲线12蝴蝶模型1>

  （这条式子的特点：不含一次项 $x$，常数项为 $-p^2$——这正是"$M$ 是弦的中点"留下的痕迹。）

  *第二步：设两条弦，用韦达定理。* 设两条弦所在直线为

  $ y = k x, quad y = k' x quad (k != k'), $

  斜率不存在的情形把 $x$、$y$ 的角色互换即可，证明完全相同。

  把 $y = k x$ 代入 (1)：

  $ (1 + beta k + gamma k^2) x^2 + delta k x - p^2 = 0. $

  设两根为 $x_1$、$x_2$（对应 $A$、$B$），并令 $s_i = 1/x_i$，则 $s_i$ 满足

  $ -p^2 s^2 + delta k s + (1 + beta k + gamma k^2) = 0, $

  由韦达定理

  $ s_1 + s_2 = (delta k)/p^2. $ <eq:圆锥曲线12蝴蝶模型2>

  同理，把 $y = k' x$ 代入可得另一条弦的两根 $x_3$、$x_4$（对应 $C$、$D$），记 $s_3 = 1/x_3$、$s_4 = 1/x_4$，则

  $ s_3 + s_4 = (delta k')/p^2. $ <eq:圆锥曲线12蝴蝶模型3>

  *第三步：求 $E$、$F$ 的横坐标。* 已知 $A(x_1, k x_1)$、$D(x_4, k' x_4)$，而直线与 $x$ 轴的交点横坐标可用"两点式的截距公式"算：

  $ x_E = frac(x_1 y_D - x_4 y_A, y_D - y_A) = frac(x_1 x_4 (k' - k), k' x_4 - k x_1) = frac(k' - k, k' s_1 - k s_4). $

  这里最后一步是把分子分母同除以 $x_1 x_4$；同理，对 $B(x_2, k x_2)$、$C(x_3, k' x_3)$：

  $ x_F = frac(k' - k, k' s_2 - k s_3). $

  *第四步：相加。* 把两式取倒数再相加：

  $
    frac(1, x_E) + frac(1, x_F) = frac(k' s_1 - k s_4 + k' s_2 - k s_3, k' - k) = frac(k' (s_1 + s_2) - k (s_3 + s_4), k' - k).
  $

  代入 (2)、(3)：

  $ frac(1, x_E) + frac(1, x_F) = frac(k' dot (delta k)/p^2 - k dot (delta k')/p^2, k' - k) = 0. $

  所以 $x_E = -x_F$，即 $E$、$F$ 关于原点（也就是 $M$）对称，

  $ M E = M F. $

  证毕。#qed

  说明：本题按"$P Q$ 水平"摆放，一般位置只需先作平移，同理可证。双曲线、抛物线（取 $x$、$y$ 角色互换或取极限）结论相同。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 二、椭圆蝴蝶模型的斜率证明

  === 命题

  设椭圆

  $ frac(x^2, a^2) + frac(y^2, b^2) = 1 $

  的长轴端点为

  $ A(-a, 0), quad B(a, 0). $

  设 $T(t, 0)$ 为长轴上一点，其中 $-a < t < a$。过 $T$ 作直线交椭圆于 $P, Q$ 两点。记

  $ k_1 = k_(A P), quad k_2 = k_(B Q). $

  则

  $ frac(k_1, k_2) = frac(a - t, a + t), $

  在斜率存在且 $k_2 != 0$ 的条件下成立。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 证明

  设直线 $P Q$ 的方程为

  $ x = m y + n. $

  因为它过点 $T(t, 0)$，代入得 $n = t$，所以

  $ P Q: quad x = m y + t. $

  （这条设法能表示过 $T$ 的一切"非水平"直线；水平的 $x$ 轴与椭圆只交于 $A$、$B$，是退化情形，不影响结论。）

  代入椭圆方程 $x^2/a^2 + y^2/b^2 = 1$，两边乘 $a^2 b^2$：

  $ b^2 (m y + t)^2 + a^2 y^2 = a^2 b^2, $

  整理成关于 $y$ 的二次方程

  $ (a^2 + b^2 m^2) y^2 + 2 b^2 m t y + b^2 (t^2 - a^2) = 0. $ <eq:圆锥曲线12蝴蝶模型1>

  设 $P(x_1, y_1)$、$Q(x_2, y_2)$，由韦达定理

  $ y_1 + y_2 = (-2 b^2 m t)/(a^2 + b^2 m^2), quad y_1 y_2 = (b^2 (t^2 - a^2))/(a^2 + b^2 m^2). $ <eq:圆锥曲线12蝴蝶模型2>

  *关键一步（非对称韦达）*：要算的式子里 $y_1$、$y_2$ 地位并不对称（一个配 $A$、一个配 $B$），单靠 $y_1 + y_2$、$y_1 y_2$ 凑不出来。把 (2) 中两式相除，$b^2$ 与分母一起消掉：

  $ (y_1 y_2)/(y_1 + y_2) = (t^2 - a^2)/(-2 m t), $

  即

  $ 2 t m y_1 y_2 = (a^2 - t^2)(y_1 + y_2). $ <eq:圆锥曲线12蝴蝶模型3>

  (3) 就是本题的"非对称韦达"关系式——它把 $y_1 y_2$ 与 $y_1 + y_2$ 挂了钩，正好用来收拾不对称的表达式（$y_1 + y_2 = 0$ 时两边同为 $0$，仍然成立）。

  下面算斜率之比。由 $A(-a, 0)$、$B(a, 0)$：

  $ k_1 = k_(A P) = y_1/(x_1 + a), quad k_2 = k_(B Q) = y_2/(x_2 - a). $

  又由直线方程 $x = m y + t$ 得 $x_1 = m y_1 + t$、$x_2 = m y_2 + t$，所以

  $ frac(k_1, k_2) = frac(y_1 (x_2 - a), y_2 (x_1 + a)) = frac(y_1 (m y_2 + t - a), y_2 (m y_1 + t + a)). $

  要证 $k_1/k_2 = (a - t)/(a + t)$，只需交叉相乘后验证

  $ (a + t) y_1 (m y_2 + t - a) = (a - t) y_2 (m y_1 + t + a). $ <eq:圆锥曲线12蝴蝶模型4>

  把 (4) 两边展开，并把含 $y_1$、$y_2$ 的项分别整理（注意 $(a + t)(t - a) = -(a^2 - t^2)$、$(a - t)(t + a) = a^2 - t^2$）：

  $ (a + t) m y_1 y_2 - (a - t) m y_1 y_2 - (a^2 - t^2) y_1 - (a^2 - t^2) y_2 = 0, $

  即

  $ 2 t m y_1 y_2 - (a^2 - t^2)(y_1 + y_2) = 0. $

  而它正是 (3)——已经由韦达定理保证成立！所以 (4) 成立，即

  #align(center)[#box($ frac(k_1, k_2) = frac(a - t, a + t) $)]

  证毕。#qed
  === 另证一（把原点移到 $T$，用齐次化）

  *第一步：平移坐标原点。* 令

  $ X = x - t, quad Y = y, $

  即把原点搬到 $T(t, 0)$。由 $A(-a, 0)$、$B(a, 0)$ 得

  $ A(-(a + t), 0), quad B(a - t, 0). $

  又 $x = X + t$，代入 $x^2/a^2 + y^2/b^2 = 1$ 并乘以 $a^2 b^2$：

  $ b^2 (X + t)^2 + a^2 Y^2 = a^2 b^2, $

  即

  $ b^2 X^2 + a^2 Y^2 + 2 b^2 t X + b^2 (t^2 - a^2) = 0. $ <eq:圆锥曲线12蝴蝶模型5>

  此时弦 $P Q$ 恰好过原点。(5) 里那个一次项 $2 b^2 t X$ 就是"原点搬到 $T$"留下的痕迹。

  *第二步：设弦、齐次化。* 弦 $P Q$ 过原点，设其方程为

  $ X = m Y quad (m != 0), $

  即 $Y = X/m$（$m = 0$ 时弦是竖直线 $X = 0$，即 $x = t$，结论可直接验证，见本节末注）。代入 (5)，让式中只出现 $X$：

  $ b^2 X^2 + (a^2)/m^2 X^2 + 2 b^2 t X + b^2 (t^2 - a^2) = 0. $

  *两边同除以 $X^2$——这就是齐次化：把各项都化成关于 $1/X$ 的同次式*。记 $u = 1/X$：

  $ b^2 (t^2 - a^2) u^2 + 2 b^2 t u + (b^2 + (a^2)/m^2) = 0. $ <eq:圆锥曲线12蝴蝶模型6>

  $P$、$Q$ 的横坐标 $X_1$、$X_2$ 对应两根 $u_1 = 1/X_1$、$u_2 = 1/X_2$，由韦达定理

  $ 1/X_1 + 1/X_2 = u_1 + u_2 = (-2 b^2 t)/(b^2 (t^2 - a^2)) = (2 t)/(a^2 - t^2). $ <eq:圆锥曲线12蝴蝶模型7>

  *关键之处*：弦的方向 $m$ 在 (7) 里完全消失了——不管弦怎么转，(7) 都成立。这正是"齐次化"的威力：把含 $m$ 的信息压进 $1/X$ 的二次方程，只留下与弦无关的关系。

  *第三步：把斜率之比化成 $1/X_1 + 1/X_2$。* 由 $P$、$Q$ 在 $X = m Y$ 上，$Y_i = X_i/m$，于是

  $ k_1 = k_(A P) = Y_1/(X_1 + a + t) = (X_1/m)/(X_1 + a + t), $

  $ k_2 = k_(B Q) = Y_2/(X_2 - (a - t)) = (X_2/m)/(X_2 - a + t). $

  取倒数：

  $ 1/k_1 = m (1 + (a + t)/X_1), quad 1/k_2 = m (1 - (a - t)/X_2). $

  所以

  $ k_1/k_2 = (1/k_2)/(1/k_1) = (1 - (a - t)/X_2)/(1 + (a + t)/X_1). $

  要证它等于 $(a - t)/(a + t)$，只需交叉相乘：

  $ (a + t) (1 - (a - t)/X_2) = (a - t) (1 + (a + t)/X_1), $

  展开、移项（注意 $(a + t) - (a - t) = 2t$）：

  $ 2 t = (a^2 - t^2)(1/X_1 + 1/X_2). $

  而它正是 (7)！于是

  #align(center)[#box($ frac(k_1, k_2) = frac(a - t, a + t) $)]

  证毕。#qed

  注：$m = 0$（弦为竖直线 $x = t$）时，两交点为 $P(0, h)$、$Q(0, -h)$，其中 $h = (b sqrt(a^2 - t^2))/a$，此时

  $ k_1 = h/(a + t), quad k_2 = h/(a - t), quad frac(k_1, k_2) = frac(a - t, a + t) $

  同样成立；$P$、$Q$ 的标号反过来也一样（两种标号给出同一个比值）。

  === 另证二（平移原点后设斜截式 $Y = k X$）

  *第一步：平移坐标原点。* 令

  $ X = x - t, quad Y = y, $

  即把原点搬到 $T(t, 0)$。在新坐标系下，左右顶点变为

  $ A(-(a + t), 0), quad B(a - t, 0), $

  弦 $P Q$ 过原点，设其方程为

  $ Y = k X quad (k != 0). $

  （$k = 0$ 时弦就是 $x$ 轴，与椭圆只交于 $A$、$B$ 两点，是退化情形；与"另证一"不同的是，这里保留斜率 $k$，而把含 $k$ 的信息交给韦达定理去消。）

  *第二步：联立，用韦达定理。* 由 $x = X + t$，椭圆方程变为

  $ frac((X + t)^2, a^2) + frac(Y^2, b^2) = 1. $

  代入 $Y = k X$ 并整理：

  $ (1/a^2 + k^2/b^2) X^2 + (2t)/a^2 X + (t^2 - a^2)/a^2 = 0. $

  设两根为 $X_1$、$X_2$（分别对应 $P$、$Q$），记 $D = 1/a^2 + k^2/b^2$，由韦达定理

  $ X_1 + X_2 = -(2t)/(a^2 D), quad X_1 X_2 = (t^2 - a^2)/(a^2 D), $

  两式相除，$a^2 D$ 被消掉：

  $ frac(X_1 X_2, X_1 + X_2) = (t^2 - a^2)/(-2t) = (a^2 - t^2)/(2t), $

  即

  $ X_1 X_2 = (a^2 - t^2)/(2t) (X_1 + X_2). quad (*) $

  *关键之处*：弦的斜率 $k$ 在 $(*)$ 里完全消失了——不论弦怎样绕 $T$ 转动，$X_1 X_2$ 与 $X_1 + X_2$ 的这个比例都不变。这与"另证一"里 $1/X_1 + 1/X_2$ 与 $m$ 无关，是同一件事的两种表述。

  *第三步：算斜率之比。* 新坐标下 $P(X_1, k X_1)$、$Q(X_2, k X_2)$，而 $A$、$B$ 的横坐标分别为 $-(a + t)$、$a - t$，所以

  $ k_1 = k_(A P) = (k X_1)/(X_1 + a + t), quad k_2 = k_(B Q) = (k X_2)/(X_2 - a + t), $

  于是

  $ frac(k_1, k_2) = frac(X_1 (X_2 - a + t), X_2 (X_1 + a + t)) = frac(X_1 X_2 - (a - t) X_1, X_1 X_2 + (a + t) X_2). $

  *第四步：代入 $(*)$ 并因式分解。* 分子：

  $
    X_1 X_2 - (a - t) X_1
    &= (a^2 - t^2)/(2t) (X_1 + X_2) - (a - t) X_1 \
    &= (a - t)/(2t) [(a + t)(X_1 + X_2) - 2t X_1] \
    &= (a - t)/(2t) [(a - t) X_1 + (a + t) X_2].
  $

  分母：

  $
    X_1 X_2 + (a + t) X_2
    &= (a^2 - t^2)/(2t) (X_1 + X_2) + (a + t) X_2 \
    &= (a + t)/(2t) [(a - t)(X_1 + X_2) + 2t X_2] \
    &= (a + t)/(2t) [(a - t) X_1 + (a + t) X_2].
  $

  分子、分母中的 $(2t)$ 与公共因式 $[(a - t) X_1 + (a + t) X_2]$ 一起约掉：

  $ frac(k_1, k_2) = frac((a - t) dot [(a - t) X_1 + (a + t) X_2], (a + t) dot [(a - t) X_1 + (a + t) X_2]) = frac(a - t, a + t). $

  说明：公共因式 $(a - t) X_1 + (a + t) X_2 = (2t)/(a - t) dot X_1 (X_2 - a + t)$，它的两个因子都非零——由 $|t| < a$ 知 $X = 0$ 不是上面那个二次方程的根，故 $X_1 != 0$；又若 $X_2 = a - t$，则 $Q$ 的横坐标为 $a$ 而纵坐标 $Y_2 = k(a - t) != 0$（$k != 0$），不在椭圆上，矛盾。所以约分是合法的。

  #align(center)[#box($ frac(k_1, k_2) = frac(a - t, a + t) $)]

  证毕。#qed

  === 三种证法的小结

  - *非对称韦达*：设 $P Q: x = m y + t$，用 $y_1 + y_2$、$y_1 y_2$ 凑出 (3) 式；
  - *齐次化（另证一）*：平移原点后设 $X = m Y$，两边除以 $X^2$，把 $m$ 压进 $1/X$ 的二次方程，得 $1/X_1 + 1/X_2$ 与 $m$ 无关；
  - *斜截式（另证二）*：平移原点后设 $Y = k X$，直接由韦达定理得 $X_1 X_2$ 与 $X_1 + X_2$ 的定比 $(*)$，再用因式分解约掉公共因式。

  三种证法的共同点：都是把"弦过定点"翻译成一个与弦的方向无关的关系式，最后一步都是把斜率之比化成只含 $X_1$、$X_2$ 的对称式。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 三、双曲线的情形

  === 命题

  把椭圆换成双曲线

  $ x^2/a^2 - y^2/b^2 = 1 quad (a > 0, b > 0), $

  $A(-a, 0)$、$B(a, 0)$ 仍是它的两个顶点。设 $T(t, 0)$ 为实轴上一点，过 $T$ 的直线与双曲线交于 $P$、$Q$ 两点，仍记 $k_1 = k_(A P)$、$k_2 = k_(B Q)$，则

  $ frac(k_1, k_2) = frac(a - t, a + t), $

  与椭圆完全一样（在斜率都存在、$k_2 != 0$、且直线确实与双曲线有两个交点的条件下）。

  而且"蝴蝶定理"本身对双曲线也成立：$M$ 是弦 $P Q$ 的中点、过 $M$ 任作两弦 $A B$、$C D$，则 $A D$、$B C$ 与 $P Q$ 的交点仍满足 $M E = M F$——第一节的证明只用到"曲线是二次曲线"和"$M$ 是弦的中点"，与 $y^2$ 项系数的正负无关。

  === 证明（非对称韦达，与椭圆逐字相同）

  设 $P Q: x = m y + t$（过 $T(t, 0)$）。代入双曲线并两边乘 $a^2 b^2$：

  $ b^2 (m y + t)^2 - a^2 y^2 = a^2 b^2, $

  整理得

  $ (b^2 m^2 - a^2) y^2 + 2 b^2 m t y + b^2 (t^2 - a^2) = 0. $ <eq:圆锥曲线12蝴蝶模型8>

  设 $P(x_1, y_1)$、$Q(x_2, y_2)$，由韦达定理

  $ y_1 + y_2 = (-2 b^2 m t)/(b^2 m^2 - a^2), quad y_1 y_2 = (b^2 (t^2 - a^2))/(b^2 m^2 - a^2). $ <eq:圆锥曲线12蝴蝶模型9>

  *与椭圆的唯一差别*是 $y^2$ 的系数由 $a^2 + b^2 m^2$ 换成了 $b^2 m^2 - a^2$——而它马上就会被约掉。把 (9) 两式相除：

  $ (y_1 y_2)/(y_1 + y_2) = (t^2 - a^2)/(-2 m t) quad => quad 2 t m y_1 y_2 = (a^2 - t^2)(y_1 + y_2). $ <eq:圆锥曲线12蝴蝶模型10>

  *(10) 与椭圆的 (3) 一模一样*：$b^2 m^2 - a^2$ 已经消失，所以"非对称韦达"关系与曲线是椭圆还是双曲线无关。

  又由 $A(-a,0)$、$B(a,0)$ 及 $x_i = m y_i + t$：

  $ frac(k_1, k_2) = frac(y_1 (m y_2 + t - a), y_2 (m y_1 + t + a)). $

  要证它等于 $(a - t)/(a + t)$，只需验证

  $ (a + t) y_1 (m y_2 + t - a) = (a - t) y_2 (m y_1 + t + a), $

  展开、移项后恰好化成

  $ 2 t m y_1 y_2 - (a^2 - t^2)(y_1 + y_2) = 0, $

  而这就是 (10)，故结论成立。

  #align(center)[#box($ frac(k_1, k_2) = frac(a - t, a + t) $)]

  证毕。#qed

  说明：第二节的"平移原点 + 齐次化"证法对双曲线也逐字适用。平移后曲线为

  $ b^2 X^2 - a^2 Y^2 + 2 b^2 t X + b^2 (t^2 - a^2) = 0, $

  与椭圆的差别只是 $a^2 Y^2$ 前多一个负号，而它在齐次化后同样被约掉，得到的仍是

  $ 1/X_1 + 1/X_2 = (2 t)/(a^2 - t^2). $

  #line(length: 100%, stroke: 0.5pt + gray)

  == 四、抛物线的情形

  === 命题

  抛物线 $y^2 = 2 p x$（$p > 0$）只有*一个*顶点

  $ A(0, 0) $

  ——对称轴与它只交于一点，不像椭圆、双曲线那样能在轴上取到两个点。设 $T(t, 0)$（$t != 0$）为对称轴上一点，过 $T$ 的直线与抛物线交于 $P$、$Q$ 两点，则从顶点引出的两条弦的斜率之*积*为定值：

  $ k_(A P) dot k_(A Q) = -(2 p)/t. $

  三种曲线对照着看，规律很清楚：

  - 椭圆、双曲线：轴上有两个基准点 $A$、$B$ ⟹ 结论是斜率之*比* $k_(A P)/k_(B Q) = (a - t)/(a + t)$ 为定值；
  - 抛物线：两个"顶点"重合成了一个点 $A$ ⟹ 基准点只剩一个，结论变成斜率之*积* $k_(A P) dot k_(A Q) = -(2 p)/t$ 为定值。

  另外，"蝴蝶定理"本身对抛物线也成立：$M$ 是弦 $P Q$ 的中点、过 $M$ 任作两弦 $A B$、$C D$，则 $A D$、$B C$ 与 $P Q$ 的两个交点仍满足 $M E = M F$。第一节的证明只要求弦不与对称轴垂直（即"$x^2$ 项的系数不为 $0$"），而抛物线的任意一条弦都不与对称轴垂直，所以那段证明逐字有效。

  === 证明

  设直线 $P Q: x = m y + t$（过 $T(t, 0)$）。代入 $y^2 = 2 p x$：

  $ y^2 = 2 p (m y + t) quad => quad y^2 - 2 p m y - 2 p t = 0. $ <eq:圆锥曲线12蝴蝶模型11>

  设 $P(x_1, y_1)$、$Q(x_2, y_2)$，由韦达定理

  $ y_1 + y_2 = 2 p m, quad y_1 y_2 = -2 p t. $ <eq:圆锥曲线12蝴蝶模型12>

  又因为 $P$、$Q$ 在抛物线上，$x_i = y_i^2/(2 p)$，所以

  $ k_(A P) = y_1/x_1 = y_1/(y_1^2/(2 p)) = (2 p)/y_1, quad k_(A Q) = (2 p)/y_2. $

  两式相乘，再用 (12)：

  $ k_(A P) dot k_(A Q) = (4 p^2)/(y_1 y_2) = (4 p^2)/(-2 p t) = -(2 p)/t. $

  #align(center)[#box($ k_(A P) dot k_(A Q) = -(2 p)/t $)]

  证毕。#qed

  注：弦的斜率 $m$ 在最终结果里消失了——不论弦怎样绕 $T$ 转动，这个乘积都是同一个定值。反过来也成立：若抛物线两条弦 $A P$、$A Q$ 的斜率之积为定值，则弦 $P Q$ 恒过对称轴上的定点。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 五、抛物线中"两次连线"的定点与定值

  === 命题

  设抛物线 $y^2 = 2 p x$（$p > 0$），$x$ 轴上有两个定点 $M(m, 0)$、$N(n, 0)$（$m, n != 0$）。过 $M$ 作直线交抛物线于 $A$、$B$ 两点；直线 $A N$ 交抛物线于另一点 $C$，直线 $B N$ 交抛物线于另一点 $D$；直线 $C D$ 交 $x$ 轴于点 $Q$。则

  (1) $Q$ 是定点：$Q(n^2/m, 0)$；

  (2) $k_(A B)/k_(C D) = n/m$ 为定值。


  === 证明（用抛物线的参数式）

  抛物线 $y^2 = 2 p x$ 上的点可统一写成

  $ (2 p t^2, 2 p t), quad t in RR, $

  设 $A$、$B$、$C$、$D$ 对应的参数依次为 $a$、$b$、$c$、$d$。

  *① 弦的方程与它在 $x$ 轴上的截距。* 过参数 $t_1$、$t_2$ 两点的弦，斜率为

  $ (2 p t_2 - 2 p t_1)/(2 p t_2^2 - 2 p t_1^2) = 1/(t_1 + t_2), $

  于是这条弦的方程是

  $ x = (t_1 + t_2) y - 2 p t_1 t_2 $

  它在 $x$ 轴上的截距为

  $ x_0 = -2 p t_1 t_2 $

  *② 把三个"过定点"的条件翻译成参数关系。*

  - $A B$ 过 $M(m, 0)$：由 (14) 得 $m = -2 p a b$，即 $a b = (-m)/(2 p)$；
  - $A C$ 过 $N(n, 0)$：$n = -2 p a c$，即 $c = (-n)/(2 p a)$；
  - $B D$ 过 $N(n, 0)$：$n = -2 p b d$，即 $d = (-n)/(2 p b)$。

  *③ 求 $Q$。* 由 (14)，$C D$ 的 $x$ 截距是 $-2 p c d$，代入 $c$、$d$：

  $ x_Q = -2 p dot (-n)/(2 p a) dot (-n)/(2 p b) = (-n^2)/(2 p a b), $

  再用 $a b = (-m)/(2 p)$：

  $ x_Q = (-n^2)/(2 p) dot (2 p)/(-m) = n^2/m. $

  所以 $Q(n^2/m, 0)$ 是定点，*(1) 成立*。

  *④ 求斜率之比。* 由 ①，$k_(A B) = 1/(a + b)$、$k_(C D) = 1/(c + d)$，而

  $ c + d = (-n)/(2 p) (1/a + 1/b) = (-n)/(2 p) dot (a + b)/(a b), $

  所以

  $ frac(k_(A B), k_(C D)) = (c + d)/(a + b) = (-n)/(2 p a b) = (-n)/(2 p) dot (2 p)/(-m) = n/m, $

  即 $k_(A B)/k_(C D) = n/m$ 为定值，*(2) 成立*。

  #align(center)[#box($ Q(n^2/m, 0), quad frac(k_(A B), k_(C D)) = n/m $)]

  证毕。#qed

  注：结果中只剩 $m$、$n$，与弦 $A B$ 的方向无关、也与 $p$ 无关，这正是"定点、定值"问题的典型标志。特别地，取 $n = -m$ 时 $Q(-m, 0)$，即 $Q$ 与 $M$ 关于顶点 $A$ 对称。

]
