// 导数压轴题破题思路（十类题型）
#set page(margin: 2cm, paper: "a4")
#set text(size: 11pt, font: ("New Computer Modern", "Songti SC", "Source Han Sans"), lang: "zh")
#set par(justify: true, leading: 0.9em)

// 题型框
#let problem-box(content) = block(
  fill: rgb("E8F5E9"),
  inset: 10pt,
  radius: 4pt,
  width: 100%,
  stroke: (left: 4pt + rgb("2E7D32")),
  content,
)

// 思路框
#let idea-box(content) = block(
  fill: rgb("FFFDE7"),
  inset: 10pt,
  radius: 4pt,
  width: 100%,
  stroke: (left: 4pt + rgb("F9A825")),
  content,
)

#align(center)[
  #text(size: 18pt, weight: "bold", fill: rgb("D84315"))[导数压轴题所有类型破题思路]
]

#v(1em)

// ==================== 1 ====================
#problem-box[
  *1. 单变量不等式证明：* 已知 $f(x) = e^x - ln(x+1)$，证明：$x >= 0$ 时，$f(x) >= 1$。
]
#idea-box[
  *破题思路：构造差函数* 令 $g(x) = f(x) - 1 = e^x - ln(x+1) - 1$，注意端点 $g(0) = 1 - 0 - 1 = 0$，只要证 $g'(x) >= 0$（$x >= 0$）即可。

  $ g'(x) = e^x - 1/(x+1). $

  用切线放缩 $e^x >= x + 1$ 得

  $ g'(x) >= (x+1) - 1/(x+1) = ((x+1)^2 - 1)/(x+1) = (x(x+2))/(x+1) >= 0 quad (x >= 0). $

  所以 $g(x)$ 在 $[0, +oo)$ 上递增，$f(x) - 1 = g(x) >= g(0) = 0$，即 $f(x) >= 1$。
]

#v(0.5em)

// ==================== 2 ====================
#problem-box[
  *2. 恒成立求参数：* 若对任意 $x >= 0$，$e^x - a x - 1 >= 0$，求 $a$ 的取值范围。
]
#idea-box[
  *破题思路：端点效应 + 必要性探路* 令 $f(x) = e^x - a x - 1$，则 $f(0) = 0$。要在 $x = 0$ 右侧恒有 $f(x) >= 0$，先看端点处的导数：

  $ f'(x) = e^x - a, quad f'(0) = 1 - a >= 0 quad => quad a <= 1. $

  再证充分：当 $a <= 1$ 时，对 $x >= 0$ 有 $a x <= x$，于是

  $ e^x - a x - 1 >= e^x - x - 1 >= 0 quad ("用" e^x >= x + 1). $

  故 $a <= 1$。
]

#v(0.5em)

// ==================== 3 ====================
#problem-box[
  *3. 零点个数问题：* $f(x) = x^2 - a ln x$ 有两个零点，求 $a$ 的取值范围。
]
#idea-box[
  *破题思路：分离参数，看两曲线交点* 由 $x^2 - a ln x = 0$ 得 $a = x^2/(ln x)$（$x > 0$、$x != 1$）。研究

  $ g(x) = x^2/(ln x), quad g'(x) = (2x ln x - x)/(ln x)^2 = (x(2 ln x - 1))/(ln x)^2. $

  在 $(1, +oo)$ 上：$g$ 在 $(1, sqrt(e))$ 上递减、在 $(sqrt(e), +oo)$ 上递增，最小值

  $ g(sqrt(e)) = (e)/(1/2) = 2e. $

  于是在 $(1, +oo)$ 上，水平线 $y = a$ 与 $y = g(x)$ 有两个交点当且仅当 $a > 2e$；再结合 $(0,1)$ 上 $g(x) < 0$ 的情形可知，$a > 2e$ 时恰有两个零点。
]

#v(0.5em)

// ==================== 4 ====================
#problem-box[
  *4. 极值点偏移（和型）：* $f(x) = ln x - a x$ 有两个零点 $x_1$、$x_2$，证明：$x_1 + x_2 > 2/a$。
]
#idea-box[
  *破题思路：对称构造函数* 极值点 $x_0 = 1/a$。构造对称函数

  $ F(x) = f(x) - f(2/a - x), quad F(2/a - x) = -F(x), quad F(1/a) = 0. $

  求导可证 $F'(x) >= 0$，故 $x < 1/a$ 时 $F(x) < F(1/a) = 0$，即

  $ f(x) < f(2/a - x) quad (x < 1/a). $

  不妨设 $x_1 < 1/a < x_2$。由 $f(x_1) = f(x_2)$ 与上式得 $f(x_2) = f(x_1) < f(2/a - x_1)$；又 $f$ 在 $(1/a, +oo)$ 上单调递减，且 $2/a - x_1 > 1/a$，所以

  $ x_2 > 2/a - x_1 quad => quad x_1 + x_2 > 2/a. $
]

#v(0.5em)

// ==================== 5 ====================
#problem-box[
  *5. 双变量乘积（积型）：* $f(x) = ln x - a x$ 有两个零点 $x_1$、$x_2$，证明：$x_1 x_2 > e^2$。
]
#idea-box[
  *破题思路：比值换元* 设 $t = x_2/x_1 > 1$。由 $ln x_1 = a x_1$、$ln x_2 = a x_2$：

  $ "相减：" ln t = a(x_2 - x_1), quad "相加：" ln(x_1 x_2) = a(x_1 + x_2), $

  两式相除消去 $a$：

  $ ln(x_1 x_2) = ln t dot (x_1 + x_2)/(x_2 - x_1) = ln t dot (t+1)/(t-1). $

  所以要证 $x_1 x_2 > e^2$，只需证 $ln t dot (t+1)/(t-1) > 2$（$t > 1$），即

  $ ln t > (2(t-1))/(t+1). $

  令 $h(t) = ln t - (2(t-1))/(t+1)$，则 $h(1) = 0$ 且

  $ h'(t) = 1/t - 4/(t+1)^2 = ((t+1)^2 - 4t)/(t(t+1)^2) = ((t-1)^2)/(t(t+1)^2) > 0, $

  故 $h(t) > h(1) = 0$，原不等式成立。
]

#v(0.5em)

// ==================== 6 ====================
#problem-box[
  *6. 双变量反向（证明 $x_1 x_2 < 1$）：* $f(x) = x - ln x - a$ 有两个零点 $x_1$、$x_2$，证明：$x_1 x_2 < 1$。
]
#idea-box[
  *破题思路：构造倒数对称* 令 $g(x) = x - ln x$。对 $x in (0, 1)$，比较 $g(1/x)$ 与 $g(x)$：

  $ g(1/x) - g(x) = (1/x - x) - ln(1/x) + ln x = (1 - x^2)/x + 2 ln x. $

  令 $u(x) = (1 - x^2)/x + 2 ln x$（$0 < x < 1$），则 $u(1) = 0$ 且

  $ u'(x) = -(1 + 1/x^2) + 2/x = -((x-1)^2)/x^2 <= 0, $

  故 $x < 1$ 时 $u(x) > u(1) = 0$，即 $g(1/x) > g(x)$。

  不妨设 $x_1 < 1 < x_2$（$g$ 在 $(1, +oo)$ 上单调递增，在 $(0,1)$ 上单调递减，且 $g(x_1) = g(x_2)$）。由上式

  $ g(x_2) = g(x_1) < g(1/x_1), $

  而 $x_2 > 1$、$1/x_1 > 1$，$g$ 在 $(1, +oo)$ 上递增，故 $x_2 < 1/x_1$，即 $x_1 x_2 < 1$。
]

#v(0.5em)

// ==================== 7 ====================
#problem-box[
  *7. 数列不等式：* 证明：$ln(n+1) < 1 + 1/2 + 1/3 + dots.c + 1/n$。
]
#idea-box[
  *破题思路：用基本切线放缩* 由 $ln(1+x) < x$（$x > 0$），取 $x = 1/k$：

  $ ln(1 + 1/k) < 1/k quad => quad ln((k+1)/k) < 1/k quad => quad ln(k+1) - ln k < 1/k. $

  令 $k = 1, 2, dots, n$ 并累加：

  #align(center)[
    $ ln 2 - ln 1 < 1/1 $ \
    $ ln 3 - ln 2 < 1/2 $ \
    $ dots.c $ \
    $ ln(n+1) - ln n < 1/n $
  ]

  左边中间项全部抵消，得

  $ ln(n+1) - ln 1 < 1 + 1/2 + 1/3 + dots.c + 1/n, $

  即 $ln(n+1) < 1 + 1/2 + 1/3 + dots.c + 1/n$。
]

#v(0.5em)

// ==================== 8 ====================
#problem-box[
  *8. 隐零点问题：* 证明：$e^x - ln x - 2 > 0$。
]
#idea-box[
  *破题思路：设而不求，整体代换* 设 $f(x) = e^x - ln x - 2$（$x > 0$），则

  $ f'(x) = e^x - 1/x, quad f''(x) = e^x + 1/x^2 > 0, $

  故 $f'$ 单调递增，又 $f'(1/2) = sqrt(e) - 2 < 0$、$f'(1) = e - 1 > 0$，所以存在唯一 $x_0$ 使

  $ e^(x_0) = 1/x_0 quad => quad x_0 = e^(-x_0) quad => quad ln x_0 = -x_0. $

  $f$ 在 $(0, x_0)$ 上递减、在 $(x_0, +oo)$ 上递增，最小值

  $ f(x_0) = e^(x_0) - ln x_0 - 2 = 1/x_0 + x_0 - 2 > 0, $

  最后一个不等号成立是因为 $x_0 != 1$（若 $x_0 = 1$ 则 $e = 1$，矛盾），而 $t + 1/t > 2$（$t > 0$、$t != 1$）。故 $f(x) >= f(x_0) > 0$。
]

#v(0.5em)

// ==================== 9 ====================
#problem-box[
  *9. 同构模型：* 证明：$e^x + x >= ln(x+1) + x + 1$。
]
#idea-box[
  *破题思路：找一个"同一个函数"* 令 $h(t) = e^t + t$（在 $RR$ 上单调递增）。把右边整理：

  $ ln(x+1) + x + 1 = ln(x+1) + e^(ln(x+1)) = h(ln(x+1)), $

  而左边 $e^x + x = h(x)$。因为 $h$ 单调递增，所以只需证

  $ x >= ln(x+1), $

  这正是基本切线放缩 $ln(1+u) <= u$（$u > -1$，取 $u = x$）。故原不等式成立。
]

#v(0.5em)

// ==================== 10 ====================
#problem-box[
  *10. 双变量比较（主元法/构造单变量）：* 比较 $a^b$ 与 $b^a$（$a, b > 0$）。
]
#idea-box[
  *破题思路：取对数、除以 $a b$，化成同一个函数* 因为 $ln$ 单调递增，

  $ a^b > b^a quad <=> quad b ln a > a ln b quad <=> quad (ln a)/a > (ln b)/b. $

  构造 $g(x) = (ln x)/x$（$x > 0$），则

  $ g'(x) = (1 - ln x)/x^2, $

  所以 $g$ 在 $(0, e)$ 上递增、在 $(e, +oo)$ 上递减，最大值 $g(e) = 1/e$。于是：

  - 若 $a > b >= e$：$g$ 递减 ⟹ $g(a) < g(b)$ ⟹ $a^b < b^a$；
  - 若 $e >= a > b > 0$：$g$ 递增 ⟹ $g(a) > g(b)$ ⟹ $a^b > b^a$；
  - 若 $a > e > b$：需比较 $a$ 与 $e^2/b$（或直接算 $g(a)$ 与 $g(b)$）的大小，再判断。
]
