#import "template.typ": *

#set document(title: [两项相乘求系数])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、两项相乘求系数：逐项配对

  求形如"（一个简单因式）$times$（一个二项展开式）"的展开式中 $x^m$ 的系数，做法是"逐项配对"：

  1. *分别写出两个因式能提供什么*：简单因式直接看，另一个因式用通项 $T_(k+1) = C_n^k a^(n-k) b^k$ 写出各项；
  2. *配对*：让两个因式中各取一项，相乘后 $x$ 的指数恰好等于 $m$（指数相加）；
  3. *相加*：把每一组合法配对所贡献的系数用加法原理合并。

  本质就是乘法分配律：展开式中每一个 $x^m$ 项，都来自"前一个因式取一项 $times$ 后一个因式取一项"，所以既不漏、也不重。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】$(1 + 1/x^2)(1 + x)^6$ 中 $x^2$ 的系数（选择题）

  *【例 1】* $(1 + 1/x^2)(1 + x)^6$ 展开式中 $x^2$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $15$], [B. $20$], [C. $30$], [D. $35$],
  )

  === 解析

  *① 写出 $(1 + x)^6$ 的展开式。*

  $ (1 + x)^6 = sum_(k=0)^6 C_6^k x^k = 1 + 6x + 15x^2 + 20x^3 + 15x^4 + 6x^5 + x^6 $

  *② 逐项配对。* 前一个因式 $1 + 1/x^2$ 只能提供两项：$1$ 与 $x^(-2)$。要凑出 $x^2$：

  - 取 $1$：需要 $(1+x)^6$ 里的 $x^2$ 项，贡献系数 $C_6^2 = 15$；
  - 取 $x^(-2)$：因为 $x^(-2) dot x^4 = x^2$，需要 $(1+x)^6$ 里的 $x^4$ 项，贡献系数 $C_6^4 = 15$。

  两种配对都成立，系数相加：

  $ C_6^2 + C_6^4 = 15 + 15 = 30 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  （检验：把乘积真正展开，得 $x^(-2) + 6 x^(-1) + 16 + 26x + 30x^2 + 26x^3 + dots$，$x^2$ 的系数确实是 $30$。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 练习

  *【练 1】* 已知 $(1 + a x)(1 + x)^5$ 的展开式中 $x^2$ 的系数为 $5$，则 $a = $（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $-4$], [B. $-3$], [C. $-2$], [D. $-1$],
  )

  === 解析

  仍旧是"逐项配对"，只是把例 1 中的 $1/x^2$ 换成了 $a x$。$(1+x)^5$ 中 $x^2$、$x$ 的系数分别是

  $ C_5^2 = 10, quad C_5^1 = 5 $

  要凑出 $x^2$：

  - 取 $1$：配 $(1+x)^5$ 的 $x^2$ 项，贡献 $C_5^2 = 10$；
  - 取 $a x$：配 $(1+x)^5$ 的 $x$ 项（$a x dot x = a x^2$），贡献 $a C_5^1 = 5 a$。

  由题意

  $ 10 + 5 a = 5 quad => quad a = -1 $

  故选 *#text(fill: rgb("#c7362e"))[D]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  *【练 2】* $(x + y)(2x - y)^5$ 的展开式中 $x^3 y^3$ 的系数为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $-80$], [B. $-40$], [C. $40$], [D. $80$],
  )

  === 解析

  式中有两个字母，配对原则不变——*两个因式各取一项，$x$ 的次数相加、$y$ 的次数相加*，凑成 $x^3 y^3$：

  - 取 $x$：需要 $(2x-y)^5$ 中的 $x^2 y^3$ 项，由通项

  $ C_5^3 (2x)^2 (-y)^3 = 10 times 4 times (-1) = -40 $

  - 取 $y$：需要 $(2x-y)^5$ 中的 $x^3 y^2$ 项，

  $ C_5^2 (2x)^3 (-y)^2 = 10 times 8 times 1 = 80 $

  两类配对相加：

  $ -40 + 80 = 40 $

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  （检验：把 $(x+y)(2x-y)^5$ 直接展开，$x^3 y^3$ 的系数正是 $40$。）

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *配对条件是指数相加等于 $m$*：$1$ 配 $x^2$，$x^(-2)$ 配 $x^4$——把"前一个因式的每一项"与"后一个因式中需要的次数"一一对上。
  - *系数用加法原理合并*：每一对配对只贡献一个系数（本题是两个 $15$），最后相加得 $30$；既不要把系数相乘，也不要漏掉其中一种配对。
  - *先看"简单因式"能提供什么*：$1 + 1/x^2$ 只有 $1$ 和 $x^(-2)$ 两项，据此反推另一个因式需要 $x^2$、$x^4$ 两项，比盲目展开快得多。
  - *别忽略常数项与负指数项*：$(1+x)^6$ 的常数项是 $1$（$k = 0$），在"配对清单"里同样要考虑。
  - *已知某次幂的系数、反求参数*（练 1）：先把该幂次的系数写成关于参数的式子（如 $10 + 5a$），再与已知值列方程求解。
  - *两个字母的因式同样"配对"*（练 2）：目标是 $x^3 y^3$ 时，只要保证"$x$ 的次数和为 $3$、$y$ 的次数和为 $3$"，再把各类配对的系数相加（注意 $(2x)^k$ 的 $2^k$ 与 $(-y)^k$ 的负号别漏）。
  - *项数不多时展开验算*：把乘积展开对照一次，是检查有没有漏配对的最快办法。
]
