# High School Math

高中数学（以圆锥曲线为主）Typst 文档集：专题定理与解题方法、配套练习，近年高考与联考真题解析；另有**导数**专题（基础、单调性、含参单调性、求参、切线、端点效应、数形结合、隐零点、极值点偏移、同构）与**排列组合**专题（特殊优先、捆绑、插空、停车位、捆插结合、多类插空、分组、分组再分配、隔板、定序、最短路径、多面手、错排、环排、元素配对、涂色），**二项式**专题（二项展开式的系数问题），**抽象函数**专题（对称性推周期、双函数联立、结合导数），以及**概率**专题（分布列、超几何/二项/正态分布、截止性问题、马尔科夫链、成对数据与回归、独立性检验、条件概率与全概率）。，以及**立体几何**专题（线面、面面平行的判定与转化）。全部文档共用根目录下的 `template.typ` 排版。

## 文档

共 **69** 篇（不含模板 `template.typ`），按主题分为 9 类：

| 分类 | 篇数 | 主题 |
|------|:---:|------|
| [专题与方法](#专题与方法) | 15 | 圆锥曲线定理与解题方法 |
| [习题与练习](#习题与练习) | 2 | 圆锥曲线配套练习 |
| [真题与联考](#真题与联考) | 5 | 高考与联考真题解析 |
| [导数](#导数) | 13 | 基础、单调性、含参单调性、求参、切线、端点效应、隐零点、极值点偏移、同构 |
| [排列组合](#排列组合) | 16 | 特殊优先、捆绑、插空、停车位、捆插结合、多类插空、分组、分组再分配、隔板、定序、最短路径、多面手、错排、环排、元素配对、涂色 |
| [二项式](#二项式) | 7 | 二项展开式的系数问题 |
| [抽象函数](#抽象函数) | 1 | 对称性推周期、双函数联立、结合导数 |
| [概率](#概率) | 7 | 分布列、超几何/二项/正态分布、截止性问题、马尔科夫链、成对数据与回归、独立性检验、条件概率与全概率 |
| [立体几何](#立体几何) | 3 | 线面平行/垂直的判定与性质 |

### 专题与方法

| 文件 | 内容 |
|------|------|
| `圆锥曲线11焦半径.typ` | 圆锥曲线焦半径（由《椭圆焦半径》《双曲焦半径》《抛物焦半径》三篇**合并**）：三种曲线的焦半径公式（`PF = a ± ex` 型与坐标型）、几何意义、第二定义证明、重要推论、公式汇总、注意事项与典型应用；含"三种圆锥曲线焦半径对比"表 |
| `圆锥曲线12蝴蝶模型.typ` | 圆锥曲线·**蝴蝶定理与蝴蝶模型**：① 标准**圆锥曲线蝴蝶定理**——`M` 是弦 `PQ` 的中点，过 `M` 任作两弦 `AB`、`CD`，则 `AD`、`BC` 与 `PQ` 的交点 `E`、`F` 满足 `ME = MF`；证明：把 `M` 摆在原点、`PQ` 摆在 x 轴（曲线化为 `x² + βxy + γy² + δy = p²`），令 `s = 1/x` 用韦达得 `s₁+s₂ = δk/p²`、`s₃+s₄ = δk′/p²`，由截距公式相加得 `1/x_E + 1/x_F = 0`（与 `γ` 的正负无关）；② **椭圆蝴蝶模型的斜率证明**——`k_AP/k_BQ = (a-t)/(a+t)`，两种解法：(a) `x = my + t` + **非对称韦达**（关键式 `2tmy₁y₂ = (a²-t²)(y₁+y₂)`）；(b) **平移原点至 `T` + 齐次化**（`X = x-t`、弦设 `X = mY`、方程除以 `X²` 得 `1/X₁+1/X₂ = 2t/(a²-t²)`）；③ **双曲线的情形**：`x²/a² - y²/b² = 1` 时结论**完全相同** `k_AP/k_BQ = (a-t)/(a+t)`——联立后 `y²` 的系数变为 `b²m²-a²` 但会约掉，关键式仍是 `2tmy₁y₂ = (a²-t²)(y₁+y₂)`；蝴蝶定理 `ME = MF` 对双曲线也成立。④ **抛物线的情形**：`y² = 2px` 只有一个顶点 `A`，故结论形态由"斜率之比"变为**斜率之积**——过 `T(t,0)` 的弦交抛物线于 `P`、`Q`，则 `k_AP·k_AQ = -2p/t`（定值，与弦无关）；证明：联立 `x = my + t` 得 `y² - 2pmy - 2pt = 0`、`y₁+y₂ = 2pm`、`y₁y₂ = -2pt`，而 `k_AP = 2p/y₁`、`k_AQ = 2p/y₂`，相乘即得。蝴蝶定理 `ME = MF` 对抛物线同样成立。⑤ **抛物线"两次连线"的定点与定值**：`M(m,0)`、`N(n,0)` 在 x 轴上，过 `M` 的弦 `AB`，`AN`、`BN` 再交抛物线于 `C`、`D`，则 `CD` 交 x 轴于定点 **`Q(n²/m, 0)`**、且 **`k_AB/k_CD = n/m`**；证明用抛物线参数式 `(2pt², 2pt)`：弦的截距为 `-2p·t₁t₂`，由 `m = -2pab`、`n = -2pac`、`n = -2pbd` 得 `c = -n/(2pa)`、`d = -n/(2pb)`，于是 `x_Q = -2pcd = n²/m`，又 `k_AB = 1/(a+b)`、`k_CD = 1/(c+d)` 得比值 `n/m`。全篇配 `#qed` |
| `圆锥曲线硬解定理(繁).typ` | 硬解定理（繁版）：直线与椭圆、双曲线、抛物线联立方程的韦达定理结论与弦长公式（两种直线设法） |
| `圆锥曲线06硬解定理.typ` | 硬解定理（简版）：椭圆与一般直线 `Ax + By + C = 0` 联立，相交条件、判别式、韦达定理与弦长公式表；习题 1（由原《硬解定理习题》迁入）：椭圆 `x²/4+y²=1`、`A(2,0)`，`l` 交椭圆于 `M,N` 且 `k_AM+k_AN = 1`，判断 `l` 是否过定点（附"硬解定理／平移后齐次化／极线对合／套公式"四条思路提示，按要求**不附解答**） |
| `圆锥曲线硬解定理(简).typ` | 圆锥曲线·**硬解定理（简版对照页）**：把椭圆与双曲线的判别式、韦达定理表格**合到横向 A4 一页的左右两栏**，便于逐行对比——左栏 `x²/a² + y²/b² = 1`、右栏 `x²/a² - y²/b² = 1`，各含"判别式"（`Δ = 4a²b²(a²A² + b²B² - C²) > 0` 等）与"韦达定理"（`x₁+x₂`、`x₁x₂`、`y₁+y₂`、`y₁y₂`、`x₁y₂+x₂y₁`）两张表；页脚给出"只差符号、把 `b²` 看成 `-b²` 即可互化"的对照记忆 |
| `圆锥曲线10定比点差法.typ` | 定比点差法：内、外分点坐标公式，定值结论 `x_P x_Q/a² + y_P y_Q/b² = 1` 的证明与综合应用 |
| `圆锥曲线04点差法与垂径定理.typ` | 点差法与椭圆垂径定理：完整推导 `k_AB·k_OM = -b²/a²`（两式相减凑平方差）；定理的等价形式（中点 `M(x₀,y₀)` ⟹ 弦斜率 `k = -b²x₀/(a²y₀)`）；推论——双曲线 `k_AB·k_OM = b²/a²`、抛物线 `k_AB·y_M = p`、圆 `k_AB·k_OM = -1`（垂径定理）、与离心率 `k_AB·k_OM = e²-1`；椭圆周角定理 `k_CA·k_CB = -b²/a²`（直径两端与椭圆上任一点）：证明用"中位线 `OM ∥ BC` 把张角化为中点弦"+ 垂径定理，圆中即"直径所对圆周角为直角"；例 1 椭圆 `x²/16+y²/4=1` 以 `M(2,1)` 为中点的弦 ⟹ `k = -1/2`，直线 `x+2y-4=0`（附交点 `(4,0)、(0,2)` 的检验）；例 2 `x²/4+y²/3=1` 左右顶点 `A₁A₂` 为直径、`k_PA₁∈[-2,-1]` 求 `k_PA₂` ⟹ `k_PA₂ = -3/(4k_PA₁) ∈ [3/8,3/4]`，选 **B**（附"设点相乘代入椭圆"的另解）；练 ① 短轴端点 `P(0,b)` 与过中心的弦 `AB` 满足 `k_PA·k_PB = -1/4` 求离心率 ⟹ `b²/a² = 1/4`、`e = √3/2`；② 顶点 `B(0,4)`、`e = √5/5` 的椭圆中 `△BMN` 重心为右焦点 `F` ⟹ 由重心得弦中点 `(3,-2)`，中点弦斜率 `k = 6/5`，直线 `6x-5y-28 = 0`；含椭圆弦中点与周角两幅示意图 |
| `圆锥曲线07仿射变换.typ` | 圆锥曲线仿射变换（底层逻辑）：把椭圆"横缩 `1/a`、纵缩 `1/b`"压成单位圆（反之横伸 `a` 倍、纵伸 `b` 倍）；斜率换算 `k_AB = (b/a)·k_A′B′`；三个保持——保平行、保共线、共线三点比例不变（中点仍是中点、定比不变）；面积换算 `S = ab·S′`、面积比不变、椭圆面积 `πab`；例 1 椭圆 `x²/16+y²/9=1` 内接三角形最大面积 ⟹ 圆里正三角形 `3√3/4`，乘 `ab = 12` 得 `9√3`；**习题 1–5（含解答，由原《仿射变换习题》迁移而来）**：① 证 `k_PA·k_PB = -b²/a²`（仿射证明 + 坐标点差另证）；② `x²/4+y²/3=1` 中 `S_△ABO` 最大值 `√3`（仿射 + 参数法）；③ `y²/a²+x²/b²=1` 过 `P(1,√2)`、`e=√2/2` ⟹ `y²/4+x²/2=1`，再由 `△PAB` 面积 `√2` 定 `m = ±2`（**两种解法**：联立求弦长 + 点线距；以及仿射到单位圆——弦变成"斜率恒为 1"的直线，`c²(1-c²/2) = 1/2 ⟹ (c²-1)² = 0 ⟹ c = ±1 ⟹ m = ±2`）；④ 内接三角形重心在原点 ⟹ 仿射到圆得正三角形，面积 `9/2`；⑤ 双曲线 `M: x²/2-y²/3=1` 上的 `P` 向 `N: x²/2-y²/3=λ` 作两切线、问 `k₁k₂` 是否为定值 ⟹ **不存在**（"定值"解出 `λ=1/2` 但此时判别式 `24λ(λ-1) = -6 < 0`，两切线不存在）；含"椭圆 ⇄ 单位圆"两幅对照示意图；方法总结含"弦长与焦点不能仿射""存在性要回代检验"等提醒 |
| `圆锥曲线09极点极线.typ` | 圆锥曲线极点极线：极线的"替换法"方程 `x₀x/a² + y₀y/b² = 1`（点在外是切点弦、点在曲线上是切线、点在椭圆内则整条极线落在椭圆外）；配极原则 `Q 在 P 的极线上 ⟺ P 在 Q 的极线上`；椭圆内接四边形的三个对角点构成自配极三角形；双曲线 `x₀x/a² - y₀y/b² = 1` 与抛物线 `y₀y = p(x+x₀)` 的极线；含两幅复原手稿的示意图：① `P` 的两条切线、两个切点与切点弦（蓝极线）；② 完全四边形（四点 `A,B,C,D` 与三组对边交点 `P`、`Q`、`R`），并给出**自配极三角形**定理（任一点的极线＝另外两点的连线）；**习题 1–6（由原《极点极线习题》迁入，每题给出「极点极线」与「常规联立」两种解法）**：① `P(4,3)` 的两条割线 ⟹ `Q=AD∩BC` 落在极线 `x+y=1` 上；② `x²/4+y²/3=1`、`P(4,0)` ⟹ `Q` 在定直线 `x=1`；③ `x²/8+y²/4=1`、`P(2,0)` ⟹ `Q` 在 `x=4`；④ `P` 在 `x+y=8` 上 ⟹ `Q` 所在的一族极线恒过 `(2, 9/8)`（即该直线的极点）；⑤ `x²/4+y²=1`、`Q` 在 `x=1` ⟹ `MN` 过 `x=1` 的极点 `(4,0)`；⑥ 斜率比 `k_AM = 3k_BN` ⟹ `Q` 在 `x=-4`，故 `MN` 过 `(-1,0)` |
| `非对称韦达定理.typ` | 非对称韦达定理：椭圆综合题（求方程、证明 `k₁/k₂` 为定值）的转化与计算技巧 |
| `圆锥曲线05齐次化.typ` | 齐次化方法：以双曲线中两割线斜率之积为定值问题的完整解答为例 |
| `双曲线斜率和.typ` | 双曲线割线斜率关系：由 `TA·TB = TP·TQ`（长度乘积相等）推导两条割线斜率的联系 |
| `蒙日圆及其反向应用.typ` | 蒙日圆（准圆）定理的代数证明与反向结论，附综合应用题（多问、多证法）完整解答 |
| `圆锥曲线手电筒模型.typ` | “手电筒”模型：过椭圆上一点作两条互相垂直的直线，割线弦恒过定点的结论与推导 |
| `椭圆垂直过定点.typ` | 椭圆中 `AM ⟂ AN` 型问题：求方程并证明存在定点 `Q`，使 `DQ` 为定值（完整解答） |

### 习题与练习

| 文件 | 内容 |
|------|------|
| `点差法练习题.typ` | 点差法练习题 4 道：每题给出点差法与快速公式两种解法 |
| `圆锥曲线练习题.typ` | 圆锥曲线练习题（双曲线综合，原 18 题 17 分）：右焦点 `F`、左顶点 `A(-1,0)`、离心率 `2` 的双曲线，① 求方程 ⟹ `x² - y²/3 = 1`；② 证明 `∠PFA = 2∠PAF`；③ 过 `F` 作 `l` 的垂线交 `OM` 于 `N`，证明 `△NPQ` 的面积大于 `9/2`（含完整解答） |

### 真题与联考

| 文件 | 内容 |
|------|------|
| `2016武汉二调解析几何.typ` | 2016 年武汉市高三二月调研考试解析几何大题：切点弦方程与斜率关系 `1/k1 + 1/k2 = 2/k0` |
| `2018浙江阿基米德三角形.typ` | 2018 年浙江卷抛物线试题：割线中点性质与阿基米德三角形面积范围 |
| `2026点石联考18题.typ` | 2026 年点石联考（东北三省一区九月份联合考试）第 18 题（椭圆）：求方程、直线过定点、`(k1 k2)/s` 为定值 |
| `2026福建9月高三第19题详解.typ` | 2026 年福建高三九月联考第 19 题（椭圆）详解：求标准方程、`FM·FN`、角平分线证明 |
| `焦点弦和极线应用.typ` | 华中师大附中 2027 届高三独立作业第 8 题：由极线方程与焦点弦公式求三角形面积最大值 |

### 导数

| 文件 | 内容 |
|------|------|
| `导数01基础.typ` | 导数基础例题 4 道（切线问题）：抽象导数 `f'(1)`、由切线图象求 `g(x) = x f(x)` 的切线、由切线方程求参数、两曲线公切线（含图象与解析） |
| `导数02单调性.typ` | 导数与单调性（6 题）：图象互推 2 题（由 `f(x)` 判断 `f'(x)`、由 `f'(x)` 判断 `f(x)`），以及求三次函数、`(1-x²)eˣ`、含参数对数函数（`a=1/2`）、`(x+1)ln x - 2(x-1)` 的单调区间与讨论（含题图、选项图、解析与方法总结） |
| `导数03含参单调性.typ` | 导数含参单调性（3 题）：讨论 `f(x) = ln x - a x + x²`、`f(x) = eˣ - m x`（`x > 0`）、`f(x) = ln(x+1) + a x²`（`a > 0`）的单调性（按判别式 / 根与定义域的位置分类讨论，含方法总结） |
| `导数04求参全分.typ` | 导数求参全分（例 1、例 2、练 2）：单调性求参、两图象交点个数求参、单调区间、闭区间零点个数求参、`ln x/x²` 型单调性与两零点求参（含分离参数、端点临界情形与方法总结） |
| `导数05求参半分.typ` | 导数求参半分（例 1、例 2、练 3）：恒成立求参（`eˣ - a x + 2a > 0` 对一切 `x ∈ R`，最值法 / 分离参数 / 切线法三种解法，含示意图与 `>`、`≥` 端点辨析）、方程解的个数求参（`e²ˣ = a(x-1)²` 在 `(0,+∞)` 上仅一解：分离参数法与两条曲线交点（相切临界）两种解法，含示意图与个数对照表）、含固有零点的零点个数求参（`h(x) = eˣ sin x + m x` 在 `[0, π/2]` 上恰两零点：分离参数法与凸性（切线 / 割线斜率）两种解法，含示意图），含方法总结 |
| `导数06切线.typ` | 导数切线（例 1–例 7、练 5，共 8 题）：求曲线在某点处的切线方程、切线斜率条件结合基本不等式求最值、已知切线过定点求切线方程、两曲线的公切线、过定点的切线有两条 / 三条求参数范围、三条切线的条件判断（选项辨析）、曲线上的点到直线的最小距离（数形结合，含示意图）（含切点验证、定义域检验与方法总结） |
| `导数07端点效应.typ` | 导数端点效应（必要性探路）：适用题型与结论（`f(b) = a` 时由 `f'(b)` 的符号缩小参数范围；`f'(b) = 0` 时看 `f''(b)`）、使用步骤（必要性探路 + 验证充分性）、易错与二次端点效应、失效情形；含两幅示意图与 4 道示例（`ln x ≤ a(x-1)`、`x ln x ≥ a(x-1)`、`(x+1)ln x - a(x-1) > 0`、`eˣ - ex - λ(x ln x - x + 1) ≥ 0`，最后一题用二次端点效应，各题均附分离参数另解） |
| `导数08指数对数.typ` | 导数指数对数（3 题）：① 含对数的一元恒成立 `x ln x ≥ a(x-1)`（`x ≥ 1`），分离参数法（端点单独讨论 + 洛必达法则求下确界）与构造函数法（比较极值点 `a` 与端点 `1`）两种解法，含两幅示意图；② 指对混合不等式证明 `x·f(x) + e⁻ˣ - x ≥ 0`（`f(x) = x + ln x`），用同构换元 `t = x eˣ` 化为 `ln t + 1/t - 1 ≥ 0`（等价于 `t ln t ≥ t - 1`），另附凸性证明与取等点讨论；③ 含 `(x+1)ln x` 型的恒成立 `(x+1)ln x - a(x-1) > 0`（`x > 1`），分离参数法（`x - 1/x - 2ln x` 判号 + 洛必达）与构造函数法（端点效应定必要、参数放缩证充分）两种解法 |
| `导数09端点失效.typ` | 导数端点失效（3 题）：① 端点处不取等（`f(b) ≠ a`）时端点效应失效的判别与替代方法；例题 `eˣ + 2ax - a ≥ 0`（`x ≥ 0`）恒成立求 `a`，三种解法（分离参数按 `x = 1/2` 分三段、图象与切线、同除 `eˣ` 化为 `a·h(x) ≥ -1`，`h(x) = (2x-1)e⁻ˣ`）均得 `a ∈ [-e^{3/2}/2, 1]`，含示意图（两条临界直线：过 `(0,1)` 的斜率 `-2` 与切于 `(3/2, e^{3/2})` 的切线）；② 退化端点 `eˣcos x ≥ m(1 + sin x)`（`x ∈ [-π/2, 0]`）恒成立求 `m`，左端点两端同时为 0（无信息），分离参数得 `m ∈ (-∞, 1]`，另附半角公式 `cos x/(1+sin x) = tan(π/4 - x/2)` 的免求导解法；③ 端点取等但一次端点效应也失效 `eˣ + ax² - x ≥ x³/2 + 1`（`x ≥ 0`）恒成立求 `a`，二次端点效应只给必要条件 `a ≥ -1/2`（不充分），分离参数得 `a ∈ [(7-e²)/4, +∞)` |
| `导数10隐零点.typ` | 导数隐零点（5 题）：① `f′(x) = 0` 是超越方程时的处理套路（设隐零点 `x₀`、先证存在唯一、用 `f′(x₀) = 0` 的关系式化简、最后用基本不等式估计）；例题 证明 `f(x) = eˣ + ln(e/x) > 3`，隐零点满足 `x eˣ = 1`（`0.5 < x₀ < 0.6`，`x₀ ≈ 0.5671`），由 `eˣ⁰ = 1/x₀`、`ln x₀ = -x₀` 得 `f(x) ≥ x₀ + 1/x₀ + 1 > 3`（实际下界 ≈ 3.3304）；② 切线条件定参数后求最值：`f(x) = x eˣ - a ln x - 1` 且 `f′(1) = 2e - 1` ⟹ `a = 1`，`g = f - x` 的导数分子恰为 `x eˣ - 1`，用同一隐零点关系式得 `min g = 0`；③ 用隐零点给极值估值：`f(x) = x² - x - x ln x` 的唯一极大值点 `x₀` 满足 `ln x₀ = 2x₀ - 2`，代入得 `f(x₀) = x₀(1-x₀)`，配方得上界 `1/4`；下界在 `x = 1/e` 处比较（`f(1/e) = 1/e²` 是干净值，且 `f′(1/e) = 2/e - 1 < 0` ⟹ `x₀ < 1/e`，由单调性得 `f(x₀) > f(1/e) = 1/e²`），另附换元法（`u = 1 - x₀` ⟹ `x₀ = e^{-2u}`、`f(x₀) = u e^{-2u}`，可把下界提高到 `1/(2e)`）；④ 证明 `f(x) = eˣ - ln(x+2) > 1/6`：隐零点满足 `eˣ⁰(x₀ + 2) = 1`（`-1/2 < x₀ < 0`），代入得 `f(x₀) = x₀ + 1/(x₀ + 2)`，在特殊点 `x = -1/2` 处比较（`φ(-1/2) = 1/6` 恰为题中常数）一步得证；⑤ 换元 + 隐零点参数化求参数范围：`f(x) = a e^{2x} - 2x eˣ`，`f(x) + 1/a ≤ 0`（`∀x ∈ ℝ`）⟹ 取 `x = 0` 定出 `a < 0`；令 `t = eˣ` 化为 `bt² + 2t ln t + 1/b ≥ 0`（`b = -a > 0`），隐零点 `t₀` 满足 `bt₀ + ln t₀ + 1 = 0`，引入 `s = bt₀ + 1` 得 `b = (s-1)eˢ`、`h(t₀) = (2-s²)/b`，条件即 `s ≤ √2`，答案 `a ∈ [-(√2-1)e^{√2}, 0)` |
| `导数11极值点偏移.typ` | 导数极值点偏移（通用方法 + 11 题）：**一、含义**；**二、通用方法（对称差值函数 `F(x) = f(x) - f(2x₀ - x)`）**——四步走（找极值点定两点位置 → 用 `F(2x₀-x) = -F(x)` 与 `F′` 定号 → 代入 `x₁` → 用右侧单调性翻译成 `x₁+x₂` 与 `2x₀` 的大小），并列出常见变式（取对数化积为和、比值换元、固定和变动差、双对称中心、因式分解/参数分离定极值点）；各例：含义与"对称函数 `F(x) = f(x) - f(2x₀ - x)`"三步法（求极值点 → 判 `F` 的符号 → 翻译成 `x₁ + x₂` 与 `2x₀` 的大小）；① `f(x) = eˣ - 3x` 与 `y = a` 有两个交点（`x₁ < ln3 < x₂`），`F′(x) = (e^{x/2} - 3e^{-x/2})² ≥ 0` 使 `x < ln3` 时 `F < 0`，得 `x₁ + x₂ < 2ln3`，选 A（C 虽真但更弱）；② `f(x) = x + 3 - ln x` 与 `y = a` 有两个交点（`x₁ < 1 < x₂`），`F′(x) = -2(x-1)²/(x(2-x)) ≤ 0` 使 `x < 1` 时 `F > 0`，得 `x₁ + x₂ > 2`，选 B（C、D 与 `a` 有关，用 `a = 5` 与 `a = 10` 各算一次即可排除），两题均含数值印证；③ 证明题 `f(x) = x e⁻ˣ`，由 `f(x₁) = f(x₂)`（`x₁ ≠ x₂`）证 `x₁ + x₂ > 2`：`f` 在 `(−∞,1)` 增、`(1,+∞)` 减，令 `F(x) = x e⁻ˣ - (2-x)e^{x-2}`，`F(2-x) = -F(x)` 使 `F(1) = 0` 自动成立，`F′(x) = e⁻ˣ(1-x)(1-e^{2x-2}) ≥ 0`（两因式同号）得 `x₁ < 1` 时 `F < 0`，从而 `x₁ + x₂ > 2`；④ 双边界证明 `f(x) = x(1-ln x)`，由 `b ln a - a ln b = a - b` 证 `2 < 1/a + 1/b < e`：条件同除 `ab` 化为 `(1+ln a)/a = (1+ln b)/b`，用 `g(1/x) = f(x)` 得 `f(x₁) = f(x₂)`（`x₁ = 1/a, x₂ = 1/b`，`x₁ < 1 < x₂ < e`）；下界以 `1` 为中心（`F′(x) = -ln(x(2-x)) ≥ 0`）得 `x₁ + x₂ > 2`，上界以 `e/2` 为中心（换元 `y = x/e` 后 `G = e·r(y)`，`r″ < 0` 且 `r(0⁺) = r(1/2) = 0` 得 `r > 0`）得 `x₁ + x₂ < e`；⑤ 含参数情形 `f(x) = ln x - ax`（`a > 0`），由 `f(x₁) = f(x₂)` 证 `x₁ + x₂ > 2/a`：极值点 `x₀ = 1/a`，`F′(x) = 1/x + 1/(2/a - x) - 2a = 2a(x - 1/a)²/(x(2/a - x)) ≥ 0`，另附两种解法：换元 `y = ax` 化归为 `a = 1`；**法 3（分离参数 + 对数平均不等式）**——由 `f(x₁) = f(x₂)` 解出 `a = (ln x₂ - ln x₁)/(x₂ - x₁)`，即 `1/a` 是对数平均 `L`，结论化为经典的对数平均不等式 `L < (x₁+x₂)/2`（令 `t = x₂/x₁` 后等价于 `ln t > 2(t-1)/(t+1)`）；⑥（2023·河南）`f(x) = x² - (a-2)x - a ln x`，由 `f(x) = c` 有两不等实根证 `f′((x₁+x₂)/2) > 0`：因式分解 `f′(x) = (x+1)(2x-a)/x` 得 `x₀ = a/2`（且"两实根"反推出 `a > 0`），化归为 `x₁+x₂ > 2x₀`；⑦ `eˣ = 2ax + 1`（`a > 0`）有两不同根，证 `(x₁+x₂)/2 < ln 2a`：令 `h(x) = eˣ - 2ax - 1`，`x₀ = ln 2a`，用 `e^{x₀} = 2a` 配方得 `F′ = (e^{x/2} - 2a e^{-x/2})² ≥ 0`，得 `x₁+x₂ < 2x₀`（两题都体现"含参可不分参"）；⑧ `f(x) = x² - 2x + 1 + aeˣ` 有两个极值点，证 `x₁ + x₂ > 4`：由 `f′(x) = 0` 分离参数得 `g(x) = 2(1-x)e⁻ˣ = a`，`g` 在 `x = 2` 取最小值 `-2e⁻²`，故 `a ∈ (-2e⁻², 0)`、`1 < x₁ < 2 < x₂`；对 `g` 用对称函数 `G(x) = g(x) - g(4-x)`（`G′ < 0`）得 `x₂ > 4 - x₁`；⑨ `f(x) = ln x - ax`（`a > 0`）有两个零点，证 `x₁x₂ > e²`：零点满足 `ln xᵢ = a xᵢ`，相加得 `ln(x₁x₂) = a(x₁+x₂)`，化为 `x₁ + x₂ > 2/a`（例 5 的结论，含对称函数证明），故 `x₁x₂ > e²`（`a → 1/e` 时取到最优下界）；⑩ `f(x) = ln x - ax - 1`，`f(x) + 2 = 0` 有两根且 `x₂ > 2x₁`，证 `x₁x₂² > 32/e³`：由两根等式相除得 `(ln x₁+1)/x₁ = (ln x₂+1)/x₂`，**比值换元** `t = x₂/x₁ > 2` 解出 `ln x₁`、`ln x₂`，结论化为 `Φ(t) = [(1+2t)ln t + 3 - 3t]/(t-1) > 5ln2 - 3`，由 `Φ′ > 0`（`P′ = (2t-1)(t-1)/t² > 0`）得证，`t = 2` 恰取等说明下界最优；⑪ `f(x) = 2ln x + x² + x`，由 `f(x₁) + f(x₂) = 4 = 2f(1)` 证 `x₁ + x₂ ≥ 2`：令 `m = (x₁+x₂)/2`、`t = (x₂-x₁)/2`，`φ(t) = 2ln(m²-t²) + 2(m²+t²) + 2m`，当 `m ≤ 1` 时 `φ′(t) = 4t[1 - 1/(m²-t²)] ≤ 0`（`φ` 关于 `t` 递减），故 `4 = φ(t) ≤ 2f(m) ≤ 2f(1) = 4` 迫使 `m = 1`，即 `x₁ + x₂ ≥ 2`（"函数值之和"型条件）；另给**和型对称函数法**（通用套路）：令 `H(x) = f(x) + f(2-x)`，`H′ = 4(1-x)³/(x(2-x))`（`x < 1` 增、`x > 1` 减），故 `H(x) ≤ H(1) = 4`，反证即得 |
| `导数12同构.typ` | 导数同构（8 题）：含义与常用变形（`x = e^{ln x}`、`x eˣ = e^{x+ln x}`、`a eˣ = e^{x+ln a}`、`x + ln x = ln(x eˣ)`）及三步套路（统一成同一内层式子 → 换元设 `t` 化为外函数最值 → 验证 `t` 能否取到该值）；例题 `f(x) = x eˣ - x - ln x` 的最小值：同构为 `g(t) = eᵗ - t`（`t = x + ln x`）得 `min = g(0) = 1`，另附两种解法：**法 2 对数侧同构**（`x + ln x = ln(x eˣ)` ⟹ 令 `u = x eˣ`，外函数 `g(u) = u - ln u` *只含对数*，`g′ = 1 - 1/u`，`g(1) = 1`，即 `ln u ≤ u - 1`）与"直接求导 + 隐零点"验证（`f′ = (x+1)(x eˣ - 1)/x`，`x₀e^{x₀} = 1`，`x₀ ≈ 0.5671`）；指数侧 `eᵗ - t`（`t = x + ln x`）与对数侧 `u - ln u`（`u = x eˣ`）互为反函数，可互换；② `f(x) = m e^{mx} - ln x` 在 `x > 0` 时恒正，求 `m`：`m e^{mx} = e^{mx + ln m}`，用 `eᵘ ≥ u + 1` 放缩得 `f(x) ≥ (mx - ln x) + (ln m + 1) ≥ 2(1 + ln m)`，必要性取 `x = 1` 与 `x = 1/m`（`f(1/m) = me + ln m ≤ 0` 当 `m ≤ 1/e`），得 `m ∈ (1/e, +∞)`；③ 对任意 `x > 0` 恒有 `a(e^{ax} + 1) ≥ 2(x + 1/x)ln x`，求 `a` 的最小值：两边同乘 `x` 凑成 `Φ(ax) ≥ Φ(ln(x²))`（`Φ(t) = t(eᵗ + 1)`），证 `Φ′ = eᵗ(1+t) + 1 ≥ Φ′(-2) = 1 - 1/e² > 0` 得 `Φ` 严格递增，脱去外函数得 `a ≥ (2ln x)/x`，最大值在 `x = e` 处为 `2/e`（临界时 `a = 2/e, x = e` 取等）；④ 若 `ln x - ax - 1/x ≤ -e^{-ax}` 对一切 `x > 0` 恒成立，求 `a`：用 `1/x = e^{-ln x}` 把两边同构成 `Ψ(ln x) ≤ Ψ(ax)`（`Ψ(t) = t - e^{-t}`，`Ψ′ = 1 + e^{-t} > 0`），脱去外函数得 `a ≥ (ln x)/x`，最大值在 `x = e` 处为 `1/e`，故 `a ∈ [1/e, +∞)`；⑤ `f(x) = a e^{x-1} - ln x + ln a ≥ 1` 恒成立求 `a`：记 `t = x - 1 + ln a` 化为 `eᵗ + ln a ≥ ln x + 1`，**两边同加 `x - 1`（即 `t - ln a`）** 凑成 `eᵗ + t ≥ ln x + x = Φ(t) ≥ Φ(ln x)`（`Φ(u) = eᵘ + u`，`Φ′ > 0`），得 `ln a ≥ ln x - x + 1`，即 `a ≥ x e^{1-x}`，最大值在 `x = 1` 处为 `1`，故 `a ∈ [1, +∞)`；⑥ `f(x) = x - ln x + a e^{2x} + ln a ≥ 0` 恒成立求 `a`：令 `w = a e^{2x}`（则 `ln a = ln w - 2x`）得 `f(x) = Ψ(w) - Ψ(x)`（`Ψ(u) = u + ln u`，`Ψ′ > 0`），脱去外函数得 `a e^{2x} ≥ x`，即 `a ≥ x e^{-2x}`，最大值在 `x = 1/2` 处为 `1/(2e)`，故 `a ∈ [1/(2e), +∞)`；⑦ `e^{-ax}ln x < a`（`a > 0`）恒成立求 `a`：`x ≤ 1` 自动成立，`x > 1` 时同乘 `e^{ax}` 得 `ln x < e^{ax+ln a}`，放缩为 `ln x - ax ≤ ln a + 1`（左端最大值 `-ln a - 1`）得 `a ≥ 1/e`，再由 `x = e` 处严格性（`f(e) = a`）排除等号，故 `a > 1/e`；⑧ `a eˣ ln x < x² + x ln a`（`∀x ∈ (0,1)`）求 `a` 的最小值：同除 `x` 后令 `u = a eˣ`（`x + ln a = ln u`），同构成 `K(u) > K(x)`（`K(t) = (ln t)/t`），分 `u > 1` 与 `u ≤ 1` 讨论得等价于 `a eˣ > x`，即 `a ≥ x e^{-x}` 的上确界 `1/e` |
| `导数数形结合01.typ` | 导数数形结合：以斜率（两点连线）视角揭示函数最值问题的几何本质 |

### 排列组合

| 文件 | 内容 |
|------|------|
| `排列组合01特殊优先.typ` | 排列组合·特殊优先：含义（特殊位置 / 特殊元素）与直接法（特殊优先）、间接法（排除法）两种写法；例题 ① 6 名选手依次演讲、甲不在两端 ⟹ `4 × 5! = 480`（间接法 `6! - 2·5!` 验证）；② 从 `{1,3,5,7,9}` 取 2 个、从 `{0,2,4,6}` 取 2 个组成无重复数字的 4 位数 ⟹ 按"是否含 `0`"分类得 `1260`（间接法 `C₅²C₄²·4! - C₅²C₃¹·3!` 验证）；练习 ① 5 人选 4 人参加四科竞赛、甲不能生物、乙只能数学 ⟹ 按"乙是否参加"分类得 `4!-3! + (C₃²·2·2! + 3!) = 36`；② 8 人选 4 人去 4 地、甲与乙不同去、甲与丙同去或同不去 ⟹ 甲丙整体分类得 `C₅²·4! + C₆⁴·4! = 240 + 360 = 600` |
| `排列组合02捆绑法.typ` | 排列组合·捆绑法（含 2 道练习）：相邻问题"捆绑 → 排列 → 松绑"三步（元素数减少 `k-1`，别忘乘内部排列数）；例题 ① 5 名志愿者与 2 位老人排一排、老人相邻且不在两端 ⟹ `4 × 5! × 2! = 960`（间接法 `6!·2! - 2·5!·2!` 验证）；② 甲乙丙丁戊站一排、甲不站两端且丙丁相邻 ⟹ `(4! - 2·3!)·2! = 24`，选 **B**（另给"先安排甲、再放丙丁"的逐位算法，并指出漏松绑会误选 `12`）；练习 ① 6 个数字 `0,5,0,9,1,9` 排密码、两个 `9` 相邻且两个 `0` 相邻 ⟹ 分别捆绑得 `4! = 24`（相同元素内部排列数为 `1`，且密码允许首位为 `0`）；② 七人连号票、丙坐正中间且甲乙相邻 ⟹ 先固定丙，再数出能容纳"甲乙"的相邻座位对 `(1,2),(2,3),(5,6),(6,7)` 共 4 对，得 `4 × 2! × 4! = 192`，选 **B** |
| `排列组合03插空法.typ` | 排列组合·插空法：不相邻问题"先排无限制元素 → 再把特殊元素插入空里"（`n` 个元素有 `n + 1` 个空，插入 `m` 个用 `A_{n+1}^m`，相同元素用 `C_{n+1}^m`）；例题 ① 3 名学生与 2 名老师排一排、老师不相邻 ⟹ `3! × A₄² = 72`（间接法 `5! - 4!·2!` 验证）；② 6 把椅子 3 人就座、两两不相邻 ⟹ 先摆 3 把空椅得 4 个空、插 3 人 `A₄³ = 24`，选 **D**（另给"枚举座位模式 `{1,3,5},{1,3,6},{1,4,6},{2,4,6}`"与"预留间隔"两种验证）；练习 4 个 `6` 与 2 个 `8` 排一行、两个 `8` 不相邻 ⟹ 相同元素插空 `C₅² = 10`，选 **D**（间接法 `C₆² - 5 = 10` 验证；选项 `480 = 4!·A₅²`、`240 = 4!·C₅²`、`15 = C₆²` 分别是"当成不同元素"与"漏条件"的典型错误） |
| `排列组合04停车位.typ` | 排列·停车位（空位分组，2 题）：把车当分隔符、空位当相同小球分配到 `n + 1` 段（一段 `k` 个空位贡献 `k - 1` 对相邻）；例题 7 个车位、红白黑 3 辆不同车、有且仅有两个空车位相邻 ⟹ 分布只能是 `2 + 1 + 1 + 0`（`4 × 3 = 12`），故 `3! × 12 = 72`；另给"先定空位模式再排车"（`12 × 3!`、按相邻对位置分类 `3+1+2+2+1+3 = 12`）与分类交叉验证（全部模式 `1 + 12 + 18 + 4 = 35 = C₇⁴`）；② "三辆车互不相邻"与"恰有两辆车相邻"概率相同，反求车位个数：约去 `3!` 后 `N₀ = C(n-2,3) = (n-2)(n-3) = N₁`，解得 `n = 10`（代回检验：`56/120` 两者相等） |
| `排列组合05捆插结合.typ` | 排列·捆绑与插空结合（2 例 + 2 练）：相邻用捆绑、不相邻用插空，注意"整体内部与外部不一定都相邻"；例题 ① 5 名老师排一排、甲乙不相邻且丙丁相邻 ⟹ `(4! - 3!·2!)·2! = 24`（另给"捆丙丁后插甲乙"`2 × A₃² × 2! = 24`）；② 5 件产品排一排、`A` 与 `B` 相邻且 `A` 与 `C` 不相邻 ⟹ `4!·2! - 3!·2 = 48 - 12 = 36`（法 2 按"整体是否与 `C` 相邻"分类：`12 × 2! + 12 × 1 = 36`，说明"整体贴住某元素时只有一侧的成员与之相邻"）；练 ① 2 名老师与 3 名学生站一排、3 名学生中恰有 2 人相邻 ⟹ 老师造 3 个空、放成 `2+1+0`（`3×2 = 6`）再排学生 `3!`，得 `2!×6×3! = 72`；② 7 件产品、`A` 与 `B` 相邻且 `A` 与 `C` 不相邻 ⟹ `6!·2 - 5!·2 = 1440 - 240 = 1200`（通式 `(n-1)!·2 - (n-2)!·2`） |
| `排列组合06多类插空.typ` | 排列·多类"同类不相邻"（1 例 + 1 练）：抓个数最多的类先排 → 同类元素分居不同的空 → 单个元素的类可"填空" → 最多的那一类中间的空必须非空；例题 3 个歌舞、2 个小品、1 个相声同类不相邻 ⟹ 固定歌舞顺序时插入方式 `12 + 4 + 4 = 20`，故 `3! × 20 = 120`（法 2 按小品相声的相对顺序插入歌舞：`2 × A₄³ + 4 × C₃² × 3! = 48 + 72 = 120` 验证）；练 `A,B,C` 三类各两件（同类也有区别）同类不相邻 ⟹ 类模式用容斥得 `90 - 90 + 36 - 6 = 30`，再乘 `2!³` 得 `240`（直接容斥 `6! - 3·5!·2 + 3·4!·2² - 3!·2³ = 240` 验证） |
| `排列组合07分组问题.typ` | 排列·分组问题（2 例 + 2 练）：分组（组无区别，遇到同样大小的组要除以相应阶乘）与分配（组有区别，分步选完即可）的区别；例题 ① 8 个不同球均分成 4 堆（每堆 2 个）⟹ `C₈²C₆²C₄²C₂²/4! = 105`；② 6 本不同书分成 `4,1,1` 三份 ⟹ `C₆⁴C₂¹C₁¹/2! = 15`（并给出"再分给三人"的变式 `15 × 3! = 90` 与通式 `n!/((m!)^k·k!)`）；练 ① 6 本不同书均分成三堆（每堆 2 本）⟹ `C₆²C₄²C₂²/3! = 15`；② 5 本不同书分成三堆（不限定每堆本数）⟹ 按堆型分类：`3+1+1` 类 `C₅³ = 10`、`2+2+1` 类 `5 × C₄²/2! = 15`，共 `25`（即 `S(5,3)`） |
| `排列组合08分组再分配.typ` | 排列·分组再分配（2 例 + 2 练）：先分组（相同大小的组除以对应阶乘）再派送（乘完整 `k!`，因相同大小的组里元素不同）或直接分步 + 容斥／分类；例题 ① 5 名医务人员派到 3 个年级且每年级至少 1 人 ⟹ 分组 `(3,1,1)` 得 `10×3! = 60`、`(2,2,1)` 得 `15×3! = 90`，共 `150`（容斥 `3⁵ - 3·2⁵ + 3 = 150` 验证）；② 6 名同学分到 2 个社团、每社至少 2 人且 `A,B` 不同社 ⟹ 按人数搭配 `(2,4),(3,3),(4,2)` 得 `8+8+12 = 28`（容斥 `50-11-11 = 28` 验证）；练 ① 4 门课在 3 学年内修完、每学年至多 2 门 ⟹ 搭配 `(2,2,0)` 得 `3×C₄² = 18`、`(2,1,1)` 得 `3×C₄²×2! = 36`，共 `54`；② 5 名同学分到 3 个社区、每社区至少 1 人且甲恰好 2 人 ⟹ `C₅²×(2³-2) = 60` |
| `排列组合09隔板法.typ` | 排列·隔板法（相同元素分给不同对象，2 例 + 2 练）：三要素（元素相同、对象不同、份数下限统一）；≥1 个用 `C(n-1,m-1)`、可为 0 用"先借后还"`C(n+m-1,m-1)`、≥k 个先扣后分 `C(n-m(k-1)-1,m-1)`；例题 ① 6 本相同的书分给甲、乙、丙且每人至少 1 本 ⟹ `C₅² = 10`（附 10 组解列举验证）；② 不限制每人几本 ⟹ 先各借 1 本化为 9 本分 3 人，`C₈² = 28`（即 `x+y+z=6` 的非负整数解个数）；练 ① 8 本相同书分给甲乙丙、甲 ≥1、乙 ≥2、丙不限 ⟹ 扣下限 `x′+y′+z′ = 5`，`C₇² = 21`；② `x₁+…+x₆ = 10` 的正整数解 `C₉⁵ = 126`、自然数解 `C₁₅⁵ = 3003` |
| `排列组合10定序问题.typ` | 排列·定序问题（"相对顺序不变"，1 例 + 2 练）：三种办法——缩小法除 `k!`（`n!/k!`）、位置法 `C_n^k·(n-k)!`、插空法逐步插入；例题 7 个节目首尾固定、中间 5 个相对顺序不变，再插入 3 个新节目 ⟹ 中间 8 个位置中选 3 个放新节目并排列，`C₈³·3! = 336`（缩小法 `8!/5! = 336` 验证）；练 ① 12 人合影，从前排 4 人后排 8 人中抽后排 2 人调到前排且各排相对顺序不变 ⟹ `C₈²×(6!/4!) = 840`；② 5 个节目中甲、乙、丙先后顺序已定 ⟹ `5!/3! = 20`（位置法 `C₅³×2! = 20` 验证） |
| `排列组合11最短路径.typ` | 排列·网格最短路径（2 例）：从 `(0,0)` 到 `(m,n)` 为 `C(m+n,m)`（选"哪几步向右"）、必须经过中间点则分段相乘、有禁行先算总数再容斥（或逐点递推）；例题 街道图（竖 5 线、横 4 线，`E(0,0)`、`F(2,2)`、`G(4,3)`）先到 `F` 再到 `G` 的最短路径 ⟹ `C₄²×C₃¹ = 6×3 = 18`，选 **B**，文档内含网格示意图（与原题图一致）；② $2 times 2 times 3$ 长方体框架从 $A$ 到 $B$ 的最近路线 ⟹ `7!/(3!·2!·2!) = 210`，其中"不连续向上"的先排 4 步再插空 `4!/(2!·2!) × C₅³ = 6 × 10 = 60`（文档含长方体示意图） |
| `排列组合12多面元素多面手.typ` | 排列·"多面手"（多面元素，1 例 + 1 练）：按"多面手在某组中的人数"分类、定下一组后另一组按剩余可用人数算、区分多面手去哪一组、注意是否允许"兼职"；例题 11 名翻译（5 只会英语、4 只会法语、2 双语）选 4 人当英语翻译、4 人当法语翻译 ⟹ 按英语组中双语人数分 `0/1/2` 三类：`C₅⁴C₆⁴ + C₂¹C₅³C₅⁴ + C₂²C₅²C₄⁴ = 75+100+10 = 185`，选 **B**（另按"双语者使用情况"分类 `5+60+120 = 185` 验证；并指出若允许兼职则为 `525`）；练 9 名教师（3 只会器乐、5 只会声乐、1 双面手）选 3 人器乐、4 人声乐且每人只参加一种 ⟹ 按双面手去向分三类：`5 + 15 + 10 = 30` |
| `排列组合13错排.typ` | 排列·错排（更列，1 例 + 1 练）：小规模 $D_1..D_5 = 0,1,2,9,44$、递推 `Dₙ = (n-1)(D_{n-1}+D_{n-2})`、通项与"恰有 `k` 个对号"模型 `Cₙᵏ·D_{n-k}`；例题 5 人坐 5 个编号座位、恰有 2 人编号与座位号一致 ⟹ `C₅²·D₃ = 10 × 2 = 20`，选 **B**（附"恰好 `k` 人一致"的完整分类表 `44+45+20+10+0+1 = 120 = 5!` 作检验）；练 6 个编号球放入 6 个编号盒、恰有 2 个编号相同 ⟹ `C₆²·D₄ = 15 × 9 = 135`，选 **B** |
| `排列组合14环排.typ` | 排列·环排（圆桌，2 例 + 2 练）：`n` 个不同元素围成一圈为 `(n-1)!`（旋转重合算一种），"坐定法"最省事、相邻用捆绑（再按环排）、不相邻时空只有 `n` 个、可翻面（项链）再除 `2`；例题 ① 5 人围桌而坐 ⟹ `(5-1)! = 24`；② 7 人围圆桌且甲、乙相邻 ⟹ `(6-1)! × 2! = 240`（另解"甲先坐定、乙坐左右" `2 × 5! = 240`）；练 ① 6 人围桌而坐 ⟹ `(6-1)! = 120`；② 三个大人带两个小孩围成一圈、两小孩不相邻 ⟹ 先排大人再插空 `(3-1)!×C₃²×2! = 12`（间接法 `4!-3!×2! = 12` 验证，注意环形只有 3 个空） |
| `排列组合15元素配对.typ` | 排列·元素配对（成双物品抽取，2 例 + 1 练）：认清"每双两件不同"（从某双取一只恒有 2 种）；"恰有 `k` 双"用 `Cₙᵏ` 选整双再对其余各双取一只（`·2^b`）；"至少一双"用间接法（总数 − `Cₙʳ·2ʳ`）；例题 ① 4 双袜子取 3 只、恰有 2 只花色相同 ⟹ `C₄¹C₃¹·2 = 24`；② 5 双袜子取 4 只、至少配成一双 ⟹ `C₁₀⁴ - C₅⁴·2⁴ = 210 - 80 = 130`，选 **C**（另按"恰一双 120 + 恰两双 10"分类验证）；练 6 双鞋取 4 只：恰两双 `C₆² = 15`、恰一双 `C₆¹C₅²·2² = 240`（并给出完整分类检验 `15 + 240 + 240 = 495 = C₁₂⁴`） |
| `排列组合16涂色问题.typ` | 排列·区域/顶点涂色（2 例）：以"有公共边界"为相邻、画相邻关系图、从相邻最多的区域入手/按"相间区域是否同色"分类、环形公式 `(k-1)ⁿ + (-1)ⁿ(k-1)`、不涂色区域的隔断作用；例题 街心花园（`A,B,C,D` 围成一圈、中心 `E` 为塑像不涂色）用 4 种花种植且相邻不同 ⟹ 按"`A` 与 `C` 是否同色"分类 `4×3×3 + 4×3×2×2 = 36+48 = 84`，选 **D**（递推法 `4×3×(1×3+2×2) = 84` 与公式 `3⁴+3 = 84` 验证，文档含区域示意图）；② 轮形图（五边形 `A..E` + 中心 `O` 五条辐条）用 5 色给 6 点涂色、相邻不同 ⟹ 先涂 `O`（5 种），外圈只能用其余 4 色且是五元环：`3⁵-3 = 240`，共 `5 × 240 = 1200`，选 **D**（附五元环 `72+72+96 = 240` 的手工分类验证，文档含轮形图） |

### 二项式

| 文件 | 内容 |
|------|------|
| `二项式02直接求系数.typ` | 二项式·直接求系数（5 例）：通项 `T_{k+1} = Cₙᵏaⁿ⁻ᵏbᵏ` 与"令指数相等解 k"三步法，区分"二项式系数"与"项的系数"；例 1 `(x-√3)¹⁰` 中 `x⁶` 的系数 ⟹ `C₁₀⁴·9 = 1890`；例 2 `(x²+2/x)⁵` 中 `x⁴` 的系数 ⟹ `C₅²·2² = 40`（选 C）；例 3 `(1/x-1)⁵` 中 `x⁻²` 的系数 ⟹ `C₅³·(-1)³ = -10`（选 D）；例 4 `(ax+1/√x)⁸` 中 `x²` 的系数为 `35/8` ⟹ `70a⁴ = 35/8`、`a² = 1/4`，故 `x⁵` 的系数 `C₈²a⁶ = 7/16`（选 C）；例 5（多选）`(1/x+x³)ⁿ` 的指数为 `4k-n` ⟹ 有常数项 ⟺ `n` 是 `4` 的倍数（如 `n=4`）、有含 `x` 的项 ⟺ `n = 4k-1`（如 `n=3`），选 **AD** |
| `二项式03两项相乘求系数.typ` | 二项式·两项相乘求系数（1 例 + 2 练）："（简单因式）$times$（二项展开式）"的逐项配对法（指数相加等于目标次数，系数用加法原理合并）；例 1 `(1 + 1/x²)(1+x)⁶` 中 `x²` 的系数 ⟹ `C₆² + C₆⁴ = 15 + 15 = 30`，选 **C**（附完整展开检验：`x⁻² + 6x⁻¹ + 16 + 26x + 30x² + …`）；练 ① `(1+ax)(1+x)⁵` 中 `x²` 的系数为 `5` ⟹ `10 + 5a = 5`、`a = -1`（选 D）；② `(x+y)(2x-y)⁵` 中 `x³y³` 的系数 ⟹ `x·(x²y³ 系数 `-40`) + y·(x³y² 系数 `80`) = 40`（选 C） |
| `二项式04多项式.typ` | 二项式·三项式（多项式）展开（2 例 + 2 练）：多项式通项 `n!/(k₁!k₂!k₃!)·a^k₁b^k₂c^k₃`（`k₁+k₂+k₃ = n`）与"几个字母列几个方程"；能分解因式时化回二项式；例 1 `(x²+x+y)⁵` 中 `x⁵y²` 的系数 ⟹ `5!/(2!1!2!) = 30`（选 C）；例 2 `(x+1/x-2)⁵` 的常数项 ⟹ 法 1 三种解相加 `-32-160-60 = -252`，法 2 化回 `(x-1)¹⁰/x⁵` 得 `-C₁₀⁵ = -252`；练 ① `(x²+3y-y²)⁷` 中 `x¹²y²` 的系数 ⟹ `k₁=6, k₃=1, k₂=0`，`7!/(6!0!1!)·(-1) = -7`（选 B）；② `(x-1+px²)⁶` 中 `x⁵` 项系数最大 ⟹ 系数 `f(p) = -60p²+60p-6 = -60(p-1/2)²+9`，`p = 1/2` |
| `二项式05系数和.typ` | 二项式·系数和与二项式系数和（2 例 + 3 练）：令 `x = 1` 得全部系数和 `f(1)`、令 `x = -1` 得交错和 `f(-1)`、令 `x = 0` 得常数项；偶次/奇次项系数和 `[f(1)±f(-1)]/2`；绝对值型先判正负（去掉绝对值后常化为 `f(1)` 或 `f(-1)`）；区分"系数和"与"二项式系数和 `2ⁿ`"；例 1 `(1-2x)⁶` 的偶次项系数和 ⟹ `(1 + 3⁶)/2 = 365`；例 2（多选）`(1-2x)⁵` ⟹ `a₀ = 1`、`a₁+…+a₅ = -2`、交错和为 `3⁵`、去掉绝对值后化为 `f(1) = -1`，选 **ACD**；练 ①（多选）`(1-2x)²⁰⁰⁹` ⟹ `a₀ = 1`、偶次项和 `(3²⁰⁰⁹-1)/2`、`a₁/2+a₂/2²+… = f(1/2)-a₀ = -1`（奇次项和应为 `-(3²⁰⁰⁹+1)/2`）⟹ 选 **ACD**；② `(3x+√x)ⁿ` 的系数和 `M = 4ⁿ`、二项式系数和 `N = 2ⁿ`，`M-17N = 480` ⟹ `n = 5`，故 `x³` 的系数 `C₅⁴·3 = 15`（选 D）；③（多选）展开式以 `(x-1)` 为基：`(2x-3)⁹ = Σaᵢ(x-1)ⁱ`，令 `t = x-1` 化为 `(2t-1)⁹` ⟹ `a₂ = -144`、`a₀ = -1`、系数和 `1`、交错和 `-3⁹`，选 **ACD** |
| `二项式06导数.typ` | 二项式与导数（1 例 + 1 练）：系数里带 `i` 时对恒等式求导——`Σ i·aᵢ = f′(1)`、`Σ i(i-1)aᵢ = f″(1)`；分母带 `i+1` 时改为积分 `∫₀¹f = Σ aᵢ/(i+1)`；注意复合函数求导别漏内层系数；例 1 `(1-ax)²⁰²⁵` 中 `a₁+2a₂+…+2025a₂₀₂₅ = -2025a` ⟹ `f′(1) = -2025a(1-a)²⁰²⁴`，由 `a[(1-a)²⁰²⁴-1] = 0` 得 `a = 0 或 2`（通常取非零的 `a = 2`）；练 展开式以 `(1+x)` 为基：`(2-x)¹⁷ = Σaᵢ(1+x)ⁱ`，令 `t = 1+x` 化为 `(3-t)¹⁷` ⟹ `a₀+…+a₁₆ = 2¹⁷ - a₁₇ = 2¹⁷+1`、`a₁+2a₂+…+16a₁₆ = F′(1) - 17a₁₇ = 17(1-2¹⁶)` |
| `二项式07n项最大.typ` | 二项式·第 n 项与最大项（2 例 + 1 练）：通项 `T_{k+1} = Cₙᵏaⁿ⁻ᵏbᵏ`，"第 m 项"对应 `k = m-1`；区分"第 m 项的二项式系数 `Cₙ^(m-1)`"与"系数"；二项式系数在中间最大（n 偶唯一、n 奇并列）；求系数最大项用比值法；例 1 `(x+1/(2x))⁶` 的第 3 项 ⟹ `T₃ = C₆²x⁴·(1/(2x))² = (15/4)x²`（选 C，附干扰项分析）；例 2 `(a+b)ⁿ` 第 5 项二项式系数最大 ⟹ `n/2 = 4`、`n = 8`（选 B；注：允许并列时 `n = 7, 9` 也满足）；练 `(ax+1/(bx))ⁿ` 只有第 6 项的二项式系数最大且第 3 项系数是第 4 项的 3 倍 ⟹ `n = 10`、`45a = 360/b`、`ab = 8` |
| `二项式08其他.typ` | 二项式·杨辉三角与拓展（2 例 + 1 练）：第 `n` 行（行号从 0 起）依次是 `Cₙ⁰, Cₙ¹, …, Cₙⁿ`；首末为 1、肩上两数之和 `Cₙᵏ = C_{n-1}^{k-1} + C_{n-1}^k`、左右对称、行和 `2ⁿ`、相邻两数之比 `Cₙᵏ/Cₙ^{k+1} = (k+1)/(n-k)`；例 1 第 10 行第 5 与第 6 个数的比值 ⟹ `C₁₀⁴/C₁₀⁵ = 5/6`（两种解法 + 位置"比下标大 1"的提醒，文档含杨辉三角列表）；例 2 `(1+x)²+(1+x)³+…+(1+x)⁹` 中 `x²` 的系数 ⟹ 斜线和 `Σ_{k=2}^{9}Cₖ² = C₁₀³ = 120`（选 D；另解用等比求和化为 `((1+x)¹⁰-(1+x)²)/x`）；练 用 `1..5` 排五位数且"个位<百位<万位"有 `n = 5!/3! = 20` 个，再求 `(1+x)³+…+(1+x)²³-x³` 的 `x²` 系数 ⟹ `Σ_{k=3}^{23}Cₖ² = C₂₄³-1 = 2023` |

### 抽象函数

| 文件 | 内容 |
|------|------|
| `抽象函数00.typ` | 抽象函数（知识解析 + 9 道真题）：先把 `f(ax+b)` 的奇偶性翻译成对称轴／对称中心，再用"两轴 `T = 2\|a-b\|`、两中心 `T = 2\|a-b\|`、一轴一中心 `T = 4\|a-b\|`"推周期，最后用"整周期 + 余项"求和；双函数题先消元，含导数条件用"求导翻译"（轴↔中心互换）。含【题型 1】例 1（`f(4x+2)` 奇、`f(2x+1)` 偶 ⟹ 周期 4、`f(2020)=0`，选 C）与练 1.1（BCD）、练 1.2（10，选 A）；【题型 2】练 2（`f'(x)` 轴 x=-1 + 中心 (1,1) ⟹ 周期 8，和为 25，选 C）；【题型 3】例 3（BC）、练 3.1（BCD）、练 3.3（ABD）；【题型 4】例 4（ABC）、练 4.2（BCD）。每题均给出完整推导，并用具体函数模型（`sin`、`cos` 型）逐项验证 |

### 概率

| 文件 | 内容 |
|------|------|
| `概率01分布列.typ` | 概率·离散型随机变量的分布列：袋中 `10` 红 `5` 黑，每次抽 1 球，抽到黑球就换成 1 个红球放回、直到抽到红球为止，抽取次数记为 `ξ`。例 1 判断"放回 5 个红球"对应的事件 ⟹ 抽到过 5 次黑球 ⟹ 前 5 次全黑、第 6 次必红 ⟹ **`ξ = 6`（选 C）**；并算出 `P(ξ=6) = 5/15·4/15·3/15·2/15·1/15 = 8/50625`（关键：球总数恒为 15，每一步分母都是 15）。方法总结 5 条；例 2 判断四张表能否作为分布列 ⟹ **选 C**（A 概率和 `1.1`、B 出现负概率 `-0.1`、D 概率和 `1.2` 且 `X` 取值重复），并给出判据 `pᵢ ≥ 0`、`Σpᵢ = 1` 与"取值互不相同"；例 3 由 `1/3 + m + 1/4 + 1/6 = 1` 得 `m = 1/4`，再把 `|X-3| = 1` 化为 `X ∈ {2,4}` 得 **`P = 5/12`**；例 4 从 `1..5` 中同时取 3 球、`X` 为最大号码 ⟹ `X ∈ {3,4,5}`，`P = C₍ₖ₋₁₎²/C₅³` 即 `1/10, 3/10, 3/5`；例 5 十张奖券（1 张 50 元、3 张 10 元、6 张无奖）任抽 2 张 ⟹ (1) 中奖概率 `2/3`（用对立事件），(2) 分布列 `X = 0,10,20,50,60`、`P = 1/3, 2/5, 1/15, 2/15, 1/15`（`C₁₀² = 45` 为总取法）；例 6 已知 `X = -1,0,1`（`P = 1/2,1/3,1/6`），`Y = 2X+3` ⟹ `E(X) = -1/3`，`E(Y) = 2E(X)+3 = 7/3`（另可按定义算 `1·1/2+3·1/3+5·1/6`）；例 7（多选）由 `Σp = 1` 得 `q = 0.1`，`E(X) = 2`、`E(X²) = 5.8` ⟹ `D(X) = 1.8`，`Y = 2X+1` ⟹ `E(Y) = 5`、`D(Y) = 4D(X) = 7.2` ⟹ 选 **ACD**；例 8 滑雪计费（$\le 1$ 时 0 元、$1\sim 2$ 时 40 元、$2\sim 3$ 时 80 元）：(1) 两人费用相同概率 `5/12`；(2) 费用之和 `X` 的分布列 `0,40,80,120,160` 对应 `1/24, 1/4, 5/12, 1/4, 1/24`，`E(X) = 80`（也可由 `E(甲)=E(乙)=40` 相加） |
| `概率02分布.typ` | 概率·**超几何分布**：总体分两类（`m` 个 + `n` 个），不放回抽 `a` 个、`X` 为其中某类个数，则 `P(X=b) = CₘᵇCₙ^{a-b}/C_{m+n}^a`，取值满足 `max(0, a-n) ≤ b ≤ min(a, m)`，期望有简捷式 `E(X) = ap`（`p` 为该类占比）；例 1 三种机器（A 5 台、B 3 台、C 1 台）任抽 4 台、`X` 为 B 型台数 ⟹ `C₉⁴ = 126`，`P = 5/42, 10/21, 5/14, 1/21`（对应 `X = 0,1,2,3`），`E(X) = 4·(3/9) = 4/3`（按定义验算同为 `168/126`）；例 2 心理学对比试验（6 男 4 女抽 5 人）：(Ⅰ) 甲组含 `A₁` 不含 `B₁` ⟹ `C₈⁴/C₁₀⁵ = 5/18`；(Ⅱ) `X` 为乙组女生数 ⟹ `X = 0..4` 对应 `1/42, 5/21, 10/21, 5/21, 1/42`，`E(X) = 5·(4/10) = 2`；**二项分布**：`X ~ B(n,p)` 时 `P(X=k) = Cₙᵏpᵏ(1-p)ⁿ⁻ᵏ`、`E(X) = np`、`D(X) = np(1-p)`，并附与超几何分布的对照表；例 3 过 10 个路口（红灯 `1/3`、绿灯 `2/3`，相互独立）⟹ `X ~ B(10, 2/3)`，`P(X=k) = C₁₀ᵏ(2/3)ᵏ(1/3)¹⁰⁻ᵏ`，`E(X) = 20/3 ≈ 6.67`（`k = 7` 时最大）；例 4 点球大战（射门/扑救各 3 方向等可能、判断正确也有 `2/3` 扑不到）⟹ 单次扑到 `p = 1/3·1/3 = 1/9`，`X ~ B(3, 1/9)`，`P = 512/729, 64/243, 8/243, 1/729`，`E(X) = 1/3`；例 5 从 3000 人（女生 1000）中不放回抽 3 人 ⟹ `P(X=k) = C₁₀₀₀ᵏC₂₀₀₀³⁻ᵏ/C₃₀₀₀³`，概率约 `0.2961, 0.4447, 0.2222, 0.0370`，`E(X) = 3·(1/3) = 1`；例 6 分层抽样 + 二项分布：抽样比 `3/180 = 1/60` ⟹ B 抽 5 份、C 抽 2 份；以频率为概率 `p = 100/300 = 1/3`，`X ~ B(5, 1/3)`，`P = 32/243, 80/243, 80/243, 40/243, 10/243, 1/243`，`E X = 5/3`；**正态分布**：密度 `f(x) = 1/(√(2π)σ)·e^{-(x-μ)²/(2σ²)}`、记 `X ~ N(μ, σ²)`（`μ` 是对称轴/期望、`σ²` 是方差），含图像特征、3σ 原则与标准正态；例 7 由 `X ~ N(10, 2²)` 写出密度、对称轴 `x = 10`、`E(X) = 10`、`D(X) = 4`，并得 `P(X>12) = P(X<8)`；例 8 两道练习：练 1 由对称轴 `x = 2` 得 `P(2<X<4) = 0.8 - 0.5 = 0.3`；练 2 `εₙ ~ N(0, 3/n)`，由 `3σ ≤ 0.75` 得 `n ≥ 48`（至少测量 48 次）；例 9 练 3 由 `ξ ~ N(1,4)` 及 `P(ξ<2m) = P(ξ>3-m)` 得 `2m + (3-m) = 2`、`m = -1`；练 4 `εₙ ~ N(0, 1/n)`，由 `2σ ≤ 0.5` 得 `n ≥ 16`（至少 16 次）；例 10（2017·全国 I 卷）抽 16 个零件、超 `3σ` 概率 `p = 0.0026` ⟹ `X ~ B(16, 0.0026)`，`P(X ≥ 1) = 1 - 0.9974¹⁶ = 0.0408`、`E(X) = 16×0.0026 = 0.0416`；(2) 用"小概率事件"原理说明监控合理 |
| `概率03截止性问题.typ` | 概率·**截止性问题**（赛制与场数）：七局四胜制，强队每场胜率 `2/3`、每场获利 200 万美元——(1) 只打 4 场 ⟹ `p⁴+q⁴ = 17/81`；(2) 获利不低于 1200 万 ⟺ 至少 6 场 ⟹ `C₅³(p⁴q²+p²q⁴) + C₆³p³q³ = 40/81`；(3) `P(X=5) = 8/27`，分布列 `153/729 + 216/729 + 200/729 + 160/729 = 1`，`E(X) = 4·17/81 + 5·8/27 + 6·200/729 + 7·160/729 = 4012/729`，获利期望 `200E(X) ≈ 1100.7` 万美元；练 1 抢答赛（先得 3 分者胜，甲单轮得分概率 `p = 1/2·4/5 + 1/2·2/5 = 3/5`）：(1) 甲先得 1 分 `3/5`；(2) 甲获胜 `3:0 + 3:1 + 3:2` ⟹ `675/3125 + 810/3125 + 648/3125 = 2133/3125`；例 2 "分差 2 分或打满 6 局"停止（甲每局胜率 `2/3`）⟹ `X ∈ {2,4,6}`（奇数局分差不可能为 2），`P = 5/9, 20/81, 16/81`（即 `p²+q²`、`2pq(p²+q²)`、`(2pq)²`），`E(X) = 2·5/9 + 4·20/81 + 6·16/81 = 266/81 ≈ 3.28`；练 2 三人羽毛球"累计两负淘汰"赛制（甲、乙先赛、丙轮空）⟹ (1) 甲连胜四场 `1/16`；(2) 需要第五场 `3/4`（四场内结束的序列共 4 种，各 `1/16`）；(3) 丙最终获胜 `7/16`（检验：甲、乙各 `9/32`，三者和为 1） |
| `概率04马尔科夫链.typ` | 概率·**马尔科夫链（递推）**：【题型 1】简单递推——$10$ 颗糖、每天至少吃一颗的吃法数：插空法得 `2⁹ = 512`；递推法 `aₙ = 1 + a₁ + … + aₙ₋₁` ⟹ `aₙ = 2aₙ₋₁`、`aₙ = 2ⁿ⁻¹`；【题型 2】概率模型（红绿球玩具）：全概率得 `Pₙ₊₁ = 3/5·Pₙ + 1/3(1-Pₙ) = (4/15)Pₙ + 1/3`，`P₂ = 7/15`；不动点 `x = 5/11` ⟹ `Pₙ = 5/11 + (1/22)(4/15)ⁿ⁻¹`（长期趋于 `5/11`）；例 3 基础题/挑战题（上一题对错决定下一题来源）：`aₖ₊₁ = 0.6 - 0.2aₖ`，`a₂ = 0.48`，`aₖ = 1/2 + (1/10)(-1/5)^{k-1}`，`E(Y) = Σaᵢ = n/2 + (1/12)[1-(-1/5)ⁿ]`；【题型 3】传球模型（甲、乙、丙三人等可能传球）：`Pₙ₊₁ = (1-Pₙ)/2`，不动点 `1/3`、公比 `-1/2`，`Pₙ = 1/3 + (2/3)(-1/2)ⁿ⁻¹`；由对称性 `Qₙ = (1-Pₙ)/2` ⟹ `Qₙ - Pₙ = -(-1/2)ⁿ⁻¹`，故 `Q₁₀ - P₁₀ = 1/512 > 0`，即 `Q₁₀ > P₁₀`；【题型 4】对称随机游走（赌徒模型，含马尔科夫链定义）：`P(n) = ½P(n+1) + ½P(n-1)` ⟹ `{P(n)}` 为等差数列，由 `P(0) = 1`、`P(B) = 0` 得公差 `d = -1/B`、`P(n) = 1 - n/B`；`A = 100` 时 `B = 200 → 1/2`、`B = 1000 → 9/10`，`B → ∞` 时 `P(A) → 1`（对称赌博"久赌必输"）；【题型 5】非对称随机游走（赌赢 `2/5`、赌输 `3/5`）：`P(n) = (2/5)P(n+1) + (3/5)P(n-1)` 移项得 `dₙ₊₁ = (q/p)dₙ`（公比 `3/2`，对称时退化为等差），由 `P(0) = 1`、`P(B) = 0` 得 `P(n) = [(3/2)ᴮ - (3/2)ⁿ]/[(3/2)ᴮ - 1]` |
| `概率05回归.typ` | 概率·**成对数据分析（相关系数与一元线性回归）**：相关关系与散点图（线性/非线性相关）；相关系数 `r = Σ(xᵢ-x̄)(yᵢ-ȳ) / (√Σ(xᵢ-x̄)²·√Σ(yᵢ-ȳ)²)`，`r ∈ [-1,1]`（`r>0` 正相关、`r<0` 负相关、`|r|` 越接近 1 越贴近直线）；经验回归方程 `ŷ = b̂x + â`，其中 `b̂ = Σ(xᵢ-x̄)(yᵢ-ȳ)/Σ(xᵢ-x̄)²`、`â = ȳ - b̂x̄`（回归直线过样本中心点、`b̂` 与 `r` 同号）；例：`(1,2),(2,3),(3,5),(4,4),(5,6)` ⟹ `x̄=3, ȳ=4`、`Σ(x-x̄)(y-ȳ)=9`、两个平方和均为 10，故 `b̂ = 0.9`、`â = 1.3`、`ŷ = 0.9x + 1.3`，`r = 0.9`（正相关较强），`x = 6` 时预测 `ŷ = 6.7`；练 1 若样本点全在直线 `y = -x/3 + 2` 上 ⟹ 完全线性相关且斜率为负 ⟹ `r = -1`（选 A；另附公式验证）；练 2（2025·湖南，多选）⟹ **ACD**（A 第 25 百分位数 `(-1+3)/2 = 1`；B 应为 `r = -1`；C 由 `np = 36`、`np(1-p) = 9` 得 `p = 3/4`、`n = 48`；D `50 × 0.6827/2 ≈ 17` 人）；练 3（2025·江西，回归方程理解）⟹ 选 **C**（A 错：过样本中心点得 `a = 0.58 - 0.025×21.2 = 0.05`；B 错：斜率正 ⟹ `r > 0`；D 错：换因变量需重新拟合；C 正确：斜率的实际含义是"`x` 每增 1，`y` 平均增 0.025"）；练 4（2025·湖南，多选）⟹ **ABD**（`b̂ = 69/538 ≈ 0.13`、`â = 3 - 0.13×12 = 1.44`；`r = 69/√5380 ≈ 0.94`；残差绝对值最小的是 `(4,2)`（C 错）；去掉残差为正的 `(4,2)` 后直线下移（D 对））；练 5（2023·河南）由 `Σy = 175` 得 `m = 36`，`b̂ = (608-525)/10 = 8.3`、`â = 10.1` ⟹ `ŷ = 8.3x + 10.1`，6 月约 **59.9 万元**；`r = 83/√7000 ≈ 0.99` ⟹ 强正相关；练 6（2023·贵州，非线性回归）细菌繁殖数据相邻比值≈2 ⟹ 选指数型 `ŷ = c₁e^{c₂x}`，令 `z = ln y` 化线性得 `ẑ = 1.1158 + 0.6902x`，还原为 `ŷ ≈ 3.0519e^{0.6902x}`（`e^{0.6902} ≈ 2` 印证"每天翻倍"）；练 7 茶水温度（向室温 25℃ 衰减）⟹ 选 `y = dcˣ + 25`，令 `w = ln(y-25)` 化线性：`Σ(x-x̄)² = 28`、`ln c = -2.24/28 = -0.08`、`ln d = 4.17` ⟹ `ŷ ≈ 64.72e^{-0.08x} + 25`；`ŷ = 60` 解得 `x ≈ 7.68` 分钟（约 8 分钟） |
| `概率06独立性检验.typ` | 概率·**列联表与 χ² 独立性检验**：分类变量与 2×2 列联表（`a,b,c,d` 与四个合计、`n = a+b+c+d`）；零假设 `H₀: X 与 Y 相互独立`；皮尔逊统计量 `χ² = Σ(O-E)²/E` 化简为 `χ² = n(ad-bc)²/((a+b)(c+d)(a+c)(b+d))`（越小越接近独立、越大越有把握认为有关）；临界值表 `α = 0.1/0.05/0.01/0.005/0.001` 对应 `x_α = 2.706/3.841/6.635/7.879/10.828`、把握 `90%/95%/99%/99.5%/99.9%`；判断规则（"认为相关 = 不独立"）、四步流程与**大题答题规范**（问"把握"与问"是否独立"两种问法的写法）；例 1 性别与喜欢数学 `χ² ≈ 8.333 > 6.635`（99% 把握）、例 2 培训与达标 `χ² = 36`（99.9%）、例 3 证据不足 `χ² ≈ 2.778 < 3.841`、例 4（2025·甘肃白银二模）补全列联表 + `P(B\|Ā) = 0.75` + `χ² ≈ 7.792`；练 1 住宿与社团 `χ² = 8`、练 2 补全表再检验 `χ² ≈ 6.929`、练 3（多选）⟹ **BC**、练 4（2025·山东济南一模，多选）⟹ **BC**（`2.727 > 2.706`、`2.727 < 6.635`）、练 5（2025·甘肃模拟，多选）⟹ **ABC**、练 6 喝茶与年龄（不能推断有关；`X ~ B(2, 1/3)`、`E(X) = 2/3`）；末附易错点清单 |
| `概率07条件概率.typ` | 概率·**条件概率、全概率与贝叶斯**：底层逻辑（古典概型计数 `P(A) = n(A)/n(Ω)`、`P(B\|A) = n(AB)/n(A)` 与定义式 `P(B\|A) = P(AB)/P(A)` 的由来）；条件概率的性质（范围、可加性、对立）；乘法公式 `P(AB) = P(A)P(B\|A)` 与三事件连用；**全概率公式**（完备事件组、两分形式、识别特征与三步法、概率树"分叉相乘、同类相加"、两盒取球示范、常见易错）；贝叶斯公式（由结果反推原因）；事件的独立性（定义、独立与互斥的区别、`Ā`/`B̄` 的独立性）；例 1 两个孩子的家庭（`1/3`）、例 2 不放回取球（`P(AB) = 5/14`、不独立）、例 3 疾病检测（全概率 `0.02097`、贝叶斯 `≈ 4.72%`）、例 4 至少命中一次（`2/3`）、例 5 抽球（已知不是白球 `3/5`）、例 6 组队（既有男生又有女生 `7/15`）、例 7 频率直方图 + 条件概率（`0.1232 ≈ 0.12`）、例 8 病例对照优势比 `R` 的证明与估计（`R = 6`）、例 9 四条流水线（`0.0315`）、例 10 食堂就餐（`9/70`）；练 1 骰子（`1/3` 与 `1/2`，但两事件独立）、练 2（多选）⟹ **ACD**、练 3 工厂两线（`1/25`、`1/2`）、练 4 摸球转移（`13/30`、`9/13`）、练 5 粽子同馅（`1/3`）、练 6 两排拍照（`4/9`）、练 7 餐厅选择 ⟹ **B**（`0.6`）、练 8 知三求一（`1/3`）；末附易错点清单 |

### 立体几何

| 文件 | 内容 |
|------|------|
| `立体几何01-03平行.typ` | 立体几何·**平行与垂直的判定体系**：六条基本判定——① 线面平行（`a ∥ b`、`b ⊂ α`、`a ⊄ α` ⟹ `a ∥ α`）② 面面平行（`α` 内两条相交直线分别平行于 `β`）③ 线面垂直（垂直于平面内两条相交直线）④ 面面垂直（一个平面过另一个平面的垂线）⑤ 线线垂直（`a ⊥ α`、`b ⊂ α` ⟹ `a ⊥ b`，证异面直线垂直的主通道）⑥ 面面垂直的性质（在垂直平面内作交线的垂线）；并给出"平行升维链"与"垂直转化链"两条主线、中位线等造平行手段与 5 条要点提示；例 1 四棱锥 `P-ABCD`（菱形底面、`E` 为 `PD` 中点）证 `PB ∥ 平面 AEC`：菱形对角线互相平分 ⟹ `O` 是 `BD` 中点，连 `OE` 得 `△PBD` 的中位线 ⟹ `OE ∥ PB` ⟹ 线面平行（并指出长度、角度等条件是多余背景，配四棱锥示意图）；练 1 直三棱柱 `ABC-A₁B₁C₁`（`D` 为 `AC` 中点）证 `AB₁ ∥ 平面 C₁BD`：矩形侧面 `BCC₁B₁` 的对角线交点 `M` 是 `B₁C` 中点，在 `△AB₁C` 中得中位线 `DM ∥ AB₁` ⟹ 线面平行（配三棱柱示意图）；例 2 立方体中 `E`（`CC₁` 中点）、`F`（`AB` 中点）证 `EF ∥ 平面 ACD₁`：取 `CD₁` 中点 `M`，中位线给 `EM ∥ C₁D₁` 且 `EM = ½C₁D₁`，与 `AF ∥ C₁D₁`、`AF = ½AB` 比较得 `AF ∥ EM` 且相等 ⟹ `AFEM` 为平行四边形 ⟹ `EF ∥ AM ⊂ 平面 ACD₁`（配立方体示意图）；练 2 菱形底面的直四棱柱（`E`、`M`、`N` 分别为 `BC`、`BB₁`、`A₁D` 中点）证 `MN ∥ 平面 C₁DE`：取 `AD` 中点 `Q` ⟹ 中位线给 `NQ ∥ AA₁` 且 `= 2`，与 `MB ∥ BB₁` 且 `= 2` 比较得 `NQBM` 平行四边形 ⟹ `MN ∥ QB`；又 `DQBE` 为平行四边形 ⟹ `QB ∥ DE` ⟹ `MN ∥ DE ⊂ 平面 C₁DE`（配直四棱柱示意图；并指出长度/角度条件本问用不到）；例 3 三棱锥（`PO` 为高、`PA = PB`、`AB ⊥ AC`、`E` 为 `PB` 中点）证 `OE ∥ 平面 PAC`：由 `PO ⊥ 底面` 与 `PA = PB` 得 `OA = OB`，结合图形 `O ∈ AB` 知 `O` 是 `AB` 中点，在 `△PAB` 中用中位线得 `OE ∥ PA ⊂ 平面 PAC`（配三棱锥示意图） |
| `立体几何04线面垂直.typ` | 立体几何·**线面垂直（判定与性质）**：判定（垂直于平面内两条相交直线）、性质（线面垂直 $=>$ 线线垂直）、证线面垂直的四类常用手段（等腰+中线、勾股逆定理、正方形/菱形、借已知线面垂直）；例 1 四棱锥 `P-ABCD`（`PA ⊥ 底面`、底面正方形、`PA = AD = 3`、`E` 为 `PD` 中点）证 `AE ⊥ 平面 PCD`：等腰直角 `△PAD` 的中线给 `AE ⊥ PD`，`CD ⊥ AD` 且 `CD ⊥ PA` 给 `CD ⊥ 平面 PAD` 从而 `CD ⊥ AE`（配四棱锥示意图）；练 1 多面体 `ABCDEF`（`平面 ABC ∥ 平面 DEF`、`AE ∥ CD`、`AE ⊥ 平面 ABC`、`ABFE` 为直角梯形、`AB = BC = 2AE = 2EF`）证 `AF ⊥ 平面 BCF`：`AE ⊥ BC` 与 `AB ⊥ BC` 得 `BC ⊥ 平面 ABFE` 从而 `BC ⊥ AF`；设 `AE = EF = 1` 算得 `AF = BF = √2`、`AB = 2` ⟹ 勾股逆定理给 `AF ⊥ BF` ⟹ 线面垂直（配多面体示意图）；练 2 四棱锥（`PD ⊥ 底面`、`CD ∥ AB`、`AD = DC = CB = 1`、`AB = 2`、`DP = √3`）证 `BD ⊥ PA`：取 `AB` 中点 `E` 得平行四边形 `AECD` 与等边 `△EBC` ⟹ `∠DAB = 60°`，余弦定理算 `BD = √3` ⟹ `AD² + BD² = AB²` 得 `BD ⊥ AD`，再结合 `PD ⊥ BD` 得 `BD ⊥ 平面 PAD`（配四棱锥示意图） |
| `立体几何05-06面面垂直及其应用.typ` | 立体几何·**面面垂直（判定与性质）**：判定（一个平面过一个平面的垂线 ⟹ 两面垂直）、性质（两面垂直 ⟹ 在其中一个面内作交线的垂线即得另一面的垂线）；例 1 直三棱柱（底面正三角形边长 2，`E`、`F` 为 `BC`、`CC₁` 中点）证 `平面 AEF ⊥ 平面 B₁BCC₁`：正三角形三线合一给 `AE ⊥ BC`，直棱柱给 `BB₁ ⊥ AE` ⟹ `AE ⊥ 平面 B₁BCC₁` ⟹ 面面垂直（配三棱柱示意图）；练 1 圆柱 `O₁O`（`AB` 为母线、`BC` 为底面直径、`D` 在圆周上）证 `平面 ABD ⊥ 平面 ADC`：直径所对圆周角给 `CD ⊥ BD`，母线给 `AB ⊥ CD` ⟹ `CD ⊥ 平面 ABD` ⟹ 面面垂直（配圆柱示意图）；练 2 四棱锥（底面正方形、`AD = 2`、`QD = QA = √5`、`QC = 3`）证 `平面 QAD ⊥ 平面 ABCD`：取 `AD` 中点 `O` ⟹ `QO ⊥ AD` 且 `QO = 2`，算得 `OC = √5` ⟹ `QO² + OC² = QC²` 给 `QO ⊥ OC` ⟹ `QO ⊥ 底面` ⟹ 面面垂直（配四棱锥示意图） |

## 构建

```powershell
# 编译全部文档（自动跳过 template.typ）
./build.ps1

# 只编译指定文档（可省略 .typ 扩展名）
./build.ps1 -Only 圆锥曲线04点差法与垂径定理

# 输出 PDF 到指定目录
./build.ps1 -OutputDir dist
```

macOS / Linux 使用等价的 Bash 脚本：`./build`（参数示例：`./build 蒙日圆及其反向应用`、`./build -o dist`）。

> 所有文档共用根目录下的 `template.typ`，用法为
> `#import "template.typ": *` 后用 `#template(...)[正文]` 包裹正文，
> 可选 `#set document(title: [...])` 在页眉右侧显示标题。
> 模板内置 `#definition`、`#important-block`、`#example`、`#theorem`、
> `#proof` 环境块，并提供页眉标题、页码/页脚（文档中通常传
> `page-footer: "第 1 页"`）、首行缩进、公式编号、字号等排版选项
> （详见模板文件头部注释）。
>
> 新增文档时仿照现有文档写即可，例如：
>
> ```typst
> #import "template.typ": *
> #set document(title: [题目或主题])
> #template(page-footer: "第 1 页")[ ……正文…… ]
> ```

编译产物为与源文件同名的 `.pdf`（已加入 `.gitignore`，不纳入版本管理）。

## 字体依赖：思源宋体（Source Han Serif）

`template.typ` 里把正文字体写成了

```typst
#let text-fonts = ("New Computer Modern Math", "Source Han Serif")
#let cjk-font = "Source Han Serif"
```

所以中文字形依赖系统里装有 **Source Han Serif**（思源宋体，Google 版的等价字体是
`Noto Serif CJK SC`）。**缺失时 Typst 不会报错，而是静默回退到系统其他中文字体**，
版面会悄悄变样——换机器编译前最好先查一下。

### 检查是否已装

```bash
typst fonts | grep -i "source han serif"       # Linux / macOS
typst fonts | Select-String "Source Han Serif"  # Windows PowerShell
```

只要列出 `Source Han Serif`（或 `Noto Serif CJK SC`）即可。本仓库已在 macOS 上实测：

- 本机装的是**可变字体**（`SourceHanSerifVF-*.ttf`），weight 250–900 全档可用；
- 正文默认 = weight 400（Regular），`#strong[...]` = 700（Bold），正常；
- 用到的非 ASCII 字符（简体、繁体、中文标点、希腊字母、数学符号）**全部有字形**；
- `Source Han Serif CN / TC / TW` 这几个**族名**查不到，但字形已被 `SC`（简体）与
  `HC / HK`（繁体、港区）覆盖，不影响使用；`Source Han Serif Light / Medium / Bold`
  这类"按字重命名的族"也查不到，属正常——字重请用 `weight:` 选择，不要换族名。

### 下载安装

| 来源 | 地址 |
|---|---|
| Adobe 官方（思源宋体，含各语言子集与可变字体） | <https://github.com/adobe-fonts/source-han-serif/releases> |
| Google Noto（等价字体 `Noto Serif CJK SC`） | <https://github.com/notofonts/noto-cjk/releases> |

装完后：

- **Windows**：右键 `.ttf` / `.otf` → "为所有用户安装"（或直接选字体文件安装）；
- **macOS**：双击字体文件 → "安装字体"（或放到 `~/Library/Fonts/`）；
- **Linux**：把字体文件复制到 `~/.local/share/fonts/`（或 `/usr/share/fonts/`），
  然后执行 `fc-cache -fv`。

### 字符覆盖自检（可选）

想把仓库里用到的所有非 ASCII 字符丢给 Typst 渲染一遍、看有没有缺字，可以跑：

```bash
python3 - <<'PY'
import glob, io
chars = set()
for p in ["template.typ"] + glob.glob("*.typ"):
    chars |= {c for c in io.open(p, encoding="utf-8").read() if ord(c) > 0x2000}
io.open("/tmp/cover.typ", "w", encoding="utf-8").write(
    '#set page(width: 18cm, height: auto, margin: 1cm)\n'
    '#set text(font: ("New Computer Modern Math", "Source Han Serif"), lang: "zh", size: 16pt)\n'
    + "".join(sorted(chars)))
print("待检测字符数:", len(chars))
PY
typst compile /tmp/cover.typ /tmp/cover.pdf    # 输出里出现 warning/missing 才说明缺字
```

### 数学模式里的中文

数学模式**不继承**正文字体列表，直接写在公式里的中文会落到系统默认黑体。模板导出了
`#cjk[...]` 辅助函数用于这种情况：

```typst
$x_(#cjk[极值点])$       // 而不是 $x_("极值点")$
```

### 换字体

若目标机器上确实没有思源宋体，也可以改用其他中文字体（把 `template.typ` 顶部的
`text-fonts` / `cjk-font` 换掉即可），例如 `"Noto Serif CJK SC"`、`"Source Han Sans SC"`、
`"Sarasa Gothic SC"`。注意只改字体**列表的最后一项**，不要用
`#set text(font: ...)` 做单字体覆盖，否则会顶掉数学字体、中文失去 CJK 回退。

## 依赖

- [Typst](https://typst.app/)
- 思源宋体（Source Han Serif）：模板的中文字体，未安装时 Typst 会**静默回退**到系统字体，
  版面会变样；检查与安装方法见上一节

