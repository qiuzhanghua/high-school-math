#import "template.typ": *

#set document(title: [导数数形结合])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  = 函数最值问题的几何本质（斜率视角）

  == 题目

  *6. 函数* $f(x) = frac(ln(frac(2, e)x), x-1) quad (0 < x < 1)$ *的最小值为*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 解题思路：化归为原生态对数曲线的斜率

  === 第一步：代数变形，构造斜率公式

  原函数为：
  $ f(x) = frac(ln(frac(2, e)x), x-1) $

  利用对数的运算法则 $ln(a b) = ln a + ln b$ 和 $ln(e) = 1$ 展开分子：
  $ f(x) = frac(ln x + ln 2 - 1, x - 1) $

  为了凑出两点连线的斜率公式 $k = (y_2 - y_1)/(x_2 - x_1)$，我们将分子重新组合：
  $ f(x) = frac(ln x - (1 - ln 2), x - 1) $

  === 第二步：赋予几何意义

  观察上式，它表示的是以下两点的连线斜率：

  - *动点* $P$：$(x, ln x)$。因为 $0 < x < 1$，动点 $P$ 在*最基础的对数曲线* $y = ln x$ 上运动。
  - *定点* $A$：$(1, 1 - ln 2)$。

  所以，*$f(x)$ 的几何意义就是：对数曲线 $y = ln x$ 上的动点 $P$ 与定点 $A(1, 1 - ln 2)$ 连线的斜率*。

  === 第三步：几何直观寻找最小斜率

  1. *观察位置关系*：
    把 $x = 1$ 代入曲线 $y = ln x$，得到 $y = ln 1 = 0$，即曲线过点 $(1, 0)$。
    因为 $ln 2 approx 0.693 < 1$，所以定点 $A$ 的纵坐标 $1 - ln 2 approx 0.307 > 0$。
    因此，定点 $A(1, 1 - ln 2)$ 在点 $(1, 0)$ 的*正上方*。
    题目限定 $0 < x < 1$，说明动点 $P$ 在定点 $A$ 的*左侧*。

  2. *寻找斜率极值*：
    想象一条直线绕着定点 $A(1, 1 - ln 2)$ 转动，并且始终与曲线 $y = ln x$ 在 $0 < x < 1$ 的部分相交。
    因为对数函数 $y = ln x$ 在定义域内是上凸函数，当直线从“割线”变成“切线”时，斜率会达到极小值。
    也就是说，当直线与曲线*相切*时，切线的斜率就是 $f(x)$ 的最小值。

  === 第四步：利用导数求切线斜率

  对曲线 $y = ln x$ 求导：
  $ y' = 1/x $

  设切点为 $(x_0, ln x_0)$。切线斜率为 $k = 1/x_0$。

  同时，这条切线经过定点 $A(1, 1 - ln 2)$，根据斜率公式：
  $ k = frac(ln x_0 - (1 - ln 2), x_0 - 1) = frac(ln x_0 + ln 2 - 1, x_0 - 1) $

  将 $k = 1/x_0$ 代入：
  $ frac(1, x_0) = frac(ln x_0 + ln 2 - 1, x_0 - 1) $

  交叉相乘并化简：
  $
    x_0 - 1 = x_0(ln x_0 + ln 2 - 1) \
    x_0 - 1 = x_0 ln(2x_0) - x_0 \
    2x_0 - 1 = x_0 ln(2x_0)
  $

  观察可知 $x_0 = 1/2$ 是该方程的一个解（左边 $= 0$，右边 $= 1/2 ln(1) = 0$）。

  === 第五步：计算最小值

  既然切点横坐标 $x_0 = 1/2$，那么最小斜率（也就是函数最小值）为：
  $ k_(min) = frac(1, x_0) = frac(1, 1/2) = 2 $

  *结论：函数 $f(x)$ 的最小值为 $bold(2)$。*

  #line(length: 100%, stroke: 0.5pt + gray)

  == 揭示出题人的本源

  其实，出题人就是先构造了一个简单的几何模型，然后用代数变形把它包装成了一道复杂的函数题。

  - *原始几何模型*：在 $y = ln x$ 上取一点 $(1/2, -ln 2)$，再取一个定点 $(1, 1 - ln 2)$，计算它们的斜率：
    $ frac(-ln 2 - (1 - ln 2), 1/2 - 1) = frac(-1, -1/2) = 2 $

  - *包装过程*：出题人通过代数变形（乘 $e$ 取对数），把上述简单的斜率模型写成了：
    $ f(x) = frac(ln(frac(2, e)x), x-1) $

  - *识别本质*：只要把 $f(x)$ 还原为 $frac(ln x - (1 - ln 2), x - 1)$，就能立刻看穿它的几何本意。

  #align(center)[
    #text(fill: gray, size: 10pt)[
      注：此解法将复杂的代数式视为斜率，直接利用原生态的对数函数 $y = ln x$ 的切线性质，巧妙避开了繁琐的求导与单调性讨论，直击问题的几何本质。
    ]
  ]
]
