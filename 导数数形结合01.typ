#import "template.typ": *

#set document(title: [导数数形结合])

#template(heading2-align: left, page-footer: "第 1 页")[
#set math.equation(numbering: "(1)")

= 函数最值问题解析


*函数* $f(x) = frac(ln(frac(2, e)x), x-1) quad (0 < x < 1)$ *的最小值为*

#line(length: 100%, stroke: 0.5pt + gray)

== 解法一：代数求导法

=== 第一步：化简函数解析式

原函数可化为：
$ f(x) = frac(ln x + ln 2 - 1, x - 1) quad (0 < x < 1) $

=== 第二步：求导数

对 $f(x)$ 求导，使用商的求导法则 $lr((u/v))' = (u'v - u v')/v^2$：
$ f'(x) = frac(frac(1, x) dot (x - 1) - (ln x + ln 2 - 1) dot 1, (x - 1)^2) $

展开并化简分子：
$ f'(x) = frac(1 - frac(1, x) - ln x - ln 2 + 1, (x - 1)^2) = frac(2 - frac(1, x) - ln(2x), (x - 1)^2) $

提取负号，为了便于判断符号，将分子化简为：
$ f'(x) = frac(-(ln(2x) + frac(1, x) - 2), (x - 1)^2) $

=== 第三步：判断导数的符号（寻找极值点）

分母 $(x-1)^2$ 在 $x in (0, 1)$ 上恒大于 $0$。因此，$f'(x)$ 的符号完全由分子中的括号部分决定。设：
$ g(x) = ln(2x) + frac(1, x) - 2 quad (0 < x < 1) $

对 $g(x)$ 求导：
$ g'(x) = frac(1, x) - frac(1, x^2) = frac(x - 1, x^2) $

因为 $0 < x < 1$，所以 $x - 1 < 0$，进而 $g'(x) < 0$。
这意味着 $g(x)$ 在 $(0, 1)$ 上*单调递减*。

接下来观察 $g(x)$ 在特殊点的值：
当 $x = 1/2$ 时：
$ g(1/2) = ln(2 times 1/2) + frac(1, 1/2) - 2 = ln(1) + 2 - 2 = 0 $

因为 $g(x)$ 单调递减，且 $g(1/2) = 0$，所以：
- 当 $0 < x < 1/2$ 时，$g(x) > 0$
- 当 $1/2 < x < 1$ 时，$g(x) < 0$

回到 $f'(x)$ 的表达式：
$ f'(x) = frac(-g(x), (x - 1)^2) $

因为分母恒正，所以：
- 当 $0 < x < 1/2$ 时，$f'(x) < 0$，函数 $f(x)$ 单调递减；
- 当 $1/2 < x < 1$ 时，$f'(x) > 0$，函数 $f(x)$ 单调递增。

=== 第四步：求最小值

由第三步可知，函数 $f(x)$ 在 $x = 1/2$ 处取得极小值，也是整个定义域 $(0, 1)$ 上的最小值。

将 $x = 1/2$ 代入原函数：
$ f(1/2) = frac(ln(frac(2, e) times 1/2), 1/2 - 1) = frac(ln(1/e), -1/2) $

因为 $ln(1/e) = -1$，所以：
$ f(1/2) = frac(-1, -1/2) = 2 $

*结论：函数 $f(x)$ 的最小值为 $bold(2)$。*

#line(length: 100%, stroke: 0.5pt + gray)

== 解法二：几何斜率法（数形结合）

原函数可以化简为：
$ f(x) = frac(ln x + ln 2 - 1, x - 1) $

我们把它变形，写成斜率的形式：
$ f(x) = frac((ln x + ln 2) - 1, x - 1) $

这其实就是*两点连线的斜率*公式 $k = (y_2 - y_1)/(x_2 - x_1)$。

- *动点* $P$：$(x, ln x + ln 2)$。因为 $0 < x < 1$，所以动点 $P$ 在曲线 $y = ln x + ln 2$ 上运动。
- *定点* $A$：$(1, 1)$。

所以，$f(x)$ 的几何意义就是：*曲线 $y = ln x + ln 2$ 上的动点 $P$ 与定点 $A(1, 1)$ 连线的斜率*。

=== 几何直观求解

1. *观察定点与曲线的位置关系*：
   把 $x = 1$ 代入曲线方程，得到 $y = ln 1 + ln 2 = ln 2$。
   因为 $ln 2 approx 0.693 < 1$，所以曲线在 $x=1$ 处的点 $(1, ln 2)$ 在定点 $A(1, 1)$ 的*正下方*。且题目限定 $0 < x < 1$，说明动点 $P$ 在定点 $A$ 的*左侧*。

2. *寻找斜率的最小值*：
   想象一条直线绕着定点 $A(1, 1)$ 转动，并且始终与曲线 $y = ln x + ln 2$ 在 $0 < x < 1$ 的部分相交。
   显然，当直线从“割线”变成“切线”时，斜率会达到一个极值。因为动点在定点左侧，且曲线是凸函数（上凸），所以当直线与曲线*相切*时，切线的斜率就是最小的斜率。

3. *利用导数求切线斜率*：
   对曲线 $y = ln x + ln 2$ 求导：
   $ y' = 1/x $
   设切点为 $(x_0, ln x_0 + ln 2)$。切线斜率为 $k = 1/x_0$。
   同时，这条切线经过定点 $A(1, 1)$，根据斜率公式：
   $ k = frac((ln x_0 + ln 2) - 1, x_0 - 1) $
   将 $k = 1/x_0$ 代入：
   $ frac(1, x_0) = frac(ln x_0 + ln 2 - 1, x_0 - 1) $
   交叉相乘并化简：
   $ x_0 - 1 = x_0(ln x_0 + ln 2 - 1) \
     x_0 - 1 = x_0 ln(2x_0) - x_0 \
     2x_0 - 1 = x_0 ln(2x_0) $

   观察可知 $x_0 = 1/2$ 是该方程的一个解（左边 $= 0$，右边 $= 1/2 ln(1) = 0$）。

4. *计算最小斜率*：
   既然切点横坐标 $x_0 = 1/2$，那么最小斜率（也就是函数最小值）为：
   $ k_(min) = frac(1, x_0) = frac(1, 1/2) = 2 $

*结论：函数 $f(x)$ 的最小值为 $bold(2)$。*
]