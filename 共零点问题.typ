#import "template.typ": *

#set document(title: [共零点问题])

#template(heading2-align: left, page-footer: "第 1 页")[

  *例 1* 设 $a > 0$，$b > e$，$f(x) = (x - a - 1)e^x - b(x^3/3 - a x^2/2)$（$e$ 为自然对数的底数）。若 $x = a$ 不是函数 $f(x)$ 的极值点，则 $b/a$ 的最小值为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 6pt,
    [A. $e$], [B. $e^2/4$], [C. $e^3/9$], [D. $e^(sqrt(2))/2$],
  )

  *答案*：*#text(fill: rgb("#c7362e"))[B]*。

  *解答：*

  *① 求导并因式分解。*

  $ f'(x) = e^x + (x - a - 1)e^x - b(x^2 - a x) = (x - a)e^x - b x (x - a) = (x - a)(e^x - b x). $

  *② 把"$x = a$ 不是极值点"翻译成"两个因式在 $x = a$ 处共零点"。* 由上式 $f'(a) = 0$ 恒成立，即 $x = a$ 一定是驻点。记 $g(x) = e^x - b x$：

  - 若 $g(a) != 0$：$f'(x) = (x - a)g(x)$ 在 $x = a$ 的两侧变号，$x = a$ 是极值点；
  - 若 $g(a) = 0$：两个因式都含有零点 $x = a$，且 $g'(a) != 0$ 时 $g(x) = (x - a)h(x)$（$h(a) = g'(a) != 0$），于是

    $ f'(x) = (x - a)^2 h(x) $

    在 $x = a$ 两侧同号，$f$ 在 $x = a$ 两侧同增（或同减），$x = a$ *不是*极值点。

  所以条件是 $g(a) = 0$，即

  $ e^a - b a = 0 quad => quad b = e^a/a. $

  此时 $g'(a) = e^a - b = e^a(1 - 1/a)$，需 $a != 1$；而 $a = 1$ 时 $b = e$，与 $b > e$ 矛盾，故这种退化情形不会出现。（另由 $a - 1 > ln a$（$a > 0$，$a != 1$）得 $b = e^a/a > e$，正与题设 $b > e$ 吻合。）

  *③ 求 $b/a$ 的最小值。* 由 $b = e^a/a$ 得

  $ b/a = e^a/a^2. $

  设 $phi(a) = e^a/a^2$（$a > 0$），则

  $ phi'(a) = (e^a a^2 - 2a e^a)/a^4 = e^a (a - 2)/a^3, $

  故 $phi$ 在 $(0, 2)$ 上递减、在 $(2, +oo)$ 上递增，在 $a = 2$ 处取最小值

  $ phi(2) = e^2/4. $

  所以 $b/a$ 的最小值为 $e^2/4$，选 *#text(fill: rgb("#c7362e"))[B]*。

  *要点*：当导数能分解成 $(x - x_0) g(x)$ 时，"$x_0$ 不是极值点"与"$g(x_0) = 0$"等价——两个因式在 $x_0$ 处*共零点*，此时 $f'$ 在 $x_0$ 附近是"二次零点"，两侧不变号；反之 $g(x_0) != 0$ 时 $x_0$ 必为极值点。判断完条件后，把 $b = e^a/a$ 代回目标式，化成一元函数求最值即可。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 2* 对任意的 $x in (0, +oo)$，不等式 $(e^x - e^a - x + a)(x^2 - e^(1-x) + 2 ln a + a - 1) >= 0$ 恒成立，则实数 $a$ 的取值集合是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 6pt,
    [A. ${1}$], [B. $(0, sqrt(2)]$], [C. $[1/2, +oo)$], [D. ${sqrt(2)}$],
  )

  *答案*：*#text(fill: rgb("#c7362e"))[A]*。

  *解答：*

  *① 第一个因式零点唯一，且在 $x = a$ 处变号。* 记

  $ P(x) = e^x - e^a - x + a = (e^x - x) - (e^a - a), $

  则 $P'(x) = e^x - 1 > 0$（$x > 0$），故 $P$ 在 $(0, +oo)$ 上严格递增；又 $P(a) = 0$，所以

  $ P(x) < 0 quad (0 < x < a), quad quad P(x) > 0 quad (x > a). $

  *② 乘积恒非负，逼迫第二个因式在同一点也取零。* 记 $Q(x) = x^2 - e^(1-x) + 2 ln a + a - 1$。

  - 若 $Q(a) > 0$：由连续性，在 $x = a$ 左侧附近仍有 $Q > 0$，而那里 $P < 0$，乘积为负，矛盾；
  - 若 $Q(a) < 0$：同理，在 $x = a$ 右侧附近乘积为负，也矛盾。

  故必有 $Q(a) = 0$，即

  $ a^2 - e^(1-a) + 2 ln a + a - 1 = 0. $

  *③ 解这个方程。* 设 $w(a) = a^2 - e^(1-a) + 2 ln a + a - 1$（$a > 0$），则

  $ w'(a) = 2a + e^(1-a) + 2/a + 1 > 0, $

  故 $w$ 在 $(0, +oo)$ 上严格递增，该方程至多一个根；又

  $ w(1) = 1 - 1 + 0 + 1 - 1 = 0, $

  所以 $a = 1$ 是唯一解。

  *④ 验证 $a = 1$ 确实满足。* 此时 $P(x) = e^x - x - (e - 1)$，$Q(x) = x^2 - e^(1-x)$，而

  $ Q'(x) = 2x + e^(1-x) > 0, quad Q(1) = 0, $

  故 $Q$ 与 $P$ 同号：$0 < x < 1$ 时两者都小于 $0$，$x > 1$ 时两者都大于 $0$，乘积恒 $>= 0$。

  所以 $a$ 的取值集合为 ${1}$，选 *#text(fill: rgb("#c7362e"))[A]*。

  *要点*：两个因式相乘恒非负时，先看"结构简单"的那个因式——本题 $P(x) = (e^x - x) - (e^a - a)$ 的零点唯一且在两侧由负变正，这就*逼迫*另一个因式在同一点也取零（*共零点*）；把共零点写成方程，再用单调性说明根唯一，最后验证充分性即可。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 3*（24 新高考 2）设函数 $f(x) = (x + a) ln(x + b)$。若 $f(x) >= 0$，则 $a^2 + b^2$ 的最小值为（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 6pt,
    [A. $1/8$], [B. $1/4$], [C. $1/2$], [D. $1$],
  )

  *答案*：*#text(fill: rgb("#c7362e"))[C]*。

  *解答：*

  *① 定义域与两个因式的零点。* 由 $x + b > 0$ 得定义域 $(-b, +oo)$。记 $P(x) = x + a$、$Q(x) = ln(x + b)$，则

  - $Q(x) = ln(x + b)$ 在 $x = 1 - b$ 处为零：$-b < x < 1 - b$ 时 $Q < 0$，$x > 1 - b$ 时 $Q > 0$；
  - $P(x) = x + a$ 在 $x = -a$ 处为零，左侧为负、右侧为正。

  *② 由 $f >= 0$ 反推两个因式必须"同时变号"。*

  - 当 $x > 1 - b$ 时 $Q > 0$，要 $f >= 0$ 就必须 $P(x) >= 0$ 对一切 $x > 1 - b$ 成立，令 $x -> (1 - b)^+$ 得 $P(1 - b) = 1 - b + a >= 0$；
  - 当 $-b < x < 1 - b$ 时 $Q < 0$，要 $f >= 0$ 就必须 $P(x) <= 0$ 对一切这样的 $x$ 成立，令 $x -> (1 - b)^-$ 得 $P(1 - b) = 1 - b + a <= 0$。

  两者合起来给出

  $ 1 - b + a = 0 quad => quad a = b - 1, $

  即 $Q$ 的零点 $x = 1 - b$ 必须同时是 $P$ 的零点——两个因式*共零点*。

  *③ 验证并求最小值。* 当 $a = b - 1$ 时，令 $t = x + b > 0$，则

  $ f(x) = (t - 1) ln t, $

  而 $t - 1$ 与 $ln t$ 同号（同时为零、同正或同负），故 $f(x) >= 0$ 恒成立——说明 $a = b - 1$ 也是充分条件。

  于是

  $ a^2 + b^2 = (b - 1)^2 + b^2 = 2b^2 - 2b + 1 = 2(b - 1/2)^2 + 1/2 >= 1/2, $

  当 $b = 1/2$（此时 $a = -1/2$）时取到等号。所以最小值为 $1/2$，选 *#text(fill: rgb("#c7362e"))[C]*。

  *要点*：因式的乘积恒非负（恒非正）时，把两个因式的零点"对在一起"——本题 $Q = ln(x + b)$ 在 $x = 1 - b$ 处变号，就要求 $P = x + a$ 在同一点也变号，即 $1 - b = -a$（*共零点*）；写出 $a$ 与 $b$ 的关系后再求目标式的最值。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 4* 已知当 $x in RR$ 时，不等式 $(a e^x - 2)(a e^x + x) >= 0$ 恒成立，则实数 $a$ 的取值范围为 #underline[#h(3em)]。

  *答案*：$a in (-oo, -1/e] union {2e^2}$。

  *解答：*

  *① 两个因式的情形要分开讨论。* 记 $F(x) = a e^x - 2$、$G(x) = a e^x + x$。

  - *$a = 0$：* $F = -2 < 0$，而 $G = x$ 会取正值，乘积不能恒非负，舍去。
  - *$a < 0$：* $F(x) = a e^x - 2 < 0$ 恒成立，故只需 $G(x) <= 0$ 恒成立。由 $G'(x) = a e^x + 1 = 0$ 得 $G$ 的最大值点 $x^* = ln(-1/a)$，且
    $ G_max = G(x^*) = a dot (-1/a) + ln(-1/a) = -1 + ln(-1/a). $
    要求 $G_max <= 0$，即 $ln(-1/a) <= 1$，亦即 $-1/a <= e$，解得 $a <= -1/e$。
  - *$a > 0$：* $F'(x) = a e^x > 0$、$G'(x) = a e^x + 1 > 0$，故 $F$、$G$ 在 $RR$ 上都*严格递增*且各恰有一个零点。

  *② $a > 0$ 时，两个因式必须共零点。* 设 $F$、$G$ 的零点分别为 $x_1$、$x_2$。若 $x_1 != x_2$（不妨设 $x_1 < x_2$），则在 $(x_1, x_2)$ 上 $F > 0 > G$，乘积为负，与恒非负矛盾；故两零点必须重合，即 $x_1 = x_2$。

  设公共零点为 $x_0$：由 $F(x_0) = 0$ 得 $a e^(x_0) = 2$；由 $G(x_0) = 0$ 得 $a e^(x_0) + x_0 = 0$。两式相减得

  $ x_0 = -2, quad "代回得" a e^(-2) = 2, quad "即" a = 2e^2. $

  *③ 验证。*

  - 当 $a = 2e^2$ 时：$F = 2e^(x+2) - 2$、$G = 2e^(x+2) + x$ 都在 $x = -2$ 处为零且严格递增，故 $x < -2$ 时二者都为负、$x > -2$ 时二者都为正，乘积恒 $>= 0$；
  - 当 $a <= -1/e$ 时：$F < 0$ 恒成立，而 $G_max = -1 + ln(-1/a) <= 0$（由 $a <= -1/e$ 得 $-1/a <= e$），故 $G <= 0$ 恒成立，乘积也恒 $>= 0$。

  所以 $a$ 的取值范围为 $a <= -1/e$ 或 $a = 2e^2$，即

  $ a in (-oo, -1/e] union {2e^2}. $

  *要点*：乘积恒非负的题要分两类看——① 两个因式*都单调*时，两个零点必须重合（*共零点*），设公共零点 $x_0$，把 $F(x_0) = 0$ 与 $G(x_0) = 0$ 相减即可解出；② 某个因式*恒负*（如 $a < 0$ 时的 $F$）时，另一个因式只需恒非正，转化为"求它的最大值 $<= 0$"。第二类最容易被漏掉。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 5*（浙江卷）设 $a in RR$，若 $x > 0$ 时 $[(a - 1)x - 1](x^2 - a x - 1) >= 0$，则 $a =$ #underline[#h(3em)]。

  *答案*：$a = 3/2$。

  *解答：*

  *① 两个因式的零点情况。* 记

  $ P(x) = (a - 1)x - 1, quad Q(x) = x^2 - a x - 1. $

  - $Q(0) = -1 < 0$，且 $x -> +oo$ 时 $Q -> +oo$；又两根之积为 $-1 < 0$（两根异号），故 $Q$ 在 $(0, +oo)$ 上*恰有一个零点* $r$，且 $0 < x < r$ 时 $Q < 0$、$x > r$ 时 $Q > 0$；
  - $P(0) = -1 < 0$。若 $a <= 1$，则 $P$ 在 $(0, +oo)$ 上恒为负（$a < 1$ 时 $P$ 递减，$a = 1$ 时 $P = -1$），而 $Q$ 会取到正值，乘积不能恒非负，故必须 $a > 1$；此时 $P$ 递增，有唯一零点

    $ x_P = 1/(a - 1), $

    且 $x < x_P$ 时 $P < 0$、$x > x_P$ 时 $P > 0$。

  *② 两个零点必须重合（共零点）。* 若 $x_P != r$（不妨设 $x_P < r$），则在 $(x_P, r)$ 上 $P > 0 > Q$，乘积为负，与恒非负矛盾。故

  $ x_P = r, quad "即" 1/(a - 1) = r, quad 且 quad r^2 - a r - 1 = 0. $

  由第一式得 $a = 1 + 1/r$，代入第二式：

  $ r^2 - (1 + 1/r) r - 1 = r^2 - r - 2 = 0 quad => quad (r - 2)(r + 1) = 0, $

  由 $r > 0$ 得 $r = 2$，从而

  $ a = 1 + 1/2 = 3/2. $

  *③ 验证。* 当 $a = 3/2$ 时，$P = x/2 - 1$、$Q = x^2 - 3x/2 - 1$，二者都在 $x = 2$ 处为零，且 $x < 2$ 时都小于 $0$、$x > 2$ 时都大于 $0$，故乘积恒 $>= 0$。

  所以 $a = 3/2$。

  *要点*：一个一次因式与一个二次因式（各自在 $(0, +oo)$ 上只有一个零点）的乘积恒非负，仍然是"零点必须重合"——先排除"$P$ 恒负"的情形（$a <= 1$），再用 $P(r) = Q(r) = 0$ 联立解出 $a$，最后验证。

  #line(length: 100%, stroke: 0.5pt + gray)

  *例 6*（浙江卷）设 $f(x) = (ln(a x) - 1)(x^2 + a x - 4)$。若 $x > 0$ 时 $f(x) >= 0$，则 $a =$ #underline[#h(3em)]。

  *答案*：$a = e/sqrt(4 - e)$。

  *解答：*

  *① 先定 $a > 0$，再看两个因式的零点。* 要使 $ln(a x)$ 对一切 $x > 0$ 都有意义，必须 $a > 0$。此时记

  $ P(x) = ln(a x) - 1, quad Q(x) = x^2 + a x - 4. $

  - $P$ 在 $(0, +oo)$ 上递增，零点为 $x = e/a$：$0 < x < e/a$ 时 $P < 0$，$x > e/a$ 时 $P > 0$；
  - $Q'(x) = 2x + a > 0$，$Q$ 在 $(0, +oo)$ 上也递增，且 $Q(0) = -4 < 0$、$x -> +oo$ 时 $Q -> +oo$，故 $Q$ 在 $(0, +oo)$ 上*恰有一个零点* $r$：$x < r$ 时 $Q < 0$，$x > r$ 时 $Q > 0$。

  *② 两个零点必须重合（共零点）。* 若 $e/a != r$（不妨设 $e/a < r$），则在 $(e/a, r)$ 上 $P > 0 > Q$，乘积为负，与 $f(x) >= 0$ 矛盾。故

  $ e/a = r, quad 且 quad r^2 + a r - 4 = 0. $

  *③ 解出 $a$。* 由第一式得 $a r = e$，代入第二式：

  $ r^2 + e - 4 = 0 quad => quad r = sqrt(4 - e) quad (r > 0), $

  于是

  $ a = e/r = e/sqrt(4 - e). $

  *④ 验证。* 取 $r = sqrt(4 - e)$、$a = e/r$，则 $P$、$Q$ 的零点都在 $x = r$ 处：$x < r$ 时两个因式都小于 $0$、$x > r$ 时都大于 $0$，乘积恒 $>= 0$ ✓。

  所以 $a = e/sqrt(4 - e)$。

  *要点*：先由"$ln(a x)$ 要对一切 $x > 0$ 有定义"定出 $a > 0$；再按"两个单调因式的零点必须重合"列式——把 $P$ 的零点 $e/a$ 与 $Q$ 的零点 $r$ 相等、并注意 $a r = e$，代入二次方程即得 $r^2 = 4 - e$，这一步是本题的关键。

]
