#import "template.typ": *

#set document(title: [导数隐零点])

#template(heading2-align: left, page-footer: "第 1 页")[

  == 一、什么是"隐零点"

  有一类问题：求导之后方程 $f'(x) = 0$ 是超越方程（如 $x e^x = 1$、$e^x = 1/x$、$ln x = 1/x$ 等），零点*无法（也不必）用初等式子表示*。这时可以这样处理：

  - 设这个零点为 $x_0$（称为"隐零点"），先用单调性说明它*存在且唯一*，并给出它所在的大致区间；
  - 由 $f'(x_0) = 0$ 写出 $x_0$ 满足的*关系式*（例如 $x_0 e^(x_0) = 1$，从而 $e^(x_0) = 1/x_0$、$ln x_0 = -x_0$）；
  - 把关系式代入 $f(x_0)$，把超越式化成初等式（如 $x_0 + 1/x_0$），再用基本不等式或基准结论估计。

  一句话：*零点求不出来，但零点满足的关系式很有用*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 1】用隐零点证明不等式

  *【例 1】* 已知 $f(x) = e^x + ln(e/x)$，求证：$f(x) > 3$。

  === 解析

  *① 化简函数，注意定义域。* 由 $ln(e/x) = ln e - ln x = 1 - ln x$（要求 $x > 0$）得

  $ f(x) = e^x + 1 - ln x quad (x > 0) $

  所以要证的就是

  $ e^x - ln x > 2 quad (x > 0) $

  *② 求导，"隐零点"出现。*

  $ f'(x) = e^x - 1/x quad (x > 0) $

  令 $f'(x) = 0$，即 $e^x = 1/x$，等价于

  $ x e^x = 1 $

  记它的正根为 $x_0$。因为 $h(x) = x e^x$ 满足 $h'(x) = (x + 1) e^x > 0$，即 $h$ 在 $(0, +oo)$ 上严格递增，且

  $ h(0^+) = 0 < 1 < h(1) = e $

  所以 $x_0$ 存在且唯一，并且 $0 < x_0 < 1$。（进一步，$h(1/2) = 1/2 sqrt(e) ≈ 0.824 < 1$、$h(0.6) ≈ 1.093 > 1$，故 $0.5 < x_0 < 0.6$；数值上 $x_0 ≈ 0.5671$，它就是方程 $x = e^(-x)$ 的根，无法用初等式子表示。）

  *③ 关键一步：用隐零点满足的关系式化简。* 由 $x_0 e^(x_0) = 1$ 可得两条非常有用的关系：

  $ e^(x_0) = 1/x_0, quad ln x_0 = -x_0 $

  （第二条是对 $x_0 = e^(-x_0)$ 两边取对数得到的。）于是

  $ f(x_0) = e^(x_0) + 1 - ln x_0 = 1/x_0 + 1 + x_0 $

  超越式 $e^(x_0)$、$ln x_0$ 都消失了，只剩初等的 $x_0 + 1/x_0$。

  *④ 求最小值并估计。* 由 $f'(x) = e^x - 1/x$ 的分子 $x e^x - 1$ 知：$0 < x < x_0$ 时 $x e^x < 1$，$f'(x) < 0$；$x > x_0$ 时 $x e^x > 1$，$f'(x) > 0$。所以 $f$ 在 $(0, x_0)$ 上单调递减、在 $(x_0, +oo)$ 上单调递增，最小值为 $f(x_0)$。

  由基本不等式（$x_0 > 0$）

  $ x_0 + 1/x_0 >= 2 sqrt(x_0 dot 1/x_0) = 2 $

  而等号要求 $x_0 = 1$，但前面已证 $0 < x_0 < 1$，所以等号取不到，从而

  $ f(x) >= f(x_0) = x_0 + 1/x_0 + 1 > 3 $

  证毕。

  （事实上得到了更强的结论：$f(x) >= 1 + x_0 + 1/x_0 ≈ 3.3304$，而 $3$ 只是它的一个较宽松的下界。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】切线条件定参数 + 隐零点求最小值

  *【例 2】* 已知函数 $f(x) = x e^x - a ln x - 1$ 的图象在 $x = 1$ 处的切线斜率为 $2e - 1$。若 $g(x) = f(x) - x$，求 $g(x)$ 的最小值。

  === 解析

  *① 先用切线斜率定出参数。* 定义域为 $x > 0$，求导：

  $ f'(x) = e^x + x e^x - a/x = (x + 1) e^x - a/x $

  由题意 $f'(1) = 2e - 1$，即

  $ 2e - a = 2e - 1 quad => quad a = 1 $

  于是

  $ g(x) = f(x) - x = x e^x - ln x - 1 - x quad (x > 0) $

  *② 求导，问题化为隐零点的符号。*

  $ g'(x) = (x + 1) e^x - 1 - 1/x = ((x + 1)(x e^x - 1))/x $

  因为 $x > 0$ 时 $(x + 1)/x > 0$，所以 $g'(x)$ 的符号完全由 $x e^x - 1$ 决定。而 $h(x) = x e^x$ 在 $(0, +oo)$ 上严格递增（$h'(x) = (x + 1) e^x > 0$），且

  $ h(0^+) = 0 < 1 < h(1) = e $

  所以方程

  $ x e^x = 1 $

  有唯一正根 $x_0$——它与例 1 中的隐零点是同一个（$0.5 < x_0 < 0.6$，$x_0 ≈ 0.5671$）。于是

  - $0 < x < x_0$ 时 $x e^x < 1$，$g'(x) < 0$，$g$ 单调递减；
  - $x > x_0$ 时 $x e^x > 1$，$g'(x) > 0$，$g$ 单调递增。

  所以 $g$ 的最小值在 $x = x_0$ 处取得。

  *③ 用隐零点的关系式求值。* 由 $x_0 e^(x_0) = 1$ 得

  $ e^(x_0) = 1/x_0, quad ln x_0 = -x_0 $

  于是

  $ g(x_0) = x_0 e^(x_0) - ln x_0 - 1 - x_0 = 1 - (-x_0) - 1 - x_0 = 0 $

  即 $g(x)$ 的最小值为

  $ 0 $

  它在 $x = x_0 ≈ 0.5671$ 处取到。顺便得到一个不等式：对一切 $x > 0$，

  $ x e^x >= x + ln x + 1 $

  等号仅在 $x = x_0$（即 $x e^x = 1$）处成立。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】用隐零点给极值"估值"

  *【例 3】* 若 $f(x) = x^2 - x - x ln x$ 存在唯一极大值点 $x_0$，证明：$1/e^2 < f(x_0) < 1/4$。

  === 解析

  *① 求导，说明极大值点就是隐零点 $x_0$。* 定义域为 $x > 0$，

  $ f'(x) = 2x - 1 - (ln x + 1) = 2x - ln x - 2, quad f''(x) = 2 - 1/x $

  在 $(0, 1/2)$ 上 $f''(x) < 0$，即 $f'$ 严格递减；又 $x -> 0^+$ 时 $f'(x) -> +oo$，而

  $ f'(1/2) = 1 - ln(1/2) - 2 = ln 2 - 1 < 0 $

  所以 $f'$ 在 $(0, 1/2)$ 上有唯一零点，记作 $x_0$，即 $0 < x_0 < 1/2$，且 $f$ 在 $x_0$ 处取极大值。又 $f'$ 在 $(1/2, +oo)$ 上严格递增，且 $f'(1) = 2 - 0 - 2 = 0$，故 $f'$ 的另一个零点是 $x = 1$（极小值点）——所以 $f$ 的*唯一极大值点*就是 $x_0$。

  $x_0$ 满足

  $ ln x_0 = 2x_0 - 2 $

  这是超越方程，$x_0$ 无法显式求出（数值上 $x_0 ≈ 0.2032$）。

  *② 用关系式把 $f(x_0)$ 化成初等式。*

  $ f(x_0) = x_0^2 - x_0 - x_0 ln x_0 = x_0^2 - x_0 - x_0 (2x_0 - 2) = x_0 - x_0^2 = x_0 (1 - x_0) $

  超越式消失了。

  *③ 上界。* 配方得

  $ x_0 (1 - x_0) = 1/4 - (x_0 - 1/2)^2 < 1/4 $

  （等号要求 $x_0 = 1/2$，但 $f'(1/2) = ln 2 - 1 != 0$，故取不到）。所以

  $ f(x_0) < 1/4 $

  *④ 下界（在 $x = 1/e$ 处比较，最省事）。* 先算一个特殊值：

  $ f(1/e) = 1/e^2 - 1/e - 1/e ln(1/e) = 1/e^2 - 1/e + 1/e = 1/e^2 $

  再注意

  $ f'(1/e) = 2/e - ln(1/e) - 2 = 2/e - 1 < 0 quad (e > 2) $

  由 ① 知 $f'$ 在 $(0, 1/2)$ 上严格递减且 $f'(x_0) = 0$，而 $1/e < 1/2$，所以

  $ 0 < x_0 < 1/e $

  再由 ① 知 $f$ 在 $(x_0, 1)$ 上单调递减，而 $1/e in (x_0, 1)$，于是

  $ f(x_0) > f(1/e) = 1/e^2 $

  证毕。

  *另解（换元，可把下界提得更高）*：令 $u = 1 - x_0 in (1/2, 1)$，由 $ln(1 - u) = -2u$ 得 $x_0 = e^(-2u)$，于是 $f(x_0) = x_0 (1 - x_0) = u e^(-2u)$；记 $g(u) = u e^(-2u)$，$g'(u) = e^(-2u)(1 - 2u) < 0$，故 $g$ 在 $(1/2, 1)$ 上递减，从而

  $ 1/e^2 = g(1) < f(x_0) < g(1/2) = 1/(2e) ≈ 0.1839 $

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】再练一次：隐零点 + 特殊点比较

  *【例 4】* 已知 $f(x) = e^x - ln(x + 2)$，求证：$f(x) > 1/6$。

  === 解析

  *① 求导，找隐零点。* 定义域为 $x > -2$，

  $ f'(x) = e^x - 1/(x + 2), quad f''(x) = e^x + 1/(x + 2)^2 > 0 $

  所以 $f'$ 在 $(-2, +oo)$ 上严格递增。又

  $ f'(-1/2) = e^(-1/2) - 2/3 = 1/sqrt(e) - 2/3 < 0 quad (4e > 9), quad f'(0) = 1 - 1/2 = 1/2 > 0 $

  所以 $f'$ 有唯一零点，记作 $x_0$，且 $x_0 in (-1/2, 0)$。于是 $f$ 在 $(-2, x_0)$ 上单调递减、在 $(x_0, +oo)$ 上单调递增，最小值为 $f(x_0)$。

  $x_0$ 满足

  $ e^(x_0) (x_0 + 2) = 1 quad 即 quad e^(x_0) = 1/(x_0 + 2), quad ln(x_0 + 2) = -x_0 $

  这是超越方程，$x_0$ 无法显式求出（数值上 $x_0 ≈ -0.4429$）。

  *② 用关系式化简。*

  $ f(x_0) = e^(x_0) - ln(x_0 + 2) = 1/(x_0 + 2) + x_0 $

  超越式消失。

  *③ 在特殊点 $x = -1/2$ 处比较。* 记

  $ phi(x) = x + 1/(x + 2) quad (x > -2) $

  则

  $ phi'(x) = 1 - 1/(x + 2)^2 > 0 quad (x > -1) $

  所以 $phi$ 在 $(-1, +oo)$ 上单调递增；由 ① 知 $-1/2 < x_0 < 0$，故

  $ f(x_0) = phi(x_0) > phi(-1/2) = -1/2 + 1/(3/2) = -1/2 + 2/3 = 1/6 $

  从而对一切 $x > -2$ 都有

  $ f(x) >= f(x_0) > 1/6 $

  证毕。

  （关键仍是"找干净值"：这里 $phi(-1/2) = 1/6$ 恰好等于题目要证的常数，而 $x_0$ 被夹在 $(-1/2, 0)$ 内，于是不等式一步到位。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】换元 + 隐零点参数化求参数范围

  *【例 5】* 已知 $f(x) = a e^(2x) - 2x e^x$。若对一切 $x in RR$ 都有 $f(x) + 1/a <= 0$，求实数 $a$ 的取值范围。

  === 解析

  *① 取特殊点 $x = 0$，先定 $a$ 的符号。* 题设蕴含 $a != 0$。在 $x = 0$ 处，

  $ f(0) + 1/a = a + 1/a <= 0 quad => quad a < 0 $

  （因为 $a > 0$ 时 $a + 1/a >= 2 > 0$，不可能成立。）

  *② 换元 $t = e^x$，化为一元不等式。* 令 $t = e^x in (0, +oo)$（$RR$ 与 $(0, +oo)$ 一一对应），则 $e^(2x) = t^2$，且由 $x = ln t$ 得 $x e^x = t ln t$。于是题设等价于

  $ g(t) = a t^2 - 2 t ln t + 1/a <= 0 quad (t > 0) $

  记 $b = -a > 0$（即 $a = -b$、$1/a = -1/b$），上式两边乘 $-1$，等价于

  $ h(t) = b t^2 + 2 t ln t + 1/b >= 0 quad (t > 0) $

  *③ 求 $h$ 的最小值，"隐零点"出现。*

  $ h'(t) = 2b t + 2 ln t + 2 = 2(b t + ln t + 1), quad h''(t) = 2b + 2/t > 0 $

  所以 $h'$ 严格递增；又 $t -> 0^+$ 时 $h'(t) -> -oo$，$t -> +oo$ 时 $h'(t) -> +oo$，故 $h'$ 有唯一零点 $t_0$，$h$ 在 $t_0$ 处取到最小值。$t_0$ 满足

  $ b t_0 + ln t_0 + 1 = 0 quad 即 quad ln t_0 = -(b t_0 + 1) $

  这是超越方程，$t_0$ *解不出来*；但可以反过来把 $b$ 用 $t_0$ 表示。

  *④ 引入 $s = b t_0 + 1$，把 $b$ 与最小值都用 $s$ 表示。* 由 $ln t_0 = -s$ 得 $t_0 = e^(-s)$，再由 $b t_0 = s - 1$ 得

  $ b = (s - 1)/t_0 = (s - 1) e^s quad (s > 1) $

  且 $b(s) = (s-1)e^s$ 在 $(1, +oo)$ 上严格递增（$b'(s) = s e^s > 0$）。再算最小值：

  $ h(t_0) = b t_0^2 + 2 t_0 ln t_0 + 1/b = b t_0^2 - 2 b t_0^2 - 2 t_0 + 1/b = -b t_0^2 - 2 t_0 + 1/b $

  两边乘 $b > 0$，并代入 $b t_0 = s - 1$：

  $ b h(t_0) = 1 - [(b t_0)^2 + 2 b t_0] = 1 - [(s - 1)^2 + 2 (s - 1)] = 2 - s^2 $

  所以

  $ h(t_0) = (2 - s^2)/b $

  *⑤ 由 $h(t_0) >= 0$ 解出 $a$。* 因为 $b > 0$，

  $ h(t) >= h(t_0) >= 0 quad <=> quad 2 - s^2 >= 0 quad <=> quad 1 < s <= sqrt(2) $

  又 $b(s) = (s-1)e^s$ 递增，所以 $s <= sqrt(2)$ 等价于 $b <= (sqrt(2) - 1) e^(sqrt(2))$，即

  $ a >= -(sqrt(2) - 1) e^(sqrt(2)) $

  结合 $a < 0$，得

  $ a in [ -(sqrt(2) - 1) e^(sqrt(2)), 0 ) $

  *验证边界：* 取 $a = -(sqrt(2)-1)e^(sqrt(2))$ 时 $s = sqrt(2)$、$h(t_0) = 0$，即 $h$ 的最小值恰为 $0$（原不等式"取等"），所以左端点可以取到；而 $a$ 更小（即 $b$ 更大）时 $h(t_0) < 0$，不等式不再恒成立。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - *识别隐零点*：求导后若 $f'(x) = 0$ 是超越方程（$x e^x = c$、$e^x = 1/x$、$ln x = 1/x$ 等），不要硬解，直接设零点为 $x_0$。
  - *先证"存在且唯一"*：用相应函数的单调性 + 零点存在定理（例 1 中 $h(x) = x e^x$ 严格递增，$h(0^+) = 0 < 1 < h(1) = e$；例 4 中 $f''(x) > 0$ 说明 $f'$ 递增，再由 $f'(-1/2) < 0 < f'(0)$ 夹出零点）。这一步还提供"取等是否可能"的信息——例 1 中 $x_0 < 1$ 使基本不等式的等号取不到，于是得到的是*严格*不等号。
  - *关系式是关键*：由 $f'(x_0) = 0$ 写出 $x_0$ 的等式并*变形*（例 1 由 $x_0 e^(x_0) = 1$ 得 $e^(x_0) = 1/x_0$、$ln x_0 = -x_0$；例 4 由 $e^(x_0)(x_0 + 2) = 1$ 得 $e^(x_0) = 1/(x_0 + 2)$、$ln(x_0 + 2) = -x_0$），用来消去目标式中的 $e^(x_0)$、$ln x_0$。
  - *最后估计*：化简后往往只剩 $x_0$ 的有理式，用基本不等式（$t + 1/t >= 2$，$t > 0$）或基准结论估计即可；必要时可用区间把 $x_0$ 夹得更精确（例 1 中 $0.5 < x_0 < 0.6$，例 4 中 $-1/2 < x_0 < 0$）。
  - *先用切线条件定参数（例 2）*："图象在 $x = 1$ 处的切线斜率为 $k$"即 $f'(1) = k$，据此解出参数（例 2 中 $a = 1$），再把问题化成熟悉的隐零点结构——例 2 的关键是 $g'(x)$ 的分子恰好是 $x e^x - 1$，与例 1 的隐零点完全相同。
  - *用隐零点给极值"估值"（例 3、例 4）*：先用 $f'(x_0) = 0$ 的关系式把 $f(x_0)$ 化成初等式（例 3 中由 $ln x_0 = 2x_0 - 2$ 得 $f(x_0) = x_0 (1 - x_0)$；例 4 中由 $e^(x_0) = 1/(x_0 + 2)$、$ln(x_0 + 2) = -x_0$ 得 $f(x_0) = x_0 + 1/(x_0 + 2)$），再用配方得一侧的界；另一侧的界往往只需*找一个特殊点比较*——例 3 中 $f(1/e) = 1/e^2$、例 4 中 $phi(-1/2) = 1/6$ 都是"干净值"，配合导数符号定出 $x_0$ 与它的位置关系，由单调性一步得证。若还想要更精确的界，可*换元后再用一次关系式*（例 3 的另解：$u = 1 - x_0$，可把下界提高到 $1/(2e)$）。
  - *含参指数的换元（例 5）*：出现 $e^(2x)$、$x e^x$ 这类结构时，令 $t = e^x$（即 $x = ln t$）可把式子化为 $t$ 与 $ln t$ 的组合（$e^(2x) = t^2$、$x e^x = t ln t$）；再用"*取特殊点定符号*（例 5 中 $x = 0$ 得 $a < 0$）+ *隐零点参数化*（把参数用 $s = b t_0 + 1$ 表示，条件化为 $s <= sqrt(2)$ 这样的简单不等式）"完成。
  - *常用基准结论*：$e^x >= x + 1$（等号仅在 $x = 0$）、$ln x <= x - 1$（等号仅在 $x = 1$）、$t + 1/t >= 2$（$t > 0$，等号仅在 $t = 1$）等。
]
