#import "template.typ": *

#set document(title: [不等式习题集锦])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  *第 1 题*（巴蜀中学高 2027 届 9 月适应性月考（二）第 11 题）

  已知 $a, b in RR$，且满足 $a^2 + b^2 = 1$，则下列说法正确的是（#h(0.6em)）

  #v(0.3em)
  #grid(
    columns: (1fr, 1fr),
    gutter: 16pt,
    [A. $a b$ 的最大值为 $1/2$], [B. $2a - b$ 的最小值为 $-sqrt(5)$],
    [C. $(a b + 2a)/(4b + 5)$ 的最大值为 $2/3$], [D. $(sqrt(3))/(2a) + (12)/(3b^2 + 2)$ 的最小值为 $9/2$],
  )
  #v(0.3em)

  *答案*：A、B、D。

  *解:*

  *(A) 用基本不等式。* 由 $a^2 + b^2 >= 2 a b$ 得

  $ a b <= (a^2 + b^2)/2 = 1/2, $

  且 $a = b = (sqrt(2))/2$ 时取到等号。故 A *正确*。

  *(B) 用三角换元。* 令 $a = cos theta$、$b = sin theta$，则

  $ 2a - b = 2 cos theta - sin theta = sqrt(5) sin(theta + phi) >= -sqrt(5), $

  其中 $phi$ 由 $tan phi = 2$ 确定（辅助角公式），等号在 $sin(theta + phi) = -1$ 时取到。故 B *正确*。

  *(C) 令平方为 $k$，再用对数求导求 $k$ 的最大值。* 取 $a = sqrt(1 - b^2)$（$a > 0$ 时分式为正），令

  $ k = ((a(b+2))/(4b+5))^2 = ((1 - b^2)(b+2)^2)/((4b+5)^2). $ <eq:e1>

  求 $k$ 关于 $b$ 的最大值（$b in [-1, 1]$）。对 @eq:e1 取对数（$k > 0$ 时成立）：

  $ ln k = ln(1 - b^2) + 2 ln(b+2) - 2 ln(4b+5), $

  求导：

  $ (k')/k = (-2b)/(1 - b^2) + 2/(b+2) - 8/(4b+5). $

  令 $k' = 0$，两边同乘 $(1 - b^2)(b+2)(4b+5) > 0$（$b in [-1, 1]$ 时 $4b + 5 >= 1$）：

  $ -2b(b+2)(4b+5) + 2(1 - b^2)(4b+5) - 8(1 - b^2)(b+2) = 0. $

  逐项展开：

  $ (-8b^3 - 26b^2 - 20b) + (-8b^3 - 10b^2 + 8b + 10) + (8b^3 + 16b^2 - 8b - 16) = 0 $

  即

  $ -8b^3 - 20b^2 - 20b - 6 = 0 quad <=> quad 4b^3 + 10b^2 + 10b + 3 = 0. $

  因式分解：

  $ 4b^3 + 10b^2 + 10b + 3 = (2b + 1)(2b^2 + 4b + 3), $

  而 $2b^2 + 4b + 3$ 的判别式 $4^2 - 4 dot 2 dot 3 = -8 < 0$，恒正，所以唯一的驻点是

  $ b = -1/2, quad a = sqrt(1 - 1/4) = (sqrt(3))/2. $

  又 $b -> +-1$ 时 $k -> 0$（因子 $1 - b^2 -> 0$），所以 $b = -1/2$ 处取到最大值：

  $ k_"max" = ((1 - 1/4) dot (3/2)^2)/((4 dot (-1/2) + 5)^2) = ((3/4) dot (9/4))/9 = (27/16)/9 = 3/16. $

  于是

  $ (a(b+2))/(4b+5) <= sqrt(3/16) = (sqrt(3))/4 approx 0.433, $

  当且仅当 $a = (sqrt(3))/2$、$b = -1/2$ 时取到等号。

  $($顺带一提：这一步也可以不走求导——由 $(1-b^2)(b+2)^2 <= k(4b+5)^2$ 整理出 $b$ 的四次方程，令其有重根同样解得 $b = -1/2$、$k = 3/16$；两种写法结果一致。$)$

  *(D) 用权方和不等式（柯西不等式的分式形式）。* 先把两项写成"分子为常数、分母为正"的形式：

  $ (sqrt(3))/(2a) + (12)/(3b^2 + 2) = 1/((2a)/(sqrt(3))) + 4/(b^2 + 2/3). $

  由 $(2a)/(sqrt(3)) <= a^2 + 1/3$（因为 $a^2 + 1/3 - (2a)/(sqrt(3)) = (a - 1/(sqrt(3)))^2 >= 0$）得

  $ 1/((2a)/(sqrt(3))) >= 1/(a^2 + 1/3), $

  再用权方和不等式 $1/X + 4/Y >= (1 + 2)^2/(X + Y)$（$X, Y > 0$）：

  $ 1/(a^2 + 1/3) + 4/(b^2 + 2/3) >= ((1 + 2)^2)/(a^2 + 1/3 + b^2 + 2/3) = 9/(a^2 + b^2 + 1) = 9/2. $

  等号条件：$a = 1/(sqrt(3))$ 且 $Y = 2X$，即 $b^2 + 2/3 = 2(a^2 + 1/3) = 4/3$，得 $b^2 = 2/3$，此时 $a^2 + b^2 = 1/3 + 2/3 = 1$，*确实能取到等号*。故 D *正确*。


]
