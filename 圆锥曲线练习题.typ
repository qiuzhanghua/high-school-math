#import "template.typ": *

#set document(title: [圆锥曲线练习题])

#template(heading2-align: left, page-footer: "第 1 页")[

  *第一题* (原18题17分)

  已知双曲线 $E: x^2/a^2 - y^2/b^2 = 1$ ($a > 0, b > 0$) 的右焦点为 $F$，左顶点为 $A(-1, 0)$，离心率为 $2$，过点 $F$ 且斜率存在的直线 $l$ 与 $E$ 的右支交于 $P, Q$ 两点（点 $P$ 在第一象限），$M$ 是 $P Q$ 的中点，$O$ 为坐标原点。

  (1) 求 $E$ 的方程；

  (2) 证明：$angle P F A = 2 angle P A F$；

  (3) 过点 $F$ 且与 $l$ 垂直的直线 $m$ 交直线 $O M$ 于点 $N$，证明：

  $triangle N P Q "的面积大于" 9/2$

  *解:*

  *(1) 求 $E$ 的方程*

  由左顶点 $A(-1, 0)$ 得 $a = 1$。又离心率 $e = c/a = 2$，故 $c = 2$。

  由 $b^2 = c^2 - a^2 = 4 - 1 = 3$，得双曲线方程为：

  $ x^2 - y^2/3 = 1 $

  *(2) 证明：$angle P F A = 2 angle P A F$*

  设 $P(x_1, y_1)$（$x_1 > 1, y_1 > 0$），直线 $l$ 的倾斜角为 $theta$，直线 $P A$ 的倾斜角为 $alpha$。

  则 $tan theta = y_1/(2 - x_1)$，$tan alpha = y_1/(x_1 + 1)$。

  由 $P$ 在双曲线上，$y_1^2 = 3(x_1^2 - 1)$。计算：

  $ tan 2alpha = (2 tan alpha)/(1 - tan^2 alpha) = (2 y_1 (x_1 + 1))/((x_1 + 1)^2 - y_1^2) $

  代入 $y_1^2 = 3(x_1^2 - 1)$，分母化简为：

  $ (x_1 + 1)^2 - 3(x_1^2 - 1) = -2(x_1 - 2)(x_1 + 1) $

  故：

  $ tan 2alpha = (2 y_1 (x_1 + 1))/(-2(x_1 - 2)(x_1 + 1)) = - y_1/(x_1 - 2) = y_1/(2 - x_1) = tan theta $

  又 $theta, 2alpha in (0, pi/2)$，故 $theta = 2alpha$。

  而 $angle P F A = theta$（$F P$ 与 $F A$ 的夹角），$angle P A F = alpha$，所以 $angle P F A = 2 angle P A F$。 $quad square$

  *(3) 证明：$triangle N P Q$ 的面积大于 $9/2$*

  设 $l: y = k(x - 2)$（$k > sqrt(3)$），代入 $3x^2 - y^2 = 3$：

  $ (3 - k^2)x^2 + 4k^2 x - (4k^2 + 3) = 0 $

  设 $P(x_1, y_1), Q(x_2, y_2)$，由韦达定理：

  $ x_1 + x_2 = (4k^2)/(k^2 - 3), quad x_1 x_2 = (4k^2 + 3)/(k^2 - 3) $

  中点 $M$ 的坐标：

  $ x_M = (2k^2)/(k^2 - 3), quad y_M = k(x_M - 2) = (6k)/(k^2 - 3) $

  直线 $O M$ 的斜率 $k_(O M) = y_M/x_M = 3/k$，即 $O M: y = 3/k x$。

  直线 $m$ 过 $F$ 且与 $l$ 垂直：$m: y = -1/k (x - 2)$。

  联立 $m$ 与 $O M$ 解得 $N$ 点坐标：

  $ N(1/2, 3/(2k)) $

  弦长 $|P Q|$：

  $ |P Q| = sqrt(1 + k^2) |x_1 - x_2| = sqrt(1 + k^2) sqrt((x_1 + x_2)^2 - 4 x_1 x_2) = (6(1 + k^2))/(k^2 - 3) $

  点 $N$ 到直线 $l: k x - y - 2k = 0$ 的距离：

  $ d = (|k dot 1/2 - 3/(2k) - 2k|)/(sqrt(k^2 + 1)) = (3 sqrt(k^2 + 1))/(2k) $

  所以 $triangle N P Q$ 的面积为：

  $ S = 1/2 |P Q| d = (9 (k^2 + 1)^(3/2))/(2k(k^2 - 3)) $

  要证 $S > 9/2$，只需证 $(k^2 + 1)^(3/2) > k(k^2 - 3)$。两边平方：

  $ (k^2 + 1)^3 - k^2 (k^2 - 3)^2 = (3k^2 - 1)^2 > 0 $

  故 $triangle N P Q$ 的面积大于 $9/2 。$
  #qed

  #line(length: 100%, stroke: 0.5pt + gray)

  *（2027 届武汉九调 T14）* 记椭圆 $Gamma: x^2/a^2 + y^2/b^2 = 1$（$a > b > 0$）的左、右焦点分别为 $F_1$、$F_2$。已知椭圆上的点 $P$ 满足 $cos angle F_1 P F_2 = 4/5$。记 $angle F_1 P F_2$ 的平分线与椭圆交于另一点 $Q$。若 $angle F_1 Q F_2 = 90degree$，则 $(|P F_1|) / (|Q F_1|) + (|P F_2|)/(|Q F_2|) = underline(#h(3em))$，离心率 $e = underline(#h(3em))$。


  #align(center)[
    #block(breakable: false, width: 260pt, height: 268pt)[
      // 椭圆（a = 95, b = 60, 中心 (150,150)）与两条坐标轴
      #place(dx: 55pt, dy: 90pt, ellipse(width: 190pt, height: 120pt, stroke: 0.8pt))
      #place(dx: 150pt, dy: 35pt, line(end: (0pt, 230pt), stroke: (paint: gray, thickness: 0.4pt)))   // y 轴
      #place(dx: 20pt, dy: 150pt, line(end: (260pt, 0pt), stroke: (paint: gray, thickness: 0.4pt)))   // x 轴
      // 焦半径 PF1、PF2、QF1、QF2
      #place(dx: 241pt, dy: 134pt, line(end: (-165pt, 16pt), stroke: 0.8pt))      // P→F1
      #place(dx: 241pt, dy: 134pt, line(end: (-17pt, 16pt), stroke: 0.8pt))       // P→F2
      #place(dx: 95pt, dy: 199pt, line(end: (-19pt, -49pt), stroke: (paint: rgb("#c7362e"), thickness: 1.1pt)))  // Q→F1
      #place(dx: 95pt, dy: 199pt, line(end: (129pt, -49pt), stroke: (paint: rgb("#c7362e"), thickness: 1.1pt)))  // Q→F2
      // 角平分线 PQ（虚线）
      #place(dx: 95pt, dy: 199pt, line(end: (146pt, -65pt), stroke: (
        paint: rgb("#1f6feb"),
        thickness: 1.1pt,
        dash: "dashed",
      )))
      // 点
      #place(dx: 76pt, dy: 150pt, circle(radius: 1.8pt, fill: black))             // F1
      #place(dx: 224pt, dy: 150pt, circle(radius: 1.8pt, fill: black))            // F2
      #place(dx: 239pt, dy: 132pt, circle(radius: 1.8pt, fill: black))            // P
      #place(dx: 93pt, dy: 197pt, circle(radius: 1.8pt, fill: rgb("#c7362e")))    // Q
      // 标签
      #place(dx: 66pt, dy: 154pt)[$F_1$]
      #place(dx: 226pt, dy: 154pt)[$F_2$]
      #place(dx: 244pt, dy: 124pt)[$P$]
      #place(dx: 76pt, dy: 202pt)[#text(fill: rgb("#c7362e"))[$Q$]]
      #place(dx: 252pt, dy: 140pt)[$x$]
      #place(dx: 156pt, dy: 40pt)[$y$]
    ]
  ]

  #v(8pt)

  图中 $P$、$Q$ 都在椭圆上，$F_1$、$F_2$ 是左、右焦点；蓝色虚线 $P Q$ 是 $angle F_1 P F_2$ 的平分线（即椭圆在 $P$ 处的法线），红线为 $Q$ 到两焦点的连线。

  *答案*：第一空 $10/3$；第二空 $e = (sqrt(15))/5$。

  *解法一（符号推导 + 韦达定理化简，一般情形；推荐解法）。*

  *(一) 一般情形的符号推导*（不代入 $cos alpha$，也不代入 $angle F_1 Q F_2$ 的具体值）：

  记号：$alpha = angle F_1 P F_2$，$gamma = angle F_1 Q F_2$，$r_1 = |P F_1|$、$r_2 = |P F_2|$、$r_1' = |Q F_1|$、$r_2' = |Q F_2|$，$beta_1 = angle P Q F_1$、$beta_2 = angle P Q F_2$。全程把 $cos alpha$、$cos gamma$ 当作符号，只在最后一步代值。

  *第一步（角平分线 + 正弦定理）。* $P Q$ 平分 $angle F_1 P F_2$，故 $angle F_1 P Q = angle Q P F_2 = (alpha)/2$。在两个三角形 $P F_1 Q$、$P F_2 Q$ 中用正弦定理：

  $ r_1/r_1' = (sin beta_1)/(sin alpha/2), quad r_2/r_2' = (sin beta_2)/(sin alpha/2). $ <eq:74761>

  又 $P$ 在 $angle F_1 Q F_2$ 内部，故

  $ beta_1 + beta_2 = gamma. $ <eq:74762>

  *第二步（定义平方）。* 由 $r_1 + r_2 = r_1' + r_2' = 2a$：

  $ r_1^2 + r_2^2 + 2 r_1 r_2 = r_1'^2 + r_2'^2 + 2 r_1' r_2'. $ <eq:74763>

  *第三步（两个余弦定理）。* 在 $triangle P F_1 F_2$、$triangle Q F_1 F_2$ 中：

  $ r_1^2 + r_2^2 - 2 r_1 r_2 cos alpha = 4 c^2, quad r_1'^2 + r_2'^2 - 2 r_1' r_2' cos gamma = 4 c^2. $ <eq:74764>

  *第四步（两式相减，消去 $r^2$ 项）。* 由 @eq:74763、@eq:74764 分别得

  $ 2 r_1 r_2 (1 + cos alpha) = 4 a^2 - 4 c^2 = 4 b^2, quad 2 r_1' r_2' (1 + cos gamma) = 4 b^2, $

  即

  $
    r_1 r_2 = (2 b^2)/(1 + cos alpha), quad r_1' r_2' = (2 b^2)/(1 + cos gamma), quad (r_1 r_2)/(r_1' r_2') = (1 + cos gamma)/(1 + cos alpha). $ <eq:74765>

  结合 @eq:74761 得

  $ sin beta_1 sin beta_2 = sin^2 (alpha/2) (1 + cos gamma)/(1 + cos alpha). $ <eq:74766>

  *第五步（求 $S = r_1/r_1' + r_2/r_2'$，全程符号）。* 由 @eq:74761

  $ S = (sin beta_1 + sin beta_2)/(sin alpha/2), quad S^2 = ((sin beta_1 + sin beta_2)^2)/(sin^2 alpha/2). $

  由 @eq:74762 用和差化积：

  $
    sin beta_1 + sin beta_2 = 2 sin (gamma/2) cos ((beta_1 - beta_2))/2, quad sin beta_1 sin beta_2 = (cos(beta_1 - beta_2) - cos gamma)/2.
  $

  于是 $cos(beta_1 - beta_2) = 2 sin beta_1 sin beta_2 + cos gamma$，代入 @eq:74766：

  $ cos(beta_1 - beta_2) = (2 sin^2 (alpha/2) (1 + cos gamma))/(1 + cos alpha) + cos gamma. $

  又 $cos^2 ((beta_1 - beta_2))/2 = (1 + cos(beta_1 - beta_2))/2$，故

  $
    (sin beta_1 + sin beta_2)^2 = 4 sin^2 (gamma)/2 dot (1 + cos(beta_1 - beta_2))/2 = 2 sin^2 (gamma)/2 (1 + cos(beta_1 - beta_2)).
  $

  把 $cos(beta_1 - beta_2)$ 代入并用 $1 + cos gamma = 2 cos^2 (gamma)/2$、$sin^2 (alpha/2) = (1 - cos alpha)/2$，化简得

  $
    (sin beta_1 + sin beta_2)^2 = 2 sin^2 (gamma)/2 (1 + cos gamma) dot (1 + (1 - cos alpha)/(1 + cos alpha)) = 2 sin^2 (gamma)/2 (1 + cos gamma) dot 2/(1 + cos alpha).
  $

  再除以 $sin^2 (alpha/2) = (1 - cos alpha)/2$，并注意 $1 - cos^2 alpha = sin^2 alpha$、$1 + cos gamma = 2 cos^2 (gamma)/2$：

  $
    S^2 = (4 sin^2 (gamma)/2 (1 + cos gamma))/(1 - cos^2 alpha) = (4 sin^2 (gamma)/2 dot 2 cos^2 (gamma)/2)/(sin^2 alpha) = (4 sin^2 gamma)/(sin^2 alpha).
  $

  因为 $S > 0$、$sin alpha > 0$，开方得

  $
    S = (2 sin gamma)/(sin alpha), quad "即" quad (|P F_1|)/(|Q F_1|) + (|P F_2|)/(|Q F_2|) = (2 sin angle F_1 Q F_2)/(sin angle F_1 P F_2).
  $

  *第六步（代值）。* 本题 $gamma = 90degree$，$cos alpha = 4/5$，于是 $sin gamma = 1$、$sin alpha = 3/5$，

  $ S = (2 dot 1)/(3/5) = 10/3, $

  即第一空为 $10/3$。


  *(二) 把 $x_1 = (|P F_1|)/(|Q F_1|)$、$x_2 = (|P F_2|)/(|Q F_2|)$ 当作"两根"，用韦达定理化简 $e^2$。*

  记 $q_1 = |Q F_1|$、$q_2 = |Q F_2|$，则 $|P F_1| = x_1 q_1$、$|P F_2| = x_2 q_2$。由椭圆定义得方程组

  $
    cases(x_1 q_1 + x_2 q_2 = 2a, q_1 + q_2 = 2a) quad => quad q_1 = (2(1-x_2))/(x_1-x_2) a, quad q_2 = (2(x_1-1))/(x_1-x_2) a. $ <eq:74767>

  而$angle F_1 Q F_2 = 90degree$：

  $ q_1^2 + q_2^2 = |F_1 F_2|^2 = 4 c^2. $ <eq:74768>

  把 @eq:74767 代入 @eq:74768：

  $
    ((2a)/(x_1-x_2))^2 [(1-x_2)^2 + (x_1-1)^2] = 4 c^2 quad => quad e^2 = ((1-x_2)^2 + (x_1-1)^2)/((x_1-x_2)^2). $ <eq:74769>

  *关键观察：@eq:74769 的右边关于 $x_1$、$x_2$ 是对称的*（交换 $x_1$、$x_2$ 后分子变成 $(1-x_1)^2 + (x_2-1)^2$，与原式相同），所以可以用"两根之和、两根之积"表出，不必把 $x_1$、$x_2$ 分别解出来：

  $ e^2 = ((x_1^2 + x_2^2) - 2(x_1 + x_2) + 2)/((x_1 + x_2)^2 - 4 x_1 x_2). $ <eq:747610>

  *两根之和与两根之积*（由上面 (一) 的符号结果）：

  $ x_1 + x_2 = (sin beta_1 + sin beta_2)/(sin (alpha/2)) = 2/(sin alpha) $

  $
    x_1 x_2 = (|P F_1| |P F_2|)/(|Q F_1| |Q F_2|) = (r_1 r_2)/(r_1' r_2') = 1/(1 + cos alpha) \
    x_1^2 + x_2^2 = (sin^2 beta_1 + sin^2 beta_2)/(sin^2 alpha/2) = 1/(sin^2 alpha/2) = 2/(1 - cos alpha).
  $


  *代入 @eq:747610。* 由上一小节的三组结果与 $cos alpha = 4/5$：

  $ x_1 + x_2 = 2/(sin alpha) = 2/(3/5) = 10/3 $
  $ quad x_1 x_2 = 1/(1 + cos alpha) = 1/(9/5) = 5/9 $
  $ quad x_1^2 + x_2^2 = 2/(1 - cos alpha) = 2/(1/5) = 10. $

  于是

  $
    e^2 = (10 - 2 dot (10)/3 + 2)/((10/3)^2 - 4 dot 5/9) = ((30 - 20 + 6)/3)/((100 - 20)/9) = (16/3)/((80)/9) = (16)/3 dot 9/(80) = 3/5,
  $

  故

  $ e = (sqrt(15))/5 $


  *解法二（坐标法，大题规范写法；全程只用 $P(x_1, y_1)$、$Q(x_2, y_2)$，不引入离心角）。*

  *① 一个公共的中间量：用坐标表示 $|P F_1| dot |P F_2|$。* 设 $P(x_1, y_1)$ 在椭圆 $x^2/a^2 + y^2/b^2 = 1$ 上，$F_1(-c, 0)$、$F_2(c, 0)$，$c^2 = a^2 - b^2$。由

  $ |P F_1|^2 + |P F_2|^2 = (x_1+c)^2 + y_1^2 + (x_1-c)^2 + y_1^2 = 2(x_1^2 + y_1^2 + c^2) $

  与 $|P F_1| + |P F_2| = 2a$，得

  $ |P F_1| dot |P F_2| = ((|P F_1| + |P F_2|)^2 - (|P F_1|^2 + |P F_2|^2))/2 = 2a^2 - (x_1^2 + y_1^2 + c^2). $

  把 $x_1^2 = a^2 - (a^2/b^2) y_1^2$（椭圆方程）代入，并用 $a^2 - c^2 = b^2$：

  $ |P F_1| dot |P F_2| = b^2 + (c^2 y_1^2)/(b^2). $

  对 $Q(x_2, y_2)$ 同理有 $|Q F_1| dot |Q F_2| = b^2 + (c^2 y_2^2)/(b^2)$。

  *② 由 $cos angle F_1 P F_2 = 4/5$ 定出 $y_1^2$。* 在 $triangle P F_1 F_2$ 中由余弦定理，并代入 $|P F_1| + |P F_2| = 2a$、$|F_1 F_2| = 2c$：

  $ cos angle F_1 P F_2 = (|P F_1|^2 + |P F_2|^2 - 4c^2)/(2 |P F_1| |P F_2|) = (2 b^2)/(|P F_1| |P F_2|) - 1, $

  故 $|P F_1| |P F_2| = (2 b^2)/(9/5) = (10 b^2)/9$，与①比较得

  $ (c^2 y_1^2)/(b^2) = b^2/9, quad "即" quad y_1^2 = (b^4)/(9 c^2). $

  *③ 由 $angle F_1 Q F_2 = 90degree$ 定出 $y_2^2$。* 此时 $|Q F_1|^2 + |Q F_2|^2 = 4c^2$，故 $2|Q F_1||Q F_2| = 4a^2 - 4c^2 = 4b^2$，即 $|Q F_1||Q F_2| = 2b^2$，与①比较得

  $ (c^2 y_2^2)/(b^2) = b^2, quad "即" quad y_2^2 = (b^4)/(c^2) = 9 y_1^2. $

  于是 $|y_2| = 3|y_1|$。

  *④ 用"单位向量之和"表示角平分线（大题可用）。* 由 $r_1^2 - r_2^2 = 4 c x_1$ 与 $r_1 + r_2 = 2a$ 得 $r_1 = a + e x_1$、$r_2 = a - e x_1$（其中 $r_1 = |P F_1|$ 等）。于是

  $ bold(v) = arrow(P F_1)/r_1 + arrow(P F_2)/r_2 = (1/(r_1 r_2)) (-(2 b^2 x_1)/a, -2 a y_1), $

  即 $bold(v) text(∥) ((b^2 x_1)/a, a y_1)$。由 $P Q$ 是内角平分线得 $arrow(P Q) text(∥) bold(v)$，即

  $ a^2 y_1 (x_2 - x_1) = b^2 x_1 (y_2 - y_1). $

  *⑤ 定符号后代入。* 逐一检验符号可知，只有 *$P$ 在第一象限（$x_1>0$、$y_1>0$）、$Q$ 在第三象限（$x_2<0$、$y_2<0$）* 时 $arrow(P Q)$ 与 $bold(v)$ 同向，故取

  $ y_2 = -3 y_1, quad x_1 = sqrt(1 - y_1^2/b^2) > 0, quad x_2 = -sqrt(1 - y_2^2/b^2) < 0. $

  代入上式并约去 $y_1 > 0$：

  $ a^2 (x_2 - x_1) = -4 b^2 x_1 quad => quad x_2 = x_1 (1 - (4 b^2)/a^2). $

  *⑥ 解出 $e$。* 记 $u = e^2$，则 $b^2 = a^2(1-u)$、$y_1^2/b^2 = b^2/(9c^2) = (1-u)/(9u)$、$y_2^2/b^2 = (1-u)/u$。把 $x_1^2 = 1 - (1-u)/(9u)$、$x_2^2 = 1 - (1-u)/u$ 代入上式并两边平方：

  $ 1 - (1-u)/u = (1 - 4(1-u))^2 (1 - (1-u)/(9u)), $

  即

  $ (2u-1)/u = ((4u-3)^2 (10u-1))/(9u) quad => quad 9(2u-1) = (4u-3)^2 (10u-1). $

  展开整理：

  $ 18u - 9 = 160u^3 - 256u^2 + 114u - 9 quad => quad 5u^2 - 8u + 3 = 0, $

  得 $u = 1$（此时 $b = 0$，舍）或 $u = 3/5$，故

  $ e^2 = 3/5, quad e = (sqrt(15))/5. $

  *⑦ 求第一空。* 由 $r_1 = a + e x_1$（到左焦点 $F_1$）、$r_2 = a - e x_1$（到右焦点 $F_2$），同理 $r_1' = a + e x_2$、$r_2' = a - e x_2$。由 ⑤ 的结果 $x_1 = (5 a)/(3 sqrt(3))$、$x_2 = -a/(sqrt(3))$ 与 $e = (sqrt(15))/5$：

  $
    e x_1 = (sqrt(15))/5 dot (5 a)/(3 sqrt(3)) = (a sqrt(5))/3, quad e x_2 = (sqrt(15))/5 dot (-a/(sqrt(3))) = -(a sqrt(5))/5.
  $

  于是

  $
    (|P F_1|)/(|Q F_1|) + (|P F_2|)/(|Q F_2|) = (a + (a sqrt(5))/3)/(a - (a sqrt(5))/5) + (a - (a sqrt(5))/3)/(a + (a sqrt(5))/5).
  $

  约分，化简得：

  $ (|P F_1|)/(|Q F_1|) + (|P F_2|)/(|Q F_2|) = (5 + 2 sqrt(5))/3 + (5 - 2 sqrt(5))/3 = 10/3. $

  所以第一空填 $10/3$。

  *解法三（填空、选择的秒杀路线，展开版）。*

  *第一步：认出"平分线就是法线"。* 由椭圆的*光学性质*（椭圆上一点处的法线平分两条焦半径的夹角），$angle F_1 P F_2$ 的平分线就是椭圆在 $P$ 处的法线，所以 $P Q$ 沿 $P$ 处法线方向。

  *第二步：用焦半径公式把两个条件写成 $cos theta$、$cos phi$ 的方程。* 记 $P(a cos theta, b sin theta)$、$Q(a cos phi, b sin phi)$，则

  $ r_1 r_2 = |P F_1| dot |P F_2| = a^2 - e^2 x_P^2 = a^2 (1 - e^2 cos^2 theta), $

  $ r_1' r_2' = |Q F_1| dot |Q F_2| = a^2 (1 - e^2 cos^2 phi). $

  由 $cos angle F_1 P F_2 = 4/5$ 及 $cos angle F_1 P F_2 = (2 b^2)/(r_1 r_2) - 1$ 得

  $ a^2 (1 - e^2 cos^2 theta) = (10 b^2)/9, quad "即" quad 1 - e^2 cos^2 theta = (10(1-e^2))/9. quad "（I）" $

  由 $angle F_1 Q F_2 = 90degree$ 及 $r_1' r_2' = 2 b^2$ 得

  $ a^2 (1 - e^2 cos^2 phi) = 2 b^2, quad "即" quad 1 - e^2 cos^2 phi = 2(1 - e^2). quad "（II）" $

  *第三步：加一条"法线条件"。* 椭圆在 $P(a cos theta, b sin theta)$ 处的法线方向为 $((cos theta)/a, (sin theta)/b)$，而 $arrow(P Q)$ 与它平行，故

  $ (a cos phi - a cos theta) dot (sin theta)/b - (b sin phi - b sin theta) dot (cos theta)/a = 0, $

  即

  $ a^2 sin theta (cos phi - cos theta) = b^2 cos theta (sin phi - sin theta). quad "（III）" $

  *第四步：定符号后解出 $e$。* 由（II）得 $cos^2 phi = (2e^2-1)/e^2$，由（I）得 $cos^2 theta = (10e^2-1)/(9e^2)$。注意到（III）要求 $cos phi$ 与 $cos theta$ *异号*（$P$ 在第一象限、$Q$ 在第三象限），于是

  $ cos theta = sqrt((10e^2-1)/(9e^2)), quad cos phi = -sqrt((2e^2-1)/e^2). $

  代入（III）并两边平方，整理得关于 $u = e^2$ 的方程

  $ 5u^2 - 8u + 3 = 0 quad => quad u = 3/5 quad ("舍去" u = 1), $

  即 $e = (sqrt(15))/5$；第一空同正解为 $10/3$。

  *第五步（填空实战：先猜后验更省时间）。* 若已由上述方程结构猜出 $e^2 = 3/5$，只需回代验证：

  $ cos^2 theta = (10 dot 3/5 - 1)/(9 dot 3/5) = 5/9 dot 5/3 = 25/27, quad cos^2 phi = (2 dot 3/5 - 1)/(3/5) = 1/3, $

  取 $cos theta = 5/(3 sqrt(3))$、$cos phi = -1/(sqrt(3))$ 时有

  $ a^2 sin theta (cos phi - cos theta) = b^2 cos theta (sin phi - sin theta), $

  即（III）成立，说明 $e^2 = 3/5$ 确实是答案。填空、选择时用"猜 + 代回验证"两三行即可收工；解答题请用前面的正解（推导完整、无跳步）。

  \

  *答题规范提示*：本题作为*填空*，可直接引用"$P$ 处法线平分 $angle F_1 P F_2$"（光学性质）与焦半径公式 $r = a +- e x$ 快速得解；若作为*解答题*，光学性质须替换为上面的"单位向量之和沿角平分线"（菱形对角线），焦半径公式也建议用坐标与余弦定理现场推出，以免因引用非课本结论而失分。


  #line(length: 100%, stroke: 0.5pt + gray)

  *（四川成都七中高三月考 T17）* 椭圆 $C: x^2/a^2 + y^2/b^2 = 1$（$a > b > 0$）的左、右顶点分别为 $A$、$B$，离心率为 $(sqrt(2))/2$，点 $M(sqrt(2), 1)$ 在椭圆 $C$ 上。

  (1) 求椭圆 $C$ 的方程；

  (2) 直线 $l$ 交椭圆 $C$ 于 $P$、$Q$ 两点，记直线 $A P$ 的斜率为 $k_1$、直线 $B Q$ 的斜率为 $k_2$，且 $k_1 = 2 k_2$。过顶点 $A$ 作直线 $P Q$ 的垂线，垂足为 $H$。问：在平面内是否存在定点 $T$，使得 $|T H|$ 为定值？若存在，求出点 $T$ 的坐标；若不存在，试说明理由。

  *答案*：(1) $x^2/4 + y^2/2 = 1$；(2) 存在，$T(-4/3, 0)$，$|T H| = 2/3$。

  #rect(stroke: 0.5pt, inset: 8pt, width: 100%)[
    *模型结论（椭圆中的"斜率比"）*

    设椭圆 $x^2/a^2 + y^2/b^2 = 1$（$a > b > 0$），左、右顶点为 $A(-a, 0)$、$B(a, 0)$。直线 $l$ 交椭圆于 $P$、$Q$ 两点，且 $l$ 与 $x$ 轴交于定点 $M(m, 0)$。记 $k_1 = k_(A P)$、$k_2 = k_(B Q)$，则

    $ k_1/k_2 = (a - m)/(a + m) quad ("与直线 l 的斜率、椭圆的 b 都无关"). $

    *为什么*：由椭圆方程 $y^2 = (b^2/a^2)(a^2 - x^2) = (b^2/a^2)(a-x)(a+x)$，

    $
      k_1^2 = (y_1^2)/(x_1+a)^2 = (b^2)/(a^2) dot (a - x_1)/(a + x_1), quad k_2^2 = (y_2^2)/(x_2-a)^2 = (b^2)/(a^2) dot (a + x_2)/(a - x_2),
    $

    再把 $x = t y + m$ 代入椭圆所得的韦达定理代入 $(a-x_1)(a-x_2)/((a+x_1)(a+x_2))$，化简即得

    $ (k_1^2)/(k_2^2) = ((a-m)/(a+m))^2, $

    结合 $k_1$、$k_2$ 的符号即得所需结论。

    *用法*：已知 $k_1/k_2$ 的值，可以*反解*出 $M$ 的横坐标 $m = a dot (1 - k_1 slash k_2)/(1 + k_1 slash k_2)$，从而得到直线 $P Q$ 恒过的定点——这正是下面第 (2) 问"$k_1 = 2 k_2$"一步定出 $N(-2/3, 0)$ 的原因（代数验证 400 组随机数据，结论全部吻合）。

    本题中 $a = 2$，由 $k_1/k_2 = 2$ 得 $(2-m)/(2+m) = 2$，解得 $m = -2/3$。
  ]

  *解答：*

  *(1) 求椭圆方程。* 由 $e = c/a = (sqrt(2))/2$ 得 $c^2 = a^2/2$，故

  $ b^2 = a^2 - c^2 = a^2/2, quad "即" quad a^2 = 2 b^2. $

  把 $M(sqrt(2), 1)$ 代入：$2/(2 b^2) + 1/b^2 = 1$，即 $1/b^2 + 1/b^2 = 1$，得 $b^2 = 2$，从而 $a^2 = 4$。所以

  $ C: x^2/4 + y^2/2 = 1, quad "其中" A(-2, 0), B(2, 0). $

  *(2) 解法一：常规联立（韦达定理）。* 设直线 $P Q$ 为 $x = t y + m$，它与 $x$ 轴交于 $N(m, 0)$；设 $P(x_1, y_1)$、$Q(x_2, y_2)$。代入 $x^2 + 2 y^2 = 4$：

  $ (t y + m)^2 + 2 y^2 = 4 quad => quad (t^2 + 2) y^2 + 2 t m y + m^2 - 4 = 0, $

  故

  $ y_1 + y_2 = -(2 t m)/(t^2 + 2), quad y_1 y_2 = (m^2 - 4)/(t^2 + 2). $

  由 $k_1 = y_1/(x_1 + 2)$、$k_2 = y_2/(x_2 - 2)$ 与 $k_1 = 2 k_2$：

  $ y_1 (x_2 - 2) = 2 y_2 (x_1 + 2). $

  把 $x_1 = t y_1 + m$、$x_2 = t y_2 + m$ 代入并展开：

  $ y_1 (t y_2 + m - 2) = 2 y_2 (t y_1 + m + 2) quad => quad -t y_1 y_2 + (m - 2) y_1 - 2(m + 2) y_2 = 0. $

  把上式与 $y_1 + y_2 = S$、$y_1 y_2 = P$ 联立（不必解出 $y_1$、$y_2$ 的具体值）：由 $y_2 = S - y_1$ 代入

  $ -t P + (m-2) y_1 - 2(m+2) y_2 = 0, $

  得

  $ (3m + 2) y_1 = t P + 2(m+2) S. $

  把 $S = -(2 t m)/(t^2+2)$、$P = (m^2-4)/(t^2+2)$ 代入右端并因式分解：

  $ t P + 2(m+2) S = (t(m^2-4) - 4 t m (m+2))/(t^2+2) = -(t(m+2)(3m+2))/(t^2+2). $

  于是

  $ (3m+2) y_1 = -(t(m+2)(3m+2))/(t^2+2) quad <=> quad (3m+2) [ y_1 + (t(m+2))/(t^2+2) ] = 0, $

  所以只有两种可能：

  - $3m + 2 = 0$，即 $m = -2/3$；
  - 或 $y_1 = -(t(m+2))/(t^2+2)$，此时再由 $y_1 y_2 = P$ 可得 $m^2 = 4$，即 $m = +-2$——这两种情形下直线都过椭圆的顶点（$m = 2$ 过 $B$，$m = -2$ 过 $A$），与"直线交椭圆于 $P$、$Q$ 两点且 $A$ 不在其中"不符，舍去。

  故*只要* $k_1 = 2 k_2$ 且 $P Q$ 不过顶点，直线 $P Q$ 就恒过定点

  $ N(-2/3, 0). $

  *(2) 解法二：极点极线（课外方法，供对照）。* 由极点极线理论，若 $P Q$ 过 $M(m, 0)$，则 $A P$ 与 $B Q$ 的交点 $K$ 在 $M$ 的极线 $x = a^2/m = 4/m$ 上，设 $K(4/m, y_K)$。由 $A$、$P$、$K$ 与 $B$、$Q$、$K$ 共线，

  $ k_1 = y_K/(4/m + 2), quad k_2 = y_K/(4/m - 2), $

  代入 $k_1 = 2 k_2$（约去 $y_K$）：$4/m - 2 = 2(4/m + 2)$，解得 $m = -2/3$，与解法一一致。（此法高中不作要求，仅用于快速验证。）

  *(3) 由垂直关系找 $H$ 的轨迹，定出 $T$。* 设 $P Q$ 与 $x$ 轴交于 $N(-2/3, 0)$。因为 $A H perp P Q$ 且 $N in P Q$，所以

  $ angle A H N = 90degree, $

  即 $H$ 在以 $A N$ 为直径的圆上（$N$ 与 $A$ 都是定点）。于是取 $A N$ 的中点 $T$，由"直角三角形斜边上的中线等于斜边的一半"得

  $ |T H| = 1/2 |A N| quad ("为定值"). $

  计算：$A(-2, 0)$、$N(-2/3, 0)$，故

  $ x_T = (-2 + (-2/3))/2 = -4/3, quad y_T = 0, quad |T H| = 1/2 |A N| = 1/2 dot |-2 - (-2/3)| = 1/2 dot 4/3 = 2/3. $

  所以*存在*定点

  $ T(-4/3, 0), quad "使得" |T H| = 2/3 "为定值". $

  *(2) 解法三：蝴蝶模型（一步定出定点）。* 由蝴蝶模型结论，直线 $P Q$ 与 $x$ 轴的交点 $M(m, 0)$ 与两斜率之比满足

  $ k_1/k_2 = (a - m)/(a + m). $

  本题 $a = 2$、$k_1/k_2 = 2$，代入得 $(2 - m)/(2 + m) = 2$，解得 $m = -2/3$，即 $P Q$ 恒过定点 $N(-2/3, 0)$（与解法一、解法二一致，但只需一步解方程）。再沿用几何论证：$A H perp P Q$ 且 $N in P Q$ 给出 $angle A H N = 90degree$，故 $H$ 在以 $A N$ 为直径的圆上，取 $A N$ 中点

  $ T(-4/3, 0), quad |T H| = 1/2 |A N| = 2/3. $

  *说明*：第 (2) 问的关键是"$k_1 = 2k_2$ $=>$ $P Q$ 过定点 $N(-2/3, 0)$"；一旦有了这个定点，第 (3) 问就变成"$H$ 在以 $A N$ 为直径的圆上"，定点 $T$ 取 $A N$ 的中点即可，这是"定点 + 定长"问题最常见的套路。极点极线法只作为快速验证，解答题请用解法一的联立（或用参数方程联立）。

  #rect(stroke: 0.5pt, inset: 8pt, width: 100%)[
    *Note：蝴蝶模型（斜率比模型）*

    *内容*：椭圆 $x^2/a^2 + y^2/b^2 = 1$ 的左、右顶点为 $A(-a, 0)$、$B(a, 0)$。过 $x$ 轴上定点 $M(m, 0)$ 任作直线交椭圆于 $P$、$Q$，则

    $ k_(A P)/k_(B Q) = (a - m)/(a + m) quad ("为定值，与直线方向、b 都无关"). $

    *几何形象*：直线 $A P$ 与 $B Q$（或 $A Q$ 与 $B P$）在椭圆内交叉，加上弦 $P Q$，形似一只蝴蝶，故称"蝴蝶模型"；它是"极点极线"在椭圆上最常用的初等化结论。

    *逆用（求定点）*：若 $k_(A P)/k_(B Q) = lambda$ 为定值，则直线 $P Q$ 恒过 $M(a dot (1 - lambda)/(1 + lambda), 0)$。本题 $a = 2$、$lambda = 2$，得 $M(-2/3, 0)$，与解法一联立所得一致。

    *由来（要点）*：把 $x = t y + m$ 代入椭圆得关于 $y$ 的二次方程，用韦达定理表示 $y_1 + y_2$、$y_1 y_2$；再由 $y^2 = (b^2/a^2)(a-x)(a+x)$ 得 $k_1^2 = (b^2/a^2) dot (a-x_1)/(a+x_1)$、$k_2^2 = (b^2/a^2) dot (a+x_2)/(a-x_2)$，代入 $(a-x_1)(a-x_2)/((a+x_1)(a+x_2))$ 化简即得 $(k_1/k_2)^2 = ((a-m)/(a+m))^2$，结合符号得结论。

    *什么时候用*：题目出现"过 $x$ 轴定点的弦"与"顶点连线的斜率之比为定值"这类条件时，本模型可在"求定点"与"求斜率比"之间互相转化，常能省去大段联立。解答题建议先按解法一（联立）写清步骤，再用本模型检验或作简解。
  ]

  #line(length: 100%, stroke: 0.5pt + gray)

  *第四题（椭圆焦点弦与斜率定值）*

  已知椭圆 $E: x^2/a^2 + y^2/b^2 = 1$（$a > b > 0$）的长轴长为 $4$，离心率 $e = 1/2$，过 $E$ 的右焦点 $F$ 且不与 $y$ 轴垂直的直线与椭圆 $E$ 相交于 $A$、$B$ 两点。

  (1) 求 $E$ 的标准方程；

  (2) 若点 $P(4, 0)$，设直线 $P A$、$P B$ 的斜率分别为 $k_1$、$k_2$，求证：$k_1 + k_2 = 0$；

  (3) 若点 $C$、$D$ 分别为 $E$ 的左、右顶点，直线 $A C$ 与 $B D$ 的交点为 $Q$，求点 $Q$ 的轨迹方程。

  *解答：*

  *(1) 求 $E$ 的标准方程。* 由长轴长为 $4$ 得 $2a = 4$，即 $a = 2$；又 $e = c/a = 1/2$，故 $c = 1$，于是

  $ b^2 = a^2 - c^2 = 4 - 1 = 3, quad E: x^2/4 + y^2/3 = 1. $

  *(2) 解法一（原坐标系，通分比较）：证明 $k_1 + k_2 = 0$。* 设直线 $A B$ 的方程为 $y = k(x - 1)$（题设"不与 $y$ 轴垂直"即 $k != 0$；若 $A B$ 为竖直直线 $x = 1$，则 $A(1, 3/2)$、$B(1, -3/2)$，直接代入同样得 $k_1 + k_2 = 0$，故下面只讨论 $k != 0$ 的情形），代入 $3x^2 + 4y^2 = 12$：

  $ (3 + 4k^2)x^2 - 8k^2 x + (4k^2 - 12) = 0. $

  设 $A(x_1, y_1)$、$B(x_2, y_2)$，则

  $ x_1 + x_2 = (8k^2)/(3 + 4k^2), quad x_1 x_2 = (4k^2 - 12)/(3 + 4k^2), quad y_i = k(x_i - 1) quad (i = 1, 2). $

  于是

  $ k_1 + k_2 = (y_1)/(x_1 - 4) + (y_2)/(x_2 - 4) = k[ (x_1 - 1)/(x_1 - 4) + (x_2 - 1)/(x_2 - 4) ]. $

  括号内通分，分子为

  $ (x_1 - 1)(x_2 - 4) + (x_2 - 1)(x_1 - 4) = 2 x_1 x_2 - 5(x_1 + x_2) + 8, $

  代入韦达定理：

  $ 2(4k^2 - 12) - 5 dot 8k^2 + 8(3 + 4k^2) = 8k^2 - 24 - 40k^2 + 24 + 32k^2 = 0. $

  所以 $k_1 + k_2 = 0$。
  #qed

  *(2) 解法二（平移原点至 $P$，免通分）：证明 $k_1 + k_2 = 0$。* 作平移

  $ u = x - 4, quad v = y, $

  即把坐标原点移到 $P(4, 0)$（此时 $P$ 就是新原点）。$A$、$B$ 在新坐标系中记为 $(u_1, v_1)$、$(u_2, v_2)$，于是

  $ k_1 + k_2 = (v_1)/(u_1) + (v_2)/(u_2) = 0 <==> u_2 v_1 + u_1 v_2 = 0, $

  *右边这个式子不含分母*——这正是"平移原点"带来的最大好处。椭圆在新坐标系中为

  $ ((u + 4)^2)/4 + v^2/3 = 1 <==> u^2/4 + v^2/3 + 2u + 3 = 0, $

  而 $F(1, 0)$ 变为 $F'(-3, 0)$，所以弦 $A B$（斜率仍记为 $k$）在新坐标系中为 $v = k(u + 3)$。代入椭圆方程并乘以 $12$：

  $ (3 + 4k^2)u^2 + 24(k^2 + 1)u + 36(k^2 + 1) = 0, $

  故

  $ u_1 + u_2 = -(24(k^2 + 1))/(3 + 4k^2), quad u_1 u_2 = (36(k^2 + 1))/(3 + 4k^2). $

  由 $v_i = k(u_i + 3)$：

  $
    u_2 v_1 + u_1 v_2 = k[u_2 (u_1 + 3) + u_1 (u_2 + 3)] = k[2 u_1 u_2 + 3(u_1 + u_2)] = k dot (72(k^2 + 1) - 72(k^2 + 1))/(3 + 4k^2) = 0,
  $

  即 $k_1 + k_2 = 0$。
  #qed

  #rect(stroke: 0.5pt, inset: 8pt, width: 100%)[
    *为什么移到 $P$ 会变简，以及它的几何背景*

    - 原解法要通分 $(x_1 - 4)(x_2 - 4)$ 再与韦达定理比对；移到 $P$ 后，"两斜率之和为 $0$"直接化为*对称式* $u_2 v_1 + u_1 v_2 = 0$，代入韦达定理时只需计算 $2u_1u_2 + 3(u_1 + u_2)$，两项恰好抵消。
    - 更本质的原因是*配极关系*：对椭圆 $x^2/a^2 + y^2/b^2 = 1$，设 $P(p, 0)$、$Q(q, 0)$ 都在 $x$ 轴上。若过 $Q$ 的弦*斜率存在且不为 $0$*（既不竖直也不水平），写成 $y = k(x - q)$ 代入椭圆，得 $x_1 + x_2$、$x_1 x_2$，再代入 $y_1(x_2 - p) + y_2(x_1 - p) = 0$（即 $k_(P A) + k_(P B) = 0$），整理即得
      $ k_(P A) + k_(P B) = 0 <==> p q = a^2. $
    - 而 $p q = a^2$ 正是"$Q$ 在 $P$ 的极线 $x = a^2/p$ 上"。因此准确的表述是：*"$Q$ 在 $P$ 的极线上"$=>$"过 $Q$ 的任意弦都满足 $k_(P A) + k_(P B) = 0$"*；反向则必须限定弦"斜率存在且不为 $0$"。两条*退化弦是例外*：竖直弦 $x = q$ 的两端点、水平弦（即 $x$ 轴）的两端点都关于 $x$ 轴对称，斜率之和恒为 $0$，与 $p q$ 是否等于 $a^2$ 无关——例如 $p = 4$、$a^2 = 4$、$q = 0.5$ 时 $p q = 2 != 4$，但这两条弦仍满足斜率之和为 $0$。题设"不与 $y$ 轴垂直"正是排除了其中的水平弦（否则 $A$、$B$ 会与两顶点 $C$、$D$ 重合）。
    - 本题 $p = 4$、$a^2 = 4$，故 $q = 4/4 = 1$，恰好就是右焦点 $F(1, 0)$——"过 $F$ 的弦"必然使 $k_1 + k_2 = 0$。

    *用法*：遇到"过 $x$ 轴上一定点的弦，证两连线斜率之和（或之积）为定值"这类问题，先把原点移到该定点，往往能把式子化成不含分母的对称式，比在原坐标系里通分省一半计算。
  ]

  *(3) 求 $Q$ 的轨迹方程。* $C(-2, 0)$、$D(2, 0)$。设 $Q(X, Y)$，由 $A$、$C$、$Q$ 共线及 $B$、$D$、$Q$ 共线：

  $ Y = (y_1)/(x_1 + 2)(X + 2), quad Y = (y_2)/(x_2 - 2)(X - 2). $

  消去 $Y$，并代入 $y_i = k(x_i - 1)$ 后约去 $k != 0$：

  $ (x_1 - 1)(x_2 - 2)(X + 2) = (x_2 - 1)(x_1 + 2)(X - 2). $

  展开并归并含 $X$ 的项：

  $ X(4 - x_1 - 3x_2) = 6x_1 - 2x_2 - 4x_1 x_2. $

  下面证明右端恰为 $4(4 - x_1 - 3x_2)$。两式相减并代入韦达定理：

  $
    6x_1 - 2x_2 - 4x_1 x_2 - 4(4 - x_1 - 3x_2) = 2[5(x_1 + x_2) - 2x_1 x_2 - 8] = (2[40k^2 - 2(4k^2 - 12) - 8(3 + 4k^2)])/(3 + 4k^2) = 0,
  $

  所以 $X(4 - x_1 - 3x_2) = 4(4 - x_1 - 3x_2)$，从而 $X = 4$，即 $Q$ 恒在定直线 $x = 4$ 上。故 $Q$ 的轨迹方程为

  $ x = 4 quad (y != 0, |y| <= 3). $

  *范围与几何解释。* $Q$ 不在 $x$ 轴上：由 $Y = (6k(x_1 - 1))/(x_1 + 2)$ 知 $Y = 0$ 需 $x_1 = 1$，而 $(1, 0)$ 不在椭圆上；当弦 $A B$ 趋于竖直位置 $x = 1$ 时 $Q -> (4, +-3)$，而有限斜率时恒有 $|y| < 3$，故 $y in [-3, 0) union (0, 3]$。（这条定直线为什么会出现？见下面的"秒解"。）

  *(3) 的秒解（极点极线，供填空速判或解答题事后验算）。* 记 $M$ 为 $A B$ 与 $C D$ 的交点、$R$ 为 $A D$ 与 $B C$ 的交点。因为 $C D$ 是长轴（即 $x$ 轴），而 $A B$ 过 $F(1, 0)$ 且不是 $x$ 轴（题设"不与 $y$ 轴垂直"已排除 $x$ 轴），所以

  $ M = F(1, 0). $

  对椭圆的内接四边形 $A B C D$，三个"对角点"$M$、$Q = A C$ 与 $B D$ 的交点、$R$ 构成*自配极三角形*：*每个对角点的极线恰好是另外两个对角点的连线*，即 $M$ 的极线就是直线 $Q R$。于是

  $ Q in "极线"(M), $

  而 $M = F(1, 0)$，$F$ 关于椭圆 $x^2/4 + y^2/3 = 1$ 的极线（把 $x^2$ 换成 $x_0 x$、$y^2$ 换成 $y_0 y$）为

  $ (1 dot x)/4 + (0 dot y)/3 = 1 quad "即" quad x = 4. $

  所以 $Q$ 恒在定直线 $x = 4$ 上——*一句话就得到第 (3) 问的轨迹*：$Q$ 的轨迹正是右焦点 $F$ 的极线（范围 $y != 0$、$|y| <= 3$ 需另作讨论，见上面正解）。

  秒解三步：$C D$ 是长轴 $=>$ $M = A B$ 与 $C D$ 的交点 $= F$；内接四边形对角点自配极 $=>$ $Q$ 在 $M$ 的极线上；代入 $M = F$ $=>$ $Q$ 在 $x = a^2/c = 4$ 上。等价说法（配极原则）：$F$ 在 $Q$ 的极线上 $<==>$ $Q$ 在 $F$ 的极线上。

  *说明*：*自配极三角形*与*配极原则*超出高中范围，解答题请用上面的正解（联立 + 韦达定理），本条只用于填空选择的速判、或写出结果后的事后检验。可推广为：*若四边形内接于圆锥曲线，且某一组对边的交点是定点 $M$，则另外两组对边的两个交点都落在 $M$ 的极线上*；本题中 $M$ 恰好是焦点，极线 $x = a^2/c$ 格外简洁。

  #line(length: 100%, stroke: 0.5pt + gray)

  *第五题（椭圆内接四边形与定点）*

  已知四边形 $A B C D$ 是椭圆 $x^2/4 + y^2/3 = 1$ 的内接四边形，$A B$ 经过左焦点 $F_1$，$A C$、$B D$ 交于右焦点 $F_2$，直线 $A B$ 与直线 $C D$ 的斜率分别为 $k_1$、$k_2$。

  (1) 证明：$k_1/k_2$ 为定值；

  (2) 证明：直线 $C D$ 过定点，并求定点坐标。

  *解答：*

  *(1) 证明 $k_1/k_2$ 为定值。* 设直线 $A B$ 的方程为 $y = k(x + 1)$（$k != 0$：$k = 0$ 时 $A$、$B$ 为椭圆的左、右顶点，四边形退化；又 $A B$ 竖直时 $C D$ 也竖直（可算出 $C$、$D$ 的横坐标同为 $13/7$），$k_2$ 不存在，均不在题设"$k_1$、$k_2$ 都存在"的范围内），交椭圆于 $A(x_1, y_1)$、$B(x_2, y_2)$。代入 $3x^2 + 4y^2 = 12$：

  $ (3 + 4k^2)x^2 + 8k^2 x + (4k^2 - 12) = 0, $

  故

  $ x_1 + x_2 = -(8k^2)/(3 + 4k^2), quad x_1 x_2 = (4k^2 - 12)/(3 + 4k^2), quad y_i = k(x_i + 1). $

  下面把 $C$、$D$ 的坐标用 $x_1$、$x_2$ 表出。设过 $F_2(1, 0)$、斜率为 $s$ 的直线与椭圆交于两点，同样代入得两根之和为 $(8s^2)/(3 + 4s^2)$。对直线 $A F_2$，斜率 $s_1 = (y_1)/(x_1 - 1)$，故 $A$、$C$ 的横坐标满足

  $ x_1 + x_C = (8s_1^2)/(3 + 4s_1^2). $

  由 $A$ 在椭圆上，$y_1^2 = 3(4 - x_1^2)/4$，所以 $4s_1^2 = (3(4 - x_1^2))/((x_1 - 1)^2)$，代入化简得

  $ x_C = (8 - 5x_1)/(5 - 2x_1), quad y_C = s_1 (x_C - 1) = -(3y_1)/(5 - 2x_1). $ <eq:747611>

  同理

  $ x_D = (8 - 5x_2)/(5 - 2x_2), quad y_D = -(3y_2)/(5 - 2x_2). $ <eq:747612>

  记 $M = (5 - 2x_1)(5 - 2x_2)$。由 @eq:747611、@eq:747612：

  $ x_D - x_C = (9(x_1 - x_2))/M, $

  $ y_D - y_C = (3[5(y_1 - y_2) - 2(y_1 x_2 - y_2 x_1)])/M = (3[5k(x_1 - x_2) + 2k(x_1 - x_2)])/M = (21k(x_1 - x_2))/M, $

  其中用了 $y_1 x_2 - y_2 x_1 = k(x_2 - x_1)$。两式相除：

  $ k_2 = (y_D - y_C)/(x_D - x_C) = (21k)/9 = 7/3 k, $

  又 $k_1 = k$，所以

  $ k_1/k_2 = 3/7 quad ("为定值"). $
  #qed

  *(2) 证明 $C D$ 过定点。* 设 $C D$ 与 $x$ 轴交于 $(X, 0)$。因 $C D$ 的斜率为 $k_2$，故

  $ X = x_C - (y_C)/(k_2) = x_C - y_C dot (x_D - x_C)/(y_D - y_C). $

  把 $(x_D - x_C)/(y_D - y_C) = 3/(7k)$ 与 @eq:747611 中的 $y_C = -(3y_1)/(5 - 2x_1)$ 代入：

  $ X = (8 - 5x_1)/(5 - 2x_1) + (9y_1)/(7k(5 - 2x_1)). $

  再用 $y_1 = k(x_1 + 1)$：

  $
    X = ((8 - 5x_1) + 9(x_1 + 1)/7)/(5 - 2x_1) = ((65 - 26x_1)/7)/(5 - 2x_1) = (13(5 - 2x_1))/7 dot 1/(5 - 2x_1) = 13/7,
  $

  即 $X$ 与 $k$ 无关。所以直线 $C D$ 恒过定点

  $ (13/7, 0). $
  #qed

  #rect(stroke: 0.5pt, inset: 8pt, width: 100%)[
    *Note：配极速算定点（极点极线视角，供填空速判或解答题事后验算）*

    由第 (1) 问已有 $k_2 = (7/3)k_1$，而 $C D$ 的位置可以用配极"一眼"定出：

    - $A C$ 与 $B D$ 都过 $F_2$，故它们的交点 $Q = F_2(1, 0)$；
    - 内接四边形的三个"对角点"$P = A B$ 与 $C D$ 的交点、$Q$、$R = A D$ 与 $B C$ 的交点构成*自配极三角形*，所以 $P$、$R$ 都在 $Q$ 的极线上；
    - $F_2(1, 0)$ 的极线为 $x/4 = 1$，即 $x = 4$；又 $P in A B$，于是 $P$ 就是 $A B$ 与直线 $x = 4$ 的交点。由 $A B: y = k_1(x + 1)$ 得

      $ P(4, 5k_1). $

    于是 $C D$ 就是过 $P(4, 5k_1)$、斜率为 $k_2 = (7/3)k_1$ 的直线，它与 $x$ 轴交点的横坐标

    $ X = 4 - (5k_1)/(k_2) = 4 - (5k_1)/((7/3)k_1) = 4 - 15/7 = 13/7, $

    *一行*即得定点 $(13/7, 0)$——省去正文中"先求 $x_C$、$y_C$ 再代 $X = x_C - y_C/k_2$"的大半计算。

    *用法与提醒*：这一路的价值是"*先用配极定出 $A B$ 与 $C D$ 的交点 $P$，再接第 (1) 问的斜率比，定点就只是一次除法*"，凡"已知斜率比（或斜率关系）求定点"的题都可照此办理；但自配极三角形与配极原则超出高中范围，解答题仍请用上面的正解，本 Note 只用于填空选择的速判或结果验算。
  ]

  #rect(stroke: 0.5pt, inset: 8pt, width: 100%)[
    *Note：解法要点*

    - $A B$ 过左焦点 $F_1$、$A C$ 与 $B D$ 过右焦点 $F_2$，于是 $C$、$D$ 的坐标都能写成 $x_i$ 的*分式线性函数*（式 @eq:747611、@eq:747612），这是本题"算得动"的关键；
    - 求 $k_2$ 时分子、分母都含因子 $(x_1 - x_2)$，可直接约去——"对称式相除"是解析几何中免解根的常用手法；
    - *自检*：取 $k = 1$ 时 $k_1/k_2 = 3/7$、$C D$ 与 $x$ 轴交点横坐标 $13/7 approx 1.857$，与另一路数值验证一致；
    - 若把两个焦点互换（$A B$ 过 $F_2$、$A C$ 与 $B D$ 过 $F_1$），结论会改变，可按同一套路重算。
  ]

]
