#import "template.typ": *
#set document(title: [抽象函数])
#template(page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 知识解析

  === 1. 轴对称
  *$ 若 f(b + a x) = f(c - a x), quad (a != 0) \
  则 f(x) "关于" x=(b + c) / 2 "对称" $*

  证明：

  $
    f(b + a x) = f((b+c)/2 +((b-c)/2 + a x))
  $
  $
    f(c - a x) = f((b+c)/2 -((b-c)/2 + a x))
  $

  令 $t=((b-c)/2 + a x)$，则其定义域与$x$相同，为$RR$。
  $ f((b+c)/2 + t) = f((b+c)/2 - t) $
  故 $f(x)$ 关于直线 $x = (b + c) / 2$ 对称。$square$


  === 2. 中心对称

  *$ (b + a x) + f(c - a x) = 2k, (a != 0) \
  则 f(x) "关于" ((b + c) / 2, k) "中心对称" $ *

  证明：
  $ f(b + a x) = f((b + c) / 2 + ((b - c) / 2 + a x)) $

  $ f(c - a x) = f((b + c) / 2 - ((b - c) / 2 + a x)) $

  令 $t = ((b - c) / 2 + a x)$，则其定义域与 $x$ 相同，为 $RR$。

  由 $f(b + a x) + f(c - a x) = 2k$ 得

  $ f((b + c) / 2 + t) + f((b + c) / 2 - t) = 2k $

  即

  $ f((b + c) / 2 + t) = 2k - f((b + c) / 2 - t) $

  故 $f(x)$ 关于点 $((b + c) / 2, k)$ 中心对称。$square$

  === 3.应用样例

  - $f(2x + 1)$为偶函数，令$g(x) = f(2x + 1)$, 则$g(-x) = g(x)$,即$f(1 - 2x) = f(1 + 2x)$,所以$f(x)$关于$x=1$对称。
  - $f(3x - 2)$为奇函数，令$g(x) = f(3x - 2)$，则 $g(x)$ 为奇函数，所以$g(-x) = -g(x)$，即$f(-3x - 2) = -f(3x - 2)$，所以$f(x)$关于$(-2, 0)$对称。

  === 4. 奇偶性与对称性

  - *$f(a x + b)$为偶函数，则其关于$x=b$对称*
  - *$f(a x + c)$为奇函数，则其关于$(c,0)$对称*

  === 5. 周期性
  *$ 若 f(a x + c) = f(a x + d), quad (a != 0) \
  则 f(x) "是周期函数"，T =｜c - d｜ $*

  令
  $ u = a x + c $
  则
  $ x = (u - c) / a $
  所以
  $ a x + d = a · (u - c) / a + d = u - c + d $
  因此已知条件变为
  $ f(u) = f(u - c + d) $
  即
  $ f(u) = f(u + (d - c)) $
  对任意 $u$ 成立。

  令
  $ T = d - c $
  则
  $ f(u) = f(u + T) $
  对任意 $u$ 成立，所以 $f(x)$ 是周期函数，周期为
  $ |T| = |d - c| = |c - d| $

  因此：
  $f(a x + c) = f(a x + d), (a != 0) ==> f(x) "是周期函数，且" T = |c - d|$
  #qed

  *$ 若 f(x)"关于"x=a, x=b"对称" \
  则 f(x) "是周期函数"，T = 2｜a - b｜ $*

  已知 $f(x)$ 关于 $x = a$ 和 $x = b$ 对称，不妨设 $a < b$。

  由关于 $x = a$ 对称，得
  $ f(a + t) = f(a - t) $
  对任意 $t$ 成立。

  由关于 $x = b$ 对称，得
  $ f(b + t) = f(b - t) $
  对任意 $t$ 成立。

  对任意 $x$，令
  $ t = x - a $
  由关于 $x = a$ 对称：
  $ f(x) = f(a + t) = f(a - t) = f(2a - x) $
  即
  $ f(x) = f(2a - x) $

  再由关于 $x = b$ 对称，令
  $ t = 2a - x - b $
  则
  $ f(2a - x) = f(b + t) = f(b - t) $
  而
  $ b - t = b - (2a - x - b) = 2b - 2a + x = x + 2(b - a) $
  所以
  $ f(2a - x) = f(x + 2(b - a)) $

  结合 $f(x) = f(2a - x)$，得
  $ f(x) = f(x + 2(b - a)) $

  即
  $ f(x) = f(x + T), quad T = 2(b - a) $

  由于 $a, b$ 的大小关系未定，取绝对值：
  $ T = 2|a - b| $

  因此：
  $f(x) "关于" x = a "和" x = b "对称" ==> f(x) "是周期函数，且" T = 2|a - b|$
  #qed

  *$ 若 f(x)"关于"(a,0), quad (b,0)"对称" \
  则 f(x) "是周期函数"，T = 2｜a - b｜ $*
  \
  *$ 若 f(x)"关于"x = a, quad (b,0)"对称" \
  则 f(x) "是周期函数"，T = 4｜a - b｜ $*

  === 练习
  1. $f(2x + 1) = f(3 - 2x)$
  2. $f(2x - 1) = f(2x + 3)$
  3. $f(2x - 1) + f(3 - 2x) = 0$
  4. $f(3x + 1) + f(-3x + 3) = 4$
  5. $f(2x + 1)$为奇函数
  6. $f(3x - 2)$为偶函数
  7. $f(2x + 3) - 3$为奇函数
  8. $f(x+1) = - f(x)$
  9. $f(x + 2) = 1/ f(x)$
  10. $f(1+2x) = f(1-2x)$，且$f(x + 1) + f(3 - x) = 2$
  11. $f(3+x) = f(5-x)$，且$f(2 + 3x) = f(2 - 3x)$

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【题型 1】单函数对称推周期

  === 【例 1】（单选）

  *【例 1】* 已知定义在 $RR$ 上的函数 $f(x)$，满足 $f(4x+2)$ 为奇函数且 $f(2x+1)$ 为偶函数，则下列结论一定正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. 函数 $f(x)$ 的周期为 $2$], [B. 函数 $f(x)$ 的周期为 $3$],
    [C. $f(2020) = 0$], [D. $f(2021) = 0$],
  )

  *解析*：

  *① 两个条件分别翻译成"对称中心"和"对称轴"。*

  - $f(4x+2)$ 为奇函数：由"$f(a x + c)$ 为奇函数则关于 $(c,0)$ 对称"，得 $f(x)$ 关于点 $(2,0)$ 中心对称，即

  $ f(2+t) = -f(2-t) quad (t in RR) $

  - $f(2x+1)$ 为偶函数：由"$f(a x + b)$ 为偶函数则关于 $x=b$ 对称"，得 $f(x)$ 关于直线 $x = 1$ 对称，即

  $ f(1+t) = f(1-t) quad (t in RR) $

  *② 一个中心、一条轴 ⟹ 周期 $T = 4|2 - 1| = 4$。* 于是 A、B 都错。

  *③ 判断 C、D。* 由中心对称取 $t = 2$：$f(4) = -f(0)$；又由周期 $4$ 得 $f(4) = f(0)$，两式比较得

  $ f(0) = 0 $

  而 $2020 = 4 times 505$，所以

  $ f(2020) = f(0) = 0 $

  *C 正确*。至于 $f(2021) = f(1)$，它并没有被条件确定（例如 $f(x) = sin((pi(x-2))/2)$ 满足条件且 $f(2021) = -1 != 0$），故 *D 错误*。

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  === 【练 1.1】（多选）

  *【练 1.1】* 已知函数 $f(x)$ 定义域为 $RR$，且 $f(-x) = -f(x)$，$f(2-x) = f(x)$，$f(1) = 1$，则（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $f(x)$ 的图象关于直线 $x = 2$ 对称], [B. $f(6) = 0$],
    [C. $f(x)$ 的图象关于点 $(-2,0)$ 中心对称], [D. $f(2x-1)$ 为偶函数],
  )

  *解析*：

  - $f(-x) = -f(x)$：$f(x)$ 为奇函数，图象关于点 $(0,0)$ 中心对称；
  - $f(2-x) = f(x)$：即 $f(1+t) = f(1-t)$，图象关于直线 $x = 1$ 对称。

  一个中心、一条轴 ⟹ 周期 $T = 4|0 - 1| = 4$。

  - *A*：对称轴是 $x = 1$ 而不是 $x = 2$，*错误*；
  - *B*：$f(6) = f(6-4) = f(2)$；由轴 $x=1$ 得 $f(2) = f(0)$，由奇函数得 $f(0) = 0$，故 $f(6) = 0$，*正确*；
  - *C*：由 $f(2-x) = f(x)$ 与 $f(-x) = -f(x)$ 可得 $f(2+t) = -f(2-t)$，即 $(2,0)$ 也是对称中心；再由周期 $4$ 得 $(-2,0)$ 也是对称中心，*正确*；
  - *D*：$f(2x-1)$ 为偶函数 $<==>$ $f(-1+t) = f(-1-t)$，即 $f(x)$ 关于 $x = -1$ 对称。由中心 $(0,0)$ 与轴 $x = 1$ 可推出轴为一切奇数（$... , -1, 1, 3, ...$），故 *正确*。

  故选 *#text(fill: rgb("#c7362e"))[BCD]*。

  === 【练 1.2】（单选）

  *【练 1.2】* 已知函数 $f(x)$ 是定义在 $RR$ 上的奇函数，$f(1-2x)$ 为偶函数，且 $f(-1) = 1$，则

  $ |f(-10)| + |f(-9)| + dots + |f(0)| + |f(1)| + dots + |f(9)| + |f(10)| = $（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $10$], [B. $20$], [C. $15$], [D. $5$],
  )

  *解析*：

  - $f(x)$ 为奇函数 ⟹ 中心 $(0,0)$；
  - $f(1-2x)$ 为偶函数 ⟹ $f(1-u) = f(1+u)$，即轴 $x = 1$。

  于是周期 $T = 4|1 - 0| = 4$。再求一个周期内的函数值：

  - $f(0) = 0$（奇函数在原点）；
  - $f(1) = -f(-1) = -1$（奇函数）；
  - $f(2) = f(0) = 0$（由轴 $x = 1$：$f(2) = f(1+1) = f(1-1) = f(0)$）；
  - $f(3) = f(-1) = 1$（周期 $4$）。

  所以 $|f(n)|$ 随 $n$ 的取值是：$n -= 0, 2 quad ("mod" 4)$ 时为 $0$；$n$ 为奇数时为 $1$。

  在 $-10$ 到 $10$ 的 $21$ 个整数中，奇数有 $-9, -7, -5, -3, -1, 1, 3, 5, 7, 9$ 共 $10$ 个，故原式

  $ = 10 times 1 = 10 $

  故选 *#text(fill: rgb("#c7362e"))[A]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【题型 2】单函数结合导数

  === 【练 2】（单选）

  *【练 2】* 已知函数 $f(x)$ 及其导函数 $f'(x)$ 的定义域均为 $RR$，且 $f(x-1)$ 为奇函数，$f'(2-x) + f'(x) = 2$，$f'(-1) = 2$，则

  $ sum_(i=1)^25 f'(2i-1) = $（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr, 1fr, 1fr),
    gutter: 4pt,
    [A. $13$], [B. $16$], [C. $25$], [D. $51$],
  )

  *解析*：

  *① 先把 $f(x-1)$ 为奇函数"求导"。* $f(x-1)$ 为奇函数给出

  $ f(-1+t) = -f(-1-t) $

  即 $f(x)$ 关于点 $(-1,0)$ 中心对称。两边对 $t$ 求导：

  $ f'(-1+t) = f'(-1-t) $

  即 *$f'(x)$ 关于直线 $x = -1$ 对称*。

  *② 再翻译第二个条件。* 把 $f'(2-x) + f'(x) = 2$ 中的 $x$ 换成 $1+t$：

  $ f'(1+t) + f'(1-t) = 2 $

  即 *$f'(x)$ 关于点 $(1,1)$ 中心对称*。

  *③ 一条轴 + 一个中心 ⟹ 周期 $T = 4|1 - (-1)| = 8$。* 于是 $f'(x+8) = f'(x)$。

  *④ 算一个周期内的四个值*（自变量取 $1, 3, 5, 7$）：

  - $f'(1)$：在 $f'(1+t) + f'(1-t) = 2$ 中取 $t = 0$，得 $2f'(1) = 2$，故 $f'(1) = 1$；
  - $f'(3)$：取 $t = 2$，得 $f'(3) + f'(-1) = 2$，而 $f'(-1) = 2$，故 $f'(3) = 0$；
  - $f'(5) = f'(5-8) = f'(-3)$，由轴 $x = -1$（取 $t = -2$：$f'(-3) = f'(1)$）得 $f'(5) = 1$；
  - $f'(7) = f'(7-8) = f'(-1) = 2$。

  所以每连续 $4$ 项（$f'(1), f'(3), f'(5), f'(7)$）之和为 $1 + 0 + 1 + 2 = 4$。

  *⑤ 求和。* $i = 1$ 到 $25$ 给出 $25$ 项，其中前 $24$ 项正好是 $6$ 个完整周期：

  $ sum_(i=1)^25 f'(2i-1) = 6 times 4 + f'(49) = 24 + f'(49) $

  而 $49 -= 1 quad ("mod" 8)$，故 $f'(49) = f'(1) = 1$，于是和为 $25$。

  故选 *#text(fill: rgb("#c7362e"))[C]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【题型 3】双函数

  === 【例 3】（多选）

  *【例 3】* 已知函数 $f(x)$、$g(x)$ 的定义域均为 $RR$，$f(x)$ 为偶函数，且 $f(x) + g(2-x) = 1$，$g(x) - f(x-4) = 3$，下列说法正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. 函数 $g(x)$ 的图象关于 $x = 1$ 对称], [B. 函数 $f(x)$ 的图象关于 $(-1,-1)$ 对称],
    [C. 函数 $f(x)$ 是以 $4$ 为周期的周期函数], [D. 函数 $g(x)$ 是以 $6$ 为周期的周期函数],
  )

  *解析*：

  *① 消去 $g$，只留 $f$。* 由 $f(x) + g(2-x) = 1$ 得 $g(2-x) = 1 - f(x)$，即 $g(t) = 1 - f(2-t)$。代入 $g(x) - f(x-4) = 3$：

  $ 1 - f(2-x) - f(x-4) = 3 quad => quad f(2-x) + f(x-4) = -2 $

  又 $f$ 为偶函数，$f(2-x) = f(x-2)$，故

  $ f(x-2) + f(x-4) = -2 quad => quad f(t) + f(t-2) = -2 quad (t in RR) $

  *② 判断 C。* 把上式中的 $t$ 换成 $t+2$，得 $f(t+2) + f(t) = -2$；与 $f(t) + f(t-2) = -2$ 比较，得 $f(t+2) = f(t-2)$，即

  $ f(t+4) = f(t) $

  所以 $f(x)$ 是周期为 $4$ 的周期函数，*C 正确*。

  *③ 判断 B。* 在 $f(t) + f(t-2) = -2$ 中取 $t = -1+s$：

  $ f(-1+s) + f(-3+s) = -2 $

  而由偶函数 $f(-3+s) = f(3-s)$，又由周期 $4$：$f(3-s) = f(-1-s)$，故

  $ f(-1+s) + f(-1-s) = -2 $

  即 $f(x)$ 的图象关于点 $(-1,-1)$ 中心对称，*B 正确*。

  *④ 判断 A、D。* 由 $g(t) = 1 - f(2-t)$ 与 $f$ 的周期 $4$ 可得 $g(t+4) = g(t)$（因为 $f(-2-t) = f(2+t)$ 可由 $f(t) + f(t-2) = -2$ 与 $f$ 为偶函数推出），所以 $g(x)$ 的周期为 $4$ 而不是 $6$，*D 错误*。

  再检验 A：$g(1+s) = 1 - f(1-s)$，$g(1-s) = 1 - f(1+s)$，二者相等要求 $f(1-s) = f(1+s)$，即 $f$ 还关于 $x = 1$ 对称，而这并未被条件保证（例如 $f(x) = -1 + cos((pi x)/2)$、$g(x) = 2 + cos((pi x)/2)$ 满足全部条件，但对 $s = 1$：$g(2) = 1$、$g(0) = 3$，并不关于 $x = 1$ 对称），*A 错误*。

  故选 *#text(fill: rgb("#c7362e"))[BC]*。

  === 【练 3.1】（多选）

  *【练 3.1】* 定义在 $RR$ 上的函数 $f(x)$ 与 $g(x)$ 满足 $f(1+x) + f(1-x) = 0$，$f(x+3) + g(x) = 2$，$f(x) + g(1-x) = 2$，则说法正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $f(x)$ 为奇函数], [B. 函数 $f(x)$ 为周期是 $4$ 的周期函数],
    [C. $f(1) + f(2) + f(3) + f(4) = 0$], [D. $sum_(n=1)^20 g(n) = 40$],
  )

  *解析*：

  *① 消去 $g$。* 由 $f(x+3) + g(x) = 2$ 得 $g(x) = 2 - f(x+3)$；由 $f(x) + g(1-x) = 2$ 得 $g(1-x) = 2 - f(x)$，即 $g(t) = 2 - f(1-t)$。两式右边相等：

  $ f(x+3) = f(1-x) $

  又由 $f(1+x) + f(1-x) = 0$ 得 $f(1-x) = -f(1+x)$，于是

  $ f(x+3) = -f(1+x) quad => quad f(t+2) = -f(t) $

  由此 $f(t+4) = -f(t+2) = f(t)$，即 *$f(x)$ 的周期为 $4$，B 正确*。

  *② 判断 A。* $f(t+2) = -f(t)$ 只说"隔半个周期变号"，并不保证 $f$ 是奇函数（取 $f(t) = sin((pi(t-1))/2)$ 即满足 $f(t+2) = -f(t)$ 与 $f(1+x) + f(1-x) = 0$，但它不是奇函数），*A 错误*。

  *③ 判断 C。* 由 $f(t+2) = -f(t)$ 得 $f(3) = -f(1)$、$f(4) = -f(2)$，于是

  $ f(1) + f(2) + f(3) + f(4) = f(1) + f(2) - f(1) - f(2) = 0 $

  *C 正确*。

  *④ 判断 D。* 由 $g(x) = 2 - f(x+3)$：

  $ sum_(n=1)^20 g(n) = 40 - sum_(n=1)^20 f(n+3) = 40 - sum_(m=4)^23 f(m) $

  而 $f$ 以 $4$ 为周期，连续 $4$ 个整数处的函数值之和都等于 $f(1) + f(2) + f(3) + f(4) = 0$，故 $sum_(m=4)^23 f(m) = 5 times 0 = 0$，从而原和为 $40$，*D 正确*。

  故选 *#text(fill: rgb("#c7362e"))[BCD]*。

  === 【练 3.3】（多选）

  *【练 3.3】* 已知函数 $f(x)$、$g(x)$ 的定义域均为 $RR$，且 $f(x) - g(2-x) = -5$，$g(x) + f(x+2) = 3$。若 $f(x)$ 的图象关于直线 $x = 1$ 对称，且 $f(3) = -3$，则下列说法中正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $g(1) = 6$], [B. $g(x)$ 的图象关于点 $(0,4)$ 对称],
    [C. $g(x)$ 是周期函数，且最小正周期为 $8$], [D. $sum_(k=1)^22 g(k) = 90$],
  )

  *解析*：

  *① 消去 $g$。* 由 $f(x) - g(2-x) = -5$ 得 $g(2-x) = f(x) + 5$，即 $g(t) = f(2-t) + 5$；由 $g(x) + f(x+2) = 3$ 得 $g(x) = 3 - f(x+2)$。两式相等：

  $ f(2-x) + 5 = 3 - f(x+2) quad => quad f(2-x) + f(x+2) = -2 $

  由对称轴 $x = 1$ 得 $f(2-x) = f(x)$，又 $f(x+2) = f(-x)$，所以

  $ f(x) + f(-x) = -2 $

  即 $f(x)$ 关于点 $(0,-1)$ 中心对称。

  *② 周期。* 一条对称轴 $x = 1$ 加一个对称中心 $(0,-1)$，得 $f$ 的周期

  $ T = 4|1 - 0| = 4 $

  于是 $g(t) = f(2-t) + 5$ 也以 $4$ 为周期，故 C 说"最小正周期为 $8$"错误，*C 错误*。

  *③ 判断 A。* $g(1) = f(1) + 5$。由中心 $(0,-1)$ 取 $t = 1$：$f(1) + f(-1) = -2$；又 $f(-1) = f(-1+4) = f(3) = -3$，故 $f(1) = 1$，

  $ g(1) = 1 + 5 = 6 $

  *A 正确*。

  *④ 判断 B。* $g(t) + g(-t) = [f(2-t) + 5] + [f(2+t) + 5] = [f(2-t) + f(2+t)] + 10 = -2 + 10 = 8$，即

  $ g(t) + g(-t) = 8 $

  所以 $g(x)$ 的图象关于点 $(0,4)$ 中心对称，*B 正确*。

  *⑤ 判断 D。* 先求一个周期上的函数值：由中心 $(0,-1)$ 取 $t = 0$ 得 $f(0) = -1$；由轴 $x = 1$ 得 $f(2) = f(0) = -1$；由周期 $4$ 得 $f(4) = f(0) = -1$；又 $f(1) = 1$、$f(3) = -3$。

  于是

  $ sum_(k=1)^22 g(k) = sum_(k=1)^22 f(2-k) + 5 times 22 = sum_(k=1)^22 f(2-k) + 110 $

  其中 $2-k$（$k = 1, 2, dots, 22$）取遍 $-20, -19, dots, 1$ 这 $22$ 个整数。它含 $5$ 个完整周期（共 $20$ 个数，其函数值之和为 $5 times [f(1) + f(2) + f(3) + f(4)] = 5 times [1 + (-1) + (-3) + (-1)] = -20$），剩下 $f(0) + f(1) = -1 + 1 = 0$。所以

  $ sum_(k=1)^22 g(k) = -20 + 110 = 90 $

  *D 正确*。

  故选 *#text(fill: rgb("#c7362e"))[ABD]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【题型 4】双函数结合导数

  === 【例 4】（多选）

  *【例 4】* 设定义在 $RR$ 上的函数 $f(x)$ 与 $g(x)$ 的导函数分别为 $f'(x)$ 和 $g'(x)$，若 $g(x) - f(3-x) = 2$，$f'(x) = g'(x-1)$，且 $g(x+2)$ 为奇函数，则下列说法中一定正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. 函数 $g(x)$ 的图象关于 $x = 1$ 对称], [B. $f(x)$ 的周期为 $4$],
    [C. $sum_(k=1)^2023 g(k) = 0$], [D. $f(2) + f(4) = 4$],
  )

  *解析*：

  *① 把"$g(x+2)$ 为奇函数"翻译成对称中心。* 即 $g(-x+2) = -g(x+2)$，也就是

  $ g(2+u) = -g(2-u) $

  所以 $g(x)$ 的图象关于点 $(2,0)$ 中心对称。

  *② 消去 $f$。* 由 $f'(x) = g'(x-1)$ 两边积分得 $f(x) = g(x-1) + C$（$C$ 为常数）。代入 $g(x) - f(3-x) = 2$：

  $ g(x) - g(2-x) - C = 2 $

  再用 $g(2-x) = -g(2+x)$：

  $ g(x) + g(2+x) = 2 + C $

  记 $2 + C = K$，即对一切 $x$ 有 $g(x) + g(x+2) = K$。把 $x$ 换成 $x+2$ 得 $g(x+2) + g(x+4) = K$，两式相减：

  $ g(x+4) = g(x) $

  故 $g$ 以 $4$ 为周期；由 $f(x) = g(x-1) + C$ 知 $f$ 也以 $4$ 为周期，*B 正确*。

  *③ 判断 A。* 由中心 $g(2+u) = -g(2-u)$ 得 $g(1+t) = -g(3-t)$、$g(1-t) = -g(3+t)$；又由 $g(u) + g(u+2) = K$ 得 $g(1-t) + g(3-t) = K$、$g(1+t) + g(3+t) = K$。把前两式代入后两式：

  $ g(1-t) - g(1+t) = K, quad g(1+t) - g(1-t) = K $

  两式相加得 $2K = 0$，即 $K = 0$，于是 $g(1-t) = g(1+t)$——$g(x)$ 的图象关于直线 $x = 1$ 对称，*A 正确*。（顺便得到 $C = -2$，即 $f(x) = g(x-1) - 2$。）

  *④ 判断 C。* 由 $K = 0$ 得 $g(u) + g(u+2) = 0$，于是 $g(1) + g(3) = 0$；又由中心对称取 $u = 0$ 得 $g(2) = -g(2)$，即 $g(2) = 0$；由周期 $4$ 得 $g(4) = g(0)$，而中心对称取 $u = 2$ 得 $g(4) = -g(0)$，故 $g(0) = 0$，$g(4) = 0$。所以每个周期上的和为

  $ g(1) + g(2) + g(3) + g(4) = 0 $

  而 $2023 = 4 times 505 + 3$，故

  $ sum_(k=1)^2023 g(k) = 505 times 0 + g(1) + g(2) + g(3) = 0 + 0 = 0 $

  *C 正确*。

  *⑤ 判断 D。* 由 $f(x) = g(x-1) - 2$：

  $ f(2) + f(4) = g(1) + g(3) - 4 = 0 - 4 = -4 != 4 $

  *D 错误*。

  故选 *#text(fill: rgb("#c7362e"))[ABC]*。

  === 【练 4.2】（多选）

  *【练 4.2】* 定义在 $RR$ 上的函数 $f(x)$ 与 $g(x)$ 的导函数分别为 $f'(x)$ 和 $g'(x)$，若 $g(x) - f(3-x) = 2$，$f'(x) = g'(x-1)$，且 $g(-x+2) = -g(x+2)$，则下列说法正确的是（#h(0.8em)）

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $g(x+2)$ 为偶函数], [B. $f'(x+2)$ 为奇函数],
    [C. 函数 $f(x)$ 为周期函数], [D. $sum_(k=1)^2024 g(k) = 0$],
  )

  *解析*：本题三个条件与例 4 完全相同（$g(-x+2) = -g(x+2)$ 就是"$g(x+2)$ 为奇函数"），所以例 4 的结论全部可用：

  - $g(x)$ 关于点 $(2,0)$ 中心对称，关于直线 $x = 1$ 对称，且以 $4$ 为周期；
  - $f(x) = g(x-1) - 2$，也以 $4$ 为周期。

  - *A*：$g(x+2)$ 是*奇*函数（已知条件），不是偶函数；$g$ 的对称中心在 $(2,0)$、对称轴在 $x = 1$，故 $g(x+2)$ 的对称中心在 $(0,0)$，*A 错误*；
  - *B*：由 $f(x) = g(x-1) - 2$ 得 $f'(x) = g'(x-1)$，于是 $f'(x+2) = g'(x+1)$。因为 $g$ 关于直线 $x = 1$ 对称，对其求导得 $g'(1+t) = -g'(1-t)$（对称轴处导数反号），所以 $g'(x+1)$ 是奇函数，即 $f'(x+2)$ 为奇函数，*B 正确*；
  - *C*：$f(x) = g(x-1) - 2$，而 $g$ 以 $4$ 为周期，故 $f$ 也以 $4$ 为周期，*C 正确*；
  - *D*：每个周期上的和为 $g(1) + g(2) + g(3) + g(4) = 0$（同例 4），而 $2024 = 4 times 506$，故 $sum_(k=1)^2024 g(k) = 506 times 0 = 0$，*D 正确*。

  故选 *#text(fill: rgb("#c7362e"))[BCD]*。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 方法总结

  - *先把"复合形式的奇偶性"翻译成对称性*：$f(a x + b)$ 为偶函数 $<==>$ 关于 $x = b$ 对称；$f(a x + c)$ 为奇函数 $<==>$ 关于 $(c,0)$ 中心对称（本节知识解析中的结论）。
  - *看"对称性组合"定周期*：两条轴 $T = 2|a-b|$；两个中心 $T = 2|a-b|$；一轴一中心 $T = 4|a-b|$。这是本类题的核心工具。
  - *注意对称中心的纵坐标*：$f(x) + f(2a-x) = 2k$ 的中心是 $(a,k)$；由"中心 + 周期"可以求出某些函数值（如中心在 $(2,0)$ 时 $f(2) = 0$、中心在 $(0,-1)$ 时 $f(0) = -1$）。
  - *双函数题先"消元"*：把 $g$ 用 $f$ 表示（或反过来）代入另一式，尽量化成一个函数的等式，再套用上面的规律；求和时先算出一个周期内的和，再用"整周期 + 余项"处理。
  - *含导数的条件优先"求导翻译"*：中心对称求导得到对称轴（如 $f(-1+t) = -f(-1-t) ==> f'(-1+t) = f'(-1-t)$），对称轴求导得到中心（$g(1+t) = g(1-t) ==> g'(1+t) = -g'(1-t)$）；积分则带来一个常数 $C$，常常要靠"中心纵坐标"把它定出来。
  - *"一定正确"要能证明，"错误"举反例即可*：多选题里对拿不准的选项，构造一个满足全部条件的初等函数（如 $sin((pi x)/2)$、$cos((pi x)/2)$ 这类周期为 $4$ 的模型）代入检验，既快又可靠。
]
