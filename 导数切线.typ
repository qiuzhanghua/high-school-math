#import "template.typ": *

#set document(title: [导数切线])

#template(heading2-align: left, page-footer: "第 1 页")[
  #set math.equation(numbering: "(1)")

  == 【例 1】求曲线在某点处的切线方程

  *【例 1】* 函数 $f(x) = x ln x + 2x^3$，则曲线 $y = f(x)$ 在点 $(1, 2)$ 处的切线方程是 #underline[#h(5em)]。

  === 解析

  函数的定义域为 $(0, +oo)$（因为含有 $ln x$）。求导：

  $ f'(x) = (x)' ln x + x (ln x)' + (2x^3)' = ln x + 1 + 6x^2 $

  先验证已知点是否在曲线上（即它是不是切点）：

  $ f(1) = 1 dot ln 1 + 2 dot 1^3 = 0 + 2 = 2 $

  与题设的纵坐标一致，说明点 $(1, 2)$ 在曲线上，它就是切点。

  于是切线的斜率为

  $ k = f'(1) = ln 1 + 1 + 6 dot 1^2 = 7 $

  由点斜式得切线方程

  $ y - 2 = 7 (x - 1) quad 即 quad y = 7x - 5 $

  化为一般式：$7x - y - 5 = 0$。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 2】切线条件与基本不等式求最值

  *【例 2】* 已知函数 $f(x) = a x^2 + b x$（$a > 0$，$b > 0$）在点 $(1, f(1))$ 处的切线斜率为 2，则 $(8a + b)/(a b)$ 的最小值是 #underline[#h(5em)]。

  === 解析

  先用切线斜率得到 $a$、$b$ 的关系。求导：

  $ f'(x) = 2a x + b $

  所以在 $x = 1$ 处的切线斜率为

  $ f'(1) = 2a + b = 2 $

  又已知 $a > 0$、$b > 0$，于是问题化为：在约束

  $ 2a + b = 2 quad (a > 0, b > 0) $

  下，求 $(8a + b)/(a b)$ 的最小值。

  先把目标式化简：

  $ (8a + b)/(a b) = (8a)/(a b) + b/(a b) = 8/b + 1/a $

  再用"乘 1 法"（把 $1$ 写成 $(2a + b)/2$）：

  $ 1/a + 8/b = (1/a + 8/b) dot (2a + b)/2 = 1/2 (2 + b/a + 16a/b + 8) = 1/2 (10 + b/a + 16a/b) $

  由基本不等式

  $ b/a + 16a/b >= 2 sqrt(b/a dot 16a/b) = 8 $

  得

  $ (8a + b)/(a b) >= 1/2 (10 + 8) = 9 $

  等号成立当且仅当 $b/a = 16a/b$，即 $b = 4a$（$a, b > 0$）；与 $2a + b = 2$ 联立解得 $a = 1/3$，$b = 4/3$，符合条件。

  所以 $(8a + b)/(a b)$ 的最小值为 9。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 3】已知切线过定点，求切线方程

  *【例 3】* 已知函数 $f(x) = x^3 - 3x$，直线 $l: y = k x + 16$ 是曲线 $y = f(x)$ 的一条切线，求直线 $l$ 的方程。

  === 解析

  注意直线 $l: y = k x + 16$ 恒过点 $(0, 16)$，而 $f(0) = 0 != 16$，即点 $(0, 16)$ 不在曲线上，所以应设切点来求解。

  设切点为 $(t, f(t))$，其中 $f(t) = t^3 - 3t$。求导：

  $ f'(x) = 3x^2 - 3 $

  故切线斜率 $k = f'(t) = 3t^2 - 3$，切线方程为

  $ y - (t^3 - 3t) = (3t^2 - 3)(x - t) $

  整理得

  $ y = (3t^2 - 3) x - 2t^3 $

  也就是说，切点横坐标为 $t$ 时，切线在 $y$ 轴上的截距为 $-2t^3$。由题意该截距等于 16：

  $ -2t^3 = 16 quad => quad t^3 = -8 quad => quad t = -2 $

  于是切点为 $(-2, -2)$，斜率为

  $ k = f'(-2) = 3 dot (-2)^2 - 3 = 9 $

  所以直线 $l$ 的方程为

  $ y = 9x + 16 $

  检验：把 $y = 9x + 16$ 代入 $y = x^3 - 3x$，得

  $ x^3 - 12x - 16 = 0 quad 即 quad (x + 2)^2 (x - 4) = 0 $

  $x = -2$ 是二重根，说明直线与曲线在 $x = -2$ 处相切，与所求一致。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 4】求两曲线的公切线

  *【例 4】* 曲线 $y = 2 sqrt(x)$ 与 $y = 2 + ln x$ 的公切线方程为 #underline[#h(5em)]。

  === 解析

  设公切线与曲线 $y = 2 sqrt(x)$ 切于点 $(a, 2 sqrt(a))$，与曲线 $y = 2 + ln x$ 切于点 $(b, 2 + ln b)$（$a > 0$，$b > 0$）。

  两条曲线的导数分别为

  $ (2 sqrt(x))' = 1/sqrt(x), quad (2 + ln x)' = 1/x $

  于是两条曲线在各自切点处的切线方程分别为

  $ y - 2 sqrt(a) = 1/sqrt(a) (x - a) quad 即 quad y = x/sqrt(a) + sqrt(a) $

  $ y - (2 + ln b) = 1/b (x - b) quad 即 quad y = x/b + 1 + ln b $

  它们是同一条直线，故斜率与纵截距分别相等：

  $ 1/sqrt(a) = 1/b, quad sqrt(a) = 1 + ln b $

  由第一式得 $b = sqrt(a)$，代入第二式得

  $ b = 1 + ln b quad 即 quad ln b = b - 1 $

  记 $h(b) = ln b - b + 1$（$b > 0$），则

  $ h'(b) = 1/b - 1 = (1 - b)/b $

  所以 $h(b)$ 在 $(0, 1)$ 上单调递增、在 $(1, +oo)$ 上单调递减，而 $h(1) = 0$，故 $h(b) <= 0$，等号仅在 $b = 1$ 时成立。因此 $b = 1$，从而 $a = b^2 = 1$。

  于是公切线的斜率为 $1/b = 1$，纵截距为 $1 + ln 1 = 1$，即

  $ y = x + 1 $

  检验：把 $y = x + 1$ 分别代入两曲线方程。

  - 与 $y = 2 sqrt(x)$：$x + 1 = 2 sqrt(x)$，即 $(sqrt(x) - 1)^2 = 0$，$x = 1$ 为二重根；
  - 与 $y = 2 + ln x$：$x + 1 = 2 + ln x$，即 $ln x = x - 1$，由 $ln x <= x - 1$（等号仅在 $x = 1$ 时成立）知 $x = 1$ 处相切。

  两处都是"相切"，故 $y = x + 1$ 确为公切线。

  （事实上两曲线都经过点 $(1, 2)$，且在该点处的切线斜率都为 1，即两条曲线在 $(1, 2)$ 处彼此相切，公切线正是这条切线。）

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 5】过定点的切线有两条，求参数范围

  *【例 5】* 若过 $A(m, m)$ 与曲线 $f(x) = x ln x$ 相切的直线有两条，则实数 $m$ 的取值范围是 #underline[#h(5em)]。

  === 解析

  函数的定义域为 $(0, +oo)$，且

  $ f'(x) = ln x + 1 $

  设切点为 $(t, t ln t)$（$t > 0$），该点处的切线为

  $ y - t ln t = (ln t + 1)(x - t) quad 即 quad y = (ln t + 1) x - t $

  切线过点 $A(m, m)$，把 $x = y = m$ 代入得

  $ m = (ln t + 1) m - t quad => quad m ln t = t $

  （$t = 1$ 时上式化为 $0 = 1$，不成立，故 $t != 1$；$m = 0$ 时化为 $0 = t$，也无正根。）当 $m != 0$ 时，把 $m ln t = t$ 两边同除以 $m t$，得

  $ 1/m = (ln t)/t $

  这样，"过 $A(m, m)$ 的切线有两条"就等价于"方程 $1/m = (ln t)/t$ 有两个正根"（$t = 1$ 时右边为 $0$，而 $1/m != 0$，故 $t = 1$ 自动舍去，不必再单独讨论）。

  令

  $ g(t) = (ln t)/t quad (t > 0) $

  求导：

  $ g'(t) = (1 - ln t)/t^2 $

  所以 $g(t)$ 在 $(0, e)$ 上单调递增，在 $(e, +oo)$ 上单调递减，图象是"单峰"的，在 $t = e$ 处取得最大值

  $ g(e) = (ln e)/e = 1/e $

  又

  $ lim_(t -> 0^+) g(t) = -oo, quad lim_(t -> +oo) g(t) = 0, quad g(1) = 0 $

  于是函数 $g(t)$ 的取值情况为：

  - 在 $(0, e]$ 上，$g(t)$ 从 $-oo$ 递增到 $1/e$，值域为 $(-oo, 1/e]$，每个值恰取到一次；
  - 在 $[e, +oo)$ 上，$g(t)$ 从 $1/e$ 递减到 $0$（取不到 $0$），值域为 $(0, 1/e]$，每个值恰取到一次。

  所以只需比较 $1/m$ 与最大值 $1/e$ 的大小，即可数出根的个数：

  - $m < 0$（$1/m < 0$）：直线 $y = 1/m$ 只与图象在 $(0, 1)$ 上相交，1 个根，共 1 条切线；
  - $0 < m < e$（$1/m > 1/e$，超过最大值）：无交点，没有切线；
  - $m = e$（$1/m = 1/e$）：只有 $t = e$ 一个根，共 1 条切线；
  - $m > e$（$0 < 1/m < 1/e$）：在 $(1, e)$ 上有 1 个根，在 $(e, +oo)$ 上还有 1 个根，共 2 条切线。

  （$m = 0$ 时方程化为 $0 = t$，无正根，没有切线。）

  所以当过 $A(m, m)$ 的切线有两条时，$m$ 的取值范围为

  $ (e, +oo) $

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【练 5】过定点的切线有三条，求参数范围

  *【练 5】* 已知函数 $f(x) = (x + 1) e^x$，过点 $M(1, t)$ 可作 3 条与曲线 $y = f(x)$ 相切的直线，则实数 $t$ 的取值范围是 #underline[#h(5em)]。

  === 解析

  求导：

  $ f'(x) = e^x + (x + 1) e^x = (x + 2) e^x $

  设切点为 $(u, (u + 1) e^u)$，该点处的切线为

  $ y - (u + 1) e^u = (u + 2) e^u (x - u) $

  切线过点 $M(1, t)$，把 $x = 1$、$y = t$ 代入并化简：

  $ t = (u + 1) e^u + (u + 2) e^u (1 - u) = e^u [(u + 1) + (u + 2)(1 - u)] = (3 - u^2) e^u $

  所以"过 $M(1, t)$ 的切线有 3 条"等价于"关于 $u$ 的方程 $t = (3 - u^2) e^u$ 有 3 个实根"。

  令

  $ F(u) = (3 - u^2) e^u $

  求导：

  $ F'(u) = -e^u (u + 3)(u - 1) $

  所以 $F(u)$ 在 $(-oo, -3)$ 上单调递减，在 $(-3, 1)$ 上单调递增，在 $(1, +oo)$ 上单调递减，两个极值为 $F(-3) = -6 e^(-3) = -6/e^3$（极小值）与 $F(1) = (3 - 1) e = 2e$（极大值）。

  又

  $ lim_(u -> -oo) F(u) = 0^-, quad lim_(u -> +oo) F(u) = -oo $

  （当 $u -> -oo$ 时 $3 - u^2 -> -oo$ 而 $e^u -> 0^+$，乘积趋于 $0^-$。）

  于是 $F(u)$ 在三段上的值域分别为：

  - $u in (-oo, -3)$：$F(u) in (-6/e^3, 0)$；
  - $u in (-3, 1)$：$F(u) in (-6/e^3, 2e)$；
  - $u in (1, +oo)$：$F(u) in (-oo, 2e)$。

  要使方程有 3 个实根，直线 $y = t$ 必须与图象在三段上*各交一次*，即 $t$ 应同时属于这三段的值域 $(-6/e^3, 0)$、$(-6/e^3, 2e)$、$(-oo, 2e)$，取交集得 $t in (-6/e^3, 0)$。

  所以 $t$ 的取值范围为

  $ (-6/e^3, 0) $

  验证边界：$t = 0$ 时只有 $u = plus.minus sqrt(3)$ 两个根（2 条切线）；$t = -6/e^3$ 时只有 $u = -3$ 以及 $(1, +oo)$ 上的 1 个根（2 条切线），均不符合"3 条"。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 6】三条切线的条件判断（含选项辨析）

  *【例 6】* 已知 $a > 0$，若过点 $(a, b)$ 可以作曲线 $y = x^3$ 的三条切线，则下列结论错误的是（#h(0.8em)）(多选题)

  #grid(
    columns: (1fr, 1fr),
    gutter: 4pt,
    [A. $b > a^3$], [B. $b (b - a^3) = 0$],
    [C. $b < 0$], [D. $0 < b < a^3$],
  )

  === 解析

  设切点为 $(u, u^3)$。因为 $y' = 3x^2$，所以该点处的切线为

  $ y - u^3 = 3u^2 (x - u) quad 即 quad y = 3u^2 x - 2u^3 $

  把点 $(a, b)$ 代入，得

  $ b = 3u^2 a - 2u^3 quad 即 quad 2u^3 - 3a u^2 + b = 0 $

  于是"过 $(a, b)$ 可以作三条切线"等价于"关于 $u$ 的方程 $2u^3 - 3a u^2 + b = 0$ 有三个不同实根"。

  令

  $ h(u) = 2u^3 - 3a u^2 quad (a > 0) $

  则方程即 $h(u) = -b$。求导：

  $ h'(u) = 6u (u - a) $

  所以 $h(u)$ 在 $(-oo, 0)$ 上单调递增，在 $(0, a)$ 上单调递减，在 $(a, +oo)$ 上单调递增，两个极值为 $h(0) = 0$（极大值）与 $h(a) = -a^3$（极小值）；又 $u -> -oo$ 时 $h(u) -> -oo$，$u -> +oo$ 时 $h(u) -> +oo$。

  于是三段的值域分别为：

  - $u in (-oo, 0)$：$h(u) in (-oo, 0)$；
  - $u in (0, a)$：$h(u) in (-a^3, 0)$；
  - $u in (a, +oo)$：$h(u) in [-a^3, +oo)$。

  要让水平线 $y = -b$ 与图象在三段上*各交一次*，需

  $ -a^3 < -b < 0 quad 即 quad 0 < b < a^3 $

  所以过 $(a, b)$ 能作三条切线的充要条件是 $0 < b < a^3$。

  据此逐项判断：

  - A：$b > a^3$ 时只有 1 条切线（$b = a^3$ 时也只有 2 条），与"三条切线"矛盾，A 错误；
  - B：$b(b - a^3) = 0$ 即 $b = 0$ 或 $b = a^3$，这两种情形都只有 2 条切线，B 错误；
  - C：$b < 0$ 时只有 1 条切线，C 错误；
  - D：$0 < b < a^3$ 正是三条切线的充要条件，D 正确。

  因此答案为A、B、C。

  #line(length: 100%, stroke: 0.5pt + gray)

  == 【例 7】曲线上的点到直线的最小距离（数形结合）

  *【例 7】* 点 $P$ 是曲线 $y = x^2 - ln x$ 上任意一点，则点 $P$ 到直线 $y = x - 2$ 的最小距离为 #underline[#h(5em)]。

  === 解析（数形结合）

  *第一步：看位置关系。* 直线 $y = x - 2$ 的斜率为 1。设

  $ g(x) = (x^2 - ln x) - (x - 2) = x^2 - x + 2 - ln x quad (x > 0) $

  则

  $ g'(x) = 2x - 1 - 1/x = ((2x + 1)(x - 1))/x $

  所以 $g(x)$ 在 $(0, 1)$ 上单调递减、在 $(1, +oo)$ 上单调递增，最小值为 $g(1) = 1 - 1 + 2 - 0 = 2 > 0$。即曲线始终在直线 $y = x - 2$ 的*上方*，两者没有公共点。

  *第二步：把"最小距离"转化为"平行的切线"。* 由图象可见（数形结合）：曲线上到直线距离最小的点，正是曲线在该点处的切线与已知直线*平行*的点——若切线与直线不平行，沿曲线朝直线一侧微移就能使距离变小。又

  $ y'' = 2 + 1/x^2 > 0 $

  说明曲线下凸，$y' = 2x - 1/x$ 单调递增，所以"切线斜率为 1"的点是唯一的。

  #v(0.3em)

  #align(center)[
    #block(breakable: false, width: 235pt, height: 195pt)[
      #let u = 50pt
      #let X(x) = 60pt + x * u
      #let Y(y) = 110pt - y * u
      #let seg(a, b, stroke: 0.8pt) = place(dx: a.at(0), dy: a.at(1))[
        #line(end: (b.at(0) - a.at(0), b.at(1) - a.at(1)), stroke: stroke)
      ]
      #let dot(p) = place(dx: p.at(0) - 1.3pt, dy: p.at(1) - 1.3pt)[#circle(radius: 1.3pt, fill: black)]
      #seg((38pt, 110pt), (216pt, 110pt))
      #seg((216pt, 110pt), (208pt, 106pt))
      #seg((216pt, 110pt), (208pt, 114pt))
      #seg((60pt, 188pt), (60pt, 12pt))
      #seg((60pt, 12pt), (56pt, 20pt))
      #seg((60pt, 12pt), (64pt, 20pt))
      // 曲线 y = x^2 - ln x
      #let cpts = range(0, 30).map(i => {
        let x = 0.2 + i * 0.05
        (X(x), Y(x * x - calc.ln(x)))
      })
      #place(dx: 0pt, dy: 0pt)[
        #curve(curve.move(cpts.first()), ..cpts.slice(1).map(p => curve.line(p)), stroke: 0.9pt)
      ]
      // 已知直线 y = x - 2 与切线 y = x（虚线）
      #seg((X(0.8), Y(-1.2)), (X(2.3), Y(0.3)))
      #seg((X(0), Y(0)), (X(1.75), Y(1.75)), stroke: (paint: black, thickness: 0.8pt, dash: "dashed"))
      // 垂线段（P 到直线的距离）
      #seg((X(1), Y(1)), (X(2), Y(0)), stroke: 1.2pt)
      #dot((X(1), Y(1)))
      #place(dx: 64pt, dy: 16pt)[$y = x^2 - ln x$]
      #place(dx: 150pt, dy: 22pt)[$y = x$]
      #place(dx: 86pt, dy: 146pt)[$y = x - 2$]
      #place(dx: 92pt, dy: 48pt)[$P(1, 1)$]
      #place(dx: 130pt, dy: 96pt)[$sqrt(2)$]
      #place(dx: 44pt, dy: 113pt)[$O$]
    ]
  ]

  #align(center)[#text(size: 9pt, fill: gray)[示意图（未按比例）]]

  #v(0.2em)

  *第三步：求该点。* 令

  $ y' = 2x - 1/x = 1 quad => quad 2x^2 - x - 1 = 0 quad => quad (2x + 1)(x - 1) = 0 $

  由 $x > 0$ 得 $x = 1$，故切点为 $P(1, 1)$（此时切线为 $y = x$，与 $y = x - 2$ 平行）。

  *第四步：算距离。* 点 $P(1, 1)$ 到直线 $x - y - 2 = 0$ 的距离为

  $ d = (|1 - 1 - 2|)/sqrt(1^2 + (-1)^2) = 2/sqrt(2) = sqrt(2) $

  所以点 $P$ 到直线的最小距离为 $sqrt(2)$。

  *另解（代数验证）：* 设 $P(x, x^2 - ln x)$，则

  $
    d(x) = (|x - (x^2 - ln x) - 2|)/sqrt(2) = (x^2 - x + 2 - ln x)/sqrt(2) = (g(x))/sqrt(2) >= (g(1))/sqrt(2) = 2/sqrt(2) = sqrt(2)
  $

  与几何结论一致。

  #line(length: 100%, stroke: 0.5pt + gray)

  === 方法总结

  - 求"曲线在点 $P$ 处的切线方程"的步骤：① 验证 $P$ 在曲线上（$P$ 即切点）；② 求导，算出 $f'(x_0)$ 作为切线斜率；③ 用点斜式 $y - y_0 = f'(x_0)(x - x_0)$ 写出方程，必要时化为一般式。
  - 若题设的点不一定在曲线上（"过点 $P$ 的切线"），应设切点为 $(x_0, f(x_0))$，由"切线过 $P$"列方程解出 $x_0$，再写出切线方程（可能有多条）。含待定参数的切线（如 $y = k x + 16$）要先找出它的*确定条件*——本题是"过定点 $(0, 16)$"（也可理解为"纵截距为 16"），再设切点求解。
  - 求导时先看定义域：本题含 $ln x$，定义域为 $x > 0$；含 $ln(x + 1)$ 则为 $x > -1$，切点必须在定义域内。
  - 若由切线条件得到的是参数的*等式约束*（如例 2 中 $2a + b = 2$），常与基本不等式结合求最值：先把目标式化简，再用约束条件"乘 1"凑出可套用基本不等式的形式，最后*检验等号能否取到*（即解出取等时的参数值，确认满足题设范围）。
  - 求两曲线的*公切线*：分别设切点 $(a, f(a))$、$(b, g(b))$，写出两条切线方程，令"*斜率相等*"与"*纵截距相等*"（或直接令两直线重合）联立，解出 $a$、$b$ 即得公切线；最后可用"联立后出现二重根"来检验。
  - 求"过定点 $P$ 的切线有几条"（如例 5、练 5）：设切点 $(t, f(t))$，由"$P$ 在切线上"得到关于 $t$ 的方程，再把参数分离到一边；若分离后形状不便讨论，可*取其倒数*化简（如例 5 中化为 $1/m = (ln t)/t$，右边的 $g(t) = (ln t)/t$ 是单峰函数，最大值 $g(e) = 1/e$，于是"有两根"等价于 $0 < 1/m < 1/e$）。数根时用*函数的值域与单调性*，并注意排除使方程失去意义的特殊切点（如 $t = 1$ 时 $m ln t = t$ 化为 $0 = 1$）。
  - 若要求"切线有 3 条"（练 5、例 6），则图象需有*三个单调段*，且水平线与三段*各交一次*——把三段的极值（值域）取交集即可：练 5 中三段值域分别为 $(-6/e^3, 0)$、$(-6/e^3, 2e)$、$(-oo, 2e)$，交集为 $(-6/e^3, 0)$；例 6 中方程 $2u^3 - 3a u^2 + b = 0$ 有三根的充要条件为 $0 < b < a^3$。
  - 判断选项型题目（如例 6）的步骤：先求出参数应满足的*充要条件*，再把四个选项与该条件逐一比对——注意 $b = a^3$、$b = 0$ 这类"边界值"往往只能得到 2 条切线，不能算作"三条"。
  - 求*曲线上的点到已知直线的最小距离*（例 7，数形结合）：先判断曲线与直线的位置关系（作差构造函数，证明曲线恒在直线某一侧），再由"最值点处的切线与已知直线平行"令 $f'(x_0) = k$ 求出最值点，最后用点到直线的距离公式计算；也可直接用距离函数求最值作代数验证。
]
